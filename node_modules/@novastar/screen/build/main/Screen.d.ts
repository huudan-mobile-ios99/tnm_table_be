/// <reference types="node" />
import { SessionAPI } from './Session';
export default class Screen {
    readonly session: SessionAPI;
    constructor(session: SessionAPI);
    private _top;
    get top(): number;
    private _left;
    get left(): number;
    static crc: (data: Buffer) => number;
    setPos(left: number, top: number): Promise<void>;
    /**
     * ScreenInfoAccessor::ReadNextSenderSoftSpace()
     */
    readHeader(): Promise<string>;
    /**
     * ScreenInfoAccessor::ReadSenderScreenConfigInfo()
     */
    readConfig(): Promise<void>;
}
