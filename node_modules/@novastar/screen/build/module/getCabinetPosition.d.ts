import { LEDDisplayInfo } from './common';
export type CabinetPosition = {
    column: number;
    row: number;
    left: number;
    top: number;
    sender: number;
    port: number;
    card: number;
};
declare const getCabinetPosition: (screen: LEDDisplayInfo, sender: number, port: number, card: number) => CabinetPosition | null;
export default getCabinetPosition;
