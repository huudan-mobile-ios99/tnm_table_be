import AddressMapping from '@novastar/native/AddressMapping';
const knownAddresses = new Map();
Object.entries(AddressMapping).forEach(([key, address]) => {
    if (typeof address === 'number') {
        const value = knownAddresses.get(address);
        if (value !== undefined)
            value.push(key);
        else
            knownAddresses.set(address, [key]);
    }
});
export default function findKnownAddresses(address) {
    return knownAddresses.get(address)?.join(', ');
}
//# sourceMappingURL=findKnownAddresses.js.map