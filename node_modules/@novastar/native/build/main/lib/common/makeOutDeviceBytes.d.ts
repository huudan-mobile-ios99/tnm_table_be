import { Buffer } from 'buffer';
import { BaudRateTypeEnum } from '../../generated/BaudRateType';
declare const makeOutDeviceBytes: (outDeviceAddress: number, baudRate: BaudRateTypeEnum, otherDeviceProtocol?: number[] | Buffer) => Buffer;
export default makeOutDeviceBytes;
