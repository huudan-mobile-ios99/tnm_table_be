import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetICNumber(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, iCNumber: number): Promise<void>;
        trySetICNumber(addr: number, portAddr: number, scanBoardAddr: number, iCNumber: number): Promise<ErrorType | null>;
    }
}
export default function createSetICNumber<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, iCNumber: number): Request<Broadcast>;
