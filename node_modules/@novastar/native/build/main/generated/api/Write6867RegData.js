"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const codec_1 = require("@novastar/codec");
const AddressMapping_1 = __importDefault(require("../AddressMapping"));
function createWrite6867RegData(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = new codec_1.Request(data, bBroadcast, 'Write6867RegData');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping_1.default.Register6867RegAddress;
    return req;
}
exports.default = createWrite6867RegData;
codec_1.Session.prototype.Write6867RegData = async function Write6867RegData(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createWrite6867RegData(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
codec_1.Session.prototype.tryWrite6867RegData = async function tryWrite6867RegData(addr, portAddr, scanBoardAddr, data) {
    const req = createWrite6867RegData(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=Write6867RegData.js.map