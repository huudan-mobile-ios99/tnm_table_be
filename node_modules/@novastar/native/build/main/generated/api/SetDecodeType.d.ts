import { ErrorType, Request } from '@novastar/codec';
import { DecodeTypeEnum } from '../DecodeType';
declare module '@novastar/codec' {
    interface API {
        SetDecodeType(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, decodeType: DecodeTypeEnum): Promise<void>;
        trySetDecodeType(addr: number, portAddr: number, scanBoardAddr: number, decodeType: DecodeTypeEnum): Promise<ErrorType | null>;
    }
}
export default function createSetDecodeType<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, decodeType: DecodeTypeEnum): Request<Broadcast>;
