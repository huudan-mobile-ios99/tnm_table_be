import { ErrorType, Request } from '@novastar/codec';
import { PowerCtrlModeEnum } from '../PowerCtrlMode';
declare module '@novastar/codec' {
    interface API {
        FuncCard_SetPowerCtrlMode(addr: number, bBroadcast: boolean, powerCtrlMode: PowerCtrlModeEnum): Promise<void>;
        tryFuncCard_SetPowerCtrlMode(addr: number, powerCtrlMode: PowerCtrlModeEnum): Promise<ErrorType | null>;
    }
}
export default function createFuncCard_SetPowerCtrlMode<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, powerCtrlMode: PowerCtrlModeEnum): Request<Broadcast>;
