import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadDataGroupSequence(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadDataGroupSequence(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadDataGroupSequence(addr: number, portAddr: number, scanBoardAddr: number): Request;
