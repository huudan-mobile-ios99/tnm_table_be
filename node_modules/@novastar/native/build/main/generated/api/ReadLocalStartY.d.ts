import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadLocalStartY(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadLocalStartY(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadLocalStartY(addr: number, portAddr: number, scanBoardAddr: number): Request;
