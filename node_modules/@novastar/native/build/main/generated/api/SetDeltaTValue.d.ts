import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetDeltaTValue(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, deltaT: number): Promise<void>;
        trySetDeltaTValue(addr: number, portAddr: number, scanBoardAddr: number, deltaT: number): Promise<ErrorType | null>;
    }
}
export default function createSetDeltaTValue<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, deltaT: number): Request<Broadcast>;
