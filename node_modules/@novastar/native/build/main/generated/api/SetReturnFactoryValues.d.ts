import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetReturnFactoryValues(addr: number, bBroadcast: boolean, returnFactoryValues: number): Promise<void>;
        trySetReturnFactoryValues(addr: number, returnFactoryValues: number): Promise<ErrorType | null>;
    }
}
export default function createSetReturnFactoryValues<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, returnFactoryValues: number): Request<Broadcast>;
