import { Packet, Request } from '@novastar/codec';
import { CardTypeEnum } from '../CardType';
declare module '@novastar/codec' {
    interface API {
        ReadDistributeNumberOfCardOrScanBoardInPort(addr: number, portAddr: number, distributeAddr: number, portIndex: number, cardType: CardTypeEnum): Promise<number>;
        tryReadDistributeNumberOfCardOrScanBoardInPort(addr: number, portAddr: number, distributeAddr: number, portIndex: number, cardType: CardTypeEnum): Promise<Packet | null>;
    }
}
export default function createReadDistributeNumberOfCardOrScanBoardInPort(addr: number, portAddr: number, distributeAddr: number, portIndex: number, cardType: CardTypeEnum): Request;
