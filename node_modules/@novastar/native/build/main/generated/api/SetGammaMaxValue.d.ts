import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetGammaMaxValue(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, GammaMaxValue2053: number): Promise<void>;
        trySetGammaMaxValue(addr: number, portAddr: number, scanBoardAddr: number, GammaMaxValue2053: number): Promise<ErrorType | null>;
    }
}
export default function createSetGammaMaxValue<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, GammaMaxValue2053: number): Request<Broadcast>;
