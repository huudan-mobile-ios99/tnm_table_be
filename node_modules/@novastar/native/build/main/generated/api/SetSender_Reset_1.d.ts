import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetSender_Reset_1(addr: number, bBroadcast: boolean): Promise<void>;
        trySetSender_Reset_1(addr: number): Promise<ErrorType | null>;
    }
}
export default function createSetSender_Reset_1<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast): Request<Broadcast>;
