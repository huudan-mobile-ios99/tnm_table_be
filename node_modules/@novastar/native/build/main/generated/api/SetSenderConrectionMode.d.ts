import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetSenderConrectionMode(addr: number, bBroadcast: boolean, isOpen: boolean): Promise<void>;
        trySetSenderConrectionMode(addr: number, isOpen: boolean): Promise<ErrorType | null>;
    }
}
export default function createSetSenderConrectionMode<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, isOpen: boolean): Request<Broadcast>;
