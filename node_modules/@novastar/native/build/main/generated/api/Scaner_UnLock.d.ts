import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        Scaner_UnLock(addr: number, bBroadcast: boolean, passWord: number): Promise<void>;
        tryScaner_UnLock(addr: number, passWord: number): Promise<ErrorType | null>;
    }
}
export default function createScaner_UnLock<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, passWord: number): Request<Broadcast>;
