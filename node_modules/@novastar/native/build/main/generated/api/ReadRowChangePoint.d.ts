import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadRowChangePoint(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadRowChangePoint(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadRowChangePoint(addr: number, portAddr: number, scanBoardAddr: number): Request;
