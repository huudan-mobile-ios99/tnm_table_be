import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetScannerRecordCmd(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, bStartRecord: boolean): Promise<void>;
        trySetScannerRecordCmd(addr: number, portAddr: number, scanBoardAddr: number, bStartRecord: boolean): Promise<ErrorType | null>;
    }
}
export default function createSetScannerRecordCmd<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, bStartRecord: boolean): Request<Broadcast>;
