/// <reference types="node" />
import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetScanner_DMFirst(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, DMFirst: number[] | Buffer): Promise<void>;
        trySetScanner_DMFirst(addr: number, portAddr: number, scanBoardAddr: number, DMFirst: number[] | Buffer): Promise<ErrorType | null>;
    }
}
export default function createSetScanner_DMFirst<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, DMFirst: number[] | Buffer): Request<Broadcast>;
