import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetScreenEncrypt(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, screenEncrypt: number): Promise<void>;
        trySetScreenEncrypt(addr: number, portAddr: number, scanBoardAddr: number, screenEncrypt: number): Promise<ErrorType | null>;
    }
}
export default function createSetScreenEncrypt<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, screenEncrypt: number): Request<Broadcast>;
