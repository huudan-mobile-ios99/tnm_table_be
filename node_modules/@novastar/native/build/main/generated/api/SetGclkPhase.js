"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const codec_1 = require("@novastar/codec");
const AddressMapping_1 = __importDefault(require("../AddressMapping"));
function createSetGclkPhase(addr, portAddr, scanBoardAddr, bBroadcast, gclkPhase) {
    const $data = (0, codec_1.encodeUIntLE)(gclkPhase, AddressMapping_1.default.GclkPhaseOccupancy);
    const req = new codec_1.Request($data, bBroadcast, 'SetGclkPhase');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping_1.default.GclkPhaseAddr;
    return req;
}
exports.default = createSetGclkPhase;
codec_1.Session.prototype.SetGclkPhase = async function SetGclkPhase(addr, portAddr, scanBoardAddr, bBroadcast, gclkPhase) {
    const req = createSetGclkPhase(addr, portAddr, scanBoardAddr, bBroadcast, gclkPhase);
    await this.connection.send(req);
};
codec_1.Session.prototype.trySetGclkPhase = async function trySetGclkPhase(addr, portAddr, scanBoardAddr, gclkPhase) {
    const req = createSetGclkPhase(addr, portAddr, scanBoardAddr, false, gclkPhase);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetGclkPhase.js.map