import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadSpecialFrameRate(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadSpecialFrameRate(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadSpecialFrameRate(addr: number, portAddr: number, scanBoardAddr: number): Request;
