import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadAutoCorrectUpload(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadAutoCorrectUpload(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadAutoCorrectUpload(addr: number, portAddr: number, scanBoardAddr: number): Request;
