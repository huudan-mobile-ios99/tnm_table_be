import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetSpecialParam(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, rowChangePoint: number): Promise<void>;
        trySetSpecialParam(addr: number, portAddr: number, scanBoardAddr: number, rowChangePoint: number): Promise<ErrorType | null>;
    }
}
export default function createSetSpecialParam<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, rowChangePoint: number): Request<Broadcast>;
