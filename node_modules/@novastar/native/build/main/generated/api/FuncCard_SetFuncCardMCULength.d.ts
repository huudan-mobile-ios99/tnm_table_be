import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        FuncCard_SetFuncCardMCULength(addr: number, bBroadcast: boolean, mcuLength: number): Promise<void>;
        tryFuncCard_SetFuncCardMCULength(addr: number, mcuLength: number): Promise<ErrorType | null>;
    }
}
export default function createFuncCard_SetFuncCardMCULength<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, mcuLength: number): Request<Broadcast>;
