import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        Read9868ABlankOptimizationLevel2(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryRead9868ABlankOptimizationLevel2(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createRead9868ABlankOptimizationLevel2(addr: number, portAddr: number, scanBoardAddr: number): Request;
