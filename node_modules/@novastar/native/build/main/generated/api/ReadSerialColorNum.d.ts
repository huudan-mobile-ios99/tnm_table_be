import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadSerialColorNum(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadSerialColorNum(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadSerialColorNum(addr: number, portAddr: number, scanBoardAddr: number): Request;
