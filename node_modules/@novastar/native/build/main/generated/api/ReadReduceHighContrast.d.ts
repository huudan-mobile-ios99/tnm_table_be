import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadReduceHighContrast(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadReduceHighContrast(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadReduceHighContrast(addr: number, portAddr: number, scanBoardAddr: number): Request;
