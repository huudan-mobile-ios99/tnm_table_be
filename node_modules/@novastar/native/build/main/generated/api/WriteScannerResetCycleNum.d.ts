import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        WriteScannerResetCycleNum(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, cycleNum: number, length: number): Promise<void>;
        tryWriteScannerResetCycleNum(addr: number, portAddr: number, scanBoardAddr: number, cycleNum: number, length: number): Promise<ErrorType | null>;
    }
}
export default function createWriteScannerResetCycleNum<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, cycleNum: number, length: number): Request<Broadcast>;
