/// <reference types="node" />
import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetMaskOpreation(Sender: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, data: number[] | Buffer): Promise<void>;
        trySetMaskOpreation(Sender: number, portAddr: number, scanBoardAddr: number, data: number[] | Buffer): Promise<ErrorType | null>;
    }
}
export default function createSetMaskOpreation<Broadcast extends boolean>(Sender: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, data: number[] | Buffer): Request<Broadcast>;
