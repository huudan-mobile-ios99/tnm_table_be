import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        Distribute_SetDistributeMCULength(addr: number, portAddr: number, distributeAddr: number, bBroadcast: boolean, mcuLength: number): Promise<void>;
        tryDistribute_SetDistributeMCULength(addr: number, portAddr: number, distributeAddr: number, mcuLength: number): Promise<ErrorType | null>;
    }
}
export default function createDistribute_SetDistributeMCULength<Broadcast extends boolean>(addr: number, portAddr: number, distributeAddr: number, bBroadcast: Broadcast, mcuLength: number): Request<Broadcast>;
