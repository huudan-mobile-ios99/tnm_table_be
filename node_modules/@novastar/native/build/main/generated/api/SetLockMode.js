"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const codec_1 = require("@novastar/codec");
const AddressMapping_1 = __importDefault(require("../AddressMapping"));
function createSetLockMode(addr, portAddr, scanBoardAddr, bBroadcast, lockMode) {
    const $data = (0, codec_1.encodeUIntLE)(lockMode, AddressMapping_1.default.LockModeOccupancy);
    const req = new codec_1.Request($data, bBroadcast, 'SetLockMode');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping_1.default.LockModeAddr;
    return req;
}
exports.default = createSetLockMode;
codec_1.Session.prototype.SetLockMode = async function SetLockMode(addr, portAddr, scanBoardAddr, bBroadcast, lockMode) {
    const req = createSetLockMode(addr, portAddr, scanBoardAddr, bBroadcast, lockMode);
    await this.connection.send(req);
};
codec_1.Session.prototype.trySetLockMode = async function trySetLockMode(addr, portAddr, scanBoardAddr, lockMode) {
    const req = createSetLockMode(addr, portAddr, scanBoardAddr, false, lockMode);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetLockMode.js.map