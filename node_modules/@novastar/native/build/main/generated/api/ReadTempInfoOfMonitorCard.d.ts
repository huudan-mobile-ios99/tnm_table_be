import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadTempInfoOfMonitorCard(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadTempInfoOfMonitorCard(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadTempInfoOfMonitorCard(addr: number, portAddr: number, scanBoardAddr: number): Request;
