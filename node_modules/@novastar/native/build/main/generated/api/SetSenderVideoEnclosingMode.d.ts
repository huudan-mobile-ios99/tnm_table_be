import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetSenderVideoEnclosingMode(senderindex: number, width: number, height: number): Promise<void>;
        trySetSenderVideoEnclosingMode(senderindex: number, width: number, height: number): Promise<ErrorType | null>;
    }
}
export default function createSetSenderVideoEnclosingMode(senderindex: number, width: number, height: number): Request;
