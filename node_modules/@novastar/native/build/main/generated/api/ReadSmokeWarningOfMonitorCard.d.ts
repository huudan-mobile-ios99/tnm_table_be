import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadSmokeWarningOfMonitorCard(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadSmokeWarningOfMonitorCard(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadSmokeWarningOfMonitorCard(addr: number, portAddr: number, scanBoardAddr: number): Request;
