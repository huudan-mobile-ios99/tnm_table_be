import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetRebootGoldenFPGAProgram(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, rebootGoldenFPGAProgram: number): Promise<void>;
        trySetRebootGoldenFPGAProgram(addr: number, portAddr: number, scanBoardAddr: number, rebootGoldenFPGAProgram: number): Promise<ErrorType | null>;
    }
}
export default function createSetRebootGoldenFPGAProgram<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, rebootGoldenFPGAProgram: number): Request<Broadcast>;
