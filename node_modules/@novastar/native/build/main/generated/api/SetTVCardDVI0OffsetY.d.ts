import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetTVCardDVI0OffsetY(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, dviOffsetY: number): Promise<void>;
        trySetTVCardDVI0OffsetY(addr: number, portAddr: number, scanBoardAddr: number, dviOffsetY: number): Promise<ErrorType | null>;
    }
}
export default function createSetTVCardDVI0OffsetY<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, dviOffsetY: number): Request<Broadcast>;
