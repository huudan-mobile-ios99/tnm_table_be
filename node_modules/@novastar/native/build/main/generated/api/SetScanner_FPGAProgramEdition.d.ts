import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetScanner_FPGAProgramEdition(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, scanner_FPGAProgramEdition: number): Promise<void>;
        trySetScanner_FPGAProgramEdition(addr: number, portAddr: number, scanBoardAddr: number, scanner_FPGAProgramEdition: number): Promise<ErrorType | null>;
    }
}
export default function createSetScanner_FPGAProgramEdition<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, scanner_FPGAProgramEdition: number): Request<Broadcast>;
