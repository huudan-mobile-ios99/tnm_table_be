import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadEnhancedMode2053(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadEnhancedMode2053(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadEnhancedMode2053(addr: number, portAddr: number, scanBoardAddr: number): Request;
