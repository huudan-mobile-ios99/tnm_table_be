import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetPortOffsetY(addr: number, bBroadcast: boolean, portOffsetY: number, index: number): Promise<void>;
        trySetPortOffsetY(addr: number, portOffsetY: number, index: number): Promise<ErrorType | null>;
    }
}
export default function createSetPortOffsetY<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, portOffsetY: number, index: number): Request<Broadcast>;
