import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetDExtendMode(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, extendMode: number): Promise<void>;
        trySetDExtendMode(addr: number, portAddr: number, scanBoardAddr: number, extendMode: number): Promise<ErrorType | null>;
    }
}
export default function createSetDExtendMode<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, extendMode: number): Request<Broadcast>;
