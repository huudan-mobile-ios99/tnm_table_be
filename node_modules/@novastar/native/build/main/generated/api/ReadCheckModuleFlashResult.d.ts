/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadCheckModuleFlashResult(addr: number, portAddr: number, scanBoardAddr: number): Promise<Buffer>;
        tryReadCheckModuleFlashResult(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadCheckModuleFlashResult(addr: number, portAddr: number, scanBoardAddr: number): Request;
