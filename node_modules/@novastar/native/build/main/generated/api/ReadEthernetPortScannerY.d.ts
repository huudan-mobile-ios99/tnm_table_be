import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadEthernetPortScannerY(addr: number, portIndex: number, scannerIndex: number): Promise<number>;
        tryReadEthernetPortScannerY(addr: number, portIndex: number, scannerIndex: number): Promise<Packet | null>;
    }
}
export default function createReadEthernetPortScannerY(addr: number, portIndex: number, scannerIndex: number): Request;
