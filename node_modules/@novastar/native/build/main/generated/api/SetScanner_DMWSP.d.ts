/// <reference types="node" />
import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetScanner_DMWSP(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, DMWSP: number[] | Buffer): Promise<void>;
        trySetScanner_DMWSP(addr: number, portAddr: number, scanBoardAddr: number, DMWSP: number[] | Buffer): Promise<ErrorType | null>;
    }
}
export default function createSetScanner_DMWSP<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, DMWSP: number[] | Buffer): Request<Broadcast>;
