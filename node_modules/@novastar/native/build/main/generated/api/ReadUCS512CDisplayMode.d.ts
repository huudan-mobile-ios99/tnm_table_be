import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadUCS512CDisplayMode(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadUCS512CDisplayMode(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadUCS512CDisplayMode(addr: number, portAddr: number, scanBoardAddr: number): Request;
