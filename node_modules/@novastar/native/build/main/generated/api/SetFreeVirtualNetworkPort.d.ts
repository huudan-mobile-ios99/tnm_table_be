import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetFreeVirtualNetworkPort(addr: number, bBroadcast: boolean, number: number, portIndex: number): Promise<void>;
        trySetFreeVirtualNetworkPort(addr: number, number: number, portIndex: number): Promise<ErrorType | null>;
    }
}
export default function createSetFreeVirtualNetworkPort<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, number: number, portIndex: number): Request<Broadcast>;
