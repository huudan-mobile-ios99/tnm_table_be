import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetLightTimePerSubField(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, lightTimePerSubField: number): Promise<void>;
        trySetLightTimePerSubField(addr: number, portAddr: number, scanBoardAddr: number, lightTimePerSubField: number): Promise<ErrorType | null>;
    }
}
export default function createSetLightTimePerSubField<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, lightTimePerSubField: number): Request<Broadcast>;
