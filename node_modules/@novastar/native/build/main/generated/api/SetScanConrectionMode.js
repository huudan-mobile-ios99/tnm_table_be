"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const codec_1 = require("@novastar/codec");
const AddressMapping_1 = __importDefault(require("../AddressMapping"));
function createSetScanConrectionMode(addr, portAddr, scanBoardAddr, bBroadcast, isOpen) {
    const req = new codec_1.Request(isOpen ? [85] : [170], bBroadcast, 'SetScanConrectionMode');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping_1.default.ScannerCorrectionModeAddr;
    return req;
}
exports.default = createSetScanConrectionMode;
codec_1.Session.prototype.SetScanConrectionMode = async function SetScanConrectionMode(addr, portAddr, scanBoardAddr, bBroadcast, isOpen) {
    const req = createSetScanConrectionMode(addr, portAddr, scanBoardAddr, bBroadcast, isOpen);
    await this.connection.send(req);
};
codec_1.Session.prototype.trySetScanConrectionMode = async function trySetScanConrectionMode(addr, portAddr, scanBoardAddr, isOpen) {
    const req = createSetScanConrectionMode(addr, portAddr, scanBoardAddr, false, isOpen);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetScanConrectionMode.js.map