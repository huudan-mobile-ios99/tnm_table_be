import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetColorAdjustMatrixAndEnableNew(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, isEnable: boolean): Promise<void>;
        trySetColorAdjustMatrixAndEnableNew(addr: number, portAddr: number, scanBoardAddr: number, isEnable: boolean): Promise<ErrorType | null>;
    }
}
export default function createSetColorAdjustMatrixAndEnableNew<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, isEnable: boolean): Request<Broadcast>;
