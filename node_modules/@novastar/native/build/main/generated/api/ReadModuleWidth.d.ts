import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadModuleWidth(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadModuleWidth(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadModuleWidth(addr: number, portAddr: number, scanBoardAddr: number): Request;
