import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadSerialRGBCode(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadSerialRGBCode(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadSerialRGBCode(addr: number, portAddr: number, scanBoardAddr: number): Request;
