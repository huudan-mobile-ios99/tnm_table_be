/// <reference types="node" />
import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetScanner_DMSecond(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, DMSecond: number[] | Buffer): Promise<void>;
        trySetScanner_DMSecond(addr: number, portAddr: number, scanBoardAddr: number, DMSecond: number[] | Buffer): Promise<ErrorType | null>;
    }
}
export default function createSetScanner_DMSecond<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, DMSecond: number[] | Buffer): Request<Broadcast>;
