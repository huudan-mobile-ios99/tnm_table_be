import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetSymmetricalOutputMode(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, outPutMode: number): Promise<void>;
        trySetSymmetricalOutputMode(addr: number, portAddr: number, scanBoardAddr: number, outPutMode: number): Promise<ErrorType | null>;
    }
}
export default function createSetSymmetricalOutputMode<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, outPutMode: number): Request<Broadcast>;
