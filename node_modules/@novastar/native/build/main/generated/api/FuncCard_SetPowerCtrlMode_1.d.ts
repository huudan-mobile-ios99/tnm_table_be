import { ErrorType, Request } from '@novastar/codec';
import { PowerCtrlModeEnum } from '../PowerCtrlMode';
declare module '@novastar/codec' {
    interface API {
        FuncCard_SetPowerCtrlMode_1(addr: number, portAddr: number, funcCardAddr: number, bBroadcast: boolean, powerCtrlMode: PowerCtrlModeEnum): Promise<void>;
        tryFuncCard_SetPowerCtrlMode_1(addr: number, portAddr: number, funcCardAddr: number, powerCtrlMode: PowerCtrlModeEnum): Promise<ErrorType | null>;
    }
}
export default function createFuncCard_SetPowerCtrlMode_1<Broadcast extends boolean>(addr: number, portAddr: number, funcCardAddr: number, bBroadcast: Broadcast, powerCtrlMode: PowerCtrlModeEnum): Request<Broadcast>;
