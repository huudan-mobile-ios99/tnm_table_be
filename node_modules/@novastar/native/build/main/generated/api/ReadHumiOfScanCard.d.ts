import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadHumiOfScanCard(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadHumiOfScanCard(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadHumiOfScanCard(addr: number, portAddr: number, scanBoardAddr: number): Request;
