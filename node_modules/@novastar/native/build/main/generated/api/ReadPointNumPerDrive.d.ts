import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadPointNumPerDrive(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadPointNumPerDrive(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadPointNumPerDrive(addr: number, portAddr: number, scanBoardAddr: number): Request;
