/// <reference types="node" />
import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetScanner_LowGrayCodeTable(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, lowGrayCodeTable: number[] | Buffer): Promise<void>;
        trySetScanner_LowGrayCodeTable(addr: number, portAddr: number, scanBoardAddr: number, lowGrayCodeTable: number[] | Buffer): Promise<ErrorType | null>;
    }
}
export default function createSetScanner_LowGrayCodeTable<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, lowGrayCodeTable: number[] | Buffer): Request<Broadcast>;
