import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetVRgain(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, vRgain: number): Promise<void>;
        trySetVRgain(addr: number, portAddr: number, scanBoardAddr: number, vRgain: number): Promise<ErrorType | null>;
    }
}
export default function createSetVRgain<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, vRgain: number): Request<Broadcast>;
