import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetTVCardDVI0Width(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, dviWidth: number): Promise<void>;
        trySetTVCardDVI0Width(addr: number, portAddr: number, scanBoardAddr: number, dviWidth: number): Promise<ErrorType | null>;
    }
}
export default function createSetTVCardDVI0Width<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, dviWidth: number): Request<Broadcast>;
