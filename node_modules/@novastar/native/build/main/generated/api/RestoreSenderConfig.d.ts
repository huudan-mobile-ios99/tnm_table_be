/// <reference types="node" />
import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        RestoreSenderConfig(addr: number, bBroadcast: boolean, writeData: number[] | Buffer): Promise<void>;
        tryRestoreSenderConfig(addr: number, writeData: number[] | Buffer): Promise<ErrorType | null>;
    }
}
export default function createRestoreSenderConfig<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, writeData: number[] | Buffer): Request<Broadcast>;
