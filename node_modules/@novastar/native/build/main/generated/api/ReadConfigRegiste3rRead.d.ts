/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadConfigRegiste3rRead(addr: number, portAddr: number, scanBoardAddr: number): Promise<Buffer>;
        tryReadConfigRegiste3rRead(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadConfigRegiste3rRead(addr: number, portAddr: number, scanBoardAddr: number): Request;
