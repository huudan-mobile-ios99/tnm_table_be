import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetSoftwareCoefficientAccelerateFlag(Sender: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, Is3x3: boolean): Promise<void>;
        trySetSoftwareCoefficientAccelerateFlag(Sender: number, portAddr: number, scanBoardAddr: number, Is3x3: boolean): Promise<ErrorType | null>;
    }
}
export default function createSetSoftwareCoefficientAccelerateFlag<Broadcast extends boolean>(Sender: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, Is3x3: boolean): Request<Broadcast>;
