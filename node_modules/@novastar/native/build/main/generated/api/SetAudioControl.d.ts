import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetAudioControl(addr: number, bBroadcast: boolean, audioControl: number): Promise<void>;
        trySetAudioControl(addr: number, audioControl: number): Promise<ErrorType | null>;
    }
}
export default function createSetAudioControl<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, audioControl: number): Request<Broadcast>;
