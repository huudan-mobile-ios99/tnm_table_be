import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetUseColorAdjustMatrix(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, isEnableClrMatrix: boolean): Promise<void>;
        trySetUseColorAdjustMatrix(addr: number, portAddr: number, scanBoardAddr: number, isEnableClrMatrix: boolean): Promise<ErrorType | null>;
    }
}
export default function createSetUseColorAdjustMatrix<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, isEnableClrMatrix: boolean): Request<Broadcast>;
