import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetUpdateCorrectionCoefficientCmd(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, valueFlag: number): Promise<void>;
        trySetUpdateCorrectionCoefficientCmd(addr: number, portAddr: number, scanBoardAddr: number, valueFlag: number): Promise<ErrorType | null>;
    }
}
export default function createSetUpdateCorrectionCoefficientCmd<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, valueFlag: number): Request<Broadcast>;
