import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SwitchAutoOrManualLinearTable(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, switchAutoOrManualEnable: boolean): Promise<void>;
        trySwitchAutoOrManualLinearTable(addr: number, portAddr: number, scanBoardAddr: number, switchAutoOrManualEnable: boolean): Promise<ErrorType | null>;
    }
}
export default function createSwitchAutoOrManualLinearTable<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, switchAutoOrManualEnable: boolean): Request<Broadcast>;
