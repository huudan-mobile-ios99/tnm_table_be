"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const codec_1 = require("@novastar/codec");
const AddressMapping_1 = __importDefault(require("../AddressMapping"));
function createExtendModuleOperCmd(addr, portAddr, scanBoardAddr, bBroadcast, data, length) {
    if (data.length !== length)
        throw new TypeError(`Invalid buffer size: ${data.length}`);
    const req = new codec_1.Request(data, bBroadcast, 'ExtendModuleOperCmd');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping_1.default.ModuleOperCmdAddr;
    return req;
}
exports.default = createExtendModuleOperCmd;
codec_1.Session.prototype.ExtendModuleOperCmd = async function ExtendModuleOperCmd(addr, portAddr, scanBoardAddr, bBroadcast, data, length) {
    const req = createExtendModuleOperCmd(addr, portAddr, scanBoardAddr, bBroadcast, data, length);
    await this.connection.send(req);
};
codec_1.Session.prototype.tryExtendModuleOperCmd = async function tryExtendModuleOperCmd(addr, portAddr, scanBoardAddr, data, length) {
    const req = createExtendModuleOperCmd(addr, portAddr, scanBoardAddr, false, data, length);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=ExtendModuleOperCmd.js.map