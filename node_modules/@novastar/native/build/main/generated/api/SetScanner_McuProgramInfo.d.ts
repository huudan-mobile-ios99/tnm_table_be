/// <reference types="node" />
import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetScanner_McuProgramInfo(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, scanner_McuProgramRemarks: number[] | Buffer): Promise<void>;
        trySetScanner_McuProgramInfo(addr: number, portAddr: number, scanBoardAddr: number, scanner_McuProgramRemarks: number[] | Buffer): Promise<ErrorType | null>;
    }
}
export default function createSetScanner_McuProgramInfo<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, scanner_McuProgramRemarks: number[] | Buffer): Request<Broadcast>;
