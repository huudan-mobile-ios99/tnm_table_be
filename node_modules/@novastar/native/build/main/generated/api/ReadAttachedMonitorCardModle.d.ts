import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadAttachedMonitorCardModle(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadAttachedMonitorCardModle(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadAttachedMonitorCardModle(addr: number, portAddr: number, scanBoardAddr: number): Request;
