import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetVirtualImageDataSource(addr: number, bBroadcast: boolean, virtuaImageDataSource: number): Promise<void>;
        trySetVirtualImageDataSource(addr: number, virtuaImageDataSource: number): Promise<ErrorType | null>;
    }
}
export default function createSetVirtualImageDataSource<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, virtuaImageDataSource: number): Request<Broadcast>;
