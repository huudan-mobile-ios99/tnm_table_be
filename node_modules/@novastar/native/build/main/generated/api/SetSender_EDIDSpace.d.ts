/// <reference types="node" />
import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetSender_EDIDSpace(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, Passward: number[] | Buffer): Promise<void>;
        trySetSender_EDIDSpace(addr: number, portAddr: number, scanBoardAddr: number, Passward: number[] | Buffer): Promise<ErrorType | null>;
    }
}
export default function createSetSender_EDIDSpace<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, Passward: number[] | Buffer): Request<Broadcast>;
