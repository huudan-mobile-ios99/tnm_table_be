import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadScanner18BitEnable(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadScanner18BitEnable(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadScanner18BitEnable(addr: number, portAddr: number, scanBoardAddr: number): Request;
