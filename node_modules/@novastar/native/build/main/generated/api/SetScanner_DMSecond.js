"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const codec_1 = require("@novastar/codec");
const AddressMapping_1 = __importDefault(require("../AddressMapping"));
function createSetScanner_DMSecond(addr, portAddr, scanBoardAddr, bBroadcast, DMSecond) {
    if (DMSecond.length !== 0)
        throw new TypeError(`Invalid buffer size: ${DMSecond.length}`);
    const req = new codec_1.Request(DMSecond, bBroadcast, 'SetScanner_DMSecond');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping_1.default.Scanner_DmSecondAddr;
    return req;
}
exports.default = createSetScanner_DMSecond;
codec_1.Session.prototype.SetScanner_DMSecond = async function SetScanner_DMSecond(addr, portAddr, scanBoardAddr, bBroadcast, DMSecond) {
    const req = createSetScanner_DMSecond(addr, portAddr, scanBoardAddr, bBroadcast, DMSecond);
    await this.connection.send(req);
};
codec_1.Session.prototype.trySetScanner_DMSecond = async function trySetScanner_DMSecond(addr, portAddr, scanBoardAddr, DMSecond) {
    const req = createSetScanner_DMSecond(addr, portAddr, scanBoardAddr, false, DMSecond);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetScanner_DMSecond.js.map