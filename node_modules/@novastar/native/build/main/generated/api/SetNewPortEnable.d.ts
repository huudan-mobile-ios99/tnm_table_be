import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetNewPortEnable(addr: number, bBroadcast: boolean, portEnable: number, index: number): Promise<void>;
        trySetNewPortEnable(addr: number, portEnable: number, index: number): Promise<ErrorType | null>;
    }
}
export default function createSetNewPortEnable<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, portEnable: number, index: number): Request<Broadcast>;
