import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadModuelCols(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadModuelCols(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadModuelCols(addr: number, portAddr: number, scanBoardAddr: number): Request;
