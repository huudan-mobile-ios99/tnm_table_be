import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetFieldRateMode(addr: number, bBroadcast: boolean, fieldRateMode: number): Promise<void>;
        trySetFieldRateMode(addr: number, fieldRateMode: number): Promise<ErrorType | null>;
    }
}
export default function createSetFieldRateMode<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, fieldRateMode: number): Request<Broadcast>;
