import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetModuelRows(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, moduleRows: number): Promise<void>;
        trySetModuelRows(addr: number, portAddr: number, scanBoardAddr: number, moduleRows: number): Promise<ErrorType | null>;
    }
}
export default function createSetModuelRows<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, moduleRows: number): Request<Broadcast>;
