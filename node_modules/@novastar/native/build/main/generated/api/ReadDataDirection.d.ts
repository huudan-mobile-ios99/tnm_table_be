import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadDataDirection(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadDataDirection(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadDataDirection(addr: number, portAddr: number, scanBoardAddr: number): Request;
