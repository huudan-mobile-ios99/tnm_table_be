import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetParameterSender3D(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, parameterSender3D: number): Promise<void>;
        trySetParameterSender3D(addr: number, portAddr: number, scanBoardAddr: number, parameterSender3D: number): Promise<ErrorType | null>;
    }
}
export default function createSetParameterSender3D<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, parameterSender3D: number): Request<Broadcast>;
