import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetSender_Enable3DStateSpace(addr: number, bBroadcast: boolean, enable3DState: number): Promise<void>;
        trySetSender_Enable3DStateSpace(addr: number, enable3DState: number): Promise<ErrorType | null>;
    }
}
export default function createSetSender_Enable3DStateSpace<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, enable3DState: number): Request<Broadcast>;
