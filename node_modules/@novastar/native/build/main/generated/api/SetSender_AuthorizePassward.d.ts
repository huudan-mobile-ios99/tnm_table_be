/// <reference types="node" />
import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetSender_AuthorizePassward(addr: number, bBroadcast: boolean, Passward: number[] | Buffer): Promise<void>;
        trySetSender_AuthorizePassward(addr: number, Passward: number[] | Buffer): Promise<ErrorType | null>;
    }
}
export default function createSetSender_AuthorizePassward<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, Passward: number[] | Buffer): Request<Broadcast>;
