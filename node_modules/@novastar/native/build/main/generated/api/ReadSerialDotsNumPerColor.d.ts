import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadSerialDotsNumPerColor(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadSerialDotsNumPerColor(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadSerialDotsNumPerColor(addr: number, portAddr: number, scanBoardAddr: number): Request;
