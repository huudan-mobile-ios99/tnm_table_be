import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadDisConnectRelayTemperature(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadDisConnectRelayTemperature(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadDisConnectRelayTemperature(addr: number, portAddr: number, scanBoardAddr: number): Request;
