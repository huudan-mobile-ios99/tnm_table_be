/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadHubMonitorStatus(addr: number, portAddr: number, scanBoardAddr: number): Promise<Buffer>;
        tryReadHubMonitorStatus(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadHubMonitorStatus(addr: number, portAddr: number, scanBoardAddr: number): Request;
