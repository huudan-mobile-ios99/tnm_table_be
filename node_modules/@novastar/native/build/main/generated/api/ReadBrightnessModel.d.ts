import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadBrightnessModel(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadBrightnessModel(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadBrightnessModel(addr: number, portAddr: number, scanBoardAddr: number): Request;
