/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadMagnitudes(addr: number, portAddr: number, scanBoardAddr: number): Promise<Buffer>;
        tryReadMagnitudes(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadMagnitudes(addr: number, portAddr: number, scanBoardAddr: number): Request;
