import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetFieldRate(addr: number, bBroadcast: boolean, setFieldRate: number): Promise<void>;
        trySetFieldRate(addr: number, setFieldRate: number): Promise<ErrorType | null>;
    }
}
export default function createSetFieldRate<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, setFieldRate: number): Request<Broadcast>;
