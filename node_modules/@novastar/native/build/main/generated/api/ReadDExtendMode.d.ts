import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadDExtendMode(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadDExtendMode(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadDExtendMode(addr: number, portAddr: number, scanBoardAddr: number): Request;
