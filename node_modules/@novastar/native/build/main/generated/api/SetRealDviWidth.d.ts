import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetRealDviWidth(addr: number, bBroadcast: boolean, dviWidth: number): Promise<void>;
        trySetRealDviWidth(addr: number, dviWidth: number): Promise<ErrorType | null>;
    }
}
export default function createSetRealDviWidth<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, dviWidth: number): Request<Broadcast>;
