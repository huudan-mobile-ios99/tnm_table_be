import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadLogicShiftUnitNumPerSubField(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadLogicShiftUnitNumPerSubField(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadLogicShiftUnitNumPerSubField(addr: number, portAddr: number, scanBoardAddr: number): Request;
