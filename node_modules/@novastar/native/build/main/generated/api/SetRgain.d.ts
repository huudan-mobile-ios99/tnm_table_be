import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetRgain(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, rgain: number): Promise<void>;
        trySetRgain(addr: number, portAddr: number, scanBoardAddr: number, rgain: number): Promise<ErrorType | null>;
    }
}
export default function createSetRgain<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, rgain: number): Request<Broadcast>;
