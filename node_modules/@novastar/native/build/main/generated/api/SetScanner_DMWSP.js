"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const codec_1 = require("@novastar/codec");
const AddressMapping_1 = __importDefault(require("../AddressMapping"));
function createSetScanner_DMWSP(addr, portAddr, scanBoardAddr, bBroadcast, DMWSP) {
    if (DMWSP.length !== 0)
        throw new TypeError(`Invalid buffer size: ${DMWSP.length}`);
    const req = new codec_1.Request(DMWSP, bBroadcast, 'SetScanner_DMWSP');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping_1.default.Scanner_DmWSPAAddr;
    return req;
}
exports.default = createSetScanner_DMWSP;
codec_1.Session.prototype.SetScanner_DMWSP = async function SetScanner_DMWSP(addr, portAddr, scanBoardAddr, bBroadcast, DMWSP) {
    const req = createSetScanner_DMWSP(addr, portAddr, scanBoardAddr, bBroadcast, DMWSP);
    await this.connection.send(req);
};
codec_1.Session.prototype.trySetScanner_DMWSP = async function trySetScanner_DMWSP(addr, portAddr, scanBoardAddr, DMWSP) {
    const req = createSetScanner_DMWSP(addr, portAddr, scanBoardAddr, false, DMWSP);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetScanner_DMWSP.js.map