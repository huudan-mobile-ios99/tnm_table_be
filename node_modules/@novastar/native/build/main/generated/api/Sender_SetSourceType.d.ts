import { ErrorType, Request } from '@novastar/codec';
import { SourceSelectModeEnum } from '../SourceSelectMode';
declare module '@novastar/codec' {
    interface API {
        Sender_SetSourceType(addr: number, bBroadcast: boolean, sourceMode: SourceSelectModeEnum): Promise<void>;
        trySender_SetSourceType(addr: number, sourceMode: SourceSelectModeEnum): Promise<ErrorType | null>;
    }
}
export default function createSender_SetSourceType<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, sourceMode: SourceSelectModeEnum): Request<Broadcast>;
