import { ErrorType, Request } from '@novastar/codec';
import { VirtualModeTypeEnum } from '../VirtualModeType';
declare module '@novastar/codec' {
    interface API {
        SetVirtualEnable(addr: number, bBroadcast: boolean, virtualEnable: VirtualModeTypeEnum): Promise<void>;
        trySetVirtualEnable(addr: number, virtualEnable: VirtualModeTypeEnum): Promise<ErrorType | null>;
    }
}
export default function createSetVirtualEnable<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, virtualEnable: VirtualModeTypeEnum): Request<Broadcast>;
