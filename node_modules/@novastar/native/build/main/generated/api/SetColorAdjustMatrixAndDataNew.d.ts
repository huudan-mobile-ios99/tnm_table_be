import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetColorAdjustMatrixAndDataNew(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, byteData: number): Promise<void>;
        trySetColorAdjustMatrixAndDataNew(addr: number, portAddr: number, scanBoardAddr: number, byteData: number): Promise<ErrorType | null>;
    }
}
export default function createSetColorAdjustMatrixAndDataNew<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, byteData: number): Request<Broadcast>;
