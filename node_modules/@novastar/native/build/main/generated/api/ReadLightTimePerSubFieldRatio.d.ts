import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadLightTimePerSubFieldRatio(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadLightTimePerSubFieldRatio(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadLightTimePerSubFieldRatio(addr: number, portAddr: number, scanBoardAddr: number): Request;
