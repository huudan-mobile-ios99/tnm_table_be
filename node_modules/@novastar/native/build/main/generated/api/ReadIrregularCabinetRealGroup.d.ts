/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadIrregularCabinetRealGroup(addr: number, portAddr: number, scanBoardAddr: number, dataLength: number): Promise<Buffer>;
        tryReadIrregularCabinetRealGroup(addr: number, portAddr: number, scanBoardAddr: number, dataLength: number): Promise<Packet | null>;
    }
}
export default function createReadIrregularCabinetRealGroup(addr: number, portAddr: number, scanBoardAddr: number, dataLength: number): Request;
