import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        WriteScanerSlavaFlashToSdram(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean): Promise<void>;
        tryWriteScanerSlavaFlashToSdram(addr: number, portAddr: number, scanBoardAddr: number): Promise<ErrorType | null>;
    }
}
export default function createWriteScanerSlavaFlashToSdram<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast): Request<Broadcast>;
