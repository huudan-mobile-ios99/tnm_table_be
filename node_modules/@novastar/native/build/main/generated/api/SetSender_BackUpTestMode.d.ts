import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetSender_BackUpTestMode(addr: number, bBroadcast: boolean, backUpTestMode: boolean): Promise<void>;
        trySetSender_BackUpTestMode(addr: number, backUpTestMode: boolean): Promise<ErrorType | null>;
    }
}
export default function createSetSender_BackUpTestMode<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, backUpTestMode: boolean): Request<Broadcast>;
