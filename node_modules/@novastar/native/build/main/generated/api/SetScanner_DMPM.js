"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const codec_1 = require("@novastar/codec");
const AddressMapping_1 = __importDefault(require("../AddressMapping"));
function createSetScanner_DMPM(addr, portAddr, scanBoardAddr, bBroadcast, DMPM) {
    if (DMPM.length !== 0)
        throw new TypeError(`Invalid buffer size: ${DMPM.length}`);
    const req = new codec_1.Request(DMPM, bBroadcast, 'SetScanner_DMPM');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping_1.default.Scanner_DmPMAAddr;
    return req;
}
exports.default = createSetScanner_DMPM;
codec_1.Session.prototype.SetScanner_DMPM = async function SetScanner_DMPM(addr, portAddr, scanBoardAddr, bBroadcast, DMPM) {
    const req = createSetScanner_DMPM(addr, portAddr, scanBoardAddr, bBroadcast, DMPM);
    await this.connection.send(req);
};
codec_1.Session.prototype.trySetScanner_DMPM = async function trySetScanner_DMPM(addr, portAddr, scanBoardAddr, DMPM) {
    const req = createSetScanner_DMPM(addr, portAddr, scanBoardAddr, false, DMPM);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetScanner_DMPM.js.map