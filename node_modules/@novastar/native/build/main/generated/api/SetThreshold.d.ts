import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetThreshold(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, threshold: number): Promise<void>;
        trySetThreshold(addr: number, portAddr: number, scanBoardAddr: number, threshold: number): Promise<ErrorType | null>;
    }
}
export default function createSetThreshold<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, threshold: number): Request<Broadcast>;
