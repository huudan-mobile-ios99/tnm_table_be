import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadTVCardDVI0Height(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadTVCardDVI0Height(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadTVCardDVI0Height(addr: number, portAddr: number, scanBoardAddr: number): Request;
