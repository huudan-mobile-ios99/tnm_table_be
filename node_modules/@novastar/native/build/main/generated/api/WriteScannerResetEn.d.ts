import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        WriteScannerResetEn(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, isOpen: boolean, length: number): Promise<void>;
        tryWriteScannerResetEn(addr: number, portAddr: number, scanBoardAddr: number, isOpen: boolean, length: number): Promise<ErrorType | null>;
    }
}
export default function createWriteScannerResetEn<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, isOpen: boolean, length: number): Request<Broadcast>;
