import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        Distribute_ReadFuncCardFPGAVersion(addr: number, portAddr: number, distributeAddr: number): Promise<number>;
        tryDistribute_ReadFuncCardFPGAVersion(addr: number, portAddr: number, distributeAddr: number): Promise<Packet | null>;
    }
}
export default function createDistribute_ReadFuncCardFPGAVersion(addr: number, portAddr: number, distributeAddr: number): Request;
