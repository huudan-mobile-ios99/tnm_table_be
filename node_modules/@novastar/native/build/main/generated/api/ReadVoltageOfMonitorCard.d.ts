/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadVoltageOfMonitorCard(addr: number, portAddr: number, scanBoardAddr: number, readCountOfValtage: number, beginIdnexOfValtage: number): Promise<Buffer>;
        tryReadVoltageOfMonitorCard(addr: number, portAddr: number, scanBoardAddr: number, readCountOfValtage: number, beginIdnexOfValtage: number): Promise<Packet | null>;
    }
}
export default function createReadVoltageOfMonitorCard(addr: number, portAddr: number, scanBoardAddr: number, readCountOfValtage: number, beginIdnexOfValtage: number): Request;
