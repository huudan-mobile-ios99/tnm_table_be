import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetSortOrder(addr: number, bBroadcast: boolean, sortOrder: number): Promise<void>;
        trySetSortOrder(addr: number, sortOrder: number): Promise<ErrorType | null>;
    }
}
export default function createSetSortOrder<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, sortOrder: number): Request<Broadcast>;
