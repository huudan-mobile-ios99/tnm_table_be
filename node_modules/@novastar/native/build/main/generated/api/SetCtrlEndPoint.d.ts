import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetCtrlEndPoint(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, ctrlEndPoint: number): Promise<void>;
        trySetCtrlEndPoint(addr: number, portAddr: number, scanBoardAddr: number, ctrlEndPoint: number): Promise<ErrorType | null>;
    }
}
export default function createSetCtrlEndPoint<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, ctrlEndPoint: number): Request<Broadcast>;
