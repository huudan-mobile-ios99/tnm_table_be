/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadRemoveChipChannelData(addr: number, portAddr: number, scanBoardAddr: number, dataLength: number): Promise<Buffer>;
        tryReadRemoveChipChannelData(addr: number, portAddr: number, scanBoardAddr: number, dataLength: number): Promise<Packet | null>;
    }
}
export default function createReadRemoveChipChannelData(addr: number, portAddr: number, scanBoardAddr: number, dataLength: number): Request;
