/// <reference types="node" />
import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetMasterOrSlaveNext(addr: number, bBroadcast: boolean, masterOrSlaveBytes: number[] | Buffer): Promise<void>;
        trySetMasterOrSlaveNext(addr: number, masterOrSlaveBytes: number[] | Buffer): Promise<ErrorType | null>;
    }
}
export default function createSetMasterOrSlaveNext<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, masterOrSlaveBytes: number[] | Buffer): Request<Broadcast>;
