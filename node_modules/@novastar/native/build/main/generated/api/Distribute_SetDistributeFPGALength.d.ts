import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        Distribute_SetDistributeFPGALength(addr: number, portAddr: number, distributAddr: number, bBroadcast: boolean, fpgaLength: number): Promise<void>;
        tryDistribute_SetDistributeFPGALength(addr: number, portAddr: number, distributAddr: number, fpgaLength: number): Promise<ErrorType | null>;
    }
}
export default function createDistribute_SetDistributeFPGALength<Broadcast extends boolean>(addr: number, portAddr: number, distributAddr: number, bBroadcast: Broadcast, fpgaLength: number): Request<Broadcast>;
