import { ErrorType, Request } from '@novastar/codec';
import { SmartSetModeEnum } from '../SmartSetMode';
declare module '@novastar/codec' {
    interface API {
        SetSmartSetMode(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, smartMode: SmartSetModeEnum): Promise<void>;
        trySetSmartSetMode(addr: number, portAddr: number, scanBoardAddr: number, smartMode: SmartSetModeEnum): Promise<ErrorType | null>;
    }
}
export default function createSetSmartSetMode<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, smartMode: SmartSetModeEnum): Request<Broadcast>;
