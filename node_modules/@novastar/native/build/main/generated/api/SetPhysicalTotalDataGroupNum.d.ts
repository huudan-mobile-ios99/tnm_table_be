import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetPhysicalTotalDataGroupNum(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, totalDataGroupNum: number): Promise<void>;
        trySetPhysicalTotalDataGroupNum(addr: number, portAddr: number, scanBoardAddr: number, totalDataGroupNum: number): Promise<ErrorType | null>;
    }
}
export default function createSetPhysicalTotalDataGroupNum<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, totalDataGroupNum: number): Request<Broadcast>;
