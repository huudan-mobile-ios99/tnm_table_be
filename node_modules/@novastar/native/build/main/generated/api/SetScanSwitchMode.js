"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const codec_1 = require("@novastar/codec");
const AddressMapping_1 = __importDefault(require("../AddressMapping"));
function createSetScanSwitchMode(addr, portAddr, scanBoardAddr, bBroadcast, switchMode) {
    const $data = (0, codec_1.encodeUIntLE)(switchMode, AddressMapping_1.default.SwitchModeOccupancy);
    const req = new codec_1.Request($data, bBroadcast, 'SetScanSwitchMode');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping_1.default.SwitchModeAddr;
    return req;
}
exports.default = createSetScanSwitchMode;
codec_1.Session.prototype.SetScanSwitchMode = async function SetScanSwitchMode(addr, portAddr, scanBoardAddr, bBroadcast, switchMode) {
    const req = createSetScanSwitchMode(addr, portAddr, scanBoardAddr, bBroadcast, switchMode);
    await this.connection.send(req);
};
codec_1.Session.prototype.trySetScanSwitchMode = async function trySetScanSwitchMode(addr, portAddr, scanBoardAddr, switchMode) {
    const req = createSetScanSwitchMode(addr, portAddr, scanBoardAddr, false, switchMode);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetScanSwitchMode.js.map