import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetSender_DVIResolutionWidth(addr: number, bBroadcast: boolean, dviWidth: number): Promise<void>;
        trySetSender_DVIResolutionWidth(addr: number, dviWidth: number): Promise<ErrorType | null>;
    }
}
export default function createSetSender_DVIResolutionWidth<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, dviWidth: number): Request<Broadcast>;
