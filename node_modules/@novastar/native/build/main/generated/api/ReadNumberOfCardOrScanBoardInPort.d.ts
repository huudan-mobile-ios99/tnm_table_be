import { Packet, Request } from '@novastar/codec';
import { CardTypeEnum } from '../CardType';
declare module '@novastar/codec' {
    interface API {
        ReadNumberOfCardOrScanBoardInPort(addr: number, portIndex: number, cardType: CardTypeEnum): Promise<number>;
        tryReadNumberOfCardOrScanBoardInPort(addr: number, portIndex: number, cardType: CardTypeEnum): Promise<Packet | null>;
    }
}
export default function createReadNumberOfCardOrScanBoardInPort(addr: number, portIndex: number, cardType: CardTypeEnum): Request;
