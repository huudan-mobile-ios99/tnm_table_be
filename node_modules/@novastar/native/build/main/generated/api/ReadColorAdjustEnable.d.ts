import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadColorAdjustEnable(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadColorAdjustEnable(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadColorAdjustEnable(addr: number, portAddr: number, scanBoardAddr: number): Request;
