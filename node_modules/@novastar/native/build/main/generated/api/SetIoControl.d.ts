import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetIoControl(addr: number, bBroadcast: boolean, ioControl: number): Promise<void>;
        trySetIoControl(addr: number, ioControl: number): Promise<ErrorType | null>;
    }
}
export default function createSetIoControl<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, ioControl: number): Request<Broadcast>;
