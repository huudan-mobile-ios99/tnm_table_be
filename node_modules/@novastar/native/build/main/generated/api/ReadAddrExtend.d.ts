import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadAddrExtend(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadAddrExtend(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadAddrExtend(addr: number, portAddr: number, scanBoardAddr: number): Request;
