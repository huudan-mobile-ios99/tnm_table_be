/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadConfigRegister(addr: number, portAddr: number, scanBoardAddr: number): Promise<Buffer>;
        tryReadConfigRegister(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadConfigRegister(addr: number, portAddr: number, scanBoardAddr: number): Request;
