import { ErrorType, Request } from '@novastar/codec';
import { ChipTypeEnum } from '../ChipType';
declare module '@novastar/codec' {
    interface API {
        SetDriverType(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, driverType: ChipTypeEnum): Promise<void>;
        trySetDriverType(addr: number, portAddr: number, scanBoardAddr: number, driverType: ChipTypeEnum): Promise<ErrorType | null>;
    }
}
export default function createSetDriverType<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, driverType: ChipTypeEnum): Request<Broadcast>;
