import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadHalfFreqSetMode(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadHalfFreqSetMode(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadHalfFreqSetMode(addr: number, portAddr: number, scanBoardAddr: number): Request;
