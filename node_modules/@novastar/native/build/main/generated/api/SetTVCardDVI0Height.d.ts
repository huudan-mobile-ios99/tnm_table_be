import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetTVCardDVI0Height(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, dviHeight: number): Promise<void>;
        trySetTVCardDVI0Height(addr: number, portAddr: number, scanBoardAddr: number, dviHeight: number): Promise<ErrorType | null>;
    }
}
export default function createSetTVCardDVI0Height<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, dviHeight: number): Request<Broadcast>;
