/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadColorManageCoefData(addr: number, portAddr: number, scanBoardAddr: number): Promise<Buffer>;
        tryReadColorManageCoefData(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadColorManageCoefData(addr: number, portAddr: number, scanBoardAddr: number): Request;
