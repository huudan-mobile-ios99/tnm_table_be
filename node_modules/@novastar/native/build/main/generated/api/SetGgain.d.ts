import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetGgain(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, ggain: number): Promise<void>;
        trySetGgain(addr: number, portAddr: number, scanBoardAddr: number, ggain: number): Promise<ErrorType | null>;
    }
}
export default function createSetGgain<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, ggain: number): Request<Broadcast>;
