import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetEndColScalePos(addr: number, bBroadcast: boolean, endColScalePos: number): Promise<void>;
        trySetEndColScalePos(addr: number, endColScalePos: number): Promise<ErrorType | null>;
    }
}
export default function createSetEndColScalePos<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, endColScalePos: number): Request<Broadcast>;
