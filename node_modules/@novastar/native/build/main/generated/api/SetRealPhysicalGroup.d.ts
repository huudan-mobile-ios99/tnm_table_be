import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetRealPhysicalGroup(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, realPhysical: number): Promise<void>;
        trySetRealPhysicalGroup(addr: number, portAddr: number, scanBoardAddr: number, realPhysical: number): Promise<ErrorType | null>;
    }
}
export default function createSetRealPhysicalGroup<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, realPhysical: number): Request<Broadcast>;
