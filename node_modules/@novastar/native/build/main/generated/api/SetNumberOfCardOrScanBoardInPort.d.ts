import { ErrorType, Request } from '@novastar/codec';
import { CardTypeEnum } from '../CardType';
declare module '@novastar/codec' {
    interface API {
        SetNumberOfCardOrScanBoardInPort(addr: number, bBroadcast: boolean, number: number, portIndex: number, cardType: CardTypeEnum): Promise<void>;
        trySetNumberOfCardOrScanBoardInPort(addr: number, number: number, portIndex: number, cardType: CardTypeEnum): Promise<ErrorType | null>;
    }
}
export default function createSetNumberOfCardOrScanBoardInPort<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, number: number, portIndex: number, cardType: CardTypeEnum): Request<Broadcast>;
