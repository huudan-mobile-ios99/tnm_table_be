import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadA4ScanCardModle(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadA4ScanCardModle(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadA4ScanCardModle(addr: number, portAddr: number, scanBoardAddr: number): Request;
