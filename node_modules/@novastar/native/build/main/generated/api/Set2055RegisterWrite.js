"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const codec_1 = require("@novastar/codec");
const AddressMapping_1 = __importDefault(require("../AddressMapping"));
function createSet2055RegisterWrite(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    if (data.length !== AddressMapping_1.default.Config2055RegisterOccupancy)
        throw new TypeError(`Invalid buffer size: ${data.length}`);
    const req = new codec_1.Request(data, bBroadcast, 'Set2055RegisterWrite');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping_1.default.Config2055RegisterAddr;
    return req;
}
exports.default = createSet2055RegisterWrite;
codec_1.Session.prototype.Set2055RegisterWrite = async function Set2055RegisterWrite(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createSet2055RegisterWrite(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
codec_1.Session.prototype.trySet2055RegisterWrite = async function trySet2055RegisterWrite(addr, portAddr, scanBoardAddr, data) {
    const req = createSet2055RegisterWrite(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=Set2055RegisterWrite.js.map