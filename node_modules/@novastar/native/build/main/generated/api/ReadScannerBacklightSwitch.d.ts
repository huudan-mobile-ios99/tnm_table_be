import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadScannerBacklightSwitch(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadScannerBacklightSwitch(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadScannerBacklightSwitch(addr: number, portAddr: number, scanBoardAddr: number): Request;
