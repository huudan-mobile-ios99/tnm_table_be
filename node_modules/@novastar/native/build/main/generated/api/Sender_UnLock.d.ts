import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        Sender_UnLock(addr: number, bBroadcast: boolean, passWord: number): Promise<void>;
        trySender_UnLock(addr: number, passWord: number): Promise<ErrorType | null>;
    }
}
export default function createSender_UnLock<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, passWord: number): Request<Broadcast>;
