import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetGreenBrightness(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, greenBrightness: number): Promise<void>;
        trySetGreenBrightness(addr: number, portAddr: number, scanBoardAddr: number, greenBrightness: number): Promise<ErrorType | null>;
    }
}
export default function createSetGreenBrightness<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, greenBrightness: number): Request<Broadcast>;
