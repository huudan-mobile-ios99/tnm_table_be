import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadCorrectionState(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadCorrectionState(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadCorrectionState(addr: number, portAddr: number, scanBoardAddr: number): Request;
