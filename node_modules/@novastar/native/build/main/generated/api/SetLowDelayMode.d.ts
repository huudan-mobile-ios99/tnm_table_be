import { ErrorType, Request } from '@novastar/codec';
import { LowDelayModeEnum } from '../LowDelayMode';
declare module '@novastar/codec' {
    interface API {
        SetLowDelayMode(addr: number, bBroadcast: boolean, lowDelayMode: LowDelayModeEnum): Promise<void>;
        trySetLowDelayMode(addr: number, lowDelayMode: LowDelayModeEnum): Promise<ErrorType | null>;
    }
}
export default function createSetLowDelayMode<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, lowDelayMode: LowDelayModeEnum): Request<Broadcast>;
