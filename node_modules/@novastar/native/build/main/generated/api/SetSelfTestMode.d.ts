import { ErrorType, Request } from '@novastar/codec';
import { TestModeEnum } from '../TestMode';
declare module '@novastar/codec' {
    interface API {
        SetSelfTestMode(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, selfTestMode: TestModeEnum): Promise<void>;
        trySetSelfTestMode(addr: number, portAddr: number, scanBoardAddr: number, selfTestMode: TestModeEnum): Promise<ErrorType | null>;
    }
}
export default function createSetSelfTestMode<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, selfTestMode: TestModeEnum): Request<Broadcast>;
