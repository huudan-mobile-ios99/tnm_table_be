import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadShiftUnitNumPerSubField(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadShiftUnitNumPerSubField(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadShiftUnitNumPerSubField(addr: number, portAddr: number, scanBoardAddr: number): Request;
