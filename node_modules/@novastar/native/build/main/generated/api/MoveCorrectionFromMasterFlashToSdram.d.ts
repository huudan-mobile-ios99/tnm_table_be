import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        MoveCorrectionFromMasterFlashToSdram(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean): Promise<void>;
        tryMoveCorrectionFromMasterFlashToSdram(addr: number, portAddr: number, scanBoardAddr: number): Promise<ErrorType | null>;
    }
}
export default function createMoveCorrectionFromMasterFlashToSdram<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast): Request<Broadcast>;
