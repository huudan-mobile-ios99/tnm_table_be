import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadConfigFileID(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadConfigFileID(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadConfigFileID(addr: number, portAddr: number, scanBoardAddr: number): Request;
