/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadMGCoefData(addr: number, portAddr: number, scanBoardAddr: number): Promise<Buffer>;
        tryReadMGCoefData(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadMGCoefData(addr: number, portAddr: number, scanBoardAddr: number): Request;
