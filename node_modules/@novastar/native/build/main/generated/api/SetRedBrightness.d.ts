import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetRedBrightness(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, redBrightness: number): Promise<void>;
        trySetRedBrightness(addr: number, portAddr: number, scanBoardAddr: number, redBrightness: number): Promise<ErrorType | null>;
    }
}
export default function createSetRedBrightness<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, redBrightness: number): Request<Broadcast>;
