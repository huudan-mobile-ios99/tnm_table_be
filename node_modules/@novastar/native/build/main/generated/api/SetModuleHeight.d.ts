import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetModuleHeight(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, moduleHeight: number): Promise<void>;
        trySetModuleHeight(addr: number, portAddr: number, scanBoardAddr: number, moduleHeight: number): Promise<ErrorType | null>;
    }
}
export default function createSetModuleHeight<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, moduleHeight: number): Request<Broadcast>;
