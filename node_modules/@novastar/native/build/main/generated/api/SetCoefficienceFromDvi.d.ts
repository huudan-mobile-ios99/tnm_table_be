import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetCoefficienceFromDvi(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, coefficienceFromDvi: number): Promise<void>;
        trySetCoefficienceFromDvi(addr: number, portAddr: number, scanBoardAddr: number, coefficienceFromDvi: number): Promise<ErrorType | null>;
    }
}
export default function createSetCoefficienceFromDvi<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, coefficienceFromDvi: number): Request<Broadcast>;
