import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetLowGrayCompensation(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, lowGrayCpt: number): Promise<void>;
        trySetLowGrayCompensation(addr: number, portAddr: number, scanBoardAddr: number, lowGrayCpt: number): Promise<ErrorType | null>;
    }
}
export default function createSetLowGrayCompensation<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, lowGrayCpt: number): Request<Broadcast>;
