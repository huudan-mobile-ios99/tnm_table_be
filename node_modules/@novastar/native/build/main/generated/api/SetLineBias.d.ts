import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetLineBias(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, lineBias: number): Promise<void>;
        trySetLineBias(addr: number, portAddr: number, scanBoardAddr: number, lineBias: number): Promise<ErrorType | null>;
    }
}
export default function createSetLineBias<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, lineBias: number): Request<Broadcast>;
