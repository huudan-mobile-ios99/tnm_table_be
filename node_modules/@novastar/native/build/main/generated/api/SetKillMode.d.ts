import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetKillMode(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, killMode: number): Promise<void>;
        trySetKillMode(addr: number, portAddr: number, scanBoardAddr: number, killMode: number): Promise<ErrorType | null>;
    }
}
export default function createSetKillMode<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, killMode: number): Request<Broadcast>;
