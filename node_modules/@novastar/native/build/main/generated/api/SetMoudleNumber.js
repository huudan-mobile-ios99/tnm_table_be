"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const codec_1 = require("@novastar/codec");
const AddressMapping_1 = __importDefault(require("../AddressMapping"));
function createSetMoudleNumber(addr, portAddr, scanBoardAddr, bBroadcast, MoudleNumber) {
    const $data = (0, codec_1.encodeUIntLE)(MoudleNumber, AddressMapping_1.default.ScannerMoudleNumberOccupancy);
    const req = new codec_1.Request($data, bBroadcast, 'SetMoudleNumber');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping_1.default.ScanneMoudleNumberAddr;
    return req;
}
exports.default = createSetMoudleNumber;
codec_1.Session.prototype.SetMoudleNumber = async function SetMoudleNumber(addr, portAddr, scanBoardAddr, bBroadcast, MoudleNumber) {
    const req = createSetMoudleNumber(addr, portAddr, scanBoardAddr, bBroadcast, MoudleNumber);
    await this.connection.send(req);
};
codec_1.Session.prototype.trySetMoudleNumber = async function trySetMoudleNumber(addr, portAddr, scanBoardAddr, MoudleNumber) {
    const req = createSetMoudleNumber(addr, portAddr, scanBoardAddr, false, MoudleNumber);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetMoudleNumber.js.map