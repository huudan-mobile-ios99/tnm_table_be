import { ErrorType, Request } from '@novastar/codec';
import { DataDirectionTypeEnum } from '../DataDirectionType';
declare module '@novastar/codec' {
    interface API {
        SetDataDirection(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, dataDriection: DataDirectionTypeEnum): Promise<void>;
        trySetDataDirection(addr: number, portAddr: number, scanBoardAddr: number, dataDriection: DataDirectionTypeEnum): Promise<ErrorType | null>;
    }
}
export default function createSetDataDirection<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, dataDriection: DataDirectionTypeEnum): Request<Broadcast>;
