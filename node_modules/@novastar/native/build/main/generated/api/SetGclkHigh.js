"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const codec_1 = require("@novastar/codec");
const AddressMapping_1 = __importDefault(require("../AddressMapping"));
function createSetGclkHigh(addr, portAddr, scanBoardAddr, bBroadcast, gclkHigh) {
    const $data = (0, codec_1.encodeUIntLE)(gclkHigh, AddressMapping_1.default.GclkHighOccupancy);
    const req = new codec_1.Request($data, bBroadcast, 'SetGclkHigh');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping_1.default.GclkHighAddr;
    return req;
}
exports.default = createSetGclkHigh;
codec_1.Session.prototype.SetGclkHigh = async function SetGclkHigh(addr, portAddr, scanBoardAddr, bBroadcast, gclkHigh) {
    const req = createSetGclkHigh(addr, portAddr, scanBoardAddr, bBroadcast, gclkHigh);
    await this.connection.send(req);
};
codec_1.Session.prototype.trySetGclkHigh = async function trySetGclkHigh(addr, portAddr, scanBoardAddr, gclkHigh) {
    const req = createSetGclkHigh(addr, portAddr, scanBoardAddr, false, gclkHigh);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetGclkHigh.js.map