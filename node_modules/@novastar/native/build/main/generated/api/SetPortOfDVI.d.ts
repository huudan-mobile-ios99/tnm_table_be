import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetPortOfDVI(addr: number, bBroadcast: boolean, dviIndex: number, portIndex: number): Promise<void>;
        trySetPortOfDVI(addr: number, dviIndex: number, portIndex: number): Promise<ErrorType | null>;
    }
}
export default function createSetPortOfDVI<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, dviIndex: number, portIndex: number): Request<Broadcast>;
