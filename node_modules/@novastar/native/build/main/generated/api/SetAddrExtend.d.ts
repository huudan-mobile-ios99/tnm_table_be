import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetAddrExtend(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, addrExtend: number): Promise<void>;
        trySetAddrExtend(addr: number, portAddr: number, scanBoardAddr: number, addrExtend: number): Promise<ErrorType | null>;
    }
}
export default function createSetAddrExtend<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, addrExtend: number): Request<Broadcast>;
