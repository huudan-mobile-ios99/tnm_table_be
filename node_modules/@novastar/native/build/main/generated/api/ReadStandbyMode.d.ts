import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadStandbyMode(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadStandbyMode(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadStandbyMode(addr: number, portAddr: number, scanBoardAddr: number): Request;
