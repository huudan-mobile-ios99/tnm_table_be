import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadDriverType(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadDriverType(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadDriverType(addr: number, portAddr: number, scanBoardAddr: number): Request;
