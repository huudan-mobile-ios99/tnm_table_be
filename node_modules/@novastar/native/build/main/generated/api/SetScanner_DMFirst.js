"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const codec_1 = require("@novastar/codec");
const AddressMapping_1 = __importDefault(require("../AddressMapping"));
function createSetScanner_DMFirst(addr, portAddr, scanBoardAddr, bBroadcast, DMFirst) {
    if (DMFirst.length !== 0)
        throw new TypeError(`Invalid buffer size: ${DMFirst.length}`);
    const req = new codec_1.Request(DMFirst, bBroadcast, 'SetScanner_DMFirst');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping_1.default.Scanner_DmFirstAddr;
    return req;
}
exports.default = createSetScanner_DMFirst;
codec_1.Session.prototype.SetScanner_DMFirst = async function SetScanner_DMFirst(addr, portAddr, scanBoardAddr, bBroadcast, DMFirst) {
    const req = createSetScanner_DMFirst(addr, portAddr, scanBoardAddr, bBroadcast, DMFirst);
    await this.connection.send(req);
};
codec_1.Session.prototype.trySetScanner_DMFirst = async function trySetScanner_DMFirst(addr, portAddr, scanBoardAddr, DMFirst) {
    const req = createSetScanner_DMFirst(addr, portAddr, scanBoardAddr, false, DMFirst);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetScanner_DMFirst.js.map