import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetVirtualFrameRate(addr: number, bBroadcast: boolean, virtualFrameRate: number): Promise<void>;
        trySetVirtualFrameRate(addr: number, virtualFrameRate: number): Promise<ErrorType | null>;
    }
}
export default function createSetVirtualFrameRate<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, virtualFrameRate: number): Request<Broadcast>;
