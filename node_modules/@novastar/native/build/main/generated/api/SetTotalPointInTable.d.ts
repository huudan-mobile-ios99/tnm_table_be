import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetTotalPointInTable(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, totalPointInTable: number): Promise<void>;
        trySetTotalPointInTable(addr: number, portAddr: number, scanBoardAddr: number, totalPointInTable: number): Promise<ErrorType | null>;
    }
}
export default function createSetTotalPointInTable<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, totalPointInTable: number): Request<Broadcast>;
