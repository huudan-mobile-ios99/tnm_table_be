"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const codec_1 = require("@novastar/codec");
const AddressMapping_1 = __importDefault(require("../AddressMapping"));
function createSetGroupSwapInfo(addr, portAddr, scanBoardAddr, bBroadcast, groupSwapInfo) {
    if (groupSwapInfo.length !== AddressMapping_1.default.GroupSwapInfoOccupancy)
        throw new TypeError(`Invalid buffer size: ${groupSwapInfo.length}`);
    const req = new codec_1.Request(groupSwapInfo, bBroadcast, 'SetGroupSwapInfo');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping_1.default.GroupSwapInfoAddr;
    return req;
}
exports.default = createSetGroupSwapInfo;
codec_1.Session.prototype.SetGroupSwapInfo = async function SetGroupSwapInfo(addr, portAddr, scanBoardAddr, bBroadcast, groupSwapInfo) {
    const req = createSetGroupSwapInfo(addr, portAddr, scanBoardAddr, bBroadcast, groupSwapInfo);
    await this.connection.send(req);
};
codec_1.Session.prototype.trySetGroupSwapInfo = async function trySetGroupSwapInfo(addr, portAddr, scanBoardAddr, groupSwapInfo) {
    const req = createSetGroupSwapInfo(addr, portAddr, scanBoardAddr, false, groupSwapInfo);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetGroupSwapInfo.js.map