import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetPortOffsetX(addr: number, bBroadcast: boolean, portOffsetX: number, index: number): Promise<void>;
        trySetPortOffsetX(addr: number, portOffsetX: number, index: number): Promise<ErrorType | null>;
    }
}
export default function createSetPortOffsetX<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, portOffsetX: number, index: number): Request<Broadcast>;
