import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadWheelReadingCureParameterToFactoryAreaData(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadWheelReadingCureParameterToFactoryAreaData(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadWheelReadingCureParameterToFactoryAreaData(addr: number, portAddr: number, scanBoardAddr: number): Request;
