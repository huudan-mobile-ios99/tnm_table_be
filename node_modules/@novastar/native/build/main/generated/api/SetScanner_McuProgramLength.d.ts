import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetScanner_McuProgramLength(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, scanner_McuProgramLength: number): Promise<void>;
        trySetScanner_McuProgramLength(addr: number, portAddr: number, scanBoardAddr: number, scanner_McuProgramLength: number): Promise<ErrorType | null>;
    }
}
export default function createSetScanner_McuProgramLength<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, scanner_McuProgramLength: number): Request<Broadcast>;
