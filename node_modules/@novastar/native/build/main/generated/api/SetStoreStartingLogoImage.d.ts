import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetStoreStartingLogoImage(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, storeStartingLogoImage: number): Promise<void>;
        trySetStoreStartingLogoImage(addr: number, portAddr: number, scanBoardAddr: number, storeStartingLogoImage: number): Promise<ErrorType | null>;
    }
}
export default function createSetStoreStartingLogoImage<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, storeStartingLogoImage: number): Request<Broadcast>;
