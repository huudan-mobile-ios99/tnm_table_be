import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetSender_AmbientLight(addr: number, bBroadcast: boolean, HDRAmbientLight: number): Promise<void>;
        trySetSender_AmbientLight(addr: number, HDRAmbientLight: number): Promise<ErrorType | null>;
    }
}
export default function createSetSender_AmbientLight<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, HDRAmbientLight: number): Request<Broadcast>;
