import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadCabinetSerialNum(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadCabinetSerialNum(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadCabinetSerialNum(addr: number, portAddr: number, scanBoardAddr: number): Request;
