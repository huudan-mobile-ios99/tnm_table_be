import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetTestPoint(addr: number, bBroadcast: boolean, testPoint: number): Promise<void>;
        trySetTestPoint(addr: number, testPoint: number): Promise<ErrorType | null>;
    }
}
export default function createSetTestPoint<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, testPoint: number): Request<Broadcast>;
