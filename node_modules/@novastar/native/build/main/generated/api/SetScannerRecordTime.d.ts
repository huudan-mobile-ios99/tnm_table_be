import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetScannerRecordTime(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean): Promise<void>;
        trySetScannerRecordTime(addr: number, portAddr: number, scanBoardAddr: number): Promise<ErrorType | null>;
    }
}
export default function createSetScannerRecordTime<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast): Request<Broadcast>;
