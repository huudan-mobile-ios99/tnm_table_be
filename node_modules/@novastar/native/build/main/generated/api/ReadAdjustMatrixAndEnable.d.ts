import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadAdjustMatrixAndEnable(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadAdjustMatrixAndEnable(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadAdjustMatrixAndEnable(addr: number, portAddr: number, scanBoardAddr: number): Request;
