import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadHumiOfMonitorCard(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadHumiOfMonitorCard(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadHumiOfMonitorCard(addr: number, portAddr: number, scanBoardAddr: number): Request;
