import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadLocalStartX(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadLocalStartX(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadLocalStartX(addr: number, portAddr: number, scanBoardAddr: number): Request;
