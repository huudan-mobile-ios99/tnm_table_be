"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const codec_1 = require("@novastar/codec");
const AddressMapping_1 = __importDefault(require("../AddressMapping"));
function createSetCorrectionOnROELine(addr, portAddr, scanBoardAddr, bBroadcast, correctionInfo) {
    if (correctionInfo.length !== AddressMapping_1.default.CorrectionOnOccupancy)
        throw new TypeError(`Invalid buffer size: ${correctionInfo.length}`);
    const req = new codec_1.Request(correctionInfo, bBroadcast, 'SetCorrectionOnROELine');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping_1.default.CorrectionOnAddr;
    return req;
}
exports.default = createSetCorrectionOnROELine;
codec_1.Session.prototype.SetCorrectionOnROELine = async function SetCorrectionOnROELine(addr, portAddr, scanBoardAddr, bBroadcast, correctionInfo) {
    const req = createSetCorrectionOnROELine(addr, portAddr, scanBoardAddr, bBroadcast, correctionInfo);
    await this.connection.send(req);
};
codec_1.Session.prototype.trySetCorrectionOnROELine = async function trySetCorrectionOnROELine(addr, portAddr, scanBoardAddr, correctionInfo) {
    const req = createSetCorrectionOnROELine(addr, portAddr, scanBoardAddr, false, correctionInfo);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetCorrectionOnROELine.js.map