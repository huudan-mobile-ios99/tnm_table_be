import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadAttachedMonitorCardProgramVersion(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadAttachedMonitorCardProgramVersion(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadAttachedMonitorCardProgramVersion(addr: number, portAddr: number, scanBoardAddr: number): Request;
