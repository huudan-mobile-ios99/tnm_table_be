import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetScanSwitchMode(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, switchMode: number): Promise<void>;
        trySetScanSwitchMode(addr: number, portAddr: number, scanBoardAddr: number, switchMode: number): Promise<ErrorType | null>;
    }
}
export default function createSetScanSwitchMode<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, switchMode: number): Request<Broadcast>;
