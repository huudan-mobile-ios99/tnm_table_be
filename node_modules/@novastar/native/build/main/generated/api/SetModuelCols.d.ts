import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetModuelCols(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, moduleCols: number): Promise<void>;
        trySetModuelCols(addr: number, portAddr: number, scanBoardAddr: number, moduleCols: number): Promise<ErrorType | null>;
    }
}
export default function createSetModuelCols<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, moduleCols: number): Request<Broadcast>;
