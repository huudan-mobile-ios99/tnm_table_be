import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetRowScalePara(addr: number, bBroadcast: boolean, rowScalePar: number): Promise<void>;
        trySetRowScalePara(addr: number, rowScalePar: number): Promise<ErrorType | null>;
    }
}
export default function createSetRowScalePara<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, rowScalePar: number): Request<Broadcast>;
