"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const codec_1 = require("@novastar/codec");
const AddressMapping_1 = __importDefault(require("../AddressMapping"));
function createSetKillMode(addr, portAddr, scanBoardAddr, bBroadcast, killMode) {
    const $data = (0, codec_1.encodeUIntLE)(killMode, AddressMapping_1.default.KillModeOccupancy);
    const req = new codec_1.Request($data, bBroadcast, 'SetKillMode');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping_1.default.KillModeAddr;
    return req;
}
exports.default = createSetKillMode;
codec_1.Session.prototype.SetKillMode = async function SetKillMode(addr, portAddr, scanBoardAddr, bBroadcast, killMode) {
    const req = createSetKillMode(addr, portAddr, scanBoardAddr, bBroadcast, killMode);
    await this.connection.send(req);
};
codec_1.Session.prototype.trySetKillMode = async function trySetKillMode(addr, portAddr, scanBoardAddr, killMode) {
    const req = createSetKillMode(addr, portAddr, scanBoardAddr, false, killMode);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetKillMode.js.map