import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetMBI5252ManualReset(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean): Promise<void>;
        trySetMBI5252ManualReset(addr: number, portAddr: number, scanBoardAddr: number): Promise<ErrorType | null>;
    }
}
export default function createSetMBI5252ManualReset<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast): Request<Broadcast>;
