"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const codec_1 = require("@novastar/codec");
const AddressMapping_1 = __importDefault(require("../AddressMapping"));
function createSetMagnitudes(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = new codec_1.Request(data, bBroadcast, 'SetMagnitudes');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping_1.default.MagnitudesAddr;
    return req;
}
exports.default = createSetMagnitudes;
codec_1.Session.prototype.SetMagnitudes = async function SetMagnitudes(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createSetMagnitudes(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
codec_1.Session.prototype.trySetMagnitudes = async function trySetMagnitudes(addr, portAddr, scanBoardAddr, data) {
    const req = createSetMagnitudes(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetMagnitudes.js.map