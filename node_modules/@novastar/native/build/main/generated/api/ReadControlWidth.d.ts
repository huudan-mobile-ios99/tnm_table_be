import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadControlWidth(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadControlWidth(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadControlWidth(addr: number, portAddr: number, scanBoardAddr: number): Request;
