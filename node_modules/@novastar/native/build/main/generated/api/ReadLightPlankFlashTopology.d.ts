/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadLightPlankFlashTopology(addr: number, portAddr: number, scanBoardAddr: number): Promise<Buffer>;
        tryReadLightPlankFlashTopology(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadLightPlankFlashTopology(addr: number, portAddr: number, scanBoardAddr: number): Request;
