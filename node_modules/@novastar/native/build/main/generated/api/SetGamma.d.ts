import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetGamma(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, gamma: number): Promise<void>;
        trySetGamma(addr: number, portAddr: number, scanBoardAddr: number, gamma: number): Promise<ErrorType | null>;
    }
}
export default function createSetGamma<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, gamma: number): Request<Broadcast>;
