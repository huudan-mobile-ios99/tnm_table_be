import { ErrorType, Request } from '@novastar/codec';
import { GhostRemoveModeTypeEnum } from '../GhostRemoveModeType';
declare module '@novastar/codec' {
    interface API {
        SetGhostRemoveMode(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, ghostRemoveMode: GhostRemoveModeTypeEnum): Promise<void>;
        trySetGhostRemoveMode(addr: number, portAddr: number, scanBoardAddr: number, ghostRemoveMode: GhostRemoveModeTypeEnum): Promise<ErrorType | null>;
    }
}
export default function createSetGhostRemoveMode<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, ghostRemoveMode: GhostRemoveModeTypeEnum): Request<Broadcast>;
