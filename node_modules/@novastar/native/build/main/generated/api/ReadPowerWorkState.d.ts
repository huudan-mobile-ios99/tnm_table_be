/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadPowerWorkState(addr: number, portAddr: number, scanBoardAddr: number, powerBackNumber: number): Promise<Buffer>;
        tryReadPowerWorkState(addr: number, portAddr: number, scanBoardAddr: number, powerBackNumber: number): Promise<Packet | null>;
    }
}
export default function createReadPowerWorkState(addr: number, portAddr: number, scanBoardAddr: number, powerBackNumber: number): Request;
