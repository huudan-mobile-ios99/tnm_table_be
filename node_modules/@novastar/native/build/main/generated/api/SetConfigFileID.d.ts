import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetConfigFileID(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, configFileID: number): Promise<void>;
        trySetConfigFileID(addr: number, portAddr: number, scanBoardAddr: number, configFileID: number): Promise<ErrorType | null>;
    }
}
export default function createSetConfigFileID<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, configFileID: number): Request<Broadcast>;
