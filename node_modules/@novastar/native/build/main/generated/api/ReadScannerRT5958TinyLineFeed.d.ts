/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadScannerRT5958TinyLineFeed(addr: number, portAddr: number, scanBoardAddr: number, readLength: number): Promise<Buffer>;
        tryReadScannerRT5958TinyLineFeed(addr: number, portAddr: number, scanBoardAddr: number, readLength: number): Promise<Packet | null>;
    }
}
export default function createReadScannerRT5958TinyLineFeed(addr: number, portAddr: number, scanBoardAddr: number, readLength: number): Request;
