/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadMBI5252AutoResetTime(addr: number, portAddr: number, scanBoardAddr: number): Promise<Buffer>;
        tryReadMBI5252AutoResetTime(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadMBI5252AutoResetTime(addr: number, portAddr: number, scanBoardAddr: number): Request;
