import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadEthernetPortScannerX(addr: number, portIndex: number, scannerIndex: number): Promise<number>;
        tryReadEthernetPortScannerX(addr: number, portIndex: number, scannerIndex: number): Promise<Packet | null>;
    }
}
export default function createReadEthernetPortScannerX(addr: number, portIndex: number, scannerIndex: number): Request;
