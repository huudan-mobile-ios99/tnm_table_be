import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetCheckModuleFlashCmd(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, checkModuleFlashCmd: number): Promise<void>;
        trySetCheckModuleFlashCmd(addr: number, portAddr: number, scanBoardAddr: number, checkModuleFlashCmd: number): Promise<ErrorType | null>;
    }
}
export default function createSetCheckModuleFlashCmd<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, checkModuleFlashCmd: number): Request<Broadcast>;
