import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetSender_VideoScalingMode(addr: number, bBoradcast: boolean, scalingMode: number): Promise<void>;
        trySetSender_VideoScalingMode(addr: number, scalingMode: number): Promise<ErrorType | null>;
    }
}
export default function createSetSender_VideoScalingMode<Broadcast extends boolean>(addr: number, bBoradcast: Broadcast, scalingMode: number): Request<Broadcast>;
