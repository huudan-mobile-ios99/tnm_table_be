"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const codec_1 = require("@novastar/codec");
function createWriteCorrectionDataToScan(addr, portAddr, scanBoardAddr, bBroadcast, data, length, registerAddr) {
    if (data.length !== length)
        throw new TypeError(`Invalid buffer size: ${data.length}`);
    const req = new codec_1.Request(data, bBroadcast, 'WriteCorrectionDataToScan');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = registerAddr;
    return req;
}
exports.default = createWriteCorrectionDataToScan;
codec_1.Session.prototype.WriteCorrectionDataToScan = async function WriteCorrectionDataToScan(addr, portAddr, scanBoardAddr, bBroadcast, data, length, registerAddr) {
    const req = createWriteCorrectionDataToScan(addr, portAddr, scanBoardAddr, bBroadcast, data, length, registerAddr);
    await this.connection.send(req);
};
codec_1.Session.prototype.tryWriteCorrectionDataToScan = async function tryWriteCorrectionDataToScan(addr, portAddr, scanBoardAddr, data, length, registerAddr) {
    const req = createWriteCorrectionDataToScan(addr, portAddr, scanBoardAddr, false, data, length, registerAddr);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=WriteCorrectionDataToScan.js.map