/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        Read2200RegisterData(addr: number, portAddr: number, scanBoardAddr: number): Promise<Buffer>;
        tryRead2200RegisterData(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createRead2200RegisterData(addr: number, portAddr: number, scanBoardAddr: number): Request;
