/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadCorrectionDataToScan(addr: number, portAddr: number, scanBoardAddr: number, readLength: number, registerAddr: number): Promise<Buffer>;
        tryReadCorrectionDataToScan(addr: number, portAddr: number, scanBoardAddr: number, readLength: number, registerAddr: number): Promise<Packet | null>;
    }
}
export default function createReadCorrectionDataToScan(addr: number, portAddr: number, scanBoardAddr: number, readLength: number, registerAddr: number): Request;
