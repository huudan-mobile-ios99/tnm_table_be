/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadColorMatrix(addr: number, portAddr: number, scanBoardAddr: number): Promise<Buffer>;
        tryReadColorMatrix(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadColorMatrix(addr: number, portAddr: number, scanBoardAddr: number): Request;
