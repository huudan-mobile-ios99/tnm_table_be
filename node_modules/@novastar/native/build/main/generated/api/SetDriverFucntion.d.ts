import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetDriverFucntion(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, driverFunc: number): Promise<void>;
        trySetDriverFucntion(addr: number, portAddr: number, scanBoardAddr: number, driverFunc: number): Promise<ErrorType | null>;
    }
}
export default function createSetDriverFucntion<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, driverFunc: number): Request<Broadcast>;
