import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetGclkUnitCycle(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, gclkUnitCycle: number): Promise<void>;
        trySetGclkUnitCycle(addr: number, portAddr: number, scanBoardAddr: number, gclkUnitCycle: number): Promise<ErrorType | null>;
    }
}
export default function createSetGclkUnitCycle<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, gclkUnitCycle: number): Request<Broadcast>;
