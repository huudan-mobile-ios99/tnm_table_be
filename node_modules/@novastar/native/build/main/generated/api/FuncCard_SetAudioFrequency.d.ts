import { ErrorType, Request } from '@novastar/codec';
import { AudioFrequencyModeEnum } from '../AudioFrequencyMode';
declare module '@novastar/codec' {
    interface API {
        FuncCard_SetAudioFrequency(addr: number, bBroadcast: boolean, freMode: AudioFrequencyModeEnum): Promise<void>;
        tryFuncCard_SetAudioFrequency(addr: number, freMode: AudioFrequencyModeEnum): Promise<ErrorType | null>;
    }
}
export default function createFuncCard_SetAudioFrequency<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, freMode: AudioFrequencyModeEnum): Request<Broadcast>;
