import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadColorAdjustMatrixType(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadColorAdjustMatrixType(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadColorAdjustMatrixType(addr: number, portAddr: number, scanBoardAddr: number): Request;
