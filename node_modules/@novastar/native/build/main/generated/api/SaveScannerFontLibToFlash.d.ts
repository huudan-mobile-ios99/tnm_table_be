import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SaveScannerFontLibToFlash(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean): Promise<void>;
        trySaveScannerFontLibToFlash(addr: number, portAddr: number, scanBoardAddr: number): Promise<ErrorType | null>;
    }
}
export default function createSaveScannerFontLibToFlash<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast): Request<Broadcast>;
