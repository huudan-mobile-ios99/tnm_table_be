import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        Set660ConfigFileSendOver(addr: number, bBroadcast: boolean, data: number): Promise<void>;
        trySet660ConfigFileSendOver(addr: number, data: number): Promise<ErrorType | null>;
    }
}
export default function createSet660ConfigFileSendOver<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, data: number): Request<Broadcast>;
