/// <reference types="node" />
import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetScanner_ColorManageCoefData(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, colorRestore: number[] | Buffer): Promise<void>;
        trySetScanner_ColorManageCoefData(addr: number, portAddr: number, scanBoardAddr: number, colorRestore: number[] | Buffer): Promise<ErrorType | null>;
    }
}
export default function createSetScanner_ColorManageCoefData<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, colorRestore: number[] | Buffer): Request<Broadcast>;
