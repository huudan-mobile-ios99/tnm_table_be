/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        GetScanerFunctionTable(Sender: number, portAddr: number, Scan: number): Promise<Buffer>;
        tryGetScanerFunctionTable(Sender: number, portAddr: number, Scan: number): Promise<Packet | null>;
    }
}
export default function createGetScanerFunctionTable(Sender: number, portAddr: number, Scan: number): Request;
