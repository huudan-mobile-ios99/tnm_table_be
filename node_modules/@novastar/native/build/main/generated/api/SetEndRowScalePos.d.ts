import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetEndRowScalePos(addr: number, bBroadcast: boolean, endRowScalePos: number): Promise<void>;
        trySetEndRowScalePos(addr: number, endRowScalePos: number): Promise<ErrorType | null>;
    }
}
export default function createSetEndRowScalePos<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, endRowScalePos: number): Request<Broadcast>;
