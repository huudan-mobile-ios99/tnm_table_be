import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetScaleEn(addr: number, bBroadcast: boolean, isEnableScale: boolean): Promise<void>;
        trySetScaleEn(addr: number, isEnableScale: boolean): Promise<ErrorType | null>;
    }
}
export default function createSetScaleEn<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, isEnableScale: boolean): Request<Broadcast>;
