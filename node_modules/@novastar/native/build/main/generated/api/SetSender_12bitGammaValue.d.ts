import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetSender_12bitGammaValue(addr: number, bBroadcast: boolean, gammaValue: number): Promise<void>;
        trySetSender_12bitGammaValue(addr: number, gammaValue: number): Promise<ErrorType | null>;
    }
}
export default function createSetSender_12bitGammaValue<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, gammaValue: number): Request<Broadcast>;
