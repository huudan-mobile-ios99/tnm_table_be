/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadRowsCtrlByDataGroup(addr: number, portAddr: number, scanBoardAddr: number): Promise<Buffer>;
        tryReadRowsCtrlByDataGroup(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadRowsCtrlByDataGroup(addr: number, portAddr: number, scanBoardAddr: number): Request;
