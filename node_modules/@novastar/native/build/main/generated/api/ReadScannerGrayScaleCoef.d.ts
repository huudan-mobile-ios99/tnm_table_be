/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadScannerGrayScaleCoef(addr: number, portAddr: number, scanBoardAddr: number): Promise<Buffer>;
        tryReadScannerGrayScaleCoef(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadScannerGrayScaleCoef(addr: number, portAddr: number, scanBoardAddr: number): Request;
