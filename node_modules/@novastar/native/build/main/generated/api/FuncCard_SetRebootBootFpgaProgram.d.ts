import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        FuncCard_SetRebootBootFpgaProgram(addr: number, bBroadcast: boolean, funcCardModle: number): Promise<void>;
        tryFuncCard_SetRebootBootFpgaProgram(addr: number, funcCardModle: number): Promise<ErrorType | null>;
    }
}
export default function createFuncCard_SetRebootBootFpgaProgram<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, funcCardModle: number): Request<Broadcast>;
