import { ErrorType, Request } from '@novastar/codec';
import { StandbyModeEnum } from '../StandbyMode';
declare module '@novastar/codec' {
    interface API {
        SetStandbyMode(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, standbyMode: StandbyModeEnum): Promise<void>;
        trySetStandbyMode(addr: number, portAddr: number, scanBoardAddr: number, standbyMode: StandbyModeEnum): Promise<ErrorType | null>;
    }
}
export default function createSetStandbyMode<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, standbyMode: StandbyModeEnum): Request<Broadcast>;
