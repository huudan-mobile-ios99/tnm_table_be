import { ErrorType, Request } from '@novastar/codec';
import { ScanTypeEnum } from '../ScanType';
declare module '@novastar/codec' {
    interface API {
        SetScanMode(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, scanMode: ScanTypeEnum): Promise<void>;
        trySetScanMode(addr: number, portAddr: number, scanBoardAddr: number, scanMode: ScanTypeEnum): Promise<ErrorType | null>;
    }
}
export default function createSetScanMode<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, scanMode: ScanTypeEnum): Request<Broadcast>;
