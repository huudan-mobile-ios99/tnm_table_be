import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadModuelRows(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadModuelRows(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadModuelRows(addr: number, portAddr: number, scanBoardAddr: number): Request;
