import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetToneValue(addr: number, bBroadcast: boolean, dht: number): Promise<void>;
        trySetToneValue(addr: number, dht: number): Promise<ErrorType | null>;
    }
}
export default function createSetToneValue<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, dht: number): Request<Broadcast>;
