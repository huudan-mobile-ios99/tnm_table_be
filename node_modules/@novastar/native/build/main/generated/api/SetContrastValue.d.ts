import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetContrastValue(addr: number, bBroadcast: boolean, dht: number): Promise<void>;
        trySetContrastValue(addr: number, dht: number): Promise<ErrorType | null>;
    }
}
export default function createSetContrastValue<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, dht: number): Request<Broadcast>;
