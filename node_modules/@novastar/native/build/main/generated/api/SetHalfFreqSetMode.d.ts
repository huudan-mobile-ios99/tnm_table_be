import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetHalfFreqSetMode(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, totalDataGroupNum: number): Promise<void>;
        trySetHalfFreqSetMode(addr: number, portAddr: number, scanBoardAddr: number, totalDataGroupNum: number): Promise<ErrorType | null>;
    }
}
export default function createSetHalfFreqSetMode<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, totalDataGroupNum: number): Request<Broadcast>;
