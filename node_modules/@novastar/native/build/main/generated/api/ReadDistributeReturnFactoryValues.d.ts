import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadDistributeReturnFactoryValues(addr: number, portAddr: number, distributeAddr: number): Promise<number>;
        tryReadDistributeReturnFactoryValues(addr: number, portAddr: number, distributeAddr: number): Promise<Packet | null>;
    }
}
export default function createReadDistributeReturnFactoryValues(addr: number, portAddr: number, distributeAddr: number): Request;
