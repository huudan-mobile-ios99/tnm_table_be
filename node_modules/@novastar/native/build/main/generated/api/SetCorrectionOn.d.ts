import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetCorrectionOn(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, bCorrectionOn: boolean): Promise<void>;
        trySetCorrectionOn(addr: number, portAddr: number, scanBoardAddr: number, bCorrectionOn: boolean): Promise<ErrorType | null>;
    }
}
export default function createSetCorrectionOn<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, bCorrectionOn: boolean): Request<Broadcast>;
