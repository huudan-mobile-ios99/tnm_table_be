import { chain } from 'fp-ts/Either';
import { pipe } from 'fp-ts/function';
import * as t from 'io-ts';
const EnumFromString = (enumObj, enumName = 'enum') => new t.Type(enumName, (u) => typeof u === 'number' && Object.values(enumObj).includes(u), (u, c) => typeof u === 'number' && Object.values(enumObj).includes(u)
    ? t.success(u)
    : pipe(t.keyof(enumObj).validate(u, c), chain(s => t.success(enumObj[s]))), a => enumObj[a]);
export default EnumFromString;
//# sourceMappingURL=EnumFromString.js.map