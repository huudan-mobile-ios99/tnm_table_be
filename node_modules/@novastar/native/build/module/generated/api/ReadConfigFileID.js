import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadConfigFileID(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ScannerConfigFileIDOccupancy, 'ReadConfigFileID');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ScannerConfigFileIDAddr;
    return req;
}
Session.prototype.ReadConfigFileID = async function ReadConfigFileID(addr, portAddr, scanBoardAddr) {
    const req = createReadConfigFileID(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadConfigFileID = async function tryReadConfigFileID(addr, portAddr, scanBoardAddr) {
    const req = createReadConfigFileID(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadConfigFileID.js.map