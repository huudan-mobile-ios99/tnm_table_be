import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadLineBias(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.LineBiasOccupancy, 'ReadLineBias');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.LineBiasAddr;
    return req;
}
Session.prototype.ReadLineBias = async function ReadLineBias(addr, portAddr, scanBoardAddr) {
    const req = createReadLineBias(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadLineBias = async function tryReadLineBias(addr, portAddr, scanBoardAddr) {
    const req = createReadLineBias(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadLineBias.js.map