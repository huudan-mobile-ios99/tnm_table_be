import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadDoubleEaraseStateCmd(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.DoubleModelCardSpaceNandFlashEaraseOccupancy, 'ReadDoubleEaraseStateCmd');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.DoubleModelCardSpaceNandFlashEarase;
    return req;
}
Session.prototype.ReadDoubleEaraseStateCmd = async function ReadDoubleEaraseStateCmd(addr, portAddr, scanBoardAddr) {
    const req = createReadDoubleEaraseStateCmd(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadDoubleEaraseStateCmd = async function tryReadDoubleEaraseStateCmd(addr, portAddr, scanBoardAddr) {
    const req = createReadDoubleEaraseStateCmd(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadDoubleEaraseStateCmd.js.map