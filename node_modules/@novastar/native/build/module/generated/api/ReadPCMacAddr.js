import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadPCMacAddr(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.PCMacAddrOccupancy, 'ReadPCMacAddr');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.PCMacAddrAddr;
    return req;
}
Session.prototype.ReadPCMacAddr = async function ReadPCMacAddr(addr, portAddr, scanBoardAddr) {
    const req = createReadPCMacAddr(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadPCMacAddr = async function tryReadPCMacAddr(addr, portAddr, scanBoardAddr) {
    const req = createReadPCMacAddr(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadPCMacAddr.js.map