import { ErrorType, Request } from '@novastar/codec';
import { CardTypeEnum } from '../CardType';
declare module '@novastar/codec' {
    interface API {
        SetDistributeNumberOfCardOrScanBoardInPort(addr: number, portAddr: number, distributeAddr: number, bBroadcast: boolean, number: number, portIndex: number, cardType: CardTypeEnum): Promise<void>;
        trySetDistributeNumberOfCardOrScanBoardInPort(addr: number, portAddr: number, distributeAddr: number, number: number, portIndex: number, cardType: CardTypeEnum): Promise<ErrorType | null>;
    }
}
export default function createSetDistributeNumberOfCardOrScanBoardInPort<Broadcast extends boolean>(addr: number, portAddr: number, distributeAddr: number, bBroadcast: Broadcast, number: number, portIndex: number, cardType: CardTypeEnum): Request<Broadcast>;
