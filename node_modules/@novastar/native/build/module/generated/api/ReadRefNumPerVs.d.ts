import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadRefNumPerVs(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadRefNumPerVs(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadRefNumPerVs(addr: number, portAddr: number, scanBoardAddr: number): Request;
