import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetSender_HLGModel(addr, bBroadcast, hLGModel) {
    const $data = encodeUIntLE(hLGModel, AddressMapping.HLGModelOccupancy);
    const req = new Request($data, bBroadcast, 'SetSender_HLGModel');
    req.destination = addr;
    req.address = AddressMapping.HLGModelAddr;
    return req;
}
Session.prototype.SetSender_HLGModel = async function SetSender_HLGModel(addr, bBroadcast, hLGModel) {
    const req = createSetSender_HLGModel(addr, bBroadcast, hLGModel);
    await this.connection.send(req);
};
Session.prototype.trySetSender_HLGModel = async function trySetSender_HLGModel(addr, hLGModel) {
    const req = createSetSender_HLGModel(addr, false, hLGModel);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetSender_HLGModel.js.map