import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSender_EncryptKey(addr) {
    const req = new Request(AddressMapping.Sender_EncryptKeyOccupancy, 'ReadSender_EncryptKey');
    req.destination = addr;
    req.address = AddressMapping.Sender_EncryptKeyAddr;
    return req;
}
Session.prototype.ReadSender_EncryptKey = async function ReadSender_EncryptKey(addr) {
    const req = createReadSender_EncryptKey(addr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadSender_EncryptKey = async function tryReadSender_EncryptKey(addr) {
    const req = createReadSender_EncryptKey(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSender_EncryptKey.js.map