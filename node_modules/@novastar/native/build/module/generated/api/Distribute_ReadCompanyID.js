import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createDistribute_ReadCompanyID(addr, portAddr, distributeAddr) {
    const req = new Request(AddressMapping.Distribute_CompanyIDOccupancy, 'Distribute_ReadCompanyID');
    req.destination = addr;
    req.deviceType = 3;
    req.port = portAddr;
    req.rcvIndex = distributeAddr;
    req.address = AddressMapping.Distribute_CompanyIDAddr;
    return req;
}
Session.prototype.Distribute_ReadCompanyID = async function Distribute_ReadCompanyID(addr, portAddr, distributeAddr) {
    const req = createDistribute_ReadCompanyID(addr, portAddr, distributeAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryDistribute_ReadCompanyID = async function tryDistribute_ReadCompanyID(addr, portAddr, distributeAddr) {
    const req = createDistribute_ReadCompanyID(addr, portAddr, distributeAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=Distribute_ReadCompanyID.js.map