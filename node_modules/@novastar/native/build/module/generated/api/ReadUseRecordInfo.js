import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadUseRecordInfo(addr, offaddr) {
    const req = new Request(AddressMapping.UseRecordInfoOccupancy, 'ReadUseRecordInfo');
    req.destination = addr;
    req.address = AddressMapping.UseRecordInfoAddr + offaddr;
    return req;
}
Session.prototype.ReadUseRecordInfo = async function ReadUseRecordInfo(addr, offaddr) {
    const req = createReadUseRecordInfo(addr, offaddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadUseRecordInfo = async function tryReadUseRecordInfo(addr, offaddr) {
    const req = createReadUseRecordInfo(addr, offaddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadUseRecordInfo.js.map