import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadCascadeDiretion(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.CascadeDiretionOccupancy, 'ReadCascadeDiretion');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.CascadeDiretionAddr;
    return req;
}
Session.prototype.ReadCascadeDiretion = async function ReadCascadeDiretion(addr, portAddr, scanBoardAddr) {
    const req = createReadCascadeDiretion(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadCascadeDiretion = async function tryReadCascadeDiretion(addr, portAddr, scanBoardAddr) {
    const req = createReadCascadeDiretion(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadCascadeDiretion.js.map