import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetModuleConfigInfo(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = new Request(data, bBroadcast, 'SetModuleConfigInfo');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ModuleConfigInfoAddr;
    return req;
}
Session.prototype.SetModuleConfigInfo = async function SetModuleConfigInfo(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createSetModuleConfigInfo(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.trySetModuleConfigInfo = async function trySetModuleConfigInfo(addr, portAddr, scanBoardAddr, data) {
    const req = createSetModuleConfigInfo(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetModuleConfigInfo.js.map