import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetDHTValue(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, dht: number): Promise<void>;
        trySetDHTValue(addr: number, portAddr: number, scanBoardAddr: number, dht: number): Promise<ErrorType | null>;
    }
}
export default function createSetDHTValue<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, dht: number): Request<Broadcast>;
