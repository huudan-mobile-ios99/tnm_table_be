import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadBackUpFinishFlag(addr) {
    const req = new Request(AddressMapping.BackUpFinishFlagAddrOccupancy, 'ReadBackUpFinishFlag');
    req.destination = addr;
    req.address = AddressMapping.BackUpFinishFlagAddr;
    return req;
}
Session.prototype.ReadBackUpFinishFlag = async function ReadBackUpFinishFlag(addr) {
    const req = createReadBackUpFinishFlag(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadBackUpFinishFlag = async function tryReadBackUpFinishFlag(addr) {
    const req = createReadBackUpFinishFlag(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadBackUpFinishFlag.js.map