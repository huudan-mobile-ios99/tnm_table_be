import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadModuleWidth(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ModuleWidthOccupancy, 'ReadModuleWidth');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ModuleWidthAddr;
    return req;
}
Session.prototype.ReadModuleWidth = async function ReadModuleWidth(addr, portAddr, scanBoardAddr) {
    const req = createReadModuleWidth(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadModuleWidth = async function tryReadModuleWidth(addr, portAddr, scanBoardAddr) {
    const req = createReadModuleWidth(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadModuleWidth.js.map