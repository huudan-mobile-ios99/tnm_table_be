import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSender_DVIRows(addr) {
    const req = new Request(AddressMapping.Sender_DVIRowsOccupancy, 'ReadSender_DVIRows');
    req.destination = addr;
    req.address = AddressMapping.Sender_DVIRowsAddr;
    return req;
}
Session.prototype.ReadSender_DVIRows = async function ReadSender_DVIRows(addr) {
    const req = createReadSender_DVIRows(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadSender_DVIRows = async function tryReadSender_DVIRows(addr) {
    const req = createReadSender_DVIRows(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSender_DVIRows.js.map