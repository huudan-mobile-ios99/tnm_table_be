import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadDoubleCardRecordCmd(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadDoubleCardRecordCmd(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadDoubleCardRecordCmd(addr: number, portAddr: number, scanBoardAddr: number): Request;
