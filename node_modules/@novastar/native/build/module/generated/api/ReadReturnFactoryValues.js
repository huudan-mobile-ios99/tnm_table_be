import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadReturnFactoryValues(addr) {
    const req = new Request(AddressMapping.ReturnFactoryValuesOccupancy, 'ReadReturnFactoryValues');
    req.destination = addr;
    req.address = AddressMapping.ReturnFactoryValuesAddr;
    return req;
}
Session.prototype.ReadReturnFactoryValues = async function ReadReturnFactoryValues(addr) {
    const req = createReadReturnFactoryValues(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadReturnFactoryValues = async function tryReadReturnFactoryValues(addr) {
    const req = createReadReturnFactoryValues(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadReturnFactoryValues.js.map