import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetAudioControl_1(addr, bBroadcast, audioCtrlMode) {
    const $data = encodeUIntLE(audioCtrlMode, AddressMapping.AudioControlOccupancy);
    const req = new Request($data, bBroadcast, 'SetAudioControl_1');
    req.destination = addr;
    req.address = AddressMapping.AudioControlAddr;
    return req;
}
Session.prototype.SetAudioControl_1 = async function SetAudioControl_1(addr, bBroadcast, audioCtrlMode) {
    const req = createSetAudioControl_1(addr, bBroadcast, audioCtrlMode);
    await this.connection.send(req);
};
Session.prototype.trySetAudioControl_1 = async function trySetAudioControl_1(addr, audioCtrlMode) {
    const req = createSetAudioControl_1(addr, false, audioCtrlMode);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetAudioControl_1.js.map