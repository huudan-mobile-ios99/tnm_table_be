/// <reference types="node" />
import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetScanner_DMThird(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, DMThird: number[] | Buffer): Promise<void>;
        trySetScanner_DMThird(addr: number, portAddr: number, scanBoardAddr: number, DMThird: number[] | Buffer): Promise<ErrorType | null>;
    }
}
export default function createSetScanner_DMThird<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, DMThird: number[] | Buffer): Request<Broadcast>;
