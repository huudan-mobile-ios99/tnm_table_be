import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadLocalStartY(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.LocalStartYOccupancy, 'ReadLocalStartY');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.LocalStartYAddr;
    return req;
}
Session.prototype.ReadLocalStartY = async function ReadLocalStartY(addr, portAddr, scanBoardAddr) {
    const req = createReadLocalStartY(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadLocalStartY = async function tryReadLocalStartY(addr, portAddr, scanBoardAddr) {
    const req = createReadLocalStartY(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadLocalStartY.js.map