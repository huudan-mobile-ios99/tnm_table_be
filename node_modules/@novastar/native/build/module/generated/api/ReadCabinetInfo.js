import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadCabinetInfo(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.CabinetOccupancy, 'ReadCabinetInfo');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.CabinetAddr;
    return req;
}
Session.prototype.ReadCabinetInfo = async function ReadCabinetInfo(addr, portAddr, scanBoardAddr) {
    const req = createReadCabinetInfo(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadCabinetInfo = async function tryReadCabinetInfo(addr, portAddr, scanBoardAddr) {
    const req = createReadCabinetInfo(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadCabinetInfo.js.map