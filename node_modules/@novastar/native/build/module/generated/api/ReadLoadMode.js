import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadLoadMode(addr) {
    const req = new Request(AddressMapping.LoadModeOccupancy, 'ReadLoadMode');
    req.destination = addr;
    req.address = AddressMapping.LoadModeAddr;
    return req;
}
Session.prototype.ReadLoadMode = async function ReadLoadMode(addr) {
    const req = createReadLoadMode(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadLoadMode = async function tryReadLoadMode(addr) {
    const req = createReadLoadMode(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadLoadMode.js.map