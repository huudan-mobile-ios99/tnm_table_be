import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadShowLastFrameWhenCableNotConected(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadShowLastFrameWhenCableNotConected(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadShowLastFrameWhenCableNotConected(addr: number, portAddr: number, scanBoardAddr: number): Request;
