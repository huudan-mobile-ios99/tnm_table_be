import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSender_ReadActiveSourceType(addr) {
    const req = new Request(AddressMapping.ActiveSourceTypeOccupancy, 'Sender_ReadActiveSourceType');
    req.destination = addr;
    req.address = AddressMapping.ActiveSourceTypeAddr;
    return req;
}
Session.prototype.Sender_ReadActiveSourceType = async function Sender_ReadActiveSourceType(addr) {
    const req = createSender_ReadActiveSourceType(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.trySender_ReadActiveSourceType = async function trySender_ReadActiveSourceType(addr) {
    const req = createSender_ReadActiveSourceType(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=Sender_ReadActiveSourceType.js.map