import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createWriteScannerDoubleLock(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    if (data.length !== 1)
        throw new TypeError(`Invalid buffer size: ${data.length}`);
    const req = new Request(data, bBroadcast, 'WriteScannerDoubleLock');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ScannerDoubleLockAddr;
    return req;
}
Session.prototype.WriteScannerDoubleLock = async function WriteScannerDoubleLock(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createWriteScannerDoubleLock(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.tryWriteScannerDoubleLock = async function tryWriteScannerDoubleLock(addr, portAddr, scanBoardAddr, data) {
    const req = createWriteScannerDoubleLock(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=WriteScannerDoubleLock.js.map