import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadGroupSwapEn(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadGroupSwapEn(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadGroupSwapEn(addr: number, portAddr: number, scanBoardAddr: number): Request;
