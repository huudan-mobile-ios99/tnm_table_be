import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetAutoCorrectUpload(addr, portAddr, scanBoardAddr, bBroadcast, AutoCorrect) {
    const req = new Request(AutoCorrect ? [85] : [5], bBroadcast, 'SetAutoCorrectUpload');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.AutoCorrectUploadAddr;
    return req;
}
Session.prototype.SetAutoCorrectUpload = async function SetAutoCorrectUpload(addr, portAddr, scanBoardAddr, bBroadcast, AutoCorrect) {
    const req = createSetAutoCorrectUpload(addr, portAddr, scanBoardAddr, bBroadcast, AutoCorrect);
    await this.connection.send(req);
};
Session.prototype.trySetAutoCorrectUpload = async function trySetAutoCorrectUpload(addr, portAddr, scanBoardAddr, AutoCorrect) {
    const req = createSetAutoCorrectUpload(addr, portAddr, scanBoardAddr, false, AutoCorrect);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetAutoCorrectUpload.js.map