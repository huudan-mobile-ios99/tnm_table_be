import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScanner_McuProgramInfo(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.Scanner_McuProgramInfoOccupancy, 'ReadScanner_McuProgramInfo');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Scanner_McuProgramInfoAddr;
    return req;
}
Session.prototype.ReadScanner_McuProgramInfo = async function ReadScanner_McuProgramInfo(addr, portAddr, scanBoardAddr) {
    const req = createReadScanner_McuProgramInfo(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadScanner_McuProgramInfo = async function tryReadScanner_McuProgramInfo(addr, portAddr, scanBoardAddr) {
    const req = createReadScanner_McuProgramInfo(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScanner_McuProgramInfo.js.map