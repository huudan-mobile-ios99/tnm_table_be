import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadDclkHighRatio(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadDclkHighRatio(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadDclkHighRatio(addr: number, portAddr: number, scanBoardAddr: number): Request;
