import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSender_HDRState(addr) {
    const req = new Request(AddressMapping.HDREnableInfoOccupancy, 'ReadSender_HDRState');
    req.destination = addr;
    req.address = AddressMapping.HDRInfoAddr;
    return req;
}
Session.prototype.ReadSender_HDRState = async function ReadSender_HDRState(addr) {
    const req = createReadSender_HDRState(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadSender_HDRState = async function tryReadSender_HDRState(addr) {
    const req = createReadSender_HDRState(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSender_HDRState.js.map