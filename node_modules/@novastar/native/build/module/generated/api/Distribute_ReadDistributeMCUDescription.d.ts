/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        Distribute_ReadDistributeMCUDescription(addr: number, portAddr: number, distributeAddr: number): Promise<Buffer>;
        tryDistribute_ReadDistributeMCUDescription(addr: number, portAddr: number, distributeAddr: number): Promise<Packet | null>;
    }
}
export default function createDistribute_ReadDistributeMCUDescription(addr: number, portAddr: number, distributeAddr: number): Request;
