import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetGrayBit(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, grayBit: number): Promise<void>;
        trySetGrayBit(addr: number, portAddr: number, scanBoardAddr: number, grayBit: number): Promise<ErrorType | null>;
    }
}
export default function createSetGrayBit<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, grayBit: number): Request<Broadcast>;
