import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadDriverFucntion(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadDriverFucntion(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadDriverFucntion(addr: number, portAddr: number, scanBoardAddr: number): Request;
