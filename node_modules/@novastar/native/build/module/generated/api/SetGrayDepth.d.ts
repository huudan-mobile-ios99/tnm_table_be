import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetGrayDepth(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, grayDepth: number): Promise<void>;
        trySetGrayDepth(addr: number, portAddr: number, scanBoardAddr: number, grayDepth: number): Promise<ErrorType | null>;
    }
}
export default function createSetGrayDepth<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, grayDepth: number): Request<Broadcast>;
