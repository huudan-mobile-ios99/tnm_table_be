import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetIrregularCabinetRealGroup(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = new Request(data, bBroadcast, 'SetIrregularCabinetRealGroup');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.IrRegular_RealPhysicalGroupsAddr;
    return req;
}
Session.prototype.SetIrregularCabinetRealGroup = async function SetIrregularCabinetRealGroup(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createSetIrregularCabinetRealGroup(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.trySetIrregularCabinetRealGroup = async function trySetIrregularCabinetRealGroup(addr, portAddr, scanBoardAddr, data) {
    const req = createSetIrregularCabinetRealGroup(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetIrregularCabinetRealGroup.js.map