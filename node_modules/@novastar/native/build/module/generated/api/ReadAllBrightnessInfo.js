import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadAllBrightnessInfo(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.AllBrightnessInfoOccupancy, 'ReadAllBrightnessInfo');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.AllBrightnessInfoAddr;
    return req;
}
Session.prototype.ReadAllBrightnessInfo = async function ReadAllBrightnessInfo(addr, portAddr, scanBoardAddr) {
    const req = createReadAllBrightnessInfo(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadAllBrightnessInfo = async function tryReadAllBrightnessInfo(addr, portAddr, scanBoardAddr) {
    const req = createReadAllBrightnessInfo(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadAllBrightnessInfo.js.map