import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadTotalUnitNumPerSubField(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.TotalUnitNumPerSubFieldOccupancy, 'ReadTotalUnitNumPerSubField');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.TotalUnitNumPerSubFieldAddr;
    return req;
}
Session.prototype.ReadTotalUnitNumPerSubField = async function ReadTotalUnitNumPerSubField(addr, portAddr, scanBoardAddr) {
    const req = createReadTotalUnitNumPerSubField(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadTotalUnitNumPerSubField = async function tryReadTotalUnitNumPerSubField(addr, portAddr, scanBoardAddr) {
    const req = createReadTotalUnitNumPerSubField(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadTotalUnitNumPerSubField.js.map