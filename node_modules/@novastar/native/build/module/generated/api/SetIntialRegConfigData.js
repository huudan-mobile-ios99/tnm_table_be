import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetIntialRegConfigData(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    if (data.length !== AddressMapping.IntialRegConfigOccupancy)
        throw new TypeError(`Invalid buffer size: ${data.length}`);
    const req = new Request(data, bBroadcast, 'SetIntialRegConfigData');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.IntialRegConfigAddr;
    return req;
}
Session.prototype.SetIntialRegConfigData = async function SetIntialRegConfigData(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createSetIntialRegConfigData(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.trySetIntialRegConfigData = async function trySetIntialRegConfigData(addr, portAddr, scanBoardAddr, data) {
    const req = createSetIntialRegConfigData(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetIntialRegConfigData.js.map