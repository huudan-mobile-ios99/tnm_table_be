import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadCtrlEndPoint(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.CtrlEndPointOccupancy, 'ReadCtrlEndPoint');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.CtrlEndPointAddr;
    return req;
}
Session.prototype.ReadCtrlEndPoint = async function ReadCtrlEndPoint(addr, portAddr, scanBoardAddr) {
    const req = createReadCtrlEndPoint(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadCtrlEndPoint = async function tryReadCtrlEndPoint(addr, portAddr, scanBoardAddr) {
    const req = createReadCtrlEndPoint(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadCtrlEndPoint.js.map