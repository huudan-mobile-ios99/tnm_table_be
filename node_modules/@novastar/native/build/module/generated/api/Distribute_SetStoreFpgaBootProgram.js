import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createDistribute_SetStoreFpgaBootProgram(addr, portAddr, distributeAddr, bBroadcast, funcCardModle) {
    const $data = encodeUIntLE(funcCardModle, AddressMapping.Distribute_StoreFpgaBootProgramOccupancy);
    const req = new Request($data, bBroadcast, 'Distribute_SetStoreFpgaBootProgram');
    req.destination = addr;
    req.deviceType = 3;
    req.port = portAddr;
    req.rcvIndex = distributeAddr;
    req.address = AddressMapping.Distribute_StoreFpgaBootProgramAddr;
    return req;
}
Session.prototype.Distribute_SetStoreFpgaBootProgram =
    async function Distribute_SetStoreFpgaBootProgram(addr, portAddr, distributeAddr, bBroadcast, funcCardModle) {
        const req = createDistribute_SetStoreFpgaBootProgram(addr, portAddr, distributeAddr, bBroadcast, funcCardModle);
        await this.connection.send(req);
    };
Session.prototype.tryDistribute_SetStoreFpgaBootProgram =
    async function tryDistribute_SetStoreFpgaBootProgram(addr, portAddr, distributeAddr, funcCardModle) {
        const req = createDistribute_SetStoreFpgaBootProgram(addr, portAddr, distributeAddr, false, funcCardModle);
        return (await this.connection.trySend(req))?.ack ?? null;
    };
//# sourceMappingURL=Distribute_SetStoreFpgaBootProgram.js.map