/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadLowAshCompensation(addr: number, portAddr: number, scanBoardAddr: number): Promise<Buffer>;
        tryReadLowAshCompensation(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadLowAshCompensation(addr: number, portAddr: number, scanBoardAddr: number): Request;
