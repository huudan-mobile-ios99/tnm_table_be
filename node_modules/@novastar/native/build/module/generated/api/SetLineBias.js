import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetLineBias(addr, portAddr, scanBoardAddr, bBroadcast, lineBias) {
    const $data = encodeUIntLE(lineBias, AddressMapping.LineBiasOccupancy);
    const req = new Request($data, bBroadcast, 'SetLineBias');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.LineBiasAddr;
    return req;
}
Session.prototype.SetLineBias = async function SetLineBias(addr, portAddr, scanBoardAddr, bBroadcast, lineBias) {
    const req = createSetLineBias(addr, portAddr, scanBoardAddr, bBroadcast, lineBias);
    await this.connection.send(req);
};
Session.prototype.trySetLineBias = async function trySetLineBias(addr, portAddr, scanBoardAddr, lineBias) {
    const req = createSetLineBias(addr, portAddr, scanBoardAddr, false, lineBias);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetLineBias.js.map