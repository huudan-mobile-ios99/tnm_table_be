import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSender_VideoInputSource(addr) {
    const req = new Request(AddressMapping.Sender_VideoInputSourceOccupancy, 'ReadSender_VideoInputSource');
    req.destination = addr;
    req.address = AddressMapping.Sender_VideoInputSourceAddr;
    return req;
}
Session.prototype.ReadSender_VideoInputSource = async function ReadSender_VideoInputSource(addr) {
    const req = createReadSender_VideoInputSource(addr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadSender_VideoInputSource = async function tryReadSender_VideoInputSource(addr) {
    const req = createReadSender_VideoInputSource(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSender_VideoInputSource.js.map