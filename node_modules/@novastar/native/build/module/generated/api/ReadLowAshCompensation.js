import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadLowAshCompensation(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.LowAshCompensationOccupancy, 'ReadLowAshCompensation');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.LowAshCompensationAddr;
    return req;
}
Session.prototype.ReadLowAshCompensation = async function ReadLowAshCompensation(addr, portAddr, scanBoardAddr) {
    const req = createReadLowAshCompensation(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadLowAshCompensation = async function tryReadLowAshCompensation(addr, portAddr, scanBoardAddr) {
    const req = createReadLowAshCompensation(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadLowAshCompensation.js.map