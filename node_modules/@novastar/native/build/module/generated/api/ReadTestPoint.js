import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadTestPoint(addr) {
    const req = new Request(AddressMapping.TestPointOccupancy, 'ReadTestPoint');
    req.destination = addr;
    req.address = AddressMapping.TestPointAddr;
    return req;
}
Session.prototype.ReadTestPoint = async function ReadTestPoint(addr) {
    const req = createReadTestPoint(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadTestPoint = async function tryReadTestPoint(addr) {
    const req = createReadTestPoint(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadTestPoint.js.map