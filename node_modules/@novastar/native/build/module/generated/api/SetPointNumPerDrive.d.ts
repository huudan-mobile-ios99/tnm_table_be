import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetPointNumPerDrive(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, pointNumPerDrive: number): Promise<void>;
        trySetPointNumPerDrive(addr: number, portAddr: number, scanBoardAddr: number, pointNumPerDrive: number): Promise<ErrorType | null>;
    }
}
export default function createSetPointNumPerDrive<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, pointNumPerDrive: number): Request<Broadcast>;
