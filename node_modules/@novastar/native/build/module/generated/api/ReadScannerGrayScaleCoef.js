import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScannerGrayScaleCoef(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ScannerGrayScaleBlueCoefOccupancy, 'ReadScannerGrayScaleCoef');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Scanner_GrayScaleRedCoefAddr;
    return req;
}
Session.prototype.ReadScannerGrayScaleCoef = async function ReadScannerGrayScaleCoef(addr, portAddr, scanBoardAddr) {
    const req = createReadScannerGrayScaleCoef(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadScannerGrayScaleCoef = async function tryReadScannerGrayScaleCoef(addr, portAddr, scanBoardAddr) {
    const req = createReadScannerGrayScaleCoef(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScannerGrayScaleCoef.js.map