import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        MoveCorrectionFromSlaveFlashToSdram(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean): Promise<void>;
        tryMoveCorrectionFromSlaveFlashToSdram(addr: number, portAddr: number, scanBoardAddr: number): Promise<ErrorType | null>;
    }
}
export default function createMoveCorrectionFromSlaveFlashToSdram<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast): Request<Broadcast>;
