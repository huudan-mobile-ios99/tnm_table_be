import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSourceSingalState(addr) {
    const req = new Request(AddressMapping.SourceSingalStateOccupancy, 'ReadSourceSingalState');
    req.destination = addr;
    req.address = AddressMapping.SourceSingalStateAddr;
    return req;
}
Session.prototype.ReadSourceSingalState = async function ReadSourceSingalState(addr) {
    const req = createReadSourceSingalState(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadSourceSingalState = async function tryReadSourceSingalState(addr) {
    const req = createReadSourceSingalState(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSourceSingalState.js.map