import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetHoldTime(addr, bBroadcast, holdTimeSec) {
    const $data = encodeUIntLE(holdTimeSec, AddressMapping.HoldTimeOccupancy);
    const req = new Request($data, bBroadcast, 'SetHoldTime');
    req.destination = addr;
    req.address = AddressMapping.HoldTimeAddr;
    return req;
}
Session.prototype.SetHoldTime = async function SetHoldTime(addr, bBroadcast, holdTimeSec) {
    const req = createSetHoldTime(addr, bBroadcast, holdTimeSec);
    await this.connection.send(req);
};
Session.prototype.trySetHoldTime = async function trySetHoldTime(addr, holdTimeSec) {
    const req = createSetHoldTime(addr, false, holdTimeSec);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetHoldTime.js.map