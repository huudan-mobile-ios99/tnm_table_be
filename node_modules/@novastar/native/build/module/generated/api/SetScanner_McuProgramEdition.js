import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetScanner_McuProgramEdition(addr, portAddr, scanBoardAddr, bBroadcast, scanner_McuProgramEdition) {
    const $data = encodeUIntLE(scanner_McuProgramEdition, AddressMapping.Scanner_McuProgramEditionOccupancy);
    const req = new Request($data, bBroadcast, 'SetScanner_McuProgramEdition');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Scanner_McuProgramEditionAddr;
    return req;
}
Session.prototype.SetScanner_McuProgramEdition = async function SetScanner_McuProgramEdition(addr, portAddr, scanBoardAddr, bBroadcast, scanner_McuProgramEdition) {
    const req = createSetScanner_McuProgramEdition(addr, portAddr, scanBoardAddr, bBroadcast, scanner_McuProgramEdition);
    await this.connection.send(req);
};
Session.prototype.trySetScanner_McuProgramEdition = async function trySetScanner_McuProgramEdition(addr, portAddr, scanBoardAddr, scanner_McuProgramEdition) {
    const req = createSetScanner_McuProgramEdition(addr, portAddr, scanBoardAddr, false, scanner_McuProgramEdition);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetScanner_McuProgramEdition.js.map