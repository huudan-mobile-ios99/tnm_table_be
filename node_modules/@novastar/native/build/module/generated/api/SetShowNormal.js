import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetShowNormal(addr, bBroadcast, data) {
    const $data = encodeUIntLE(data, AddressMapping.ShowNormalOccupancy);
    const req = new Request($data, bBroadcast, 'SetShowNormal');
    req.destination = addr;
    req.address = AddressMapping.ShowNormalAddr;
    return req;
}
Session.prototype.SetShowNormal = async function SetShowNormal(addr, bBroadcast, data) {
    const req = createSetShowNormal(addr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.trySetShowNormal = async function trySetShowNormal(addr, data) {
    const req = createSetShowNormal(addr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetShowNormal.js.map