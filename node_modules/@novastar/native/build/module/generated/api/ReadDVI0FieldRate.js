import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadDVI0FieldRate(addr) {
    const req = new Request(AddressMapping.DVI0FieldRateOccupancy, 'ReadDVI0FieldRate');
    req.destination = addr;
    req.address = AddressMapping.DVI0FieldRateAddr;
    return req;
}
Session.prototype.ReadDVI0FieldRate = async function ReadDVI0FieldRate(addr) {
    const req = createReadDVI0FieldRate(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadDVI0FieldRate = async function tryReadDVI0FieldRate(addr) {
    const req = createReadDVI0FieldRate(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadDVI0FieldRate.js.map