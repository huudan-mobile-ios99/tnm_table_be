import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetGrayMode(addr, portAddr, scanBoardAddr, bBroadcast, grayRealize, grayMode) {
    const $data = encodeUIntLE((grayRealize << 4) + grayMode, AddressMapping.GrayModeOccupancy);
    const req = new Request($data, bBroadcast, 'SetGrayMode');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.GrayModeAddr;
    return req;
}
Session.prototype.SetGrayMode = async function SetGrayMode(addr, portAddr, scanBoardAddr, bBroadcast, grayRealize, grayMode) {
    const req = createSetGrayMode(addr, portAddr, scanBoardAddr, bBroadcast, grayRealize, grayMode);
    await this.connection.send(req);
};
Session.prototype.trySetGrayMode = async function trySetGrayMode(addr, portAddr, scanBoardAddr, grayRealize, grayMode) {
    const req = createSetGrayMode(addr, portAddr, scanBoardAddr, false, grayRealize, grayMode);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetGrayMode.js.map