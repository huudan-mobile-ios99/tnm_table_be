import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetPortEnable(addr: number, bBroadcast: boolean, portEnable: number, index: number): Promise<void>;
        trySetPortEnable(addr: number, portEnable: number, index: number): Promise<ErrorType | null>;
    }
}
export default function createSetPortEnable<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, portEnable: number, index: number): Request<Broadcast>;
