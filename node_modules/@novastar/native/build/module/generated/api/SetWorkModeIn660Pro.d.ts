import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetWorkModeIn660Pro(addr: number, bBroadcast: boolean, data: number): Promise<void>;
        trySetWorkModeIn660Pro(addr: number, data: number): Promise<ErrorType | null>;
    }
}
export default function createSetWorkModeIn660Pro<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, data: number): Request<Broadcast>;
