import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadClearViewEnableInfo(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ScannerClearViewEnable, 'ReadClearViewEnableInfo');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ScannerClearViewAddr;
    return req;
}
Session.prototype.ReadClearViewEnableInfo = async function ReadClearViewEnableInfo(addr, portAddr, scanBoardAddr) {
    const req = createReadClearViewEnableInfo(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadClearViewEnableInfo = async function tryReadClearViewEnableInfo(addr, portAddr, scanBoardAddr) {
    const req = createReadClearViewEnableInfo(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadClearViewEnableInfo.js.map