import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetControlWidth(addr, portAddr, scanBoardAddr, bBroadcast, controlWidth) {
    const $data = encodeUIntLE(controlWidth, AddressMapping.ControlWidthOccupancy);
    const req = new Request($data, bBroadcast, 'SetControlWidth');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ControlWidthAddr;
    return req;
}
Session.prototype.SetControlWidth = async function SetControlWidth(addr, portAddr, scanBoardAddr, bBroadcast, controlWidth) {
    const req = createSetControlWidth(addr, portAddr, scanBoardAddr, bBroadcast, controlWidth);
    await this.connection.send(req);
};
Session.prototype.trySetControlWidth = async function trySetControlWidth(addr, portAddr, scanBoardAddr, controlWidth) {
    const req = createSetControlWidth(addr, portAddr, scanBoardAddr, false, controlWidth);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetControlWidth.js.map