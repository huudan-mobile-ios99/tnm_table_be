import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetRealDviWidth(addr, bBroadcast, dviWidth) {
    const $data = encodeUIntLE(dviWidth, AddressMapping.RealDviWidthOccupancy);
    const req = new Request($data, bBroadcast, 'SetRealDviWidth');
    req.destination = addr;
    req.address = AddressMapping.RealDviWidthAddr;
    return req;
}
Session.prototype.SetRealDviWidth = async function SetRealDviWidth(addr, bBroadcast, dviWidth) {
    const req = createSetRealDviWidth(addr, bBroadcast, dviWidth);
    await this.connection.send(req);
};
Session.prototype.trySetRealDviWidth = async function trySetRealDviWidth(addr, dviWidth) {
    const req = createSetRealDviWidth(addr, false, dviWidth);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetRealDviWidth.js.map