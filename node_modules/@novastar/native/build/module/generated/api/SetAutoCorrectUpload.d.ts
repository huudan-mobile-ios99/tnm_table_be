import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetAutoCorrectUpload(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, AutoCorrect: boolean): Promise<void>;
        trySetAutoCorrectUpload(addr: number, portAddr: number, scanBoardAddr: number, AutoCorrect: boolean): Promise<ErrorType | null>;
    }
}
export default function createSetAutoCorrectUpload<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, AutoCorrect: boolean): Request<Broadcast>;
