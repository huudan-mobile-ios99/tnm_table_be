import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadDVI1FieldRate(addr) {
    const req = new Request(AddressMapping.DVI1FieldRateOccupancy, 'ReadDVI1FieldRate');
    req.destination = addr;
    req.address = AddressMapping.DVI1FieldRateAddr;
    return req;
}
Session.prototype.ReadDVI1FieldRate = async function ReadDVI1FieldRate(addr) {
    const req = createReadDVI1FieldRate(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadDVI1FieldRate = async function tryReadDVI1FieldRate(addr) {
    const req = createReadDVI1FieldRate(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadDVI1FieldRate.js.map