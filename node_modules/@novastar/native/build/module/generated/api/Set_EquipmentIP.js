import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSet_EquipmentIP(addr, bBoradcast, data) {
    const req = new Request(data, bBoradcast, 'Set_EquipmentIP');
    req.destination = addr;
    req.address = AddressMapping.Sender_VideoEquipmentIPAddr;
    return req;
}
Session.prototype.Set_EquipmentIP = async function Set_EquipmentIP(addr, bBoradcast, data) {
    const req = createSet_EquipmentIP(addr, bBoradcast, data);
    await this.connection.send(req);
};
Session.prototype.trySet_EquipmentIP = async function trySet_EquipmentIP(addr, data) {
    const req = createSet_EquipmentIP(addr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=Set_EquipmentIP.js.map