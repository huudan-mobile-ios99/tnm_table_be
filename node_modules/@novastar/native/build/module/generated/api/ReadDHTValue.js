import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadDHTValue(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.DHTOccupancy, 'ReadDHTValue');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.DHTAddr;
    return req;
}
Session.prototype.ReadDHTValue = async function ReadDHTValue(addr, portAddr, scanBoardAddr) {
    const req = createReadDHTValue(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadDHTValue = async function tryReadDHTValue(addr, portAddr, scanBoardAddr) {
    const req = createReadDHTValue(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadDHTValue.js.map