import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetMCUWorkProgramStore(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, MCUWorkProgramStore: number): Promise<void>;
        trySetMCUWorkProgramStore(addr: number, portAddr: number, scanBoardAddr: number, MCUWorkProgramStore: number): Promise<ErrorType | null>;
    }
}
export default function createSetMCUWorkProgramStore<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, MCUWorkProgramStore: number): Request<Broadcast>;
