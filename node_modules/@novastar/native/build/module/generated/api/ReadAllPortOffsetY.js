import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadAllPortOffsetY(addr) {
    const req = new Request(AddressMapping.Sender_AllDataSpaceOccupancy, 'ReadAllPortOffsetY');
    req.destination = addr;
    req.address = AddressMapping.Sender_AllDataAddr;
    return req;
}
Session.prototype.ReadAllPortOffsetY = async function ReadAllPortOffsetY(addr) {
    const req = createReadAllPortOffsetY(addr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadAllPortOffsetY = async function tryReadAllPortOffsetY(addr) {
    const req = createReadAllPortOffsetY(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadAllPortOffsetY.js.map