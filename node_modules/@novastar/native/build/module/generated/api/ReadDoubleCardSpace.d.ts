import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadDoubleCardSpace(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadDoubleCardSpace(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadDoubleCardSpace(addr: number, portAddr: number, scanBoardAddr: number): Request;
