import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetGlobalBrightness(addr, portAddr, scanBoardAddr, bBroadcast, globalBrightness) {
    const $data = encodeUIntLE(globalBrightness, AddressMapping.GlobalBrightnessOccupancy);
    const req = new Request($data, bBroadcast, 'SetGlobalBrightness');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.GlobalBrightnessAddr;
    return req;
}
Session.prototype.SetGlobalBrightness = async function SetGlobalBrightness(addr, portAddr, scanBoardAddr, bBroadcast, globalBrightness) {
    const req = createSetGlobalBrightness(addr, portAddr, scanBoardAddr, bBroadcast, globalBrightness);
    await this.connection.send(req);
};
Session.prototype.trySetGlobalBrightness = async function trySetGlobalBrightness(addr, portAddr, scanBoardAddr, globalBrightness) {
    const req = createSetGlobalBrightness(addr, portAddr, scanBoardAddr, false, globalBrightness);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetGlobalBrightness.js.map