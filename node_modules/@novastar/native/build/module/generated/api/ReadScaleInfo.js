import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScaleInfo(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ScaleInfoOccupancy, 'ReadScaleInfo');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ScaleInfoAddr;
    return req;
}
Session.prototype.ReadScaleInfo = async function ReadScaleInfo(addr, portAddr, scanBoardAddr) {
    const req = createReadScaleInfo(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadScaleInfo = async function tryReadScaleInfo(addr, portAddr, scanBoardAddr) {
    const req = createReadScaleInfo(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScaleInfo.js.map