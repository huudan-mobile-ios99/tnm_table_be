/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadScanner_MutiChipRamA(addr: number, portAddr: number, scanBoardAddr: number, readLength: number): Promise<Buffer>;
        tryReadScanner_MutiChipRamA(addr: number, portAddr: number, scanBoardAddr: number, readLength: number): Promise<Packet | null>;
    }
}
export default function createReadScanner_MutiChipRamA(addr: number, portAddr: number, scanBoardAddr: number, readLength: number): Request;
