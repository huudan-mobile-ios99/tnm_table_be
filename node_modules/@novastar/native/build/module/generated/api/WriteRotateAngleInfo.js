import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createWriteRotateAngleInfo(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    if (data.length !== AddressMapping.RotateAngleOccupancy)
        throw new TypeError(`Invalid buffer size: ${data.length}`);
    const req = new Request(data, bBroadcast, 'WriteRotateAngleInfo');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.RotateAngleAddr;
    return req;
}
Session.prototype.WriteRotateAngleInfo = async function WriteRotateAngleInfo(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createWriteRotateAngleInfo(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.tryWriteRotateAngleInfo = async function tryWriteRotateAngleInfo(addr, portAddr, scanBoardAddr, data) {
    const req = createWriteRotateAngleInfo(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=WriteRotateAngleInfo.js.map