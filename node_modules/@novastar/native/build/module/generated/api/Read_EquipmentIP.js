import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createRead_EquipmentIP(addr) {
    const req = new Request(AddressMapping.Sender_VideoEquipmentIPOccupancy, 'Read_EquipmentIP');
    req.destination = addr;
    req.address = AddressMapping.Sender_VideoEquipmentIPAddr;
    return req;
}
Session.prototype.Read_EquipmentIP = async function Read_EquipmentIP(addr) {
    const req = createRead_EquipmentIP(addr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryRead_EquipmentIP = async function tryRead_EquipmentIP(addr) {
    const req = createRead_EquipmentIP(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=Read_EquipmentIP.js.map