import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadIsSerdes(addr) {
    const req = new Request(AddressMapping.SerdesEnableOccupancy, 'ReadIsSerdes');
    req.destination = addr;
    req.address = AddressMapping.SerdesEnableAddr;
    return req;
}
Session.prototype.ReadIsSerdes = async function ReadIsSerdes(addr) {
    const req = createReadIsSerdes(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadIsSerdes = async function tryReadIsSerdes(addr) {
    const req = createReadIsSerdes(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadIsSerdes.js.map