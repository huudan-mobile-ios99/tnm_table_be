import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadTwentyDataGroup(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadTwentyDataGroup(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadTwentyDataGroup(addr: number, portAddr: number, scanBoardAddr: number): Request;
