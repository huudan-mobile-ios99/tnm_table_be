import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadModuelRows(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ModuleRowsOccupancy, 'ReadModuelRows');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ModuleRowsAddr;
    return req;
}
Session.prototype.ReadModuelRows = async function ReadModuelRows(addr, portAddr, scanBoardAddr) {
    const req = createReadModuelRows(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadModuelRows = async function tryReadModuelRows(addr, portAddr, scanBoardAddr) {
    const req = createReadModuelRows(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadModuelRows.js.map