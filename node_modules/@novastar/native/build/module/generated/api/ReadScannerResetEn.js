import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScannerResetEn(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ResetEnPointOccupancy, 'ReadScannerResetEn');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ScannerResetEnAddr;
    return req;
}
Session.prototype.ReadScannerResetEn = async function ReadScannerResetEn(addr, portAddr, scanBoardAddr) {
    const req = createReadScannerResetEn(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadScannerResetEn = async function tryReadScannerResetEn(addr, portAddr, scanBoardAddr) {
    const req = createReadScannerResetEn(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScannerResetEn.js.map