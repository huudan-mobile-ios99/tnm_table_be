import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createWriteDviOf4KInfo(addr, bBroadcast, dviInfoBytes) {
    const req = new Request(dviInfoBytes, bBroadcast, 'WriteDviOf4KInfo');
    req.destination = addr;
    req.address = AddressMapping.DVIOf4KInfoAddr;
    return req;
}
Session.prototype.WriteDviOf4KInfo = async function WriteDviOf4KInfo(addr, bBroadcast, dviInfoBytes) {
    const req = createWriteDviOf4KInfo(addr, bBroadcast, dviInfoBytes);
    await this.connection.send(req);
};
Session.prototype.tryWriteDviOf4KInfo = async function tryWriteDviOf4KInfo(addr, dviInfoBytes) {
    const req = createWriteDviOf4KInfo(addr, false, dviInfoBytes);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=WriteDviOf4KInfo.js.map