import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadDviMode(addr) {
    const req = new Request(AddressMapping.DviModeOccupancy, 'ReadDviMode');
    req.destination = addr;
    req.address = AddressMapping.DviModeAddr;
    return req;
}
Session.prototype.ReadDviMode = async function ReadDviMode(addr) {
    const req = createReadDviMode(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadDviMode = async function tryReadDviMode(addr) {
    const req = createReadDviMode(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadDviMode.js.map