import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSender_VideoRealDvi(addr) {
    const req = new Request(AddressMapping.Sender_VideoRealDviAddrOccupancy, 'ReadSender_VideoRealDvi');
    req.destination = addr;
    req.address = AddressMapping.Sender_VideoRealDviAddr;
    return req;
}
Session.prototype.ReadSender_VideoRealDvi = async function ReadSender_VideoRealDvi(addr) {
    const req = createReadSender_VideoRealDvi(addr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadSender_VideoRealDvi = async function tryReadSender_VideoRealDvi(addr) {
    const req = createReadSender_VideoRealDvi(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSender_VideoRealDvi.js.map