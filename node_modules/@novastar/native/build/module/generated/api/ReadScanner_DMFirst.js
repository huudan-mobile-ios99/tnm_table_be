import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScanner_DMFirst(addr, portAddr, scanBoardAddr, readLength) {
    const req = new Request(readLength, 'ReadScanner_DMFirst');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Scanner_DmFirstAddr;
    return req;
}
Session.prototype.ReadScanner_DMFirst = async function ReadScanner_DMFirst(addr, portAddr, scanBoardAddr, readLength) {
    const req = createReadScanner_DMFirst(addr, portAddr, scanBoardAddr, readLength);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadScanner_DMFirst = async function tryReadScanner_DMFirst(addr, portAddr, scanBoardAddr, readLength) {
    const req = createReadScanner_DMFirst(addr, portAddr, scanBoardAddr, readLength);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScanner_DMFirst.js.map