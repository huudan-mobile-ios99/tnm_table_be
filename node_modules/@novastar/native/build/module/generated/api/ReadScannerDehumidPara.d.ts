import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadScannerDehumidPara(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadScannerDehumidPara(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadScannerDehumidPara(addr: number, portAddr: number, scanBoardAddr: number): Request;
