import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetSenderConrectionMode(addr, bBroadcast, isOpen) {
    const req = new Request(isOpen ? [168] : [0], bBroadcast, 'SetSenderConrectionMode');
    req.destination = addr;
    req.address = AddressMapping.SenderCorrectionModeAddr;
    return req;
}
Session.prototype.SetSenderConrectionMode = async function SetSenderConrectionMode(addr, bBroadcast, isOpen) {
    const req = createSetSenderConrectionMode(addr, bBroadcast, isOpen);
    await this.connection.send(req);
};
Session.prototype.trySetSenderConrectionMode = async function trySetSenderConrectionMode(addr, isOpen) {
    const req = createSetSenderConrectionMode(addr, false, isOpen);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetSenderConrectionMode.js.map