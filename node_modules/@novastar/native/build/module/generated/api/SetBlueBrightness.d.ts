import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetBlueBrightness(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, blueBrightness: number): Promise<void>;
        trySetBlueBrightness(addr: number, portAddr: number, scanBoardAddr: number, blueBrightness: number): Promise<ErrorType | null>;
    }
}
export default function createSetBlueBrightness<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, blueBrightness: number): Request<Broadcast>;
