import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadFieldRateMode(addr) {
    const req = new Request(AddressMapping.FieldRateModeOccupancy, 'ReadFieldRateMode');
    req.destination = addr;
    req.address = AddressMapping.FieldRateModeAddr;
    return req;
}
Session.prototype.ReadFieldRateMode = async function ReadFieldRateMode(addr) {
    const req = createReadFieldRateMode(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadFieldRateMode = async function tryReadFieldRateMode(addr) {
    const req = createReadFieldRateMode(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadFieldRateMode.js.map