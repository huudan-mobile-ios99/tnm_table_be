import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createWriteSixthGroupRegistorGain(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    if (data.length !== AddressMapping.SixthGroupRegistorGainOccupancy)
        throw new TypeError(`Invalid buffer size: ${data.length}`);
    const req = new Request(data, bBroadcast, 'WriteSixthGroupRegistorGain');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.SixthGroupRegistorGainAddr;
    return req;
}
Session.prototype.WriteSixthGroupRegistorGain = async function WriteSixthGroupRegistorGain(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createWriteSixthGroupRegistorGain(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.tryWriteSixthGroupRegistorGain = async function tryWriteSixthGroupRegistorGain(addr, portAddr, scanBoardAddr, data) {
    const req = createWriteSixthGroupRegistorGain(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=WriteSixthGroupRegistorGain.js.map