import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSender_WebProgramEdition(addr) {
    const req = new Request(AddressMapping.Sender_WebProgramEditionOccupancy, 'ReadSender_WebProgramEdition');
    req.destination = addr;
    req.address = AddressMapping.Sender_WebProgramEditAddr;
    return req;
}
Session.prototype.ReadSender_WebProgramEdition = async function ReadSender_WebProgramEdition(addr) {
    const req = createReadSender_WebProgramEdition(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadSender_WebProgramEdition = async function tryReadSender_WebProgramEdition(addr) {
    const req = createReadSender_WebProgramEdition(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSender_WebProgramEdition.js.map