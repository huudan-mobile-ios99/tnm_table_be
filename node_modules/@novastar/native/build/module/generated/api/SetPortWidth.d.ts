import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetPortWidth(addr: number, bBroadcast: boolean, portWidth: number, index: number): Promise<void>;
        trySetPortWidth(addr: number, portWidth: number, index: number): Promise<ErrorType | null>;
    }
}
export default function createSetPortWidth<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, portWidth: number, index: number): Request<Broadcast>;
