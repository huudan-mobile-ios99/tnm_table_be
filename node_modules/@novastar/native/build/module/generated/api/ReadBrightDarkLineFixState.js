import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadBrightDarkLineFixState(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.BrightDarkLineFixStateOccupancy, 'ReadBrightDarkLineFixState');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.BrightDarkLineFixStateAddr;
    return req;
}
Session.prototype.ReadBrightDarkLineFixState = async function ReadBrightDarkLineFixState(addr, portAddr, scanBoardAddr) {
    const req = createReadBrightDarkLineFixState(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadBrightDarkLineFixState = async function tryReadBrightDarkLineFixState(addr, portAddr, scanBoardAddr) {
    const req = createReadBrightDarkLineFixState(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadBrightDarkLineFixState.js.map