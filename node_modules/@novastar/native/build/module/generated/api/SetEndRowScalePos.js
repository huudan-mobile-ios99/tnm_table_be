import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetEndRowScalePos(addr, bBroadcast, endRowScalePos) {
    const $data = encodeUIntLE(endRowScalePos, AddressMapping.EndRowScalePosOccupancy);
    const req = new Request($data, bBroadcast, 'SetEndRowScalePos');
    req.destination = addr;
    req.address = AddressMapping.EndRowScalePosAddr;
    return req;
}
Session.prototype.SetEndRowScalePos = async function SetEndRowScalePos(addr, bBroadcast, endRowScalePos) {
    const req = createSetEndRowScalePos(addr, bBroadcast, endRowScalePos);
    await this.connection.send(req);
};
Session.prototype.trySetEndRowScalePos = async function trySetEndRowScalePos(addr, endRowScalePos) {
    const req = createSetEndRowScalePos(addr, false, endRowScalePos);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetEndRowScalePos.js.map