import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createDistribute_ReadDistributeMCUDescription(addr, portAddr, distributeAddr) {
    const req = new Request(AddressMapping.Distribute_MCUDescriptionOccupancy, 'Distribute_ReadDistributeMCUDescription');
    req.destination = addr;
    req.deviceType = 3;
    req.port = portAddr;
    req.rcvIndex = distributeAddr;
    req.address = AddressMapping.Distribute_MCUDescriptionAddr;
    return req;
}
Session.prototype.Distribute_ReadDistributeMCUDescription =
    async function Distribute_ReadDistributeMCUDescription(addr, portAddr, distributeAddr) {
        const req = createDistribute_ReadDistributeMCUDescription(addr, portAddr, distributeAddr);
        return (await this.connection.send(req)).data;
    };
Session.prototype.tryDistribute_ReadDistributeMCUDescription =
    async function tryDistribute_ReadDistributeMCUDescription(addr, portAddr, distributeAddr) {
        const req = createDistribute_ReadDistributeMCUDescription(addr, portAddr, distributeAddr);
        return this.connection.trySend(req);
    };
//# sourceMappingURL=Distribute_ReadDistributeMCUDescription.js.map