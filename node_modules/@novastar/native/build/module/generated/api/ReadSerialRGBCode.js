import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSerialRGBCode(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.SerialRGBCodeOccupancy, 'ReadSerialRGBCode');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.SerialRGBCodeAddr;
    return req;
}
Session.prototype.ReadSerialRGBCode = async function ReadSerialRGBCode(addr, portAddr, scanBoardAddr) {
    const req = createReadSerialRGBCode(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadSerialRGBCode = async function tryReadSerialRGBCode(addr, portAddr, scanBoardAddr) {
    const req = createReadSerialRGBCode(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSerialRGBCode.js.map