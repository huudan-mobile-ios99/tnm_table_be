import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetGclkPhase(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, gclkPhase: number): Promise<void>;
        trySetGclkPhase(addr: number, portAddr: number, scanBoardAddr: number, gclkPhase: number): Promise<ErrorType | null>;
    }
}
export default function createSetGclkPhase<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, gclkPhase: number): Request<Broadcast>;
