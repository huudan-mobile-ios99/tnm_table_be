import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadAllPortOffsetX(addr, index) {
    const req = new Request(AddressMapping.Sender_AllDataSpaceOccupancy, 'ReadAllPortOffsetX');
    req.destination = addr;
    req.address = AddressMapping.Sender_AllDataAddr + AddressMapping.PortOccupancy * index;
    return req;
}
Session.prototype.ReadAllPortOffsetX = async function ReadAllPortOffsetX(addr, index) {
    const req = createReadAllPortOffsetX(addr, index);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadAllPortOffsetX = async function tryReadAllPortOffsetX(addr, index) {
    const req = createReadAllPortOffsetX(addr, index);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadAllPortOffsetX.js.map