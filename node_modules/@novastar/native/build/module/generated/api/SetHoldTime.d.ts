import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetHoldTime(addr: number, bBroadcast: boolean, holdTimeSec: number): Promise<void>;
        trySetHoldTime(addr: number, holdTimeSec: number): Promise<ErrorType | null>;
    }
}
export default function createSetHoldTime<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, holdTimeSec: number): Request<Broadcast>;
