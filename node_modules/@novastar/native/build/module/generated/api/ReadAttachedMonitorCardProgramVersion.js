import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadAttachedMonitorCardProgramVersion(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.AttachedMonitorCardProgramVersionOccupancy, 'ReadAttachedMonitorCardProgramVersion');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.AttachedMonitorCardProgramVersionAddr;
    return req;
}
Session.prototype.ReadAttachedMonitorCardProgramVersion =
    async function ReadAttachedMonitorCardProgramVersion(addr, portAddr, scanBoardAddr) {
        const req = createReadAttachedMonitorCardProgramVersion(addr, portAddr, scanBoardAddr);
        return decodeUIntLE(await this.connection.send(req));
    };
Session.prototype.tryReadAttachedMonitorCardProgramVersion =
    async function tryReadAttachedMonitorCardProgramVersion(addr, portAddr, scanBoardAddr) {
        const req = createReadAttachedMonitorCardProgramVersion(addr, portAddr, scanBoardAddr);
        return this.connection.trySend(req);
    };
//# sourceMappingURL=ReadAttachedMonitorCardProgramVersion.js.map