import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetRefNumPerVs(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, refNumPerVs: number): Promise<void>;
        trySetRefNumPerVs(addr: number, portAddr: number, scanBoardAddr: number, refNumPerVs: number): Promise<ErrorType | null>;
    }
}
export default function createSetRefNumPerVs<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, refNumPerVs: number): Request<Broadcast>;
