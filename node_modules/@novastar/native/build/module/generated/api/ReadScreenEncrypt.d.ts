import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadScreenEncrypt(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadScreenEncrypt(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadScreenEncrypt(addr: number, portAddr: number, scanBoardAddr: number): Request;
