import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetScanConrectionMode(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, isOpen: boolean): Promise<void>;
        trySetScanConrectionMode(addr: number, portAddr: number, scanBoardAddr: number, isOpen: boolean): Promise<ErrorType | null>;
    }
}
export default function createSetScanConrectionMode<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, isOpen: boolean): Request<Broadcast>;
