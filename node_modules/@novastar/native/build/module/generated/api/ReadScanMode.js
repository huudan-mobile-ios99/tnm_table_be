import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScanMode(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ScanModeOccupancy, 'ReadScanMode');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ScanModeAddr;
    return req;
}
Session.prototype.ReadScanMode = async function ReadScanMode(addr, portAddr, scanBoardAddr) {
    const req = createReadScanMode(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadScanMode = async function tryReadScanMode(addr, portAddr, scanBoardAddr) {
    const req = createReadScanMode(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScanMode.js.map