import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadGrayMode(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.GrayModeOccupancy, 'ReadGrayMode');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.GrayModeAddr;
    return req;
}
Session.prototype.ReadGrayMode = async function ReadGrayMode(addr, portAddr, scanBoardAddr) {
    const req = createReadGrayMode(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadGrayMode = async function tryReadGrayMode(addr, portAddr, scanBoardAddr) {
    const req = createReadGrayMode(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadGrayMode.js.map