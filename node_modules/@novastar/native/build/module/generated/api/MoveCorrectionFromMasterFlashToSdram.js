import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createMoveCorrectionFromMasterFlashToSdram(addr, portAddr, scanBoardAddr, bBroadcast) {
    const req = new Request([5], bBroadcast, 'MoveCorrectionFromMasterFlashToSdram');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Correction_ApplicationAddr;
    return req;
}
Session.prototype.MoveCorrectionFromMasterFlashToSdram =
    async function MoveCorrectionFromMasterFlashToSdram(addr, portAddr, scanBoardAddr, bBroadcast) {
        const req = createMoveCorrectionFromMasterFlashToSdram(addr, portAddr, scanBoardAddr, bBroadcast);
        await this.connection.send(req);
    };
Session.prototype.tryMoveCorrectionFromMasterFlashToSdram =
    async function tryMoveCorrectionFromMasterFlashToSdram(addr, portAddr, scanBoardAddr) {
        const req = createMoveCorrectionFromMasterFlashToSdram(addr, portAddr, scanBoardAddr, false);
        return (await this.connection.trySend(req))?.ack ?? null;
    };
//# sourceMappingURL=MoveCorrectionFromMasterFlashToSdram.js.map