import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadLanguageLibraryFlag(addr) {
    const req = new Request(AddressMapping.LanguageLibraryFlagOccupancy, 'ReadLanguageLibraryFlag');
    req.destination = addr;
    req.address = AddressMapping.LanguageLibraryFlagAddr;
    return req;
}
Session.prototype.ReadLanguageLibraryFlag = async function ReadLanguageLibraryFlag(addr) {
    const req = createReadLanguageLibraryFlag(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadLanguageLibraryFlag = async function tryReadLanguageLibraryFlag(addr) {
    const req = createReadLanguageLibraryFlag(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadLanguageLibraryFlag.js.map