import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadDoubleCardEaraseState(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadDoubleCardEaraseState(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadDoubleCardEaraseState(addr: number, portAddr: number, scanBoardAddr: number): Request;
