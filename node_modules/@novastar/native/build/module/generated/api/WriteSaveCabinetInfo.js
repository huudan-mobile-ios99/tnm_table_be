import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createWriteSaveCabinetInfo(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    if (data.length !== AddressMapping.FlashCabinetOccupancy)
        throw new TypeError(`Invalid buffer size: ${data.length}`);
    const req = new Request(data, bBroadcast, 'WriteSaveCabinetInfo');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.FlashCabinetAddr;
    return req;
}
Session.prototype.WriteSaveCabinetInfo = async function WriteSaveCabinetInfo(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createWriteSaveCabinetInfo(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.tryWriteSaveCabinetInfo = async function tryWriteSaveCabinetInfo(addr, portAddr, scanBoardAddr, data) {
    const req = createWriteSaveCabinetInfo(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=WriteSaveCabinetInfo.js.map