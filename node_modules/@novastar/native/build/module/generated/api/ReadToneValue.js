import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadToneValue(addr) {
    const req = new Request(AddressMapping.SenderToneResultOccupancy, 'ReadToneValue');
    req.destination = addr;
    req.address = AddressMapping.SenderToneResultddr;
    return req;
}
Session.prototype.ReadToneValue = async function ReadToneValue(addr) {
    const req = createReadToneValue(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadToneValue = async function tryReadToneValue(addr) {
    const req = createReadToneValue(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadToneValue.js.map