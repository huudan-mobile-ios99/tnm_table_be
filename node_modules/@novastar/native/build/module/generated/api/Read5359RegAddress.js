import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createRead5359RegAddress(addr, portAddr, scanBoardAddr, dataLength) {
    const req = new Request(dataLength, 'Read5359RegAddress');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Register5359Addres;
    return req;
}
Session.prototype.Read5359RegAddress = async function Read5359RegAddress(addr, portAddr, scanBoardAddr, dataLength) {
    const req = createRead5359RegAddress(addr, portAddr, scanBoardAddr, dataLength);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryRead5359RegAddress = async function tryRead5359RegAddress(addr, portAddr, scanBoardAddr, dataLength) {
    const req = createRead5359RegAddress(addr, portAddr, scanBoardAddr, dataLength);
    return this.connection.trySend(req);
};
//# sourceMappingURL=Read5359RegAddress.js.map