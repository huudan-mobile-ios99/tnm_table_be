/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadScanner_LowGrayCodeTable(addr: number, portAddr: number, scanBoardAddr: number): Promise<Buffer>;
        tryReadScanner_LowGrayCodeTable(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadScanner_LowGrayCodeTable(addr: number, portAddr: number, scanBoardAddr: number): Request;
