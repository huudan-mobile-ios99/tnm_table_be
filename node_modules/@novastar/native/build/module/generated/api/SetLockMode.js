import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetLockMode(addr, portAddr, scanBoardAddr, bBroadcast, lockMode) {
    const $data = encodeUIntLE(lockMode, AddressMapping.LockModeOccupancy);
    const req = new Request($data, bBroadcast, 'SetLockMode');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.LockModeAddr;
    return req;
}
Session.prototype.SetLockMode = async function SetLockMode(addr, portAddr, scanBoardAddr, bBroadcast, lockMode) {
    const req = createSetLockMode(addr, portAddr, scanBoardAddr, bBroadcast, lockMode);
    await this.connection.send(req);
};
Session.prototype.trySetLockMode = async function trySetLockMode(addr, portAddr, scanBoardAddr, lockMode) {
    const req = createSetLockMode(addr, portAddr, scanBoardAddr, false, lockMode);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetLockMode.js.map