import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetABCDRollOver(addr, portAddr, scanBoardAddr, bBroadcast, isRollOver) {
    const req = new Request(!isRollOver ? Buffer.alloc(1) : [1], bBroadcast, 'SetABCDRollOver');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ABCDRollOverAddr;
    return req;
}
Session.prototype.SetABCDRollOver = async function SetABCDRollOver(addr, portAddr, scanBoardAddr, bBroadcast, isRollOver) {
    const req = createSetABCDRollOver(addr, portAddr, scanBoardAddr, bBroadcast, isRollOver);
    await this.connection.send(req);
};
Session.prototype.trySetABCDRollOver = async function trySetABCDRollOver(addr, portAddr, scanBoardAddr, isRollOver) {
    const req = createSetABCDRollOver(addr, portAddr, scanBoardAddr, false, isRollOver);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetABCDRollOver.js.map