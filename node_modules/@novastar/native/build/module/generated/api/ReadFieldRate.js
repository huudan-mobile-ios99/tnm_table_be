import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadFieldRate(addr) {
    const req = new Request(AddressMapping.SetFieldRateOccupancy, 'ReadFieldRate');
    req.destination = addr;
    req.address = AddressMapping.SetFieldRateAddr;
    return req;
}
Session.prototype.ReadFieldRate = async function ReadFieldRate(addr) {
    const req = createReadFieldRate(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadFieldRate = async function tryReadFieldRate(addr) {
    const req = createReadFieldRate(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadFieldRate.js.map