import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadScanner_FPGAProgramEdition(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadScanner_FPGAProgramEdition(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadScanner_FPGAProgramEdition(addr: number, portAddr: number, scanBoardAddr: number): Request;
