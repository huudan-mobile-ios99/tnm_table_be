import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSender_HDRInfo(addr) {
    const req = new Request(AddressMapping.HDRInfoOccupancy, 'ReadSender_HDRInfo');
    req.destination = addr;
    req.address = AddressMapping.HDRInfoAddr;
    return req;
}
Session.prototype.ReadSender_HDRInfo = async function ReadSender_HDRInfo(addr) {
    const req = createReadSender_HDRInfo(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadSender_HDRInfo = async function tryReadSender_HDRInfo(addr) {
    const req = createReadSender_HDRInfo(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSender_HDRInfo.js.map