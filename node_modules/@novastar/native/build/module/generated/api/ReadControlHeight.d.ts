import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadControlHeight(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadControlHeight(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadControlHeight(addr: number, portAddr: number, scanBoardAddr: number): Request;
