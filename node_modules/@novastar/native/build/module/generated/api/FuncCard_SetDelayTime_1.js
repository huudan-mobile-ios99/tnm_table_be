import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createFuncCard_SetDelayTime_1(addr, portAddr, funcCardAddr, bBroadcast, delayTime) {
    const $data = encodeUIntLE(delayTime, AddressMapping.FuncCard_DelayTimeOccupancy);
    const req = new Request($data, bBroadcast, 'FuncCard_SetDelayTime_1');
    req.destination = addr;
    req.deviceType = 2;
    req.port = portAddr;
    req.rcvIndex = funcCardAddr;
    req.address = AddressMapping.FuncCard_DelayTimeAddr;
    return req;
}
Session.prototype.FuncCard_SetDelayTime_1 = async function FuncCard_SetDelayTime_1(addr, portAddr, funcCardAddr, bBroadcast, delayTime) {
    const req = createFuncCard_SetDelayTime_1(addr, portAddr, funcCardAddr, bBroadcast, delayTime);
    await this.connection.send(req);
};
Session.prototype.tryFuncCard_SetDelayTime_1 = async function tryFuncCard_SetDelayTime_1(addr, portAddr, funcCardAddr, delayTime) {
    const req = createFuncCard_SetDelayTime_1(addr, portAddr, funcCardAddr, false, delayTime);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=FuncCard_SetDelayTime_1.js.map