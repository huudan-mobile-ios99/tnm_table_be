import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        Set2038SDelayTime(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, delayTime: number): Promise<void>;
        trySet2038SDelayTime(addr: number, portAddr: number, scanBoardAddr: number, delayTime: number): Promise<ErrorType | null>;
    }
}
export default function createSet2038SDelayTime<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, delayTime: number): Request<Broadcast>;
