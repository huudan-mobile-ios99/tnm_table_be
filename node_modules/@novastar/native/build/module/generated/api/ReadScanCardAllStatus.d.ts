import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadScanCardAllStatus(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadScanCardAllStatus(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadScanCardAllStatus(addr: number, portAddr: number, scanBoardAddr: number): Request;
