import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSender_VideoScreenOffset(addr) {
    const req = new Request(AddressMapping.Sender_VideoScreenOffsetOccupancy, 'ReadSender_VideoScreenOffset');
    req.destination = addr;
    req.address = AddressMapping.Sender_VideoScreenOffsetAddr;
    return req;
}
Session.prototype.ReadSender_VideoScreenOffset = async function ReadSender_VideoScreenOffset(addr) {
    const req = createReadSender_VideoScreenOffset(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadSender_VideoScreenOffset = async function tryReadSender_VideoScreenOffset(addr) {
    const req = createReadSender_VideoScreenOffset(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSender_VideoScreenOffset.js.map