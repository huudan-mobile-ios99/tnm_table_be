import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetScanner_DMPM(addr, portAddr, scanBoardAddr, bBroadcast, DMPM) {
    if (DMPM.length !== 0)
        throw new TypeError(`Invalid buffer size: ${DMPM.length}`);
    const req = new Request(DMPM, bBroadcast, 'SetScanner_DMPM');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Scanner_DmPMAAddr;
    return req;
}
Session.prototype.SetScanner_DMPM = async function SetScanner_DMPM(addr, portAddr, scanBoardAddr, bBroadcast, DMPM) {
    const req = createSetScanner_DMPM(addr, portAddr, scanBoardAddr, bBroadcast, DMPM);
    await this.connection.send(req);
};
Session.prototype.trySetScanner_DMPM = async function trySetScanner_DMPM(addr, portAddr, scanBoardAddr, DMPM) {
    const req = createSetScanner_DMPM(addr, portAddr, scanBoardAddr, false, DMPM);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetScanner_DMPM.js.map