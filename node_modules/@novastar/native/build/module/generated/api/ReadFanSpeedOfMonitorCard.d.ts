/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadFanSpeedOfMonitorCard(addr: number, portAddr: number, scanBoardAddr: number, readCountOfFan: number, beginIdnexOfFan: number): Promise<Buffer>;
        tryReadFanSpeedOfMonitorCard(addr: number, portAddr: number, scanBoardAddr: number, readCountOfFan: number, beginIdnexOfFan: number): Promise<Packet | null>;
    }
}
export default function createReadFanSpeedOfMonitorCard(addr: number, portAddr: number, scanBoardAddr: number, readCountOfFan: number, beginIdnexOfFan: number): Request;
