import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadTotalLightCdfRes(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadTotalLightCdfRes(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadTotalLightCdfRes(addr: number, portAddr: number, scanBoardAddr: number): Request;
