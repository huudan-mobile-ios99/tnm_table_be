import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetModuleHeight(addr, portAddr, scanBoardAddr, bBroadcast, moduleHeight) {
    const $data = encodeUIntLE(moduleHeight, AddressMapping.ModuleHeightOccupancy);
    const req = new Request($data, bBroadcast, 'SetModuleHeight');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ModuleHeightAddr;
    return req;
}
Session.prototype.SetModuleHeight = async function SetModuleHeight(addr, portAddr, scanBoardAddr, bBroadcast, moduleHeight) {
    const req = createSetModuleHeight(addr, portAddr, scanBoardAddr, bBroadcast, moduleHeight);
    await this.connection.send(req);
};
Session.prototype.trySetModuleHeight = async function trySetModuleHeight(addr, portAddr, scanBoardAddr, moduleHeight) {
    const req = createSetModuleHeight(addr, portAddr, scanBoardAddr, false, moduleHeight);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetModuleHeight.js.map