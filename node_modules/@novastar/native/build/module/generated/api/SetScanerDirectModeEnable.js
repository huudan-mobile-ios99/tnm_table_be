import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetScanerDirectModeEnable(addr, portAddr, scanBoardAddr, broadcast, enable) {
    const req = new Request(enable ? [5] : [0], broadcast, 'SetScanerDirectModeEnable');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ScannderDirectModeEnableAddr;
    return req;
}
Session.prototype.SetScanerDirectModeEnable = async function SetScanerDirectModeEnable(addr, portAddr, scanBoardAddr, broadcast, enable) {
    const req = createSetScanerDirectModeEnable(addr, portAddr, scanBoardAddr, broadcast, enable);
    await this.connection.send(req);
};
Session.prototype.trySetScanerDirectModeEnable = async function trySetScanerDirectModeEnable(addr, portAddr, scanBoardAddr, enable) {
    const req = createSetScanerDirectModeEnable(addr, portAddr, scanBoardAddr, false, enable);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetScanerDirectModeEnable.js.map