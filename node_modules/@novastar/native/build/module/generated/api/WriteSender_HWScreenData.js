import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createWriteSender_HWScreenData(addr, bBoradcast, data) {
    const req = new Request(data, bBoradcast, 'WriteSender_HWScreenData');
    req.destination = addr;
    req.address = AddressMapping.Sender_HWScreenDisplayDataAddr;
    return req;
}
Session.prototype.WriteSender_HWScreenData = async function WriteSender_HWScreenData(addr, bBoradcast, data) {
    const req = createWriteSender_HWScreenData(addr, bBoradcast, data);
    await this.connection.send(req);
};
Session.prototype.tryWriteSender_HWScreenData = async function tryWriteSender_HWScreenData(addr, data) {
    const req = createWriteSender_HWScreenData(addr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=WriteSender_HWScreenData.js.map