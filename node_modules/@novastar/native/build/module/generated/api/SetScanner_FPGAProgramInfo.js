import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetScanner_FPGAProgramInfo(addr, portAddr, scanBoardAddr, bBroadcast, scanner_FPGAProgramRemarks) {
    if (scanner_FPGAProgramRemarks.length !== AddressMapping.Scanner_FPGAProgramInfoOccupancy)
        throw new TypeError(`Invalid buffer size: ${scanner_FPGAProgramRemarks.length}`);
    const req = new Request(scanner_FPGAProgramRemarks, bBroadcast, 'SetScanner_FPGAProgramInfo');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Scanner_FPGAProgramInfoAddr;
    return req;
}
Session.prototype.SetScanner_FPGAProgramInfo = async function SetScanner_FPGAProgramInfo(addr, portAddr, scanBoardAddr, bBroadcast, scanner_FPGAProgramRemarks) {
    const req = createSetScanner_FPGAProgramInfo(addr, portAddr, scanBoardAddr, bBroadcast, scanner_FPGAProgramRemarks);
    await this.connection.send(req);
};
Session.prototype.trySetScanner_FPGAProgramInfo = async function trySetScanner_FPGAProgramInfo(addr, portAddr, scanBoardAddr, scanner_FPGAProgramRemarks) {
    const req = createSetScanner_FPGAProgramInfo(addr, portAddr, scanBoardAddr, false, scanner_FPGAProgramRemarks);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetScanner_FPGAProgramInfo.js.map