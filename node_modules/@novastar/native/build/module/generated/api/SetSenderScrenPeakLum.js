import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetSenderScrenPeakLum(addr, bBroadcast, info) {
    const req = new Request(info, bBroadcast, 'SetSenderScrenPeakLum');
    req.destination = addr;
    req.address = AddressMapping.ScrenPeakLumAddr;
    return req;
}
Session.prototype.SetSenderScrenPeakLum = async function SetSenderScrenPeakLum(addr, bBroadcast, info) {
    const req = createSetSenderScrenPeakLum(addr, bBroadcast, info);
    await this.connection.send(req);
};
Session.prototype.trySetSenderScrenPeakLum = async function trySetSenderScrenPeakLum(addr, info) {
    const req = createSetSenderScrenPeakLum(addr, false, info);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetSenderScrenPeakLum.js.map