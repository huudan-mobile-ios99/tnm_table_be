import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetModuleWidth(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, moduleWidth: number): Promise<void>;
        trySetModuleWidth(addr: number, portAddr: number, scanBoardAddr: number, moduleWidth: number): Promise<ErrorType | null>;
    }
}
export default function createSetModuleWidth<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, moduleWidth: number): Request<Broadcast>;
