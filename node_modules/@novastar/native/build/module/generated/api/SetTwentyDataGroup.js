import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetTwentyDataGroup(addr, portAddr, scanBoardAddr, bBroadcast, isEanbleTwentyDataGroup) {
    const req = new Request(isEanbleTwentyDataGroup ? [5] : [0], bBroadcast, 'SetTwentyDataGroup');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.TwentyDataGroupAddr;
    return req;
}
Session.prototype.SetTwentyDataGroup = async function SetTwentyDataGroup(addr, portAddr, scanBoardAddr, bBroadcast, isEanbleTwentyDataGroup) {
    const req = createSetTwentyDataGroup(addr, portAddr, scanBoardAddr, bBroadcast, isEanbleTwentyDataGroup);
    await this.connection.send(req);
};
Session.prototype.trySetTwentyDataGroup = async function trySetTwentyDataGroup(addr, portAddr, scanBoardAddr, isEanbleTwentyDataGroup) {
    const req = createSetTwentyDataGroup(addr, portAddr, scanBoardAddr, false, isEanbleTwentyDataGroup);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetTwentyDataGroup.js.map