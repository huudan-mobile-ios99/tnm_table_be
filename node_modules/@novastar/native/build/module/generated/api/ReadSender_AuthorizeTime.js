import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSender_AuthorizeTime(addr) {
    const req = new Request(AddressMapping.Sender_AuthorizeTimeOccupancy, 'ReadSender_AuthorizeTime');
    req.destination = addr;
    req.address = AddressMapping.Sender_AuthorizeTimeAddr;
    return req;
}
Session.prototype.ReadSender_AuthorizeTime = async function ReadSender_AuthorizeTime(addr) {
    const req = createReadSender_AuthorizeTime(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadSender_AuthorizeTime = async function tryReadSender_AuthorizeTime(addr) {
    const req = createReadSender_AuthorizeTime(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSender_AuthorizeTime.js.map