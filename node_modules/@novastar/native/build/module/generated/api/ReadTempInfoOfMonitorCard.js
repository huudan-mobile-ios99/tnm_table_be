import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadTempInfoOfMonitorCard(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.Scanner_TempInfoOfMonitorCardOccupancy, 'ReadTempInfoOfMonitorCard');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Scanner_TempInfoOfMonitorCardAddr;
    return req;
}
Session.prototype.ReadTempInfoOfMonitorCard = async function ReadTempInfoOfMonitorCard(addr, portAddr, scanBoardAddr) {
    const req = createReadTempInfoOfMonitorCard(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadTempInfoOfMonitorCard = async function tryReadTempInfoOfMonitorCard(addr, portAddr, scanBoardAddr) {
    const req = createReadTempInfoOfMonitorCard(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadTempInfoOfMonitorCard.js.map