import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadTVCardDVI0OffsetX(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.TVCardDVI0OffsetXOccupancy, 'ReadTVCardDVI0OffsetX');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.TVCardDVI0OffsetXAddr;
    return req;
}
Session.prototype.ReadTVCardDVI0OffsetX = async function ReadTVCardDVI0OffsetX(addr, portAddr, scanBoardAddr) {
    const req = createReadTVCardDVI0OffsetX(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadTVCardDVI0OffsetX = async function tryReadTVCardDVI0OffsetX(addr, portAddr, scanBoardAddr) {
    const req = createReadTVCardDVI0OffsetX(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadTVCardDVI0OffsetX.js.map