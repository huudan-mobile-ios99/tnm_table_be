import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadRestoreFinishFlag(addr, length) {
    const req = new Request(length, 'ReadRestoreFinishFlag');
    req.destination = addr;
    req.address = AddressMapping.RestoreFinishFlagAddr;
    return req;
}
Session.prototype.ReadRestoreFinishFlag = async function ReadRestoreFinishFlag(addr, length) {
    const req = createReadRestoreFinishFlag(addr, length);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadRestoreFinishFlag = async function tryReadRestoreFinishFlag(addr, length) {
    const req = createReadRestoreFinishFlag(addr, length);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadRestoreFinishFlag.js.map