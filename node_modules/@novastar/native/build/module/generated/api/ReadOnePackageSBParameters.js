import { Request, Session } from '@novastar/codec';
import MaxValueInfo from '../MaxValueInfo';
export default function createReadOnePackageSBParameters(addr, portAddr, scanBoardAddr) {
    const req = new Request(MaxValueInfo.MAX_PARAMETER_TABLE_LEN, 'ReadOnePackageSBParameters');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = 33554432;
    return req;
}
Session.prototype.ReadOnePackageSBParameters = async function ReadOnePackageSBParameters(addr, portAddr, scanBoardAddr) {
    const req = createReadOnePackageSBParameters(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadOnePackageSBParameters = async function tryReadOnePackageSBParameters(addr, portAddr, scanBoardAddr) {
    const req = createReadOnePackageSBParameters(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadOnePackageSBParameters.js.map