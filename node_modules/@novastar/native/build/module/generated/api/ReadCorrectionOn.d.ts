import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadCorrectionOn(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadCorrectionOn(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadCorrectionOn(addr: number, portAddr: number, scanBoardAddr: number): Request;
