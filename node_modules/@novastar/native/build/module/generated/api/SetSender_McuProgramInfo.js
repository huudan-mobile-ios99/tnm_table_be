import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetSender_McuProgramInfo(addr, portAddr, scanBoardAddr, bBroadcast, sender_McuProgramRemarks) {
    if (sender_McuProgramRemarks.length !== AddressMapping.Sender_McuProgramInfoOccupancy)
        throw new TypeError(`Invalid buffer size: ${sender_McuProgramRemarks.length}`);
    const req = new Request(sender_McuProgramRemarks, bBroadcast, 'SetSender_McuProgramInfo');
    req.destination = addr;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Sender_McuProgramInfoAddr;
    return req;
}
Session.prototype.SetSender_McuProgramInfo = async function SetSender_McuProgramInfo(addr, portAddr, scanBoardAddr, bBroadcast, sender_McuProgramRemarks) {
    const req = createSetSender_McuProgramInfo(addr, portAddr, scanBoardAddr, bBroadcast, sender_McuProgramRemarks);
    await this.connection.send(req);
};
Session.prototype.trySetSender_McuProgramInfo = async function trySetSender_McuProgramInfo(addr, portAddr, scanBoardAddr, sender_McuProgramRemarks) {
    const req = createSetSender_McuProgramInfo(addr, portAddr, scanBoardAddr, false, sender_McuProgramRemarks);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetSender_McuProgramInfo.js.map