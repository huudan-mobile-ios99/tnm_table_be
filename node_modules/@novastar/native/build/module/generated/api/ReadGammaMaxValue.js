import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadGammaMaxValue(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.GammaMaxValue2053Occupancy, 'ReadGammaMaxValue');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.GammaMaxValue2053Addr;
    return req;
}
Session.prototype.ReadGammaMaxValue = async function ReadGammaMaxValue(addr, portAddr, scanBoardAddr) {
    const req = createReadGammaMaxValue(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadGammaMaxValue = async function tryReadGammaMaxValue(addr, portAddr, scanBoardAddr) {
    const req = createReadGammaMaxValue(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadGammaMaxValue.js.map