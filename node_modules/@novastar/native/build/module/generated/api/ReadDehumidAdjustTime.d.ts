import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadDehumidAdjustTime(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadDehumidAdjustTime(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadDehumidAdjustTime(addr: number, portAddr: number, scanBoardAddr: number): Request;
