import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadAttachedMonitorCardExist(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.AttachedMonitorCardExistOccupancy, 'ReadAttachedMonitorCardExist');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.AttachedMonitorCardExistAddr;
    return req;
}
Session.prototype.ReadAttachedMonitorCardExist = async function ReadAttachedMonitorCardExist(addr, portAddr, scanBoardAddr) {
    const req = createReadAttachedMonitorCardExist(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadAttachedMonitorCardExist = async function tryReadAttachedMonitorCardExist(addr, portAddr, scanBoardAddr) {
    const req = createReadAttachedMonitorCardExist(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadAttachedMonitorCardExist.js.map