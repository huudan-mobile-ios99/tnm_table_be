/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadScanerBurningProgramPackage(addr: number, portAddr: number, scanBoardAddr: number, readLength: number): Promise<Buffer>;
        tryReadScanerBurningProgramPackage(addr: number, portAddr: number, scanBoardAddr: number, readLength: number): Promise<Packet | null>;
    }
}
export default function createReadScanerBurningProgramPackage(addr: number, portAddr: number, scanBoardAddr: number, readLength: number): Request;
