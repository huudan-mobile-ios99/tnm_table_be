import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetColScalePara(addr: number, bBroadcast: boolean, colScalePar: number): Promise<void>;
        trySetColScalePara(addr: number, colScalePar: number): Promise<ErrorType | null>;
    }
}
export default function createSetColScalePara<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, colScalePar: number): Request<Broadcast>;
