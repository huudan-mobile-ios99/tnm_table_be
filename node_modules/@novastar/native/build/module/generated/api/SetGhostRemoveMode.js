import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetGhostRemoveMode(addr, portAddr, scanBoardAddr, bBroadcast, ghostRemoveMode) {
    const $data = encodeUIntLE(ghostRemoveMode, AddressMapping.GhostRemoveModeOccupancy);
    const req = new Request($data, bBroadcast, 'SetGhostRemoveMode');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.GhostRemoveModeAddr;
    return req;
}
Session.prototype.SetGhostRemoveMode = async function SetGhostRemoveMode(addr, portAddr, scanBoardAddr, bBroadcast, ghostRemoveMode) {
    const req = createSetGhostRemoveMode(addr, portAddr, scanBoardAddr, bBroadcast, ghostRemoveMode);
    await this.connection.send(req);
};
Session.prototype.trySetGhostRemoveMode = async function trySetGhostRemoveMode(addr, portAddr, scanBoardAddr, ghostRemoveMode) {
    const req = createSetGhostRemoveMode(addr, portAddr, scanBoardAddr, false, ghostRemoveMode);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetGhostRemoveMode.js.map