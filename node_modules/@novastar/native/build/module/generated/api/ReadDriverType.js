import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadDriverType(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.DriverTypeOccupancy, 'ReadDriverType');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.DriverTypeAddr;
    return req;
}
Session.prototype.ReadDriverType = async function ReadDriverType(addr, portAddr, scanBoardAddr) {
    const req = createReadDriverType(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadDriverType = async function tryReadDriverType(addr, portAddr, scanBoardAddr) {
    const req = createReadDriverType(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadDriverType.js.map