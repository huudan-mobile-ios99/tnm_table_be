/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        Read2055RegisterData(addr: number, portAddr: number, scanBoardAddr: number): Promise<Buffer>;
        tryRead2055RegisterData(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createRead2055RegisterData(addr: number, portAddr: number, scanBoardAddr: number): Request;
