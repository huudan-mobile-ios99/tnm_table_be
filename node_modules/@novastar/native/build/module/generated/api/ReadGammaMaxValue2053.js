import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadGammaMaxValue2053(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.GammaMaxValue2053Occupancy, 'ReadGammaMaxValue2053');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.GammaMaxValue2053Addr;
    return req;
}
Session.prototype.ReadGammaMaxValue2053 = async function ReadGammaMaxValue2053(addr, portAddr, scanBoardAddr) {
    const req = createReadGammaMaxValue2053(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadGammaMaxValue2053 = async function tryReadGammaMaxValue2053(addr, portAddr, scanBoardAddr) {
    const req = createReadGammaMaxValue2053(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadGammaMaxValue2053.js.map