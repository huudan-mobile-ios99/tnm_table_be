import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetDclkHighRatio(addr, portAddr, scanBoardAddr, bBroadcast, dclkHighRatio) {
    const $data = encodeUIntLE(dclkHighRatio, AddressMapping.DclkHighRatioOccupancy);
    const req = new Request($data, bBroadcast, 'SetDclkHighRatio');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.DclkHighRatioAddr;
    return req;
}
Session.prototype.SetDclkHighRatio = async function SetDclkHighRatio(addr, portAddr, scanBoardAddr, bBroadcast, dclkHighRatio) {
    const req = createSetDclkHighRatio(addr, portAddr, scanBoardAddr, bBroadcast, dclkHighRatio);
    await this.connection.send(req);
};
Session.prototype.trySetDclkHighRatio = async function trySetDclkHighRatio(addr, portAddr, scanBoardAddr, dclkHighRatio) {
    const req = createSetDclkHighRatio(addr, portAddr, scanBoardAddr, false, dclkHighRatio);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetDclkHighRatio.js.map