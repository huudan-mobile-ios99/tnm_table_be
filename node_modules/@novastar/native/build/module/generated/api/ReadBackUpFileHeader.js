import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadBackUpFileHeader(addr) {
    const req = new Request(AddressMapping.BackUpFileHeaderAddrOccupancy, 'ReadBackUpFileHeader');
    req.destination = addr;
    req.address = AddressMapping.BackUpFileHeaderAddr;
    return req;
}
Session.prototype.ReadBackUpFileHeader = async function ReadBackUpFileHeader(addr) {
    const req = createReadBackUpFileHeader(addr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadBackUpFileHeader = async function tryReadBackUpFileHeader(addr) {
    const req = createReadBackUpFileHeader(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadBackUpFileHeader.js.map