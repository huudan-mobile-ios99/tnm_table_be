import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSet5pinControlLight(addr, portAddr, scanBoardAddr, bBroadcast, tem) {
    const $data = encodeUIntLE(tem, AddressMapping.ControlLightOccupancy);
    const req = new Request($data, bBroadcast, 'Set5pinControlLight');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ControlLightAddr;
    return req;
}
Session.prototype.Set5pinControlLight = async function Set5pinControlLight(addr, portAddr, scanBoardAddr, bBroadcast, tem) {
    const req = createSet5pinControlLight(addr, portAddr, scanBoardAddr, bBroadcast, tem);
    await this.connection.send(req);
};
Session.prototype.trySet5pinControlLight = async function trySet5pinControlLight(addr, portAddr, scanBoardAddr, tem) {
    const req = createSet5pinControlLight(addr, portAddr, scanBoardAddr, false, tem);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=Set5pinControlLight.js.map