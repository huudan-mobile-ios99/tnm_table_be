import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        Switch2053IP(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, switchIP2053Enable: boolean): Promise<void>;
        trySwitch2053IP(addr: number, portAddr: number, scanBoardAddr: number, switchIP2053Enable: boolean): Promise<ErrorType | null>;
    }
}
export default function createSwitch2053IP<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, switchIP2053Enable: boolean): Request<Broadcast>;
