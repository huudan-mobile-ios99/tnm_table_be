import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadControlHeight(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ControlHeightOccupancy, 'ReadControlHeight');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ControlHeightAddr;
    return req;
}
Session.prototype.ReadControlHeight = async function ReadControlHeight(addr, portAddr, scanBoardAddr) {
    const req = createReadControlHeight(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadControlHeight = async function tryReadControlHeight(addr, portAddr, scanBoardAddr) {
    const req = createReadControlHeight(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadControlHeight.js.map