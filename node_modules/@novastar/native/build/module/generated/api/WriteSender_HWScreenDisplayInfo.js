import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createWriteSender_HWScreenDisplayInfo(addr, bBoradcast, data) {
    const req = new Request(data, bBoradcast, 'WriteSender_HWScreenDisplayInfo');
    req.destination = addr;
    req.address = AddressMapping.Sender_HWScreenDisplayBaseAddr;
    return req;
}
Session.prototype.WriteSender_HWScreenDisplayInfo = async function WriteSender_HWScreenDisplayInfo(addr, bBoradcast, data) {
    const req = createWriteSender_HWScreenDisplayInfo(addr, bBoradcast, data);
    await this.connection.send(req);
};
Session.prototype.tryWriteSender_HWScreenDisplayInfo =
    async function tryWriteSender_HWScreenDisplayInfo(addr, data) {
        const req = createWriteSender_HWScreenDisplayInfo(addr, false, data);
        return (await this.connection.trySend(req))?.ack ?? null;
    };
//# sourceMappingURL=WriteSender_HWScreenDisplayInfo.js.map