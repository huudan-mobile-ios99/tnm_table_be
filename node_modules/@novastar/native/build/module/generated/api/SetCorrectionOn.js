import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetCorrectionOn(addr, portAddr, scanBoardAddr, bBroadcast, bCorrectionOn) {
    const req = new Request(bCorrectionOn ? [1] : [0], bBroadcast, 'SetCorrectionOn');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.CorrectionOnAddr;
    return req;
}
Session.prototype.SetCorrectionOn = async function SetCorrectionOn(addr, portAddr, scanBoardAddr, bBroadcast, bCorrectionOn) {
    const req = createSetCorrectionOn(addr, portAddr, scanBoardAddr, bBroadcast, bCorrectionOn);
    await this.connection.send(req);
};
Session.prototype.trySetCorrectionOn = async function trySetCorrectionOn(addr, portAddr, scanBoardAddr, bCorrectionOn) {
    const req = createSetCorrectionOn(addr, portAddr, scanBoardAddr, false, bCorrectionOn);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetCorrectionOn.js.map