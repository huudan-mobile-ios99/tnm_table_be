/// <reference types="node" />
import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetCorrectionOnROELine(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, correctionInfo: number[] | Buffer): Promise<void>;
        trySetCorrectionOnROELine(addr: number, portAddr: number, scanBoardAddr: number, correctionInfo: number[] | Buffer): Promise<ErrorType | null>;
    }
}
export default function createSetCorrectionOnROELine<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, correctionInfo: number[] | Buffer): Request<Broadcast>;
