import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createWriteFourthGroupRegistorGain(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    if (data.length !== AddressMapping.FourthGroupRegistorGainOccupancy)
        throw new TypeError(`Invalid buffer size: ${data.length}`);
    const req = new Request(data, bBroadcast, 'WriteFourthGroupRegistorGain');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.FourthGroupRegistorGainAddr;
    return req;
}
Session.prototype.WriteFourthGroupRegistorGain = async function WriteFourthGroupRegistorGain(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createWriteFourthGroupRegistorGain(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.tryWriteFourthGroupRegistorGain = async function tryWriteFourthGroupRegistorGain(addr, portAddr, scanBoardAddr, data) {
    const req = createWriteFourthGroupRegistorGain(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=WriteFourthGroupRegistorGain.js.map