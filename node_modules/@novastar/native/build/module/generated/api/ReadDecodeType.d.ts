import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadDecodeType(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadDecodeType(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadDecodeType(addr: number, portAddr: number, scanBoardAddr: number): Request;
