import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetWorkModeIn660Pro(addr, bBroadcast, data) {
    const req = new Request([data], bBroadcast, 'SetWorkModeIn660Pro');
    req.destination = addr;
    req.address = AddressMapping.WorkModeIn660ProAddr;
    return req;
}
Session.prototype.SetWorkModeIn660Pro = async function SetWorkModeIn660Pro(addr, bBroadcast, data) {
    const req = createSetWorkModeIn660Pro(addr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.trySetWorkModeIn660Pro = async function trySetWorkModeIn660Pro(addr, data) {
    const req = createSetWorkModeIn660Pro(addr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetWorkModeIn660Pro.js.map