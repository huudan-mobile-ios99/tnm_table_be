import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadABCDRollOver(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ABCDRollOverOccupancy, 'ReadABCDRollOver');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ABCDRollOverAddr;
    return req;
}
Session.prototype.ReadABCDRollOver = async function ReadABCDRollOver(addr, portAddr, scanBoardAddr) {
    const req = createReadABCDRollOver(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadABCDRollOver = async function tryReadABCDRollOver(addr, portAddr, scanBoardAddr) {
    const req = createReadABCDRollOver(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadABCDRollOver.js.map