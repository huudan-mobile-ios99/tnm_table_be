import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        StartAutoLinearTable(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean): Promise<void>;
        tryStartAutoLinearTable(addr: number, portAddr: number, scanBoardAddr: number): Promise<ErrorType | null>;
    }
}
export default function createStartAutoLinearTable<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast): Request<Broadcast>;
