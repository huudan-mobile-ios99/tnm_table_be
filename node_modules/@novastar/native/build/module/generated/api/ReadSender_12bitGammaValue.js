import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSender_12bitGammaValue(addr) {
    const req = new Request(AddressMapping.Sender_12bitGammaValueOccupancy, 'ReadSender_12bitGammaValue');
    req.destination = addr;
    req.address = AddressMapping.Sender_12bitGammaValueAddr;
    return req;
}
Session.prototype.ReadSender_12bitGammaValue = async function ReadSender_12bitGammaValue(addr) {
    const req = createReadSender_12bitGammaValue(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadSender_12bitGammaValue = async function tryReadSender_12bitGammaValue(addr) {
    const req = createReadSender_12bitGammaValue(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSender_12bitGammaValue.js.map