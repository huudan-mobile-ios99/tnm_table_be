import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadLockMode(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.LockModeOccupancy, 'ReadLockMode');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.LockModeAddr;
    return req;
}
Session.prototype.ReadLockMode = async function ReadLockMode(addr, portAddr, scanBoardAddr) {
    const req = createReadLockMode(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadLockMode = async function tryReadLockMode(addr, portAddr, scanBoardAddr) {
    const req = createReadLockMode(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadLockMode.js.map