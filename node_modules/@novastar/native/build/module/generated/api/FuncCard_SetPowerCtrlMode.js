import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createFuncCard_SetPowerCtrlMode(addr, bBroadcast, powerCtrlMode) {
    const $data = encodeUIntLE(powerCtrlMode, AddressMapping.FuncCard_PowerCtrlModeOccupancy);
    const req = new Request($data, bBroadcast, 'FuncCard_SetPowerCtrlMode');
    req.destination = addr;
    req.address = AddressMapping.FuncCard_PowerCtrlModeAddr;
    return req;
}
Session.prototype.FuncCard_SetPowerCtrlMode = async function FuncCard_SetPowerCtrlMode(addr, bBroadcast, powerCtrlMode) {
    const req = createFuncCard_SetPowerCtrlMode(addr, bBroadcast, powerCtrlMode);
    await this.connection.send(req);
};
Session.prototype.tryFuncCard_SetPowerCtrlMode = async function tryFuncCard_SetPowerCtrlMode(addr, powerCtrlMode) {
    const req = createFuncCard_SetPowerCtrlMode(addr, false, powerCtrlMode);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=FuncCard_SetPowerCtrlMode.js.map