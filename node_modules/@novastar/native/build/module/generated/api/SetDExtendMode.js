import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetDExtendMode(addr, portAddr, scanBoardAddr, bBroadcast, extendMode) {
    const $data = encodeUIntLE(extendMode, AddressMapping.DExtendModeOccupancy);
    const req = new Request($data, bBroadcast, 'SetDExtendMode');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.DExtendModeAddr;
    return req;
}
Session.prototype.SetDExtendMode = async function SetDExtendMode(addr, portAddr, scanBoardAddr, bBroadcast, extendMode) {
    const req = createSetDExtendMode(addr, portAddr, scanBoardAddr, bBroadcast, extendMode);
    await this.connection.send(req);
};
Session.prototype.trySetDExtendMode = async function trySetDExtendMode(addr, portAddr, scanBoardAddr, extendMode) {
    const req = createSetDExtendMode(addr, portAddr, scanBoardAddr, false, extendMode);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetDExtendMode.js.map