import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadResolutionExtend(addr) {
    const req = new Request(AddressMapping.Sender_ResolutionExtendOccupancy, 'ReadResolutionExtend');
    req.destination = addr;
    req.address = AddressMapping.Sender_ResolutionExtendAddr;
    return req;
}
Session.prototype.ReadResolutionExtend = async function ReadResolutionExtend(addr) {
    const req = createReadResolutionExtend(addr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadResolutionExtend = async function tryReadResolutionExtend(addr) {
    const req = createReadResolutionExtend(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadResolutionExtend.js.map