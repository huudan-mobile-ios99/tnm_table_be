import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createDistribute_SetRebootBootFpgaProgram(addr, portAddr, distributeAddr, bBroadcast, funcCardModle) {
    const $data = encodeUIntLE(funcCardModle, AddressMapping.Distribute_RebootBootFpgaProgramOccupancy);
    const req = new Request($data, bBroadcast, 'Distribute_SetRebootBootFpgaProgram');
    req.destination = addr;
    req.deviceType = 3;
    req.port = portAddr;
    req.rcvIndex = distributeAddr;
    req.address = AddressMapping.Distribute_RebootBootFpgaProgramAddr;
    return req;
}
Session.prototype.Distribute_SetRebootBootFpgaProgram =
    async function Distribute_SetRebootBootFpgaProgram(addr, portAddr, distributeAddr, bBroadcast, funcCardModle) {
        const req = createDistribute_SetRebootBootFpgaProgram(addr, portAddr, distributeAddr, bBroadcast, funcCardModle);
        await this.connection.send(req);
    };
Session.prototype.tryDistribute_SetRebootBootFpgaProgram =
    async function tryDistribute_SetRebootBootFpgaProgram(addr, portAddr, distributeAddr, funcCardModle) {
        const req = createDistribute_SetRebootBootFpgaProgram(addr, portAddr, distributeAddr, false, funcCardModle);
        return (await this.connection.trySend(req))?.ack ?? null;
    };
//# sourceMappingURL=Distribute_SetRebootBootFpgaProgram.js.map