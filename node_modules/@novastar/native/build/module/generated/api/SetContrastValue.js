import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetContrastValue(addr, bBroadcast, dht) {
    const $data = encodeUIntLE(dht, AddressMapping.SenderContrastResultOccupancy);
    const req = new Request($data, bBroadcast, 'SetContrastValue');
    req.destination = addr;
    req.address = AddressMapping.SenderContrastResultAddr;
    return req;
}
Session.prototype.SetContrastValue = async function SetContrastValue(addr, bBroadcast, dht) {
    const req = createSetContrastValue(addr, bBroadcast, dht);
    await this.connection.send(req);
};
Session.prototype.trySetContrastValue = async function trySetContrastValue(addr, dht) {
    const req = createSetContrastValue(addr, false, dht);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetContrastValue.js.map