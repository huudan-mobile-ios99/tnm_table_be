import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetScannerBacklightSwitch(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, isOpen: boolean): Promise<void>;
        trySetScannerBacklightSwitch(addr: number, portAddr: number, scanBoardAddr: number, isOpen: boolean): Promise<ErrorType | null>;
    }
}
export default function createSetScannerBacklightSwitch<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, isOpen: boolean): Request<Broadcast>;
