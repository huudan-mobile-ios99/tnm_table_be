import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetCorrectionOnROELine_1(addr, portAddr, scanBoardAddr, bBroadcast, isOpenCorrect, type, retainClrInfo) {
    const req = new Request(isOpenCorrect
        ? [0 | 64]
        : [0, 0 | (isOpenCorrect ? 1 : 0), 0 | (type << 1), 0 | (!retainClrInfo ? 4 : 0)], bBroadcast, 'SetCorrectionOnROELine_1');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.CorrectionOnAddr;
    return req;
}
Session.prototype.SetCorrectionOnROELine_1 = async function SetCorrectionOnROELine_1(addr, portAddr, scanBoardAddr, bBroadcast, isOpenCorrect, type, retainClrInfo) {
    const req = createSetCorrectionOnROELine_1(addr, portAddr, scanBoardAddr, bBroadcast, isOpenCorrect, type, retainClrInfo);
    await this.connection.send(req);
};
Session.prototype.trySetCorrectionOnROELine_1 = async function trySetCorrectionOnROELine_1(addr, portAddr, scanBoardAddr, isOpenCorrect, type, retainClrInfo) {
    const req = createSetCorrectionOnROELine_1(addr, portAddr, scanBoardAddr, false, isOpenCorrect, type, retainClrInfo);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetCorrectionOnROELine_1.js.map