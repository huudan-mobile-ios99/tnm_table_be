import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadAverMatrix(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.AverMatrixOccupancy, 'ReadAverMatrix');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.AverMatrixAddr;
    return req;
}
Session.prototype.ReadAverMatrix = async function ReadAverMatrix(addr, portAddr, scanBoardAddr) {
    const req = createReadAverMatrix(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadAverMatrix = async function tryReadAverMatrix(addr, portAddr, scanBoardAddr) {
    const req = createReadAverMatrix(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadAverMatrix.js.map