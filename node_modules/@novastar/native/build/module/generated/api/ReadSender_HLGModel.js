import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSender_HLGModel(addr) {
    const req = new Request(AddressMapping.HLGModelOccupancy, 'ReadSender_HLGModel');
    req.destination = addr;
    req.address = AddressMapping.HLGModelAddr;
    return req;
}
Session.prototype.ReadSender_HLGModel = async function ReadSender_HLGModel(addr) {
    const req = createReadSender_HLGModel(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadSender_HLGModel = async function tryReadSender_HLGModel(addr) {
    const req = createReadSender_HLGModel(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSender_HLGModel.js.map