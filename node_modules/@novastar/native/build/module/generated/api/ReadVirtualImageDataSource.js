import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadVirtualImageDataSource(addr) {
    const req = new Request(AddressMapping.VirtualImageDataSourceOccupancy, 'ReadVirtualImageDataSource');
    req.destination = addr;
    req.address = AddressMapping.VirtualImageDataSourceAddr;
    return req;
}
Session.prototype.ReadVirtualImageDataSource = async function ReadVirtualImageDataSource(addr) {
    const req = createReadVirtualImageDataSource(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadVirtualImageDataSource = async function tryReadVirtualImageDataSource(addr) {
    const req = createReadVirtualImageDataSource(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadVirtualImageDataSource.js.map