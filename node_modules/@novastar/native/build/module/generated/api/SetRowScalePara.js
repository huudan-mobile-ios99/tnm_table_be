import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetRowScalePara(addr, bBroadcast, rowScalePar) {
    const $data = encodeUIntLE(rowScalePar, AddressMapping.ColScaleParAOccupancy);
    const req = new Request($data, bBroadcast, 'SetRowScalePara');
    req.destination = addr;
    req.address = AddressMapping.RowScaleParaAddr;
    return req;
}
Session.prototype.SetRowScalePara = async function SetRowScalePara(addr, bBroadcast, rowScalePar) {
    const req = createSetRowScalePara(addr, bBroadcast, rowScalePar);
    await this.connection.send(req);
};
Session.prototype.trySetRowScalePara = async function trySetRowScalePara(addr, rowScalePar) {
    const req = createSetRowScalePara(addr, false, rowScalePar);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetRowScalePara.js.map