import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadRowsCtrlByDataGroup(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.RowsCtrlByDataGroupOccupancy * AddressMapping.RowsCtrlByDataGroupNum, 'ReadRowsCtrlByDataGroup');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.RowsCtrlByDataGroupAddr;
    return req;
}
Session.prototype.ReadRowsCtrlByDataGroup = async function ReadRowsCtrlByDataGroup(addr, portAddr, scanBoardAddr) {
    const req = createReadRowsCtrlByDataGroup(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadRowsCtrlByDataGroup = async function tryReadRowsCtrlByDataGroup(addr, portAddr, scanBoardAddr) {
    const req = createReadRowsCtrlByDataGroup(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadRowsCtrlByDataGroup.js.map