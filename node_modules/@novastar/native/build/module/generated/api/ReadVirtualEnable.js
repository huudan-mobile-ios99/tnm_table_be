import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadVirtualEnable(addr) {
    const req = new Request(AddressMapping.VirtualEnableOccupancy, 'ReadVirtualEnable');
    req.destination = addr;
    req.address = AddressMapping.VirtualEnableAddr;
    return req;
}
Session.prototype.ReadVirtualEnable = async function ReadVirtualEnable(addr) {
    const req = createReadVirtualEnable(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadVirtualEnable = async function tryReadVirtualEnable(addr) {
    const req = createReadVirtualEnable(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadVirtualEnable.js.map