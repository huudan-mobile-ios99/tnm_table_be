import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadModuleScanSequenceInfo(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.Module_ScanSequenceInfoOccupancy, 'ReadModuleScanSequenceInfo');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Module_ScanSequenceInfoAddr;
    return req;
}
Session.prototype.ReadModuleScanSequenceInfo = async function ReadModuleScanSequenceInfo(addr, portAddr, scanBoardAddr) {
    const req = createReadModuleScanSequenceInfo(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadModuleScanSequenceInfo = async function tryReadModuleScanSequenceInfo(addr, portAddr, scanBoardAddr) {
    const req = createReadModuleScanSequenceInfo(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadModuleScanSequenceInfo.js.map