/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadExtendModuleConfigInfo(addr: number, portAddr: number, scanBoardAddr: number, readLength: number): Promise<Buffer>;
        tryReadExtendModuleConfigInfo(addr: number, portAddr: number, scanBoardAddr: number, readLength: number): Promise<Packet | null>;
    }
}
export default function createReadExtendModuleConfigInfo(addr: number, portAddr: number, scanBoardAddr: number, readLength: number): Request;
