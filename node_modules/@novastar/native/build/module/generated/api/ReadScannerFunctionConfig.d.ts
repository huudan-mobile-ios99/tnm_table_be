/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadScannerFunctionConfig(addr: number, portAddr: number, scanBoardAddr: number): Promise<Buffer>;
        tryReadScannerFunctionConfig(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadScannerFunctionConfig(addr: number, portAddr: number, scanBoardAddr: number): Request;
