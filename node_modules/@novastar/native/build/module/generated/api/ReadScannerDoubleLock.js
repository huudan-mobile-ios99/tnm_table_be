import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScannerDoubleLock(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.DoubleLockPointOccupancy, 'ReadScannerDoubleLock');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ScannerDoubleLockAddr;
    return req;
}
Session.prototype.ReadScannerDoubleLock = async function ReadScannerDoubleLock(addr, portAddr, scanBoardAddr) {
    const req = createReadScannerDoubleLock(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadScannerDoubleLock = async function tryReadScannerDoubleLock(addr, portAddr, scanBoardAddr) {
    const req = createReadScannerDoubleLock(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScannerDoubleLock.js.map