import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadClearViewEnableInfo(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadClearViewEnableInfo(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadClearViewEnableInfo(addr: number, portAddr: number, scanBoardAddr: number): Request;
