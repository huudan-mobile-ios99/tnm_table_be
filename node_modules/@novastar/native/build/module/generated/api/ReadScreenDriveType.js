import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScreenDriveType(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ScreenDriveTypeOccupancy, 'ReadScreenDriveType');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ScreenDriveTypeAddr;
    return req;
}
Session.prototype.ReadScreenDriveType = async function ReadScreenDriveType(addr, portAddr, scanBoardAddr) {
    const req = createReadScreenDriveType(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadScreenDriveType = async function tryReadScreenDriveType(addr, portAddr, scanBoardAddr) {
    const req = createReadScreenDriveType(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScreenDriveType.js.map