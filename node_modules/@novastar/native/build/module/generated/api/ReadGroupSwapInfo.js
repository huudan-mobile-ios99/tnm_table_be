import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadGroupSwapInfo(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.GroupSwapInfoOccupancy, 'ReadGroupSwapInfo');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.GroupSwapInfoAddr;
    return req;
}
Session.prototype.ReadGroupSwapInfo = async function ReadGroupSwapInfo(addr, portAddr, scanBoardAddr) {
    const req = createReadGroupSwapInfo(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadGroupSwapInfo = async function tryReadGroupSwapInfo(addr, portAddr, scanBoardAddr) {
    const req = createReadGroupSwapInfo(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadGroupSwapInfo.js.map