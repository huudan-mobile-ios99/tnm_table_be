import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScannerRecordTime(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ScannerRecordTimeOccupancy, 'ReadScannerRecordTime');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ScannerRecordTimeAddr;
    return req;
}
Session.prototype.ReadScannerRecordTime = async function ReadScannerRecordTime(addr, portAddr, scanBoardAddr) {
    const req = createReadScannerRecordTime(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadScannerRecordTime = async function tryReadScannerRecordTime(addr, portAddr, scanBoardAddr) {
    const req = createReadScannerRecordTime(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScannerRecordTime.js.map