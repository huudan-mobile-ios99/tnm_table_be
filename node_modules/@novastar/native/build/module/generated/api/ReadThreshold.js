import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadThreshold(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ThresholdOccupancy, 'ReadThreshold');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ThresholdAddr;
    return req;
}
Session.prototype.ReadThreshold = async function ReadThreshold(addr, portAddr, scanBoardAddr) {
    const req = createReadThreshold(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadThreshold = async function tryReadThreshold(addr, portAddr, scanBoardAddr) {
    const req = createReadThreshold(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadThreshold.js.map