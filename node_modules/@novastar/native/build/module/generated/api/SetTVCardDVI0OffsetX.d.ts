import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetTVCardDVI0OffsetX(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, dviOffsetX: number): Promise<void>;
        trySetTVCardDVI0OffsetX(addr: number, portAddr: number, scanBoardAddr: number, dviOffsetX: number): Promise<ErrorType | null>;
    }
}
export default function createSetTVCardDVI0OffsetX<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, dviOffsetX: number): Request<Broadcast>;
