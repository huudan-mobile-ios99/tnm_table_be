import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetRgbCode(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, red: number, green: number, blue: number, virtualRed: number): Promise<void>;
        trySetRgbCode(addr: number, portAddr: number, scanBoardAddr: number, red: number, green: number, blue: number, virtualRed: number): Promise<ErrorType | null>;
    }
}
export default function createSetRgbCode<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, red: number, green: number, blue: number, virtualRed: number): Request<Broadcast>;
