import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadCoefficientSource(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadCoefficientSource(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadCoefficientSource(addr: number, portAddr: number, scanBoardAddr: number): Request;
