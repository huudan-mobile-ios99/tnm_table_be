import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetKillMode(addr, portAddr, scanBoardAddr, bBroadcast, killMode) {
    const $data = encodeUIntLE(killMode, AddressMapping.KillModeOccupancy);
    const req = new Request($data, bBroadcast, 'SetKillMode');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.KillModeAddr;
    return req;
}
Session.prototype.SetKillMode = async function SetKillMode(addr, portAddr, scanBoardAddr, bBroadcast, killMode) {
    const req = createSetKillMode(addr, portAddr, scanBoardAddr, bBroadcast, killMode);
    await this.connection.send(req);
};
Session.prototype.trySetKillMode = async function trySetKillMode(addr, portAddr, scanBoardAddr, killMode) {
    const req = createSetKillMode(addr, portAddr, scanBoardAddr, false, killMode);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetKillMode.js.map