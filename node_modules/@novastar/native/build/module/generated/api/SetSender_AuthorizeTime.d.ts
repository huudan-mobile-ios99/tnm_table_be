/// <reference types="node" />
import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetSender_AuthorizeTime(addr: number, bBroadcast: boolean, authorizeTimeBytes: number[] | Buffer): Promise<void>;
        trySetSender_AuthorizeTime(addr: number, authorizeTimeBytes: number[] | Buffer): Promise<ErrorType | null>;
    }
}
export default function createSetSender_AuthorizeTime<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, authorizeTimeBytes: number[] | Buffer): Request<Broadcast>;
