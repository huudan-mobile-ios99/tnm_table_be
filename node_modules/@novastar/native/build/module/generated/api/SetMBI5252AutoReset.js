import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetMBI5252AutoReset(addr, portAddr, scanBoardAddr, bBroadcast, mutiChipRamA) {
    if (mutiChipRamA.length !== 0)
        throw new TypeError(`Invalid buffer size: ${mutiChipRamA.length}`);
    const req = new Request(mutiChipRamA, bBroadcast, 'SetMBI5252AutoReset');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.MBI5252SetAutoResetAddr;
    return req;
}
Session.prototype.SetMBI5252AutoReset = async function SetMBI5252AutoReset(addr, portAddr, scanBoardAddr, bBroadcast, mutiChipRamA) {
    const req = createSetMBI5252AutoReset(addr, portAddr, scanBoardAddr, bBroadcast, mutiChipRamA);
    await this.connection.send(req);
};
Session.prototype.trySetMBI5252AutoReset = async function trySetMBI5252AutoReset(addr, portAddr, scanBoardAddr, mutiChipRamA) {
    const req = createSetMBI5252AutoReset(addr, portAddr, scanBoardAddr, false, mutiChipRamA);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetMBI5252AutoReset.js.map