import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadColorAdjustMatrixType(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ScannerColorInfoOccupancy, 'ReadColorAdjustMatrixType');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ColorAdjustMatrixTypeAddr;
    return req;
}
Session.prototype.ReadColorAdjustMatrixType = async function ReadColorAdjustMatrixType(addr, portAddr, scanBoardAddr) {
    const req = createReadColorAdjustMatrixType(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadColorAdjustMatrixType = async function tryReadColorAdjustMatrixType(addr, portAddr, scanBoardAddr) {
    const req = createReadColorAdjustMatrixType(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadColorAdjustMatrixType.js.map