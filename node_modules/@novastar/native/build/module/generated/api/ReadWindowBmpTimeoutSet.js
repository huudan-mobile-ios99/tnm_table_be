import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadWindowBmpTimeoutSet(addr) {
    const req = new Request(AddressMapping.VirtualWindowBmpTimeoutSetOccupancy, 'ReadWindowBmpTimeoutSet');
    req.destination = addr;
    req.address = AddressMapping.VirtualWindowBmpTimeoutSetAddr;
    return req;
}
Session.prototype.ReadWindowBmpTimeoutSet = async function ReadWindowBmpTimeoutSet(addr) {
    const req = createReadWindowBmpTimeoutSet(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadWindowBmpTimeoutSet = async function tryReadWindowBmpTimeoutSet(addr) {
    const req = createReadWindowBmpTimeoutSet(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadWindowBmpTimeoutSet.js.map