import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createWriteCabinetInfo(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    if (data.length !== AddressMapping.CabinetOccupancy)
        throw new TypeError(`Invalid buffer size: ${data.length}`);
    const req = new Request(data, bBroadcast, 'WriteCabinetInfo');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.CabinetAddr;
    return req;
}
Session.prototype.WriteCabinetInfo = async function WriteCabinetInfo(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createWriteCabinetInfo(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.tryWriteCabinetInfo = async function tryWriteCabinetInfo(addr, portAddr, scanBoardAddr, data) {
    const req = createWriteCabinetInfo(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=WriteCabinetInfo.js.map