import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadLicenseInfo(addr) {
    const req = new Request(AddressMapping.LicenseInfoOccupancy, 'ReadLicenseInfo');
    req.destination = addr;
    req.address = AddressMapping.LicenseInfoAddr;
    return req;
}
Session.prototype.ReadLicenseInfo = async function ReadLicenseInfo(addr) {
    const req = createReadLicenseInfo(addr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadLicenseInfo = async function tryReadLicenseInfo(addr) {
    const req = createReadLicenseInfo(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadLicenseInfo.js.map