import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadDclkUnitCycle(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadDclkUnitCycle(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadDclkUnitCycle(addr: number, portAddr: number, scanBoardAddr: number): Request;
