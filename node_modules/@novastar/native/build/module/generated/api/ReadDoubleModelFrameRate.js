import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadDoubleModelFrameRate(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.DoubleModelCardSpaceRecordFreOccupancy, 'ReadDoubleModelFrameRate');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.DoubleModelCardSpaceRecordFre;
    return req;
}
Session.prototype.ReadDoubleModelFrameRate = async function ReadDoubleModelFrameRate(addr, portAddr, scanBoardAddr) {
    const req = createReadDoubleModelFrameRate(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadDoubleModelFrameRate = async function tryReadDoubleModelFrameRate(addr, portAddr, scanBoardAddr) {
    const req = createReadDoubleModelFrameRate(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadDoubleModelFrameRate.js.map