import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetSender_HDR10MinLum(addr, bBroadcast, HDR10MinLum) {
    const req = new Request([HDR10MinLum], bBroadcast, 'SetSender_HDR10MinLum');
    req.destination = addr;
    req.address = AddressMapping.HDR10MinLum;
    return req;
}
Session.prototype.SetSender_HDR10MinLum = async function SetSender_HDR10MinLum(addr, bBroadcast, HDR10MinLum) {
    const req = createSetSender_HDR10MinLum(addr, bBroadcast, HDR10MinLum);
    await this.connection.send(req);
};
Session.prototype.trySetSender_HDR10MinLum = async function trySetSender_HDR10MinLum(addr, HDR10MinLum) {
    const req = createSetSender_HDR10MinLum(addr, false, HDR10MinLum);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetSender_HDR10MinLum.js.map