import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadAllPortHeight(addr) {
    const req = new Request(AddressMapping.Sender_AllDataSpaceOccupancy, 'ReadAllPortHeight');
    req.destination = addr;
    req.address = AddressMapping.Sender_AllDataAddr;
    return req;
}
Session.prototype.ReadAllPortHeight = async function ReadAllPortHeight(addr) {
    const req = createReadAllPortHeight(addr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadAllPortHeight = async function tryReadAllPortHeight(addr) {
    const req = createReadAllPortHeight(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadAllPortHeight.js.map