import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetEnSwipErrorPoint(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    if (data.length !== AddressMapping.EnSwipErrorPointOccupancy)
        throw new TypeError(`Invalid buffer size: ${data.length}`);
    const req = new Request(data, bBroadcast, 'SetEnSwipErrorPoint');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.EnSwipErrorPointAddr;
    return req;
}
Session.prototype.SetEnSwipErrorPoint = async function SetEnSwipErrorPoint(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createSetEnSwipErrorPoint(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.trySetEnSwipErrorPoint = async function trySetEnSwipErrorPoint(addr, portAddr, scanBoardAddr, data) {
    const req = createSetEnSwipErrorPoint(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetEnSwipErrorPoint.js.map