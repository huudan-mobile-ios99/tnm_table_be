import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSender_OpticalWorkMode(addr) {
    const req = new Request(AddressMapping.OpticalWorkModeOccupancy, 'ReadSender_OpticalWorkMode');
    req.destination = addr;
    req.address = AddressMapping.OpticalWorkModeAddr;
    return req;
}
Session.prototype.ReadSender_OpticalWorkMode = async function ReadSender_OpticalWorkMode(addr) {
    const req = createReadSender_OpticalWorkMode(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadSender_OpticalWorkMode = async function tryReadSender_OpticalWorkMode(addr) {
    const req = createReadSender_OpticalWorkMode(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSender_OpticalWorkMode.js.map