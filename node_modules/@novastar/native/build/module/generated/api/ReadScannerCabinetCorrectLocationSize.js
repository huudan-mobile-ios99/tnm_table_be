import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScannerCabinetCorrectLocationSize(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ScannerCabinetCorrectLocationSizeOccupancy, 'ReadScannerCabinetCorrectLocationSize');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ScannerCabinetCorrectLocationSizeAddr;
    return req;
}
Session.prototype.ReadScannerCabinetCorrectLocationSize =
    async function ReadScannerCabinetCorrectLocationSize(addr, portAddr, scanBoardAddr) {
        const req = createReadScannerCabinetCorrectLocationSize(addr, portAddr, scanBoardAddr);
        return (await this.connection.send(req)).data;
    };
Session.prototype.tryReadScannerCabinetCorrectLocationSize =
    async function tryReadScannerCabinetCorrectLocationSize(addr, portAddr, scanBoardAddr) {
        const req = createReadScannerCabinetCorrectLocationSize(addr, portAddr, scanBoardAddr);
        return this.connection.trySend(req);
    };
//# sourceMappingURL=ReadScannerCabinetCorrectLocationSize.js.map