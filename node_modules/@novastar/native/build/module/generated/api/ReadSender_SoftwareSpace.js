import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSender_SoftwareSpace(addr, dataLength, addrOffset) {
    const req = new Request(dataLength, 'ReadSender_SoftwareSpace');
    req.destination = addr;
    req.address = AddressMapping.Sender_SoftwareSpaceAddr + addrOffset;
    return req;
}
Session.prototype.ReadSender_SoftwareSpace = async function ReadSender_SoftwareSpace(addr, dataLength, addrOffset) {
    const req = createReadSender_SoftwareSpace(addr, dataLength, addrOffset);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadSender_SoftwareSpace = async function tryReadSender_SoftwareSpace(addr, dataLength, addrOffset) {
    const req = createReadSender_SoftwareSpace(addr, dataLength, addrOffset);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSender_SoftwareSpace.js.map