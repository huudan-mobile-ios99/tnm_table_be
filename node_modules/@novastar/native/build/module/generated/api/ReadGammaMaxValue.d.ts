import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadGammaMaxValue(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadGammaMaxValue(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadGammaMaxValue(addr: number, portAddr: number, scanBoardAddr: number): Request;
