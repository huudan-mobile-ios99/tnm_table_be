import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadCoefficientSource(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.CoefficientSourceOccupancy, 'ReadCoefficientSource');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.CoefficientSourceAddr;
    return req;
}
Session.prototype.ReadCoefficientSource = async function ReadCoefficientSource(addr, portAddr, scanBoardAddr) {
    const req = createReadCoefficientSource(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadCoefficientSource = async function tryReadCoefficientSource(addr, portAddr, scanBoardAddr) {
    const req = createReadCoefficientSource(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadCoefficientSource.js.map