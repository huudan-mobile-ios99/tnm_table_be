import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadMGEnParmData(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.MGEnParmccupancy, 'ReadMGEnParmData');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.MGEnParmAddr;
    return req;
}
Session.prototype.ReadMGEnParmData = async function ReadMGEnParmData(addr, portAddr, scanBoardAddr) {
    const req = createReadMGEnParmData(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadMGEnParmData = async function tryReadMGEnParmData(addr, portAddr, scanBoardAddr) {
    const req = createReadMGEnParmData(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadMGEnParmData.js.map