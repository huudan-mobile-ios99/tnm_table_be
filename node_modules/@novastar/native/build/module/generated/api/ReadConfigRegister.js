import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadConfigRegister(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ReadConfigRegisterOccupancy, 'ReadConfigRegister');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ReadConfigRegisterAddr;
    return req;
}
Session.prototype.ReadConfigRegister = async function ReadConfigRegister(addr, portAddr, scanBoardAddr) {
    const req = createReadConfigRegister(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadConfigRegister = async function tryReadConfigRegister(addr, portAddr, scanBoardAddr) {
    const req = createReadConfigRegister(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadConfigRegister.js.map