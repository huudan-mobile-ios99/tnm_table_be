import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadScannerDoubleLock(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadScannerDoubleLock(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadScannerDoubleLock(addr: number, portAddr: number, scanBoardAddr: number): Request;
