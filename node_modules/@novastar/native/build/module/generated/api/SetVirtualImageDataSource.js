import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetVirtualImageDataSource(addr, bBroadcast, virtuaImageDataSource) {
    const $data = encodeUIntLE(virtuaImageDataSource, AddressMapping.VirtualImageDataSourceOccupancy);
    const req = new Request($data, bBroadcast, 'SetVirtualImageDataSource');
    req.destination = addr;
    req.address = AddressMapping.VirtualImageDataSourceAddr;
    return req;
}
Session.prototype.SetVirtualImageDataSource = async function SetVirtualImageDataSource(addr, bBroadcast, virtuaImageDataSource) {
    const req = createSetVirtualImageDataSource(addr, bBroadcast, virtuaImageDataSource);
    await this.connection.send(req);
};
Session.prototype.trySetVirtualImageDataSource = async function trySetVirtualImageDataSource(addr, virtuaImageDataSource) {
    const req = createSetVirtualImageDataSource(addr, false, virtuaImageDataSource);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetVirtualImageDataSource.js.map