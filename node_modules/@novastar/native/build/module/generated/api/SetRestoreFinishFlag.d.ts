import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetRestoreFinishFlag(addr: number, bBroadcast: boolean, writeData: number): Promise<void>;
        trySetRestoreFinishFlag(addr: number, writeData: number): Promise<ErrorType | null>;
    }
}
export default function createSetRestoreFinishFlag<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, writeData: number): Request<Broadcast>;
