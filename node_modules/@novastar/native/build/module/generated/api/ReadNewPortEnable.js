import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadNewPortEnable(addr, index) {
    const req = new Request(AddressMapping.PortEnableOccupancy, 'ReadNewPortEnable');
    req.destination = addr;
    req.address =
        AddressMapping.PortEnableNextAddr +
            AddressMapping.PortOccupancy * (index - AddressMapping.NewPortOccupancy);
    return req;
}
Session.prototype.ReadNewPortEnable = async function ReadNewPortEnable(addr, index) {
    const req = createReadNewPortEnable(addr, index);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadNewPortEnable = async function tryReadNewPortEnable(addr, index) {
    const req = createReadNewPortEnable(addr, index);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadNewPortEnable.js.map