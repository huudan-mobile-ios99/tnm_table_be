import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSender_HWScreenDisplayFlag(addr) {
    const req = new Request(AddressMapping.Sender_HWScreenDisplayFlagOccupancy, 'ReadSender_HWScreenDisplayFlag');
    req.destination = addr;
    req.address = AddressMapping.Sender_HWScreenDisplayBaseAddr;
    return req;
}
Session.prototype.ReadSender_HWScreenDisplayFlag = async function ReadSender_HWScreenDisplayFlag(addr) {
    const req = createReadSender_HWScreenDisplayFlag(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadSender_HWScreenDisplayFlag =
    async function tryReadSender_HWScreenDisplayFlag(addr) {
        const req = createReadSender_HWScreenDisplayFlag(addr);
        return this.connection.trySend(req);
    };
//# sourceMappingURL=ReadSender_HWScreenDisplayFlag.js.map