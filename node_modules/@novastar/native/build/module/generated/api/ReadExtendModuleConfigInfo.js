import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadExtendModuleConfigInfo(addr, portAddr, scanBoardAddr, readLength) {
    const req = new Request(readLength, 'ReadExtendModuleConfigInfo');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ModuleConfigInfoAddr;
    return req;
}
Session.prototype.ReadExtendModuleConfigInfo = async function ReadExtendModuleConfigInfo(addr, portAddr, scanBoardAddr, readLength) {
    const req = createReadExtendModuleConfigInfo(addr, portAddr, scanBoardAddr, readLength);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadExtendModuleConfigInfo = async function tryReadExtendModuleConfigInfo(addr, portAddr, scanBoardAddr, readLength) {
    const req = createReadExtendModuleConfigInfo(addr, portAddr, scanBoardAddr, readLength);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadExtendModuleConfigInfo.js.map