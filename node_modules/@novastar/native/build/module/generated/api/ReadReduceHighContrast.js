import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadReduceHighContrast(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ReduceHighContrastOccupancy, 'ReadReduceHighContrast');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ReduceHighContrastAddr;
    return req;
}
Session.prototype.ReadReduceHighContrast = async function ReadReduceHighContrast(addr, portAddr, scanBoardAddr) {
    const req = createReadReduceHighContrast(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadReduceHighContrast = async function tryReadReduceHighContrast(addr, portAddr, scanBoardAddr) {
    const req = createReadReduceHighContrast(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadReduceHighContrast.js.map