import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSender_EncryptAllInfo(addr) {
    const req = new Request(AddressMapping.Sender_EncryptAllInfoOccupancy, 'ReadSender_EncryptAllInfo');
    req.destination = addr;
    req.address = AddressMapping.Sender_EncryptAllInfoAddr;
    return req;
}
Session.prototype.ReadSender_EncryptAllInfo = async function ReadSender_EncryptAllInfo(addr) {
    const req = createReadSender_EncryptAllInfo(addr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadSender_EncryptAllInfo = async function tryReadSender_EncryptAllInfo(addr) {
    const req = createReadSender_EncryptAllInfo(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSender_EncryptAllInfo.js.map