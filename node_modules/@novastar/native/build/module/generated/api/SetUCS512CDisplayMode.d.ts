import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetUCS512CDisplayMode(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, displayMode: number): Promise<void>;
        trySetUCS512CDisplayMode(addr: number, portAddr: number, scanBoardAddr: number, displayMode: number): Promise<ErrorType | null>;
    }
}
export default function createSetUCS512CDisplayMode<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, displayMode: number): Request<Broadcast>;
