import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScannerFunctionInfo(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ScanCardFunctionOccupancy, 'ReadScannerFunctionInfo');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ScanCardFunctionAddr;
    return req;
}
Session.prototype.ReadScannerFunctionInfo = async function ReadScannerFunctionInfo(addr, portAddr, scanBoardAddr) {
    const req = createReadScannerFunctionInfo(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadScannerFunctionInfo = async function tryReadScannerFunctionInfo(addr, portAddr, scanBoardAddr) {
    const req = createReadScannerFunctionInfo(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScannerFunctionInfo.js.map