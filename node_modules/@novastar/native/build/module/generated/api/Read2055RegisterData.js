import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createRead2055RegisterData(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.Config2055RegisterOccupancy, 'Read2055RegisterData');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Config2055RegisterAddr;
    return req;
}
Session.prototype.Read2055RegisterData = async function Read2055RegisterData(addr, portAddr, scanBoardAddr) {
    const req = createRead2055RegisterData(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryRead2055RegisterData = async function tryRead2055RegisterData(addr, portAddr, scanBoardAddr) {
    const req = createRead2055RegisterData(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=Read2055RegisterData.js.map