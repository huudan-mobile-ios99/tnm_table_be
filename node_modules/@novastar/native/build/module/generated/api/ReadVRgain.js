import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadVRgain(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.VRgainOccupancy, 'ReadVRgain');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.VRgainAddr;
    return req;
}
Session.prototype.ReadVRgain = async function ReadVRgain(addr, portAddr, scanBoardAddr) {
    const req = createReadVRgain(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadVRgain = async function tryReadVRgain(addr, portAddr, scanBoardAddr) {
    const req = createReadVRgain(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadVRgain.js.map