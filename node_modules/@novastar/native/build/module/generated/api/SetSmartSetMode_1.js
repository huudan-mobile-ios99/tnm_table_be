import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetSmartSetMode_1(addr, portAddr, scanBoardAddr, bBroadcast, smartMode) {
    const $data = encodeUIntLE(smartMode, AddressMapping.SmartSetModeOccupancy);
    const req = new Request($data, bBroadcast, 'SetSmartSetMode_1');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.SmartSetModeAddr;
    return req;
}
Session.prototype.SetSmartSetMode_1 = async function SetSmartSetMode_1(addr, portAddr, scanBoardAddr, bBroadcast, smartMode) {
    const req = createSetSmartSetMode_1(addr, portAddr, scanBoardAddr, bBroadcast, smartMode);
    await this.connection.send(req);
};
Session.prototype.trySetSmartSetMode_1 = async function trySetSmartSetMode_1(addr, portAddr, scanBoardAddr, smartMode) {
    const req = createSetSmartSetMode_1(addr, portAddr, scanBoardAddr, false, smartMode);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetSmartSetMode_1.js.map