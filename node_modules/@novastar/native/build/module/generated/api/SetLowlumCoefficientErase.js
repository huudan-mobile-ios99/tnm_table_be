import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetLowlumCoefficientErase(addr, portAddr, scanBoardAddr, bBroadcast, coefficientErase) {
    const req = new Request(coefficientErase, bBroadcast, 'SetLowlumCoefficientErase');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.CoefficientInIIcFlashEraseAddr;
    return req;
}
Session.prototype.SetLowlumCoefficientErase = async function SetLowlumCoefficientErase(addr, portAddr, scanBoardAddr, bBroadcast, coefficientErase) {
    const req = createSetLowlumCoefficientErase(addr, portAddr, scanBoardAddr, bBroadcast, coefficientErase);
    await this.connection.send(req);
};
Session.prototype.trySetLowlumCoefficientErase = async function trySetLowlumCoefficientErase(addr, portAddr, scanBoardAddr, coefficientErase) {
    const req = createSetLowlumCoefficientErase(addr, portAddr, scanBoardAddr, false, coefficientErase);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetLowlumCoefficientErase.js.map