import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadColorManageCoefData(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ColorManageInfoAddrOccupancy, 'ReadColorManageCoefData');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ColorManageInfoAddr;
    return req;
}
Session.prototype.ReadColorManageCoefData = async function ReadColorManageCoefData(addr, portAddr, scanBoardAddr) {
    const req = createReadColorManageCoefData(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadColorManageCoefData = async function tryReadColorManageCoefData(addr, portAddr, scanBoardAddr) {
    const req = createReadColorManageCoefData(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadColorManageCoefData.js.map