import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSerialColorNum(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.SerialColorNumOccupancy, 'ReadSerialColorNum');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.SerialColorNumAddr;
    return req;
}
Session.prototype.ReadSerialColorNum = async function ReadSerialColorNum(addr, portAddr, scanBoardAddr) {
    const req = createReadSerialColorNum(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadSerialColorNum = async function tryReadSerialColorNum(addr, portAddr, scanBoardAddr) {
    const req = createReadSerialColorNum(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSerialColorNum.js.map