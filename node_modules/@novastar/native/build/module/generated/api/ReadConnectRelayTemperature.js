import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadConnectRelayTemperature(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ConnectRelayTemperatureOccupancy, 'ReadConnectRelayTemperature');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ConnectRelayTemperatureAddr;
    return req;
}
Session.prototype.ReadConnectRelayTemperature = async function ReadConnectRelayTemperature(addr, portAddr, scanBoardAddr) {
    const req = createReadConnectRelayTemperature(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadConnectRelayTemperature = async function tryReadConnectRelayTemperature(addr, portAddr, scanBoardAddr) {
    const req = createReadConnectRelayTemperature(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadConnectRelayTemperature.js.map