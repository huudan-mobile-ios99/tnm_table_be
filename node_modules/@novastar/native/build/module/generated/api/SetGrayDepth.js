import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetGrayDepth(addr, portAddr, scanBoardAddr, bBroadcast, grayDepth) {
    const $data = encodeUIntLE(grayDepth, AddressMapping.GrayDepthOccupancy);
    const req = new Request($data, bBroadcast, 'SetGrayDepth');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.GrayDepthAddr;
    return req;
}
Session.prototype.SetGrayDepth = async function SetGrayDepth(addr, portAddr, scanBoardAddr, bBroadcast, grayDepth) {
    const req = createSetGrayDepth(addr, portAddr, scanBoardAddr, bBroadcast, grayDepth);
    await this.connection.send(req);
};
Session.prototype.trySetGrayDepth = async function trySetGrayDepth(addr, portAddr, scanBoardAddr, grayDepth) {
    const req = createSetGrayDepth(addr, portAddr, scanBoardAddr, false, grayDepth);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetGrayDepth.js.map