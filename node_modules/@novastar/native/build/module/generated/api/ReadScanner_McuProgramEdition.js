import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScanner_McuProgramEdition(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.Scanner_McuProgramEditionOccupancy, 'ReadScanner_McuProgramEdition');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Scanner_McuProgramEditionAddr;
    return req;
}
Session.prototype.ReadScanner_McuProgramEdition = async function ReadScanner_McuProgramEdition(addr, portAddr, scanBoardAddr) {
    const req = createReadScanner_McuProgramEdition(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadScanner_McuProgramEdition =
    async function tryReadScanner_McuProgramEdition(addr, portAddr, scanBoardAddr) {
        const req = createReadScanner_McuProgramEdition(addr, portAddr, scanBoardAddr);
        return this.connection.trySend(req);
    };
//# sourceMappingURL=ReadScanner_McuProgramEdition.js.map