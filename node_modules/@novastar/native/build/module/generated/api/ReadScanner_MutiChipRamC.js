import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScanner_MutiChipRamC(addr, portAddr, scanBoardAddr, readLength) {
    const req = new Request(readLength, 'ReadScanner_MutiChipRamC');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Scanner_MutiChipRamCAddr;
    return req;
}
Session.prototype.ReadScanner_MutiChipRamC = async function ReadScanner_MutiChipRamC(addr, portAddr, scanBoardAddr, readLength) {
    const req = createReadScanner_MutiChipRamC(addr, portAddr, scanBoardAddr, readLength);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadScanner_MutiChipRamC = async function tryReadScanner_MutiChipRamC(addr, portAddr, scanBoardAddr, readLength) {
    const req = createReadScanner_MutiChipRamC(addr, portAddr, scanBoardAddr, readLength);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScanner_MutiChipRamC.js.map