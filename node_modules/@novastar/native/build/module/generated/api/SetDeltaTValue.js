import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetDeltaTValue(addr, portAddr, scanBoardAddr, bBroadcast, deltaT) {
    const req = new Request([deltaT], bBroadcast, 'SetDeltaTValue');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.DeltaTAddr;
    return req;
}
Session.prototype.SetDeltaTValue = async function SetDeltaTValue(addr, portAddr, scanBoardAddr, bBroadcast, deltaT) {
    const req = createSetDeltaTValue(addr, portAddr, scanBoardAddr, bBroadcast, deltaT);
    await this.connection.send(req);
};
Session.prototype.trySetDeltaTValue = async function trySetDeltaTValue(addr, portAddr, scanBoardAddr, deltaT) {
    const req = createSetDeltaTValue(addr, portAddr, scanBoardAddr, false, deltaT);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetDeltaTValue.js.map