import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createFuncCard_ReadFuncCardAllStatus(addr) {
    const req = new Request(AddressMapping.FuncCard_FuncCardAllStatusOccupancy, 'FuncCard_ReadFuncCardAllStatus');
    req.destination = addr;
    req.address = AddressMapping.FuncCard_FuncCardAllStatusAddr;
    return req;
}
Session.prototype.FuncCard_ReadFuncCardAllStatus = async function FuncCard_ReadFuncCardAllStatus(addr) {
    const req = createFuncCard_ReadFuncCardAllStatus(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryFuncCard_ReadFuncCardAllStatus =
    async function tryFuncCard_ReadFuncCardAllStatus(addr) {
        const req = createFuncCard_ReadFuncCardAllStatus(addr);
        return this.connection.trySend(req);
    };
//# sourceMappingURL=FuncCard_ReadFuncCardAllStatus.js.map