import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createFuncCard_ReadTempInfoOfFuncCard(addr) {
    const req = new Request(AddressMapping.FuncCard_TempInfoOfFuncCardOccupancy, 'FuncCard_ReadTempInfoOfFuncCard');
    req.destination = addr;
    req.address = AddressMapping.FuncCard_TempInfoOfFuncCardAddr;
    return req;
}
Session.prototype.FuncCard_ReadTempInfoOfFuncCard = async function FuncCard_ReadTempInfoOfFuncCard(addr) {
    const req = createFuncCard_ReadTempInfoOfFuncCard(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryFuncCard_ReadTempInfoOfFuncCard =
    async function tryFuncCard_ReadTempInfoOfFuncCard(addr) {
        const req = createFuncCard_ReadTempInfoOfFuncCard(addr);
        return this.connection.trySend(req);
    };
//# sourceMappingURL=FuncCard_ReadTempInfoOfFuncCard.js.map