import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadBrightDarkLineFixState(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadBrightDarkLineFixState(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadBrightDarkLineFixState(addr: number, portAddr: number, scanBoardAddr: number): Request;
