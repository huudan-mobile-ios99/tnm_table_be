import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetSaveDistributeParameters(addr, portAddr, distributeAddr, bBroadcast, saveSendCardsParameters) {
    const req = new Request([saveSendCardsParameters], bBroadcast, 'SetSaveDistributeParameters');
    req.destination = addr;
    req.deviceType = 3;
    req.port = portAddr;
    req.rcvIndex = distributeAddr;
    req.address = AddressMapping.SaveDistributeParameterAddr;
    return req;
}
Session.prototype.SetSaveDistributeParameters = async function SetSaveDistributeParameters(addr, portAddr, distributeAddr, bBroadcast, saveSendCardsParameters) {
    const req = createSetSaveDistributeParameters(addr, portAddr, distributeAddr, bBroadcast, saveSendCardsParameters);
    await this.connection.send(req);
};
Session.prototype.trySetSaveDistributeParameters = async function trySetSaveDistributeParameters(addr, portAddr, distributeAddr, saveSendCardsParameters) {
    const req = createSetSaveDistributeParameters(addr, portAddr, distributeAddr, false, saveSendCardsParameters);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetSaveDistributeParameters.js.map