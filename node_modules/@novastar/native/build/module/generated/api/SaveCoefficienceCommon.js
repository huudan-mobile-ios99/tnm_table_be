import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSaveCoefficienceCommon(addr, portAddr, scanBoardAddr, bBroadcast, val) {
    const req = new Request([val], bBroadcast, 'SaveCoefficienceCommon');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.SaveCommonCoefficienceAddr;
    return req;
}
Session.prototype.SaveCoefficienceCommon = async function SaveCoefficienceCommon(addr, portAddr, scanBoardAddr, bBroadcast, val) {
    const req = createSaveCoefficienceCommon(addr, portAddr, scanBoardAddr, bBroadcast, val);
    await this.connection.send(req);
};
Session.prototype.trySaveCoefficienceCommon = async function trySaveCoefficienceCommon(addr, portAddr, scanBoardAddr, val) {
    const req = createSaveCoefficienceCommon(addr, portAddr, scanBoardAddr, false, val);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SaveCoefficienceCommon.js.map