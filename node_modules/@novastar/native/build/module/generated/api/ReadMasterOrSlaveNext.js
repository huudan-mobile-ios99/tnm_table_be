import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadMasterOrSlaveNext(addr) {
    const req = new Request(AddressMapping.MasterOrSlaveOccupancy, 'ReadMasterOrSlaveNext');
    req.destination = addr;
    req.address = AddressMapping.MasterOrSlaveNewAddr;
    return req;
}
Session.prototype.ReadMasterOrSlaveNext = async function ReadMasterOrSlaveNext(addr) {
    const req = createReadMasterOrSlaveNext(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadMasterOrSlaveNext = async function tryReadMasterOrSlaveNext(addr) {
    const req = createReadMasterOrSlaveNext(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadMasterOrSlaveNext.js.map