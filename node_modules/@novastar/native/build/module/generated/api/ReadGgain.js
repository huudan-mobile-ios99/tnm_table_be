import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadGgain(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.GgainOccupancy, 'ReadGgain');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.GgainAddr;
    return req;
}
Session.prototype.ReadGgain = async function ReadGgain(addr, portAddr, scanBoardAddr) {
    const req = createReadGgain(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadGgain = async function tryReadGgain(addr, portAddr, scanBoardAddr) {
    const req = createReadGgain(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadGgain.js.map