import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetLCDSwitch(addr, portAddr, scanBoardAddr, bBroadcast, tem) {
    const $data = encodeUIntLE(tem, AddressMapping.LCDSwitchOccupancy);
    const req = new Request($data, bBroadcast, 'SetLCDSwitch');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.LCDSwitchAddr;
    return req;
}
Session.prototype.SetLCDSwitch = async function SetLCDSwitch(addr, portAddr, scanBoardAddr, bBroadcast, tem) {
    const req = createSetLCDSwitch(addr, portAddr, scanBoardAddr, bBroadcast, tem);
    await this.connection.send(req);
};
Session.prototype.trySetLCDSwitch = async function trySetLCDSwitch(addr, portAddr, scanBoardAddr, tem) {
    const req = createSetLCDSwitch(addr, portAddr, scanBoardAddr, false, tem);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetLCDSwitch.js.map