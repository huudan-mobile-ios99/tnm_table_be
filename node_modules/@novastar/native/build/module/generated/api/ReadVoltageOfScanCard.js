import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadVoltageOfScanCard(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.Scanner_VoltageOfScanCardOccupancy, 'ReadVoltageOfScanCard');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Scanner_VoltageOfScanCardAddr;
    return req;
}
Session.prototype.ReadVoltageOfScanCard = async function ReadVoltageOfScanCard(addr, portAddr, scanBoardAddr) {
    const req = createReadVoltageOfScanCard(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadVoltageOfScanCard = async function tryReadVoltageOfScanCard(addr, portAddr, scanBoardAddr) {
    const req = createReadVoltageOfScanCard(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadVoltageOfScanCard.js.map