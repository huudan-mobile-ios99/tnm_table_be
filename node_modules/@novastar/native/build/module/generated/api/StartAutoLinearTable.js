import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createStartAutoLinearTable(addr, portAddr, scanBoardAddr, bBroadcast) {
    const $data = encodeUIntLE(1, AddressMapping.StartAutoLinearTableOccupancy);
    const req = new Request($data, bBroadcast, 'StartAutoLinearTable');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.StartAutoLinearTableAddr;
    return req;
}
Session.prototype.StartAutoLinearTable = async function StartAutoLinearTable(addr, portAddr, scanBoardAddr, bBroadcast) {
    const req = createStartAutoLinearTable(addr, portAddr, scanBoardAddr, bBroadcast);
    await this.connection.send(req);
};
Session.prototype.tryStartAutoLinearTable = async function tryStartAutoLinearTable(addr, portAddr, scanBoardAddr) {
    const req = createStartAutoLinearTable(addr, portAddr, scanBoardAddr, false);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=StartAutoLinearTable.js.map