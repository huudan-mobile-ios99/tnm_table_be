import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetCorrectionOnEx(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, bCorrectionOn: boolean): Promise<void>;
        trySetCorrectionOnEx(addr: number, portAddr: number, scanBoardAddr: number, bCorrectionOn: boolean): Promise<ErrorType | null>;
    }
}
export default function createSetCorrectionOnEx<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, bCorrectionOn: boolean): Request<Broadcast>;
