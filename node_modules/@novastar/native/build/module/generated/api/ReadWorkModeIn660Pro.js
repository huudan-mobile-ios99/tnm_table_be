import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadWorkModeIn660Pro(addr) {
    const req = new Request(AddressMapping.WorkModeIn660ProOccupancy, 'ReadWorkModeIn660Pro');
    req.destination = addr;
    req.address = AddressMapping.WorkModeIn660ProAddr;
    return req;
}
Session.prototype.ReadWorkModeIn660Pro = async function ReadWorkModeIn660Pro(addr) {
    const req = createReadWorkModeIn660Pro(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadWorkModeIn660Pro = async function tryReadWorkModeIn660Pro(addr) {
    const req = createReadWorkModeIn660Pro(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadWorkModeIn660Pro.js.map