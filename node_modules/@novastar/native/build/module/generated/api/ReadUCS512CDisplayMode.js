import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadUCS512CDisplayMode(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.GrayBitOccupancy, 'ReadUCS512CDisplayMode');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.UCS512CDisplayModeAddr;
    return req;
}
Session.prototype.ReadUCS512CDisplayMode = async function ReadUCS512CDisplayMode(addr, portAddr, scanBoardAddr) {
    const req = createReadUCS512CDisplayMode(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadUCS512CDisplayMode = async function tryReadUCS512CDisplayMode(addr, portAddr, scanBoardAddr) {
    const req = createReadUCS512CDisplayMode(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadUCS512CDisplayMode.js.map