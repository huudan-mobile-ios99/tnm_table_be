import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadSingleChipMicyocoSpace(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadSingleChipMicyocoSpace(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadSingleChipMicyocoSpace(addr: number, portAddr: number, scanBoardAddr: number): Request;
