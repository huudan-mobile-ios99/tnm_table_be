import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadTotalPointInTable(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.TotalPointInTableOccupancy, 'ReadTotalPointInTable');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.TotalPointInTableAddr;
    return req;
}
Session.prototype.ReadTotalPointInTable = async function ReadTotalPointInTable(addr, portAddr, scanBoardAddr) {
    const req = createReadTotalPointInTable(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadTotalPointInTable = async function tryReadTotalPointInTable(addr, portAddr, scanBoardAddr) {
    const req = createReadTotalPointInTable(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadTotalPointInTable.js.map