import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadVirtualMap(addr) {
    const req = new Request(AddressMapping.VirtualMapOccupancy, 'ReadVirtualMap');
    req.destination = addr;
    req.address = AddressMapping.VirtualMapAddr;
    return req;
}
Session.prototype.ReadVirtualMap = async function ReadVirtualMap(addr) {
    const req = createReadVirtualMap(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadVirtualMap = async function tryReadVirtualMap(addr) {
    const req = createReadVirtualMap(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadVirtualMap.js.map