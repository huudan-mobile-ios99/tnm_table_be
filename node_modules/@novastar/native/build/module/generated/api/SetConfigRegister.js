import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetConfigRegister(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    if (data.length !== AddressMapping.WriteConfigRegisterOccupancy)
        throw new TypeError(`Invalid buffer size: ${data.length}`);
    const req = new Request(data, bBroadcast, 'SetConfigRegister');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.WriteConfigRegisterAddr;
    return req;
}
Session.prototype.SetConfigRegister = async function SetConfigRegister(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createSetConfigRegister(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.trySetConfigRegister = async function trySetConfigRegister(addr, portAddr, scanBoardAddr, data) {
    const req = createSetConfigRegister(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetConfigRegister.js.map