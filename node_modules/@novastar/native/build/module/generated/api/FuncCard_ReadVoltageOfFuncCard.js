import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createFuncCard_ReadVoltageOfFuncCard(addr) {
    const req = new Request(AddressMapping.FuncCard_VoltageOfFuncCardOccupancy, 'FuncCard_ReadVoltageOfFuncCard');
    req.destination = addr;
    req.address = AddressMapping.FuncCard_VoltageOfFuncCardAddr;
    return req;
}
Session.prototype.FuncCard_ReadVoltageOfFuncCard = async function FuncCard_ReadVoltageOfFuncCard(addr) {
    const req = createFuncCard_ReadVoltageOfFuncCard(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryFuncCard_ReadVoltageOfFuncCard =
    async function tryFuncCard_ReadVoltageOfFuncCard(addr) {
        const req = createFuncCard_ReadVoltageOfFuncCard(addr);
        return this.connection.trySend(req);
    };
//# sourceMappingURL=FuncCard_ReadVoltageOfFuncCard.js.map