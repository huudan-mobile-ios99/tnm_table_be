import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createDistribute_ReadDistributeModle(addr, portAddr, distributeAddr) {
    const req = new Request(AddressMapping.Distribute_ModleOccupancy, 'Distribute_ReadDistributeModle');
    req.destination = addr;
    req.deviceType = 3;
    req.port = portAddr;
    req.rcvIndex = distributeAddr;
    req.address = AddressMapping.Distribute_ModleAddr;
    return req;
}
Session.prototype.Distribute_ReadDistributeModle = async function Distribute_ReadDistributeModle(addr, portAddr, distributeAddr) {
    const req = createDistribute_ReadDistributeModle(addr, portAddr, distributeAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryDistribute_ReadDistributeModle =
    async function tryDistribute_ReadDistributeModle(addr, portAddr, distributeAddr) {
        const req = createDistribute_ReadDistributeModle(addr, portAddr, distributeAddr);
        return this.connection.trySend(req);
    };
//# sourceMappingURL=Distribute_ReadDistributeModle.js.map