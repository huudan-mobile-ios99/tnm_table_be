import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadDoubleModelFrameRate(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadDoubleModelFrameRate(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadDoubleModelFrameRate(addr: number, portAddr: number, scanBoardAddr: number): Request;
