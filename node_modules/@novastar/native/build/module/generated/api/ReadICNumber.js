import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadICNumber(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ICNumberOccupancy, 'ReadICNumber');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ICNumberAddr;
    return req;
}
Session.prototype.ReadICNumber = async function ReadICNumber(addr, portAddr, scanBoardAddr) {
    const req = createReadICNumber(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadICNumber = async function tryReadICNumber(addr, portAddr, scanBoardAddr) {
    const req = createReadICNumber(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadICNumber.js.map