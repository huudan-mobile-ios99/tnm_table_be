import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetSender_12bitGammaValue(addr, bBroadcast, gammaValue) {
    const req = new Request([gammaValue], bBroadcast, 'SetSender_12bitGammaValue');
    req.destination = addr;
    req.address = AddressMapping.Sender_12bitGammaValueAddr;
    return req;
}
Session.prototype.SetSender_12bitGammaValue = async function SetSender_12bitGammaValue(addr, bBroadcast, gammaValue) {
    const req = createSetSender_12bitGammaValue(addr, bBroadcast, gammaValue);
    await this.connection.send(req);
};
Session.prototype.trySetSender_12bitGammaValue = async function trySetSender_12bitGammaValue(addr, gammaValue) {
    const req = createSetSender_12bitGammaValue(addr, false, gammaValue);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetSender_12bitGammaValue.js.map