import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        FuncCard_SetDelayTime_1(addr: number, portAddr: number, funcCardAddr: number, bBroadcast: boolean, delayTime: number): Promise<void>;
        tryFuncCard_SetDelayTime_1(addr: number, portAddr: number, funcCardAddr: number, delayTime: number): Promise<ErrorType | null>;
    }
}
export default function createFuncCard_SetDelayTime_1<Broadcast extends boolean>(addr: number, portAddr: number, funcCardAddr: number, bBroadcast: Broadcast, delayTime: number): Request<Broadcast>;
