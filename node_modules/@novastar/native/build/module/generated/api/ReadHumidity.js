import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadHumidity(addr) {
    const req = new Request(AddressMapping.HumidityOccupancy, 'ReadHumidity');
    req.destination = addr;
    req.address = AddressMapping.HumidityAddr;
    return req;
}
Session.prototype.ReadHumidity = async function ReadHumidity(addr) {
    const req = createReadHumidity(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadHumidity = async function tryReadHumidity(addr) {
    const req = createReadHumidity(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadHumidity.js.map