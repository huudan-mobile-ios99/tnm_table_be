import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScanner_DMPM(addr, portAddr, scanBoardAddr, readLength) {
    const req = new Request(readLength, 'ReadScanner_DMPM');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Scanner_DmPMAAddr;
    return req;
}
Session.prototype.ReadScanner_DMPM = async function ReadScanner_DMPM(addr, portAddr, scanBoardAddr, readLength) {
    const req = createReadScanner_DMPM(addr, portAddr, scanBoardAddr, readLength);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadScanner_DMPM = async function tryReadScanner_DMPM(addr, portAddr, scanBoardAddr, readLength) {
    const req = createReadScanner_DMPM(addr, portAddr, scanBoardAddr, readLength);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScanner_DMPM.js.map