import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadEndRowScalePos(addr) {
    const req = new Request(AddressMapping.EndRowScalePosOccupancy, 'ReadEndRowScalePos');
    req.destination = addr;
    req.address = AddressMapping.EndRowScalePosAddr;
    return req;
}
Session.prototype.ReadEndRowScalePos = async function ReadEndRowScalePos(addr) {
    const req = createReadEndRowScalePos(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadEndRowScalePos = async function tryReadEndRowScalePos(addr) {
    const req = createReadEndRowScalePos(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadEndRowScalePos.js.map