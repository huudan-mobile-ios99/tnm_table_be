import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadMGCoefData(addr, portAddr, scanBoardAddr) {
    const req = new Request(256, 'ReadMGCoefData');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.MagicGrayLutAddr;
    return req;
}
Session.prototype.ReadMGCoefData = async function ReadMGCoefData(addr, portAddr, scanBoardAddr) {
    const req = createReadMGCoefData(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadMGCoefData = async function tryReadMGCoefData(addr, portAddr, scanBoardAddr) {
    const req = createReadMGCoefData(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadMGCoefData.js.map