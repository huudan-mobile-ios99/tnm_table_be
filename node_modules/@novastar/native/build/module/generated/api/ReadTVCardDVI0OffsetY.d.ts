import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadTVCardDVI0OffsetY(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadTVCardDVI0OffsetY(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadTVCardDVI0OffsetY(addr: number, portAddr: number, scanBoardAddr: number): Request;
