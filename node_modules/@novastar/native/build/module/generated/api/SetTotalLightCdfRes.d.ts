import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetTotalLightCdfRes(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, totalLightCdfRes: number): Promise<void>;
        trySetTotalLightCdfRes(addr: number, portAddr: number, scanBoardAddr: number, totalLightCdfRes: number): Promise<ErrorType | null>;
    }
}
export default function createSetTotalLightCdfRes<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, totalLightCdfRes: number): Request<Broadcast>;
