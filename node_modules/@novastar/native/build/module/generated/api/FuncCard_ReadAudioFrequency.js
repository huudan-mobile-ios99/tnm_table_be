import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createFuncCard_ReadAudioFrequency(addr) {
    const req = new Request(AddressMapping.FuncCard_AudioFrequencyOccupancy, 'FuncCard_ReadAudioFrequency');
    req.destination = addr;
    req.address = AddressMapping.FuncCard_AudioFrequencyAddr;
    return req;
}
Session.prototype.FuncCard_ReadAudioFrequency = async function FuncCard_ReadAudioFrequency(addr) {
    const req = createFuncCard_ReadAudioFrequency(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryFuncCard_ReadAudioFrequency = async function tryFuncCard_ReadAudioFrequency(addr) {
    const req = createFuncCard_ReadAudioFrequency(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=FuncCard_ReadAudioFrequency.js.map