import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetOEPority(addr, portAddr, scanBoardAddr, bBroadcast, oEPority) {
    const $data = encodeUIntLE(oEPority, AddressMapping.OEPorityOccupancy);
    const req = new Request($data, bBroadcast, 'SetOEPority');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.OEPorityAddr;
    return req;
}
Session.prototype.SetOEPority = async function SetOEPority(addr, portAddr, scanBoardAddr, bBroadcast, oEPority) {
    const req = createSetOEPority(addr, portAddr, scanBoardAddr, bBroadcast, oEPority);
    await this.connection.send(req);
};
Session.prototype.trySetOEPority = async function trySetOEPority(addr, portAddr, scanBoardAddr, oEPority) {
    const req = createSetOEPority(addr, portAddr, scanBoardAddr, false, oEPority);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetOEPority.js.map