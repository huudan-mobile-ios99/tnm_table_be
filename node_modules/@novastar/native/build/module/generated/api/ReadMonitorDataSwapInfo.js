import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadMonitorDataSwapInfo(addr, portAddr, scanBoardAddr, readLength) {
    const req = new Request(readLength, 'ReadMonitorDataSwapInfo');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.MonitorGroupSwapInfoAndEnAddr;
    return req;
}
Session.prototype.ReadMonitorDataSwapInfo = async function ReadMonitorDataSwapInfo(addr, portAddr, scanBoardAddr, readLength) {
    const req = createReadMonitorDataSwapInfo(addr, portAddr, scanBoardAddr, readLength);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadMonitorDataSwapInfo = async function tryReadMonitorDataSwapInfo(addr, portAddr, scanBoardAddr, readLength) {
    const req = createReadMonitorDataSwapInfo(addr, portAddr, scanBoardAddr, readLength);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadMonitorDataSwapInfo.js.map