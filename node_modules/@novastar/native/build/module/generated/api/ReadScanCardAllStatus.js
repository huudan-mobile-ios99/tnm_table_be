import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScanCardAllStatus(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.Scanner_ScanCardAllStatusOccupancy, 'ReadScanCardAllStatus');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Scanner_ScanCardAllStatusAddr;
    return req;
}
Session.prototype.ReadScanCardAllStatus = async function ReadScanCardAllStatus(addr, portAddr, scanBoardAddr) {
    const req = createReadScanCardAllStatus(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadScanCardAllStatus = async function tryReadScanCardAllStatus(addr, portAddr, scanBoardAddr) {
    const req = createReadScanCardAllStatus(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScanCardAllStatus.js.map