import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadAllPortWidth(addr) {
    const req = new Request(AddressMapping.Sender_AllDataSpaceOccupancy, 'ReadAllPortWidth');
    req.destination = addr;
    req.address = AddressMapping.Sender_AllDataAddr;
    return req;
}
Session.prototype.ReadAllPortWidth = async function ReadAllPortWidth(addr) {
    const req = createReadAllPortWidth(addr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadAllPortWidth = async function tryReadAllPortWidth(addr) {
    const req = createReadAllPortWidth(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadAllPortWidth.js.map