import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createFuncCard_SetDelayTime(addr, bBroadcast, delayTime) {
    const $data = encodeUIntLE(delayTime, AddressMapping.FuncCard_DelayTimeOccupancy);
    const req = new Request($data, bBroadcast, 'FuncCard_SetDelayTime');
    req.destination = addr;
    req.address = AddressMapping.FuncCard_DelayTimeAddr;
    return req;
}
Session.prototype.FuncCard_SetDelayTime = async function FuncCard_SetDelayTime(addr, bBroadcast, delayTime) {
    const req = createFuncCard_SetDelayTime(addr, bBroadcast, delayTime);
    await this.connection.send(req);
};
Session.prototype.tryFuncCard_SetDelayTime = async function tryFuncCard_SetDelayTime(addr, delayTime) {
    const req = createFuncCard_SetDelayTime(addr, false, delayTime);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=FuncCard_SetDelayTime.js.map