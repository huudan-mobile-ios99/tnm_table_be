import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        Distribute_SetRebootBootFpgaProgram(addr: number, portAddr: number, distributeAddr: number, bBroadcast: boolean, funcCardModle: number): Promise<void>;
        tryDistribute_SetRebootBootFpgaProgram(addr: number, portAddr: number, distributeAddr: number, funcCardModle: number): Promise<ErrorType | null>;
    }
}
export default function createDistribute_SetRebootBootFpgaProgram<Broadcast extends boolean>(addr: number, portAddr: number, distributeAddr: number, bBroadcast: Broadcast, funcCardModle: number): Request<Broadcast>;
