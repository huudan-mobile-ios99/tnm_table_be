import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadControllerSnHigh(addr) {
    const req = new Request(AddressMapping.ControllerSnHighOccupancy, 'ReadControllerSnHigh');
    req.destination = addr;
    req.address = AddressMapping.ControllerSnHighAddr;
    return req;
}
Session.prototype.ReadControllerSnHigh = async function ReadControllerSnHigh(addr) {
    const req = createReadControllerSnHigh(addr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadControllerSnHigh = async function tryReadControllerSnHigh(addr) {
    const req = createReadControllerSnHigh(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadControllerSnHigh.js.map