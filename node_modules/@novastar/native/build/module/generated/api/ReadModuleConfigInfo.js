import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadModuleConfigInfo(addr, portAddr, scanBoardAddr, readLength) {
    const req = new Request(readLength, 'ReadModuleConfigInfo');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ModuleConfigInfoAddr;
    return req;
}
Session.prototype.ReadModuleConfigInfo = async function ReadModuleConfigInfo(addr, portAddr, scanBoardAddr, readLength) {
    const req = createReadModuleConfigInfo(addr, portAddr, scanBoardAddr, readLength);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadModuleConfigInfo = async function tryReadModuleConfigInfo(addr, portAddr, scanBoardAddr, readLength) {
    const req = createReadModuleConfigInfo(addr, portAddr, scanBoardAddr, readLength);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadModuleConfigInfo.js.map