import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetRebootWorkFPGAProgram(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, rebootWorkFPGAProgram: number): Promise<void>;
        trySetRebootWorkFPGAProgram(addr: number, portAddr: number, scanBoardAddr: number, rebootWorkFPGAProgram: number): Promise<ErrorType | null>;
    }
}
export default function createSetRebootWorkFPGAProgram<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, rebootWorkFPGAProgram: number): Request<Broadcast>;
