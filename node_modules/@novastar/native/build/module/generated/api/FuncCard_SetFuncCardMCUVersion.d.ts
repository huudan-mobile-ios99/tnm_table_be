import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        FuncCard_SetFuncCardMCUVersion(addr: number, bBroadcast: boolean, mcuVersion: number): Promise<void>;
        tryFuncCard_SetFuncCardMCUVersion(addr: number, mcuVersion: number): Promise<ErrorType | null>;
    }
}
export default function createFuncCard_SetFuncCardMCUVersion<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, mcuVersion: number): Request<Broadcast>;
