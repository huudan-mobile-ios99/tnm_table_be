import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetScreenDecrypt(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, screenDecrypt: number): Promise<void>;
        trySetScreenDecrypt(addr: number, portAddr: number, scanBoardAddr: number, screenDecrypt: number): Promise<ErrorType | null>;
    }
}
export default function createSetScreenDecrypt<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, screenDecrypt: number): Request<Broadcast>;
