import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetGrayCoefficientEN(Sender, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = new Request([data], bBroadcast, 'SetGrayCoefficientEN');
    req.destination = Sender;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.GrayCoefficientENAddr;
    return req;
}
Session.prototype.SetGrayCoefficientEN = async function SetGrayCoefficientEN(Sender, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createSetGrayCoefficientEN(Sender, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.trySetGrayCoefficientEN = async function trySetGrayCoefficientEN(Sender, portAddr, scanBoardAddr, data) {
    const req = createSetGrayCoefficientEN(Sender, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetGrayCoefficientEN.js.map