import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSmartSetMode(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.SmartSetModeOccupancy, 'ReadSmartSetMode');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.SmartSetModeAddr;
    return req;
}
Session.prototype.ReadSmartSetMode = async function ReadSmartSetMode(addr, portAddr, scanBoardAddr) {
    const req = createReadSmartSetMode(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadSmartSetMode = async function tryReadSmartSetMode(addr, portAddr, scanBoardAddr) {
    const req = createReadSmartSetMode(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSmartSetMode.js.map