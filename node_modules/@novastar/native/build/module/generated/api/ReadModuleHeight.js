import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadModuleHeight(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ModuleHeightOccupancy, 'ReadModuleHeight');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ModuleHeightAddr;
    return req;
}
Session.prototype.ReadModuleHeight = async function ReadModuleHeight(addr, portAddr, scanBoardAddr) {
    const req = createReadModuleHeight(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadModuleHeight = async function tryReadModuleHeight(addr, portAddr, scanBoardAddr) {
    const req = createReadModuleHeight(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadModuleHeight.js.map