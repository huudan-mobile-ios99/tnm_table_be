import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadBlueBrightness(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.BlueBrightnessOccupancy, 'ReadBlueBrightness');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.BlueBrightnessAddr;
    return req;
}
Session.prototype.ReadBlueBrightness = async function ReadBlueBrightness(addr, portAddr, scanBoardAddr) {
    const req = createReadBlueBrightness(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadBlueBrightness = async function tryReadBlueBrightness(addr, portAddr, scanBoardAddr) {
    const req = createReadBlueBrightness(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadBlueBrightness.js.map