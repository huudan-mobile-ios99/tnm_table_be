import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetBrightnessModel(addr, portAddr, scanBoardAddr, bBroadcast, brightnessmodel) {
    const $data = encodeUIntLE(brightnessmodel, AddressMapping.BrightnessModelOccupancy);
    const req = new Request($data, bBroadcast, 'SetBrightnessModel');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.BrightnessModelAddr;
    return req;
}
Session.prototype.SetBrightnessModel = async function SetBrightnessModel(addr, portAddr, scanBoardAddr, bBroadcast, brightnessmodel) {
    const req = createSetBrightnessModel(addr, portAddr, scanBoardAddr, bBroadcast, brightnessmodel);
    await this.connection.send(req);
};
Session.prototype.trySetBrightnessModel = async function trySetBrightnessModel(addr, portAddr, scanBoardAddr, brightnessmodel) {
    const req = createSetBrightnessModel(addr, portAddr, scanBoardAddr, false, brightnessmodel);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetBrightnessModel.js.map