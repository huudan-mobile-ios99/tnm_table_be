import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createScaner_UnLock(addr, bBroadcast, passWord) {
    const $data = encodeUIntLE(passWord, AddressMapping.Scaner_UnLockOccupancy);
    const req = new Request($data, bBroadcast, 'Scaner_UnLock');
    req.destination = addr;
    req.address = AddressMapping.Scaner_UnLockAddr;
    return req;
}
Session.prototype.Scaner_UnLock = async function Scaner_UnLock(addr, bBroadcast, passWord) {
    const req = createScaner_UnLock(addr, bBroadcast, passWord);
    await this.connection.send(req);
};
Session.prototype.tryScaner_UnLock = async function tryScaner_UnLock(addr, passWord) {
    const req = createScaner_UnLock(addr, false, passWord);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=Scaner_UnLock.js.map