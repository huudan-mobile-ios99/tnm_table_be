import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadDviEncyptEn(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadDviEncyptEn(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadDviEncyptEn(addr: number, portAddr: number, scanBoardAddr: number): Request;
