import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetDviSelect(addr: number, bBroadcast: boolean, dviSelect: number): Promise<void>;
        trySetDviSelect(addr: number, dviSelect: number): Promise<ErrorType | null>;
    }
}
export default function createSetDviSelect<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, dviSelect: number): Request<Broadcast>;
