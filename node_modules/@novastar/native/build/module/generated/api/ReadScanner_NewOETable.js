import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScanner_NewOETable(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.NewOETableOccupancy, 'ReadScanner_NewOETable');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.NewOETableAddr;
    return req;
}
Session.prototype.ReadScanner_NewOETable = async function ReadScanner_NewOETable(addr, portAddr, scanBoardAddr) {
    const req = createReadScanner_NewOETable(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadScanner_NewOETable = async function tryReadScanner_NewOETable(addr, portAddr, scanBoardAddr) {
    const req = createReadScanner_NewOETable(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScanner_NewOETable.js.map