import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetSender_EnableProgramBright(addr, bBroadcast, isEnable) {
    const req = new Request([isEnable ? 91 : 255], bBroadcast, 'SetSender_EnableProgramBright');
    req.destination = addr;
    req.address = AddressMapping.Sender_EnableProgramBrightAddr;
    return req;
}
Session.prototype.SetSender_EnableProgramBright = async function SetSender_EnableProgramBright(addr, bBroadcast, isEnable) {
    const req = createSetSender_EnableProgramBright(addr, bBroadcast, isEnable);
    await this.connection.send(req);
};
Session.prototype.trySetSender_EnableProgramBright =
    async function trySetSender_EnableProgramBright(addr, isEnable) {
        const req = createSetSender_EnableProgramBright(addr, false, isEnable);
        return (await this.connection.trySend(req))?.ack ?? null;
    };
//# sourceMappingURL=SetSender_EnableProgramBright.js.map