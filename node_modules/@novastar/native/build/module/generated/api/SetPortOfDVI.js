import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetPortOfDVI(addr, bBroadcast, dviIndex, portIndex) {
    const req = new Request([dviIndex], bBroadcast, 'SetPortOfDVI');
    req.destination = addr;
    req.address = AddressMapping.DVIOfPortInfoAddr + portIndex;
    return req;
}
Session.prototype.SetPortOfDVI = async function SetPortOfDVI(addr, bBroadcast, dviIndex, portIndex) {
    const req = createSetPortOfDVI(addr, bBroadcast, dviIndex, portIndex);
    await this.connection.send(req);
};
Session.prototype.trySetPortOfDVI = async function trySetPortOfDVI(addr, dviIndex, portIndex) {
    const req = createSetPortOfDVI(addr, false, dviIndex, portIndex);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetPortOfDVI.js.map