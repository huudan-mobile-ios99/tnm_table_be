import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadAllScaleInfo(addr) {
    const req = new Request(AddressMapping.AllScaleInfoOccupancy, 'ReadAllScaleInfo');
    req.destination = addr;
    req.address = AddressMapping.AllScaleInfoAddr;
    return req;
}
Session.prototype.ReadAllScaleInfo = async function ReadAllScaleInfo(addr) {
    const req = createReadAllScaleInfo(addr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadAllScaleInfo = async function tryReadAllScaleInfo(addr) {
    const req = createReadAllScaleInfo(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadAllScaleInfo.js.map