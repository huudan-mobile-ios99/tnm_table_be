import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadHalfFreqSetMode(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.HalfFreqSetModeOccupancy, 'ReadHalfFreqSetMode');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.HalfFreqSetModeAddr;
    return req;
}
Session.prototype.ReadHalfFreqSetMode = async function ReadHalfFreqSetMode(addr, portAddr, scanBoardAddr) {
    const req = createReadHalfFreqSetMode(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadHalfFreqSetMode = async function tryReadHalfFreqSetMode(addr, portAddr, scanBoardAddr) {
    const req = createReadHalfFreqSetMode(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadHalfFreqSetMode.js.map