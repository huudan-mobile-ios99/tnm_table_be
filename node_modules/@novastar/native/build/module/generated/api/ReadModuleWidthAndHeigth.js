import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadModuleWidthAndHeigth(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ModuleWidthAndHeightOccupancy, 'ReadModuleWidthAndHeigth');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ModuleWidthAddr;
    return req;
}
Session.prototype.ReadModuleWidthAndHeigth = async function ReadModuleWidthAndHeigth(addr, portAddr, scanBoardAddr) {
    const req = createReadModuleWidthAndHeigth(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadModuleWidthAndHeigth = async function tryReadModuleWidthAndHeigth(addr, portAddr, scanBoardAddr) {
    const req = createReadModuleWidthAndHeigth(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadModuleWidthAndHeigth.js.map