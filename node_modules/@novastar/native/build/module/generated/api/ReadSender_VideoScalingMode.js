import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSender_VideoScalingMode(addr) {
    const req = new Request(AddressMapping.Sender_VideoScalingModeOccupancy, 'ReadSender_VideoScalingMode');
    req.destination = addr;
    req.address = AddressMapping.Sender_VideoScalingModeAddr;
    return req;
}
Session.prototype.ReadSender_VideoScalingMode = async function ReadSender_VideoScalingMode(addr) {
    const req = createReadSender_VideoScalingMode(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadSender_VideoScalingMode = async function tryReadSender_VideoScalingMode(addr) {
    const req = createReadSender_VideoScalingMode(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSender_VideoScalingMode.js.map