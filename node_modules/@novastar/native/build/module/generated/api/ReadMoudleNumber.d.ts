import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadMoudleNumber(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadMoudleNumber(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadMoudleNumber(addr: number, portAddr: number, scanBoardAddr: number): Request;
