import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createRead5pinControlLight(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ControlLightOccupancy, 'Read5pinControlLight');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ControlLightAddr;
    return req;
}
Session.prototype.Read5pinControlLight = async function Read5pinControlLight(addr, portAddr, scanBoardAddr) {
    const req = createRead5pinControlLight(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryRead5pinControlLight = async function tryRead5pinControlLight(addr, portAddr, scanBoardAddr) {
    const req = createRead5pinControlLight(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=Read5pinControlLight.js.map