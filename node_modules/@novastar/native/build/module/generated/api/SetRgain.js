import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetRgain(addr, portAddr, scanBoardAddr, bBroadcast, rgain) {
    const $data = encodeUIntLE(rgain, AddressMapping.RgainOccupancy);
    const req = new Request($data, bBroadcast, 'SetRgain');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.RgainAddr;
    return req;
}
Session.prototype.SetRgain = async function SetRgain(addr, portAddr, scanBoardAddr, bBroadcast, rgain) {
    const req = createSetRgain(addr, portAddr, scanBoardAddr, bBroadcast, rgain);
    await this.connection.send(req);
};
Session.prototype.trySetRgain = async function trySetRgain(addr, portAddr, scanBoardAddr, rgain) {
    const req = createSetRgain(addr, portAddr, scanBoardAddr, false, rgain);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetRgain.js.map