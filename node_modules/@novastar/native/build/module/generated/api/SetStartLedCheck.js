import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetStartLedCheck(addr, portAddr, scanBoardAddr, bBroadcast, startLedCheck) {
    const $data = encodeUIntLE(startLedCheck, AddressMapping.StartLedCheckOccupancy);
    const req = new Request($data, bBroadcast, 'SetStartLedCheck');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.StartLedCheckAddr;
    return req;
}
Session.prototype.SetStartLedCheck = async function SetStartLedCheck(addr, portAddr, scanBoardAddr, bBroadcast, startLedCheck) {
    const req = createSetStartLedCheck(addr, portAddr, scanBoardAddr, bBroadcast, startLedCheck);
    await this.connection.send(req);
};
Session.prototype.trySetStartLedCheck = async function trySetStartLedCheck(addr, portAddr, scanBoardAddr, startLedCheck) {
    const req = createSetStartLedCheck(addr, portAddr, scanBoardAddr, false, startLedCheck);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetStartLedCheck.js.map