import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetTotalLightCdfResault(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, TotalLightCdfResault: number): Promise<void>;
        trySetTotalLightCdfResault(addr: number, portAddr: number, scanBoardAddr: number, TotalLightCdfResault: number): Promise<ErrorType | null>;
    }
}
export default function createSetTotalLightCdfResault<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, TotalLightCdfResault: number): Request<Broadcast>;
