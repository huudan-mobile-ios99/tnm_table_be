import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetTemprature(addr, portAddr, scanBoardAddr, bBroadcast, tem) {
    const $data = encodeUIntLE(tem, AddressMapping.TempratureOccupancy);
    const req = new Request($data, bBroadcast, 'SetTemprature');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.TempratureAddr;
    return req;
}
Session.prototype.SetTemprature = async function SetTemprature(addr, portAddr, scanBoardAddr, bBroadcast, tem) {
    const req = createSetTemprature(addr, portAddr, scanBoardAddr, bBroadcast, tem);
    await this.connection.send(req);
};
Session.prototype.trySetTemprature = async function trySetTemprature(addr, portAddr, scanBoardAddr, tem) {
    const req = createSetTemprature(addr, portAddr, scanBoardAddr, false, tem);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetTemprature.js.map