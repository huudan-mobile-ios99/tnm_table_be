import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadRgbCode(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.RgbCodeOccupancy, 'ReadRgbCode');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.RgbCodeAddr;
    return req;
}
Session.prototype.ReadRgbCode = async function ReadRgbCode(addr, portAddr, scanBoardAddr) {
    const req = createReadRgbCode(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadRgbCode = async function tryReadRgbCode(addr, portAddr, scanBoardAddr) {
    const req = createReadRgbCode(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadRgbCode.js.map