import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadTotalPointInTable(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadTotalPointInTable(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadTotalPointInTable(addr: number, portAddr: number, scanBoardAddr: number): Request;
