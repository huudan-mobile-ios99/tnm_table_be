import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadDisplayModeWhenNoSignal(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ShowLastFrameWhenCableNotConectedOccupancy, 'ReadDisplayModeWhenNoSignal');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ShowLastFrameWhenCableNotConectedAddr;
    return req;
}
Session.prototype.ReadDisplayModeWhenNoSignal = async function ReadDisplayModeWhenNoSignal(addr, portAddr, scanBoardAddr) {
    const req = createReadDisplayModeWhenNoSignal(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadDisplayModeWhenNoSignal = async function tryReadDisplayModeWhenNoSignal(addr, portAddr, scanBoardAddr) {
    const req = createReadDisplayModeWhenNoSignal(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadDisplayModeWhenNoSignal.js.map