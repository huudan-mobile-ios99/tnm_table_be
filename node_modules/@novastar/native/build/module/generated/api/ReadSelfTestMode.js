import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSelfTestMode(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.SelfTestModeOccupancy, 'ReadSelfTestMode');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.SelfTestModeAddr;
    return req;
}
Session.prototype.ReadSelfTestMode = async function ReadSelfTestMode(addr, portAddr, scanBoardAddr) {
    const req = createReadSelfTestMode(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadSelfTestMode = async function tryReadSelfTestMode(addr, portAddr, scanBoardAddr) {
    const req = createReadSelfTestMode(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSelfTestMode.js.map