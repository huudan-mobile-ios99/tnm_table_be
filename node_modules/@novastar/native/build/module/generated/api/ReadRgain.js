import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadRgain(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.RgainOccupancy, 'ReadRgain');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.RgainAddr;
    return req;
}
Session.prototype.ReadRgain = async function ReadRgain(addr, portAddr, scanBoardAddr) {
    const req = createReadRgain(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadRgain = async function tryReadRgain(addr, portAddr, scanBoardAddr) {
    const req = createReadRgain(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadRgain.js.map