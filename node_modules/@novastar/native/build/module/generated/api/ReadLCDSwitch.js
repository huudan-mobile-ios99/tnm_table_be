import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadLCDSwitch(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.LCDSwitchOccupancy, 'ReadLCDSwitch');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.LCDSwitchAddr;
    return req;
}
Session.prototype.ReadLCDSwitch = async function ReadLCDSwitch(addr, portAddr, scanBoardAddr) {
    const req = createReadLCDSwitch(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadLCDSwitch = async function tryReadLCDSwitch(addr, portAddr, scanBoardAddr) {
    const req = createReadLCDSwitch(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadLCDSwitch.js.map