import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadModuleWidthAndHeigth(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadModuleWidthAndHeigth(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadModuleWidthAndHeigth(addr: number, portAddr: number, scanBoardAddr: number): Request;
