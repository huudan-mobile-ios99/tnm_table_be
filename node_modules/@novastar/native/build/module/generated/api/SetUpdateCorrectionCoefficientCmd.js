import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetUpdateCorrectionCoefficientCmd(addr, portAddr, scanBoardAddr, bBroadcast, valueFlag) {
    const $data = encodeUIntLE(valueFlag, AddressMapping.UpdateCorrectionCoefficientCmdOccupancy);
    const req = new Request($data, bBroadcast, 'SetUpdateCorrectionCoefficientCmd');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.UpdateCorrectionCoefficientCmdAddr;
    return req;
}
Session.prototype.SetUpdateCorrectionCoefficientCmd =
    async function SetUpdateCorrectionCoefficientCmd(addr, portAddr, scanBoardAddr, bBroadcast, valueFlag) {
        const req = createSetUpdateCorrectionCoefficientCmd(addr, portAddr, scanBoardAddr, bBroadcast, valueFlag);
        await this.connection.send(req);
    };
Session.prototype.trySetUpdateCorrectionCoefficientCmd =
    async function trySetUpdateCorrectionCoefficientCmd(addr, portAddr, scanBoardAddr, valueFlag) {
        const req = createSetUpdateCorrectionCoefficientCmd(addr, portAddr, scanBoardAddr, false, valueFlag);
        return (await this.connection.trySend(req))?.ack ?? null;
    };
//# sourceMappingURL=SetUpdateCorrectionCoefficientCmd.js.map