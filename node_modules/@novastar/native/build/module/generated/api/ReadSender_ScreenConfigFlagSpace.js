import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSender_ScreenConfigFlagSpace(addr) {
    const req = new Request(AddressMapping.Sender_ScreenConfigFlagOccupancy, 'ReadSender_ScreenConfigFlagSpace');
    req.destination = addr;
    req.address = AddressMapping.Sender_ScreenConfigFlagAddr;
    return req;
}
Session.prototype.ReadSender_ScreenConfigFlagSpace =
    async function ReadSender_ScreenConfigFlagSpace(addr) {
        const req = createReadSender_ScreenConfigFlagSpace(addr);
        return decodeUIntLE(await this.connection.send(req));
    };
Session.prototype.tryReadSender_ScreenConfigFlagSpace =
    async function tryReadSender_ScreenConfigFlagSpace(addr) {
        const req = createReadSender_ScreenConfigFlagSpace(addr);
        return this.connection.trySend(req);
    };
//# sourceMappingURL=ReadSender_ScreenConfigFlagSpace.js.map