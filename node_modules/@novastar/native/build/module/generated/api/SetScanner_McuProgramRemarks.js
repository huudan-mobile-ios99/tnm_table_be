import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetScanner_McuProgramRemarks(addr, portAddr, scanBoardAddr, bBroadcast, scanner_McuProgramRemarks) {
    if (scanner_McuProgramRemarks.length !== AddressMapping.Scanner_McuProgramRemarksOccupancy)
        throw new TypeError(`Invalid buffer size: ${scanner_McuProgramRemarks.length}`);
    const req = new Request(scanner_McuProgramRemarks, bBroadcast, 'SetScanner_McuProgramRemarks');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Scanner_McuProgramRemarksAddr;
    return req;
}
Session.prototype.SetScanner_McuProgramRemarks = async function SetScanner_McuProgramRemarks(addr, portAddr, scanBoardAddr, bBroadcast, scanner_McuProgramRemarks) {
    const req = createSetScanner_McuProgramRemarks(addr, portAddr, scanBoardAddr, bBroadcast, scanner_McuProgramRemarks);
    await this.connection.send(req);
};
Session.prototype.trySetScanner_McuProgramRemarks = async function trySetScanner_McuProgramRemarks(addr, portAddr, scanBoardAddr, scanner_McuProgramRemarks) {
    const req = createSetScanner_McuProgramRemarks(addr, portAddr, scanBoardAddr, false, scanner_McuProgramRemarks);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetScanner_McuProgramRemarks.js.map