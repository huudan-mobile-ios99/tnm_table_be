import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetRecaculateParameter(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, recaculateParameter: number): Promise<void>;
        trySetRecaculateParameter(addr: number, portAddr: number, scanBoardAddr: number, recaculateParameter: number): Promise<ErrorType | null>;
    }
}
export default function createSetRecaculateParameter<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, recaculateParameter: number): Request<Broadcast>;
