import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadLightTimePerSubFieldRatio(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.LightTimePerSubFieldRatioOccupancy, 'ReadLightTimePerSubFieldRatio');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.LightTimePerSubFieldRatioAddr;
    return req;
}
Session.prototype.ReadLightTimePerSubFieldRatio = async function ReadLightTimePerSubFieldRatio(addr, portAddr, scanBoardAddr) {
    const req = createReadLightTimePerSubFieldRatio(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadLightTimePerSubFieldRatio =
    async function tryReadLightTimePerSubFieldRatio(addr, portAddr, scanBoardAddr) {
        const req = createReadLightTimePerSubFieldRatio(addr, portAddr, scanBoardAddr);
        return this.connection.trySend(req);
    };
//# sourceMappingURL=ReadLightTimePerSubFieldRatio.js.map