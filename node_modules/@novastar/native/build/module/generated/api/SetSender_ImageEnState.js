import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetSender_ImageEnState(addr, bBroadcast, info) {
    const req = new Request([info], bBroadcast, 'SetSender_ImageEnState');
    req.destination = addr;
    req.address = AddressMapping.SenderABLOpreationAddr;
    return req;
}
Session.prototype.SetSender_ImageEnState = async function SetSender_ImageEnState(addr, bBroadcast, info) {
    const req = createSetSender_ImageEnState(addr, bBroadcast, info);
    await this.connection.send(req);
};
Session.prototype.trySetSender_ImageEnState = async function trySetSender_ImageEnState(addr, info) {
    const req = createSetSender_ImageEnState(addr, false, info);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetSender_ImageEnState.js.map