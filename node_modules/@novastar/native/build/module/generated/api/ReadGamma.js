import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadGamma(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.GammaOccupancy, 'ReadGamma');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.GammaAddr;
    return req;
}
Session.prototype.ReadGamma = async function ReadGamma(addr, portAddr, scanBoardAddr) {
    const req = createReadGamma(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadGamma = async function tryReadGamma(addr, portAddr, scanBoardAddr) {
    const req = createReadGamma(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadGamma.js.map