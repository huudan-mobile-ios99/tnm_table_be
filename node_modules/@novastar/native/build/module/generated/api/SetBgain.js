import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetBgain(addr, portAddr, scanBoardAddr, bBroadcast, bgain) {
    const $data = encodeUIntLE(bgain, AddressMapping.BgainOccupancy);
    const req = new Request($data, bBroadcast, 'SetBgain');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.BgainAddr;
    return req;
}
Session.prototype.SetBgain = async function SetBgain(addr, portAddr, scanBoardAddr, bBroadcast, bgain) {
    const req = createSetBgain(addr, portAddr, scanBoardAddr, bBroadcast, bgain);
    await this.connection.send(req);
};
Session.prototype.trySetBgain = async function trySetBgain(addr, portAddr, scanBoardAddr, bgain) {
    const req = createSetBgain(addr, portAddr, scanBoardAddr, false, bgain);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetBgain.js.map