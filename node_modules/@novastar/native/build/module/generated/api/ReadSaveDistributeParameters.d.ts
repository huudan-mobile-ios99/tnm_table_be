import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadSaveDistributeParameters(addr: number, portAddr: number, distributeAddr: number): Promise<number>;
        tryReadSaveDistributeParameters(addr: number, portAddr: number, distributeAddr: number): Promise<Packet | null>;
    }
}
export default function createReadSaveDistributeParameters(addr: number, portAddr: number, distributeAddr: number): Request;
