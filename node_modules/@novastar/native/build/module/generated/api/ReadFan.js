import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadFan(addr) {
    const req = new Request(AddressMapping.FanOccupancy, 'ReadFan');
    req.destination = addr;
    req.address = AddressMapping.FanAddr;
    return req;
}
Session.prototype.ReadFan = async function ReadFan(addr) {
    const req = createReadFan(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadFan = async function tryReadFan(addr) {
    const req = createReadFan(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadFan.js.map