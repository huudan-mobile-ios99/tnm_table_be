import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createFuncCard_ReadOutDeviceValue_3(addr, portAddr, funcCardAddr, dataLength) {
    const req = new Request(dataLength, 'FuncCard_ReadOutDeviceValue_3');
    req.destination = addr;
    req.deviceType = 2;
    req.port = portAddr;
    req.rcvIndex = funcCardAddr;
    req.address = AddressMapping.FuncCard_ReadOutDeviceAddr;
    return req;
}
Session.prototype.FuncCard_ReadOutDeviceValue_3 = async function FuncCard_ReadOutDeviceValue_3(addr, portAddr, funcCardAddr, dataLength) {
    const req = createFuncCard_ReadOutDeviceValue_3(addr, portAddr, funcCardAddr, dataLength);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryFuncCard_ReadOutDeviceValue_3 =
    async function tryFuncCard_ReadOutDeviceValue_3(addr, portAddr, funcCardAddr, dataLength) {
        const req = createFuncCard_ReadOutDeviceValue_3(addr, portAddr, funcCardAddr, dataLength);
        return this.connection.trySend(req);
    };
//# sourceMappingURL=FuncCard_ReadOutDeviceValue_3.js.map