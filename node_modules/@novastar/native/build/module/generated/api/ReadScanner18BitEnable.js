import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScanner18BitEnable(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.Scanner18BitEnableOccupancy, 'ReadScanner18BitEnable');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Scanner18BitEnableAddr;
    return req;
}
Session.prototype.ReadScanner18BitEnable = async function ReadScanner18BitEnable(addr, portAddr, scanBoardAddr) {
    const req = createReadScanner18BitEnable(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadScanner18BitEnable = async function tryReadScanner18BitEnable(addr, portAddr, scanBoardAddr) {
    const req = createReadScanner18BitEnable(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScanner18BitEnable.js.map