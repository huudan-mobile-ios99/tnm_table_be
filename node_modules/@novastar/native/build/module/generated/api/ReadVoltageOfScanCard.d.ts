import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadVoltageOfScanCard(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadVoltageOfScanCard(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadVoltageOfScanCard(addr: number, portAddr: number, scanBoardAddr: number): Request;
