import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetScaleInfo(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, scaleInfo: number): Promise<void>;
        trySetScaleInfo(addr: number, portAddr: number, scanBoardAddr: number, scaleInfo: number): Promise<ErrorType | null>;
    }
}
export default function createSetScaleInfo<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, scaleInfo: number): Request<Broadcast>;
