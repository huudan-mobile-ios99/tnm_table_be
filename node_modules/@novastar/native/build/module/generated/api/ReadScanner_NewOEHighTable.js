import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScanner_NewOEHighTable(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.NewOEHighTableOccupancy, 'ReadScanner_NewOEHighTable');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.NewOEHighTableAddr;
    return req;
}
Session.prototype.ReadScanner_NewOEHighTable = async function ReadScanner_NewOEHighTable(addr, portAddr, scanBoardAddr) {
    const req = createReadScanner_NewOEHighTable(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadScanner_NewOEHighTable = async function tryReadScanner_NewOEHighTable(addr, portAddr, scanBoardAddr) {
    const req = createReadScanner_NewOEHighTable(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScanner_NewOEHighTable.js.map