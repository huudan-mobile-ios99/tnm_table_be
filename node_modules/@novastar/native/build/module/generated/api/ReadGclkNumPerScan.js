import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadGclkNumPerScan(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.GclkNumPerScanOccupancy, 'ReadGclkNumPerScan');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.GclkNumPerScanAddr;
    return req;
}
Session.prototype.ReadGclkNumPerScan = async function ReadGclkNumPerScan(addr, portAddr, scanBoardAddr) {
    const req = createReadGclkNumPerScan(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadGclkNumPerScan = async function tryReadGclkNumPerScan(addr, portAddr, scanBoardAddr) {
    const req = createReadGclkNumPerScan(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadGclkNumPerScan.js.map