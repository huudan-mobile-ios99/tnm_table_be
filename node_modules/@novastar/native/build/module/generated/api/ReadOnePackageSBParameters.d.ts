/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadOnePackageSBParameters(addr: number, portAddr: number, scanBoardAddr: number): Promise<Buffer>;
        tryReadOnePackageSBParameters(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadOnePackageSBParameters(addr: number, portAddr: number, scanBoardAddr: number): Request;
