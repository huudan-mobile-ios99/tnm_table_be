import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetShowNormal(addr: number, bBroadcast: boolean, data: number): Promise<void>;
        trySetShowNormal(addr: number, data: number): Promise<ErrorType | null>;
    }
}
export default function createSetShowNormal<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, data: number): Request<Broadcast>;
