import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetConfigRegisterFor2020(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    if (data.length !== 1)
        throw new TypeError(`Invalid buffer size: ${data.length}`);
    const req = new Request(data, bBroadcast, 'SetConfigRegisterFor2020');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.WriteConfigRegisterAddrFor2020;
    return req;
}
Session.prototype.SetConfigRegisterFor2020 = async function SetConfigRegisterFor2020(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createSetConfigRegisterFor2020(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.trySetConfigRegisterFor2020 = async function trySetConfigRegisterFor2020(addr, portAddr, scanBoardAddr, data) {
    const req = createSetConfigRegisterFor2020(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetConfigRegisterFor2020.js.map