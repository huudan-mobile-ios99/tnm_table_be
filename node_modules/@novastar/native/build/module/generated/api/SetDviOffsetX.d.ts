import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetDviOffsetX(addr: number, bBroadcast: boolean, dviOffsetX: number, index: number): Promise<void>;
        trySetDviOffsetX(addr: number, dviOffsetX: number, index: number): Promise<ErrorType | null>;
    }
}
export default function createSetDviOffsetX<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, dviOffsetX: number, index: number): Request<Broadcast>;
