import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetReadColorCoef(addr, portAddr, scanBoardAddr, bBroadcast, val) {
    const req = new Request([val], bBroadcast, 'SetReadColorCoef');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.SetReadColorCoefAddr;
    return req;
}
Session.prototype.SetReadColorCoef = async function SetReadColorCoef(addr, portAddr, scanBoardAddr, bBroadcast, val) {
    const req = createSetReadColorCoef(addr, portAddr, scanBoardAddr, bBroadcast, val);
    await this.connection.send(req);
};
Session.prototype.trySetReadColorCoef = async function trySetReadColorCoef(addr, portAddr, scanBoardAddr, val) {
    const req = createSetReadColorCoef(addr, portAddr, scanBoardAddr, false, val);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetReadColorCoef.js.map