import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetGroupSwapEn(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, isEnableGroupSwapEn: boolean): Promise<void>;
        trySetGroupSwapEn(addr: number, portAddr: number, scanBoardAddr: number, isEnableGroupSwapEn: boolean): Promise<ErrorType | null>;
    }
}
export default function createSetGroupSwapEn<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, isEnableGroupSwapEn: boolean): Request<Broadcast>;
