import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetScanner_FPGAProgramLength(addr, portAddr, scanBoardAddr, bBroadcast, scanner_FPGAProgramLength) {
    const $data = encodeUIntLE(scanner_FPGAProgramLength, AddressMapping.Scanner_FPGAProgramLengthOccupancy);
    const req = new Request($data, bBroadcast, 'SetScanner_FPGAProgramLength');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Scanner_FPGAProgramLengthAddr;
    return req;
}
Session.prototype.SetScanner_FPGAProgramLength = async function SetScanner_FPGAProgramLength(addr, portAddr, scanBoardAddr, bBroadcast, scanner_FPGAProgramLength) {
    const req = createSetScanner_FPGAProgramLength(addr, portAddr, scanBoardAddr, bBroadcast, scanner_FPGAProgramLength);
    await this.connection.send(req);
};
Session.prototype.trySetScanner_FPGAProgramLength = async function trySetScanner_FPGAProgramLength(addr, portAddr, scanBoardAddr, scanner_FPGAProgramLength) {
    const req = createSetScanner_FPGAProgramLength(addr, portAddr, scanBoardAddr, false, scanner_FPGAProgramLength);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetScanner_FPGAProgramLength.js.map