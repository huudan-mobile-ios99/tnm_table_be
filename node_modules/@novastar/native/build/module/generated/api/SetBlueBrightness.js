import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetBlueBrightness(addr, portAddr, scanBoardAddr, bBroadcast, blueBrightness) {
    const $data = encodeUIntLE(blueBrightness, AddressMapping.BlueBrightnessOccupancy);
    const req = new Request($data, bBroadcast, 'SetBlueBrightness');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.BlueBrightnessAddr;
    return req;
}
Session.prototype.SetBlueBrightness = async function SetBlueBrightness(addr, portAddr, scanBoardAddr, bBroadcast, blueBrightness) {
    const req = createSetBlueBrightness(addr, portAddr, scanBoardAddr, bBroadcast, blueBrightness);
    await this.connection.send(req);
};
Session.prototype.trySetBlueBrightness = async function trySetBlueBrightness(addr, portAddr, scanBoardAddr, blueBrightness) {
    const req = createSetBlueBrightness(addr, portAddr, scanBoardAddr, false, blueBrightness);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetBlueBrightness.js.map