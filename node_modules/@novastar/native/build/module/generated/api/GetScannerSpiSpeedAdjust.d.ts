import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        GetScannerSpiSpeedAdjust(Sender: number, portAddr: number, Scan: number): Promise<number>;
        tryGetScannerSpiSpeedAdjust(Sender: number, portAddr: number, Scan: number): Promise<Packet | null>;
    }
}
export default function createGetScannerSpiSpeedAdjust(Sender: number, portAddr: number, Scan: number): Request;
