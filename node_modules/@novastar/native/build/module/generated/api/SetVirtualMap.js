import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetVirtualMap(addr, bBroadcast, virtualMap) {
    const $data = encodeUIntLE(virtualMap, AddressMapping.VirtualMapOccupancy);
    const req = new Request($data, bBroadcast, 'SetVirtualMap');
    req.destination = addr;
    req.address = AddressMapping.VirtualMapAddr;
    return req;
}
Session.prototype.SetVirtualMap = async function SetVirtualMap(addr, bBroadcast, virtualMap) {
    const req = createSetVirtualMap(addr, bBroadcast, virtualMap);
    await this.connection.send(req);
};
Session.prototype.trySetVirtualMap = async function trySetVirtualMap(addr, virtualMap) {
    const req = createSetVirtualMap(addr, false, virtualMap);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetVirtualMap.js.map