import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetDviWidth(addr: number, bBroadcast: boolean, dviWidth: number, index: number): Promise<void>;
        trySetDviWidth(addr: number, dviWidth: number, index: number): Promise<ErrorType | null>;
    }
}
export default function createSetDviWidth<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, dviWidth: number, index: number): Request<Broadcast>;
