import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadLocalStartX(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.LocalStartXOccupancy, 'ReadLocalStartX');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.LocalStartXAddr;
    return req;
}
Session.prototype.ReadLocalStartX = async function ReadLocalStartX(addr, portAddr, scanBoardAddr) {
    const req = createReadLocalStartX(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadLocalStartX = async function tryReadLocalStartX(addr, portAddr, scanBoardAddr) {
    const req = createReadLocalStartX(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadLocalStartX.js.map