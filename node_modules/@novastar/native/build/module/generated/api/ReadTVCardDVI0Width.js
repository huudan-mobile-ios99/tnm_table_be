import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadTVCardDVI0Width(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.TVCardDVI0WidthOccupancy, 'ReadTVCardDVI0Width');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.TVCardDVI0WidthAddr;
    return req;
}
Session.prototype.ReadTVCardDVI0Width = async function ReadTVCardDVI0Width(addr, portAddr, scanBoardAddr) {
    const req = createReadTVCardDVI0Width(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadTVCardDVI0Width = async function tryReadTVCardDVI0Width(addr, portAddr, scanBoardAddr) {
    const req = createReadTVCardDVI0Width(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadTVCardDVI0Width.js.map