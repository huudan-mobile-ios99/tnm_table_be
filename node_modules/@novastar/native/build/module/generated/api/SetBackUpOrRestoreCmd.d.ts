import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetBackUpOrRestoreCmd(addr: number, bBroadcast: boolean, writeData: number): Promise<void>;
        trySetBackUpOrRestoreCmd(addr: number, writeData: number): Promise<ErrorType | null>;
    }
}
export default function createSetBackUpOrRestoreCmd<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, writeData: number): Request<Broadcast>;
