import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadColScalePara(addr) {
    const req = new Request(AddressMapping.ColScaleParAOccupancy, 'ReadColScalePara');
    req.destination = addr;
    req.address = AddressMapping.ColScaleParaAddr;
    return req;
}
Session.prototype.ReadColScalePara = async function ReadColScalePara(addr) {
    const req = createReadColScalePara(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadColScalePara = async function tryReadColScalePara(addr) {
    const req = createReadColScalePara(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadColScalePara.js.map