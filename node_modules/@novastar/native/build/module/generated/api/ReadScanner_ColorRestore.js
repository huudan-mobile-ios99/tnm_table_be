import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScanner_ColorRestore(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ColorRestoreOccupancy, 'ReadScanner_ColorRestore');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ColorRestoreAddr;
    return req;
}
Session.prototype.ReadScanner_ColorRestore = async function ReadScanner_ColorRestore(addr, portAddr, scanBoardAddr) {
    const req = createReadScanner_ColorRestore(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadScanner_ColorRestore = async function tryReadScanner_ColorRestore(addr, portAddr, scanBoardAddr) {
    const req = createReadScanner_ColorRestore(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScanner_ColorRestore.js.map