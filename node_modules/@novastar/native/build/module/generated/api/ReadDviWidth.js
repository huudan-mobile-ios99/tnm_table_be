import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadDviWidth(addr, index) {
    const req = new Request(AddressMapping.DviWidthOccupancy, 'ReadDviWidth');
    req.destination = addr;
    req.address = AddressMapping.DviWidthAddr + AddressMapping.DviOccupancy * index;
    return req;
}
Session.prototype.ReadDviWidth = async function ReadDviWidth(addr, index) {
    const req = createReadDviWidth(addr, index);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadDviWidth = async function tryReadDviWidth(addr, index) {
    const req = createReadDviWidth(addr, index);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadDviWidth.js.map