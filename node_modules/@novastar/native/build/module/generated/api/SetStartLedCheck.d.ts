import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetStartLedCheck(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, startLedCheck: number): Promise<void>;
        trySetStartLedCheck(addr: number, portAddr: number, scanBoardAddr: number, startLedCheck: number): Promise<ErrorType | null>;
    }
}
export default function createSetStartLedCheck<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, startLedCheck: number): Request<Broadcast>;
