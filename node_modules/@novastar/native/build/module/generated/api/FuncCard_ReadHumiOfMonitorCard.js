import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createFuncCard_ReadHumiOfMonitorCard(addr) {
    const req = new Request(AddressMapping.FuncCard_HumiOfMonitorCardOccupancy, 'FuncCard_ReadHumiOfMonitorCard');
    req.destination = addr;
    req.address = AddressMapping.FuncCard_HumiOfMonitorCardAddr;
    return req;
}
Session.prototype.FuncCard_ReadHumiOfMonitorCard = async function FuncCard_ReadHumiOfMonitorCard(addr) {
    const req = createFuncCard_ReadHumiOfMonitorCard(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryFuncCard_ReadHumiOfMonitorCard =
    async function tryFuncCard_ReadHumiOfMonitorCard(addr) {
        const req = createFuncCard_ReadHumiOfMonitorCard(addr);
        return this.connection.trySend(req);
    };
//# sourceMappingURL=FuncCard_ReadHumiOfMonitorCard.js.map