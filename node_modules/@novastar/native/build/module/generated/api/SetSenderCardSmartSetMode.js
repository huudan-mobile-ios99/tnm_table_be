import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetSenderCardSmartSetMode(addr, bBroadcast, data) {
    const req = new Request(data, bBroadcast, 'SetSenderCardSmartSetMode');
    req.destination = addr;
    req.address = AddressMapping.SenderCardSmartSetModeAddr;
    return req;
}
Session.prototype.SetSenderCardSmartSetMode = async function SetSenderCardSmartSetMode(addr, bBroadcast, data) {
    const req = createSetSenderCardSmartSetMode(addr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.trySetSenderCardSmartSetMode = async function trySetSenderCardSmartSetMode(addr, data) {
    const req = createSetSenderCardSmartSetMode(addr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetSenderCardSmartSetMode.js.map