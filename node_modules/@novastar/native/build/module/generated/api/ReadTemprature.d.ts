import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadTemprature(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadTemprature(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadTemprature(addr: number, portAddr: number, scanBoardAddr: number): Request;
