import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSender_ScreenConfigSpace(addr) {
    const req = new Request(AddressMapping.Sender_ScreenConfigSpaceOccupancy, 'ReadSender_ScreenConfigSpace');
    req.destination = addr;
    req.address = AddressMapping.Sender_ScreenConfigSpaceAddr;
    return req;
}
Session.prototype.ReadSender_ScreenConfigSpace = async function ReadSender_ScreenConfigSpace(addr) {
    const req = createReadSender_ScreenConfigSpace(addr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadSender_ScreenConfigSpace = async function tryReadSender_ScreenConfigSpace(addr) {
    const req = createReadSender_ScreenConfigSpace(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSender_ScreenConfigSpace.js.map