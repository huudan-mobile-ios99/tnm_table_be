import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetSmartSetMode(addr, portAddr, scanBoardAddr, bBroadcast, smartMode) {
    const $data = encodeUIntLE(smartMode, AddressMapping.SmartSetModeOccupancy);
    const req = new Request($data, bBroadcast, 'SetSmartSetMode');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.SmartSetModeAddr;
    return req;
}
Session.prototype.SetSmartSetMode = async function SetSmartSetMode(addr, portAddr, scanBoardAddr, bBroadcast, smartMode) {
    const req = createSetSmartSetMode(addr, portAddr, scanBoardAddr, bBroadcast, smartMode);
    await this.connection.send(req);
};
Session.prototype.trySetSmartSetMode = async function trySetSmartSetMode(addr, portAddr, scanBoardAddr, smartMode) {
    const req = createSetSmartSetMode(addr, portAddr, scanBoardAddr, false, smartMode);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetSmartSetMode.js.map