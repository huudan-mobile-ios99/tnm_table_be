import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadDviSelect(addr) {
    const req = new Request(AddressMapping.DviSelectOccupancy, 'ReadDviSelect');
    req.destination = addr;
    req.address = AddressMapping.DviSelectAddr;
    return req;
}
Session.prototype.ReadDviSelect = async function ReadDviSelect(addr) {
    const req = createReadDviSelect(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadDviSelect = async function tryReadDviSelect(addr) {
    const req = createReadDviSelect(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadDviSelect.js.map