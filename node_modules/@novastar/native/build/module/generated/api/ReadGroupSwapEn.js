import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadGroupSwapEn(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.GroupSwapEnOccupancy, 'ReadGroupSwapEn');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.GroupSwapEnAddr;
    return req;
}
Session.prototype.ReadGroupSwapEn = async function ReadGroupSwapEn(addr, portAddr, scanBoardAddr) {
    const req = createReadGroupSwapEn(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadGroupSwapEn = async function tryReadGroupSwapEn(addr, portAddr, scanBoardAddr) {
    const req = createReadGroupSwapEn(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadGroupSwapEn.js.map