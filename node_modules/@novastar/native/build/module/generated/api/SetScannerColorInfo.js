import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetScannerColorInfo(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = new Request(data, bBroadcast, 'SetScannerColorInfo');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ScannerColorInfoAddr;
    return req;
}
Session.prototype.SetScannerColorInfo = async function SetScannerColorInfo(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createSetScannerColorInfo(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.trySetScannerColorInfo = async function trySetScannerColorInfo(addr, portAddr, scanBoardAddr, data) {
    const req = createSetScannerColorInfo(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetScannerColorInfo.js.map