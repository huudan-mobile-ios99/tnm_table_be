import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetSender_EnableProgramBright(addr: number, bBroadcast: boolean, isEnable: boolean): Promise<void>;
        trySetSender_EnableProgramBright(addr: number, isEnable: boolean): Promise<ErrorType | null>;
    }
}
export default function createSetSender_EnableProgramBright<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, isEnable: boolean): Request<Broadcast>;
