import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadGroupNumInModule(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.GroupNumInModuleOccupancy, 'ReadGroupNumInModule');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.GroupNumInModuleAddr;
    return req;
}
Session.prototype.ReadGroupNumInModule = async function ReadGroupNumInModule(addr, portAddr, scanBoardAddr) {
    const req = createReadGroupNumInModule(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadGroupNumInModule = async function tryReadGroupNumInModule(addr, portAddr, scanBoardAddr) {
    const req = createReadGroupNumInModule(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadGroupNumInModule.js.map