import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createMoveCorrectionFromSdramToSlaveFlash(addr, portAddr, scanBoardAddr, bBroadcast) {
    const req = new Request([5], bBroadcast, 'MoveCorrectionFromSdramToSlaveFlash');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Correction_SolidificationAddr;
    return req;
}
Session.prototype.MoveCorrectionFromSdramToSlaveFlash =
    async function MoveCorrectionFromSdramToSlaveFlash(addr, portAddr, scanBoardAddr, bBroadcast) {
        const req = createMoveCorrectionFromSdramToSlaveFlash(addr, portAddr, scanBoardAddr, bBroadcast);
        await this.connection.send(req);
    };
Session.prototype.tryMoveCorrectionFromSdramToSlaveFlash =
    async function tryMoveCorrectionFromSdramToSlaveFlash(addr, portAddr, scanBoardAddr) {
        const req = createMoveCorrectionFromSdramToSlaveFlash(addr, portAddr, scanBoardAddr, false);
        return (await this.connection.trySend(req))?.ack ?? null;
    };
//# sourceMappingURL=MoveCorrectionFromSdramToSlaveFlash.js.map