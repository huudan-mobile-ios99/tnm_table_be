import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createFuncCard_SetPowerPortCtrlTotal_1(addr, portAddr, funcCardAddr, bBroadcast, operType) {
    const $data = encodeUIntLE(operType, AddressMapping.FuncCard_PowerPortCtrlTotalOccupancy);
    const req = new Request($data, bBroadcast, 'FuncCard_SetPowerPortCtrlTotal_1');
    req.destination = addr;
    req.deviceType = 2;
    req.port = portAddr;
    req.rcvIndex = funcCardAddr;
    req.address = AddressMapping.FuncCard_PowerPortCtrlTotalAddr;
    return req;
}
Session.prototype.FuncCard_SetPowerPortCtrlTotal_1 =
    async function FuncCard_SetPowerPortCtrlTotal_1(addr, portAddr, funcCardAddr, bBroadcast, operType) {
        const req = createFuncCard_SetPowerPortCtrlTotal_1(addr, portAddr, funcCardAddr, bBroadcast, operType);
        await this.connection.send(req);
    };
Session.prototype.tryFuncCard_SetPowerPortCtrlTotal_1 =
    async function tryFuncCard_SetPowerPortCtrlTotal_1(addr, portAddr, funcCardAddr, operType) {
        const req = createFuncCard_SetPowerPortCtrlTotal_1(addr, portAddr, funcCardAddr, false, operType);
        return (await this.connection.trySend(req))?.ack ?? null;
    };
//# sourceMappingURL=FuncCard_SetPowerPortCtrlTotal_1.js.map