import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetTemprature(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, tem: number): Promise<void>;
        trySetTemprature(addr: number, portAddr: number, scanBoardAddr: number, tem: number): Promise<ErrorType | null>;
    }
}
export default function createSetTemprature<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, tem: number): Request<Broadcast>;
