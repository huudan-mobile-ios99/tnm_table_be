import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetSender_EDEState(addr, bBroadcast, info) {
    const req = new Request([info], bBroadcast, 'SetSender_EDEState');
    req.destination = addr;
    req.address = AddressMapping.SenderEDEOpreationAddr;
    return req;
}
Session.prototype.SetSender_EDEState = async function SetSender_EDEState(addr, bBroadcast, info) {
    const req = createSetSender_EDEState(addr, bBroadcast, info);
    await this.connection.send(req);
};
Session.prototype.trySetSender_EDEState = async function trySetSender_EDEState(addr, info) {
    const req = createSetSender_EDEState(addr, false, info);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetSender_EDEState.js.map