import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createFuncCard_ReadHumiOfFuncCard(addr) {
    const req = new Request(AddressMapping.FuncCard_HumidityOfFuncCardOccupancy, 'FuncCard_ReadHumiOfFuncCard');
    req.destination = addr;
    req.address = AddressMapping.FuncCard_HumidityOfFuncCardAddr;
    return req;
}
Session.prototype.FuncCard_ReadHumiOfFuncCard = async function FuncCard_ReadHumiOfFuncCard(addr) {
    const req = createFuncCard_ReadHumiOfFuncCard(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryFuncCard_ReadHumiOfFuncCard = async function tryFuncCard_ReadHumiOfFuncCard(addr) {
    const req = createFuncCard_ReadHumiOfFuncCard(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=FuncCard_ReadHumiOfFuncCard.js.map