import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScanner_DMWSP(addr, portAddr, scanBoardAddr, readLength) {
    const req = new Request(readLength, 'ReadScanner_DMWSP');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Scanner_DmWSPAAddr;
    return req;
}
Session.prototype.ReadScanner_DMWSP = async function ReadScanner_DMWSP(addr, portAddr, scanBoardAddr, readLength) {
    const req = createReadScanner_DMWSP(addr, portAddr, scanBoardAddr, readLength);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadScanner_DMWSP = async function tryReadScanner_DMWSP(addr, portAddr, scanBoardAddr, readLength) {
    const req = createReadScanner_DMWSP(addr, portAddr, scanBoardAddr, readLength);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScanner_DMWSP.js.map