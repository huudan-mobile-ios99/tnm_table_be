import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetSender_DVICols(addr: number, bBroadcast: boolean, dviCols: number): Promise<void>;
        trySetSender_DVICols(addr: number, dviCols: number): Promise<ErrorType | null>;
    }
}
export default function createSetSender_DVICols<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, dviCols: number): Request<Broadcast>;
