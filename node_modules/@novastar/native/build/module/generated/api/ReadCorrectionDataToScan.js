import { Request, Session } from '@novastar/codec';
export default function createReadCorrectionDataToScan(addr, portAddr, scanBoardAddr, readLength, registerAddr) {
    const req = new Request(readLength, 'ReadCorrectionDataToScan');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = registerAddr;
    return req;
}
Session.prototype.ReadCorrectionDataToScan = async function ReadCorrectionDataToScan(addr, portAddr, scanBoardAddr, readLength, registerAddr) {
    const req = createReadCorrectionDataToScan(addr, portAddr, scanBoardAddr, readLength, registerAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadCorrectionDataToScan = async function tryReadCorrectionDataToScan(addr, portAddr, scanBoardAddr, readLength, registerAddr) {
    const req = createReadCorrectionDataToScan(addr, portAddr, scanBoardAddr, readLength, registerAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadCorrectionDataToScan.js.map