import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadDriverFucntion(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.DriverFucntionOccupancy, 'ReadDriverFucntion');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.DriverFucntionAddr;
    return req;
}
Session.prototype.ReadDriverFucntion = async function ReadDriverFucntion(addr, portAddr, scanBoardAddr) {
    const req = createReadDriverFucntion(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadDriverFucntion = async function tryReadDriverFucntion(addr, portAddr, scanBoardAddr) {
    const req = createReadDriverFucntion(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadDriverFucntion.js.map