import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadDeviceTable(addr, readLen) {
    const req = new Request(readLen, 'ReadDeviceTable');
    req.destination = addr;
    req.address = AddressMapping.DeviceTableAddr;
    return req;
}
Session.prototype.ReadDeviceTable = async function ReadDeviceTable(addr, readLen) {
    const req = createReadDeviceTable(addr, readLen);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadDeviceTable = async function tryReadDeviceTable(addr, readLen) {
    const req = createReadDeviceTable(addr, readLen);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadDeviceTable.js.map