import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadGclkNumPerScan(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadGclkNumPerScan(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadGclkNumPerScan(addr: number, portAddr: number, scanBoardAddr: number): Request;
