import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScannerLowDelayEnable(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ScannerLowDelayEnableOccupancy, 'ReadScannerLowDelayEnable');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ScannerLowDelayEnableAddr;
    return req;
}
Session.prototype.ReadScannerLowDelayEnable = async function ReadScannerLowDelayEnable(addr, portAddr, scanBoardAddr) {
    const req = createReadScannerLowDelayEnable(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadScannerLowDelayEnable = async function tryReadScannerLowDelayEnable(addr, portAddr, scanBoardAddr) {
    const req = createReadScannerLowDelayEnable(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScannerLowDelayEnable.js.map