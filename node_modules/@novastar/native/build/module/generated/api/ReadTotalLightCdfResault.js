import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadTotalLightCdfResault(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ScannerTotalLightCdfResaultOccupancy, 'ReadTotalLightCdfResault');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ScannerTotalLightCdfResaultddr;
    return req;
}
Session.prototype.ReadTotalLightCdfResault = async function ReadTotalLightCdfResault(addr, portAddr, scanBoardAddr) {
    const req = createReadTotalLightCdfResault(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadTotalLightCdfResault = async function tryReadTotalLightCdfResault(addr, portAddr, scanBoardAddr) {
    const req = createReadTotalLightCdfResault(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadTotalLightCdfResault.js.map