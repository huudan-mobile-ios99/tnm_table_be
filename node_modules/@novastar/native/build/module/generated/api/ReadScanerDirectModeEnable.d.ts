import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadScanerDirectModeEnable(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadScanerDirectModeEnable(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadScanerDirectModeEnable(addr: number, portAddr: number, scanBoardAddr: number): Request;
