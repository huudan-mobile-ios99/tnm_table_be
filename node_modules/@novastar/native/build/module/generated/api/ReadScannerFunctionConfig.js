import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScannerFunctionConfig(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ScanBoardFunctionConfigOccupancy, 'ReadScannerFunctionConfig');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ScanBoardFunctionConfigAddr;
    return req;
}
Session.prototype.ReadScannerFunctionConfig = async function ReadScannerFunctionConfig(addr, portAddr, scanBoardAddr) {
    const req = createReadScannerFunctionConfig(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadScannerFunctionConfig = async function tryReadScannerFunctionConfig(addr, portAddr, scanBoardAddr) {
    const req = createReadScannerFunctionConfig(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScannerFunctionConfig.js.map