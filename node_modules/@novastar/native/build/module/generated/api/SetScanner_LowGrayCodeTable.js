import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetScanner_LowGrayCodeTable(addr, portAddr, scanBoardAddr, bBroadcast, lowGrayCodeTable) {
    if (lowGrayCodeTable.length !== AddressMapping.LowGrayCodeTableOccupancy)
        throw new TypeError(`Invalid buffer size: ${lowGrayCodeTable.length}`);
    const req = new Request(lowGrayCodeTable, bBroadcast, 'SetScanner_LowGrayCodeTable');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.LowGrayCodeTableAddr;
    return req;
}
Session.prototype.SetScanner_LowGrayCodeTable = async function SetScanner_LowGrayCodeTable(addr, portAddr, scanBoardAddr, bBroadcast, lowGrayCodeTable) {
    const req = createSetScanner_LowGrayCodeTable(addr, portAddr, scanBoardAddr, bBroadcast, lowGrayCodeTable);
    await this.connection.send(req);
};
Session.prototype.trySetScanner_LowGrayCodeTable = async function trySetScanner_LowGrayCodeTable(addr, portAddr, scanBoardAddr, lowGrayCodeTable) {
    const req = createSetScanner_LowGrayCodeTable(addr, portAddr, scanBoardAddr, false, lowGrayCodeTable);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetScanner_LowGrayCodeTable.js.map