import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetScannerRecordTime(addr, portAddr, scanBoardAddr, bBroadcast) {
    const $data = encodeUIntLE(0, AddressMapping.ScannerRecordTimeOccupancy);
    const req = new Request($data, bBroadcast, 'SetScannerRecordTime');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ScannerRecordTimeAddr;
    return req;
}
Session.prototype.SetScannerRecordTime = async function SetScannerRecordTime(addr, portAddr, scanBoardAddr, bBroadcast) {
    const req = createSetScannerRecordTime(addr, portAddr, scanBoardAddr, bBroadcast);
    await this.connection.send(req);
};
Session.prototype.trySetScannerRecordTime = async function trySetScannerRecordTime(addr, portAddr, scanBoardAddr) {
    const req = createSetScannerRecordTime(addr, portAddr, scanBoardAddr, false);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetScannerRecordTime.js.map