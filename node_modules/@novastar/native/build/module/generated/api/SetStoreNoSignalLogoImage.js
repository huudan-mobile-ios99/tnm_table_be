import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetStoreNoSignalLogoImage(addr, portAddr, scanBoardAddr, bBroadcast, storeNoSignalLogoImage) {
    const $data = encodeUIntLE(storeNoSignalLogoImage, AddressMapping.StoreNoSignalLogoImageOccupancy);
    const req = new Request($data, bBroadcast, 'SetStoreNoSignalLogoImage');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.StoreNoSignalLogoImageAddr;
    return req;
}
Session.prototype.SetStoreNoSignalLogoImage = async function SetStoreNoSignalLogoImage(addr, portAddr, scanBoardAddr, bBroadcast, storeNoSignalLogoImage) {
    const req = createSetStoreNoSignalLogoImage(addr, portAddr, scanBoardAddr, bBroadcast, storeNoSignalLogoImage);
    await this.connection.send(req);
};
Session.prototype.trySetStoreNoSignalLogoImage = async function trySetStoreNoSignalLogoImage(addr, portAddr, scanBoardAddr, storeNoSignalLogoImage) {
    const req = createSetStoreNoSignalLogoImage(addr, portAddr, scanBoardAddr, false, storeNoSignalLogoImage);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetStoreNoSignalLogoImage.js.map