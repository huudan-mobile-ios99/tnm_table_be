import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadRowScalePara(addr) {
    const req = new Request(AddressMapping.RowScaleParAOccupancy, 'ReadRowScalePara');
    req.destination = addr;
    req.address = AddressMapping.RowScaleParaAddr;
    return req;
}
Session.prototype.ReadRowScalePara = async function ReadRowScalePara(addr) {
    const req = createReadRowScalePara(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadRowScalePara = async function tryReadRowScalePara(addr) {
    const req = createReadRowScalePara(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadRowScalePara.js.map