import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetAreaCoefsOperate(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    if (data.length !== AddressMapping.AreaCoefsOperateOccupancy)
        throw new TypeError(`Invalid buffer size: ${data.length}`);
    const req = new Request(data, bBroadcast, 'SetAreaCoefsOperate');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.AreaCoefsOperateAddr;
    return req;
}
Session.prototype.SetAreaCoefsOperate = async function SetAreaCoefsOperate(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createSetAreaCoefsOperate(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.trySetAreaCoefsOperate = async function trySetAreaCoefsOperate(addr, portAddr, scanBoardAddr, data) {
    const req = createSetAreaCoefsOperate(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetAreaCoefsOperate.js.map