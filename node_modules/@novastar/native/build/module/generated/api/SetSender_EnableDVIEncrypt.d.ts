import { ErrorType, Request } from '@novastar/codec';
import { DVIEncryptTypeEnum } from '../DVIEncryptType';
declare module '@novastar/codec' {
    interface API {
        SetSender_EnableDVIEncrypt(addr: number, bBroadcast: boolean, encryptType: DVIEncryptTypeEnum): Promise<void>;
        trySetSender_EnableDVIEncrypt(addr: number, encryptType: DVIEncryptTypeEnum): Promise<ErrorType | null>;
    }
}
export default function createSetSender_EnableDVIEncrypt<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, encryptType: DVIEncryptTypeEnum): Request<Broadcast>;
