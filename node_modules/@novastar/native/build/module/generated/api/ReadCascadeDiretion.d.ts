import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadCascadeDiretion(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadCascadeDiretion(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadCascadeDiretion(addr: number, portAddr: number, scanBoardAddr: number): Request;
