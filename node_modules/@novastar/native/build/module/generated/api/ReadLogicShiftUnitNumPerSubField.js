import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadLogicShiftUnitNumPerSubField(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.LogicShiftUnitNumPerSubFieldOccupancy, 'ReadLogicShiftUnitNumPerSubField');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.LogicShiftUnitNumPerSubFieldAddr;
    return req;
}
Session.prototype.ReadLogicShiftUnitNumPerSubField =
    async function ReadLogicShiftUnitNumPerSubField(addr, portAddr, scanBoardAddr) {
        const req = createReadLogicShiftUnitNumPerSubField(addr, portAddr, scanBoardAddr);
        return decodeUIntLE(await this.connection.send(req));
    };
Session.prototype.tryReadLogicShiftUnitNumPerSubField =
    async function tryReadLogicShiftUnitNumPerSubField(addr, portAddr, scanBoardAddr) {
        const req = createReadLogicShiftUnitNumPerSubField(addr, portAddr, scanBoardAddr);
        return this.connection.trySend(req);
    };
//# sourceMappingURL=ReadLogicShiftUnitNumPerSubField.js.map