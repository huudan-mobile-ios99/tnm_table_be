import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScanner_FPGAProgramRemarks(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.Scanner_FPGAProgramRemarksOccupancy, 'ReadScanner_FPGAProgramRemarks');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Scanner_FPGAProgramRemarksAddr;
    return req;
}
Session.prototype.ReadScanner_FPGAProgramRemarks = async function ReadScanner_FPGAProgramRemarks(addr, portAddr, scanBoardAddr) {
    const req = createReadScanner_FPGAProgramRemarks(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadScanner_FPGAProgramRemarks =
    async function tryReadScanner_FPGAProgramRemarks(addr, portAddr, scanBoardAddr) {
        const req = createReadScanner_FPGAProgramRemarks(addr, portAddr, scanBoardAddr);
        return this.connection.trySend(req);
    };
//# sourceMappingURL=ReadScanner_FPGAProgramRemarks.js.map