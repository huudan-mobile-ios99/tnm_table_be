import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSubField(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.PartNumPerRefOccupancy +
        AddressMapping.SubFieldNum * AddressMapping.SubFieldOccupancy, 'ReadSubField');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.PartNumPerRefAddr;
    return req;
}
Session.prototype.ReadSubField = async function ReadSubField(addr, portAddr, scanBoardAddr) {
    const req = createReadSubField(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadSubField = async function tryReadSubField(addr, portAddr, scanBoardAddr) {
    const req = createReadSubField(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSubField.js.map