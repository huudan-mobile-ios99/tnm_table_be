import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createRead2038SDelayTime(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.DelayTimeOccupancy, 'Read2038SDelayTime');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.DelayTimeAddr;
    return req;
}
Session.prototype.Read2038SDelayTime = async function Read2038SDelayTime(addr, portAddr, scanBoardAddr) {
    const req = createRead2038SDelayTime(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryRead2038SDelayTime = async function tryRead2038SDelayTime(addr, portAddr, scanBoardAddr) {
    const req = createRead2038SDelayTime(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=Read2038SDelayTime.js.map