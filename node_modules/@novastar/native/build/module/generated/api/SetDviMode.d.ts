import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetDviMode(addr: number, bBroadcast: boolean, dviMode: number): Promise<void>;
        trySetDviMode(addr: number, dviMode: number): Promise<ErrorType | null>;
    }
}
export default function createSetDviMode<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, dviMode: number): Request<Broadcast>;
