import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetIsSerdes(addr: number, bBroadcast: boolean, IsSerdes: boolean): Promise<void>;
        trySetIsSerdes(addr: number, IsSerdes: boolean): Promise<ErrorType | null>;
    }
}
export default function createSetIsSerdes<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, IsSerdes: boolean): Request<Broadcast>;
