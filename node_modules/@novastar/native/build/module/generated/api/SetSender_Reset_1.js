import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetSender_Reset_1(addr, bBroadcast) {
    const req = new Request(Buffer.alloc(1), bBroadcast, 'SetSender_Reset_1');
    req.destination = addr;
    req.port = 255;
    req.rcvIndex = 65535;
    req.address = AddressMapping.Sender_McuResetAddr;
    return req;
}
Session.prototype.SetSender_Reset_1 = async function SetSender_Reset_1(addr, bBroadcast) {
    const req = createSetSender_Reset_1(addr, bBroadcast);
    await this.connection.send(req);
};
Session.prototype.trySetSender_Reset_1 = async function trySetSender_Reset_1(addr) {
    const req = createSetSender_Reset_1(addr, false);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetSender_Reset_1.js.map