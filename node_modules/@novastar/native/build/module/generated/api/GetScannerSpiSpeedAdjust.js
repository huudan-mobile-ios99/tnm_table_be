import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createGetScannerSpiSpeedAdjust(Sender, portAddr, Scan) {
    const req = new Request(AddressMapping.ScannerSpiSpeedAdjustOccupancy, 'GetScannerSpiSpeedAdjust');
    req.destination = Sender;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = Scan;
    req.address = AddressMapping.ScannerSpiSpeedAdjustAddr;
    return req;
}
Session.prototype.GetScannerSpiSpeedAdjust = async function GetScannerSpiSpeedAdjust(Sender, portAddr, Scan) {
    const req = createGetScannerSpiSpeedAdjust(Sender, portAddr, Scan);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryGetScannerSpiSpeedAdjust = async function tryGetScannerSpiSpeedAdjust(Sender, portAddr, Scan) {
    const req = createGetScannerSpiSpeedAdjust(Sender, portAddr, Scan);
    return this.connection.trySend(req);
};
//# sourceMappingURL=GetScannerSpiSpeedAdjust.js.map