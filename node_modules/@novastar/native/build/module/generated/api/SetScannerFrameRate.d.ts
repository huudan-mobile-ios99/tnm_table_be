import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetScannerFrameRate(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, frameRate: number): Promise<void>;
        trySetScannerFrameRate(addr: number, portAddr: number, scanBoardAddr: number, frameRate: number): Promise<ErrorType | null>;
    }
}
export default function createSetScannerFrameRate<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, frameRate: number): Request<Broadcast>;
