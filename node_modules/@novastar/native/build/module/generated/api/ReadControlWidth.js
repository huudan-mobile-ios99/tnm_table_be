import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadControlWidth(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ControlWidthOccupancy, 'ReadControlWidth');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ControlWidthAddr;
    return req;
}
Session.prototype.ReadControlWidth = async function ReadControlWidth(addr, portAddr, scanBoardAddr) {
    const req = createReadControlWidth(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadControlWidth = async function tryReadControlWidth(addr, portAddr, scanBoardAddr) {
    const req = createReadControlWidth(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadControlWidth.js.map