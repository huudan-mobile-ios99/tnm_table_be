import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetSender_DVIRows(addr, bBroadcast, dviRows) {
    const $data = encodeUIntLE(dviRows, AddressMapping.Sender_DVIRowsOccupancy);
    const req = new Request($data, bBroadcast, 'SetSender_DVIRows');
    req.destination = addr;
    req.address = AddressMapping.Sender_DVIRowsAddr;
    return req;
}
Session.prototype.SetSender_DVIRows = async function SetSender_DVIRows(addr, bBroadcast, dviRows) {
    const req = createSetSender_DVIRows(addr, bBroadcast, dviRows);
    await this.connection.send(req);
};
Session.prototype.trySetSender_DVIRows = async function trySetSender_DVIRows(addr, dviRows) {
    const req = createSetSender_DVIRows(addr, false, dviRows);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetSender_DVIRows.js.map