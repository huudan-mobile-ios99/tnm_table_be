import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createDistribute_ReadDistributeFPGALength(addr, portAddr, distributeAddr) {
    const req = new Request(AddressMapping.Distribute_FPGALengthOccupancy, 'Distribute_ReadDistributeFPGALength');
    req.destination = addr;
    req.deviceType = 3;
    req.port = portAddr;
    req.rcvIndex = distributeAddr;
    req.address = AddressMapping.Distribute_FPGALengthAddr;
    return req;
}
Session.prototype.Distribute_ReadDistributeFPGALength =
    async function Distribute_ReadDistributeFPGALength(addr, portAddr, distributeAddr) {
        const req = createDistribute_ReadDistributeFPGALength(addr, portAddr, distributeAddr);
        return decodeUIntLE(await this.connection.send(req));
    };
Session.prototype.tryDistribute_ReadDistributeFPGALength =
    async function tryDistribute_ReadDistributeFPGALength(addr, portAddr, distributeAddr) {
        const req = createDistribute_ReadDistributeFPGALength(addr, portAddr, distributeAddr);
        return this.connection.trySend(req);
    };
//# sourceMappingURL=Distribute_ReadDistributeFPGALength.js.map