/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadEthernetPortScannerXY(addr: number, portIndex: number, scannerIndex: number): Promise<Buffer>;
        tryReadEthernetPortScannerXY(addr: number, portIndex: number, scannerIndex: number): Promise<Packet | null>;
    }
}
export default function createReadEthernetPortScannerXY(addr: number, portIndex: number, scannerIndex: number): Request;
