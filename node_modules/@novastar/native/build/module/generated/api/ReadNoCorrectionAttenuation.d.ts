/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadNoCorrectionAttenuation(addr: number, portAddr: number, scanBoardAddr: number): Promise<Buffer>;
        tryReadNoCorrectionAttenuation(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadNoCorrectionAttenuation(addr: number, portAddr: number, scanBoardAddr: number): Request;
