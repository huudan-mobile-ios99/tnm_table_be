import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetVirtualMap(addr: number, bBroadcast: boolean, virtualMap: number): Promise<void>;
        trySetVirtualMap(addr: number, virtualMap: number): Promise<ErrorType | null>;
    }
}
export default function createSetVirtualMap<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, virtualMap: number): Request<Broadcast>;
