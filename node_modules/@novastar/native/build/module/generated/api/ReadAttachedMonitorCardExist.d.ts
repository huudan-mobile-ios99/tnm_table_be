import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadAttachedMonitorCardExist(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadAttachedMonitorCardExist(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadAttachedMonitorCardExist(addr: number, portAddr: number, scanBoardAddr: number): Request;
