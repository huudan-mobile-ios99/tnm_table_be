import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetReduceHighContrast(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, reduceHighData: number): Promise<void>;
        trySetReduceHighContrast(addr: number, portAddr: number, scanBoardAddr: number, reduceHighData: number): Promise<ErrorType | null>;
    }
}
export default function createSetReduceHighContrast<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, reduceHighData: number): Request<Broadcast>;
