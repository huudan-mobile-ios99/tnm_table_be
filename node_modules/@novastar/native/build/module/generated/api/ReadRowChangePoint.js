import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadRowChangePoint(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.RowChangePointOccupancy, 'ReadRowChangePoint');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.RowChangePointAddr;
    return req;
}
Session.prototype.ReadRowChangePoint = async function ReadRowChangePoint(addr, portAddr, scanBoardAddr) {
    const req = createReadRowChangePoint(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadRowChangePoint = async function tryReadRowChangePoint(addr, portAddr, scanBoardAddr) {
    const req = createReadRowChangePoint(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadRowChangePoint.js.map