import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createWriteSecondGroupRegistorGain(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    if (data.length !== AddressMapping.SecondGroupRegistorGainOccupancy)
        throw new TypeError(`Invalid buffer size: ${data.length}`);
    const req = new Request(data, bBroadcast, 'WriteSecondGroupRegistorGain');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.SecondGroupRegistorGainAddr;
    return req;
}
Session.prototype.WriteSecondGroupRegistorGain = async function WriteSecondGroupRegistorGain(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createWriteSecondGroupRegistorGain(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.tryWriteSecondGroupRegistorGain = async function tryWriteSecondGroupRegistorGain(addr, portAddr, scanBoardAddr, data) {
    const req = createWriteSecondGroupRegistorGain(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=WriteSecondGroupRegistorGain.js.map