import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetSender_DVIResolutionHeight(addr: number, bBroadcast: boolean, dviHeight: number): Promise<void>;
        trySetSender_DVIResolutionHeight(addr: number, dviHeight: number): Promise<ErrorType | null>;
    }
}
export default function createSetSender_DVIResolutionHeight<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, dviHeight: number): Request<Broadcast>;
