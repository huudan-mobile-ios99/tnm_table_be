import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadGhostRemoveMode(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadGhostRemoveMode(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadGhostRemoveMode(addr: number, portAddr: number, scanBoardAddr: number): Request;
