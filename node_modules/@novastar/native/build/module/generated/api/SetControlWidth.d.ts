import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetControlWidth(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, controlWidth: number): Promise<void>;
        trySetControlWidth(addr: number, portAddr: number, scanBoardAddr: number, controlWidth: number): Promise<ErrorType | null>;
    }
}
export default function createSetControlWidth<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, controlWidth: number): Request<Broadcast>;
