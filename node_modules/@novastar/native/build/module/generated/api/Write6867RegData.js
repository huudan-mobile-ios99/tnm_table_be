import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createWrite6867RegData(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = new Request(data, bBroadcast, 'Write6867RegData');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Register6867RegAddress;
    return req;
}
Session.prototype.Write6867RegData = async function Write6867RegData(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createWrite6867RegData(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.tryWrite6867RegData = async function tryWrite6867RegData(addr, portAddr, scanBoardAddr, data) {
    const req = createWrite6867RegData(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=Write6867RegData.js.map