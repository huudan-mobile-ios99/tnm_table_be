import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetABCDESignalDelay(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, ABCDESignalDelay: number): Promise<void>;
        trySetABCDESignalDelay(addr: number, portAddr: number, scanBoardAddr: number, ABCDESignalDelay: number): Promise<ErrorType | null>;
    }
}
export default function createSetABCDESignalDelay<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, ABCDESignalDelay: number): Request<Broadcast>;
