import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetTotalPointInTable(addr, portAddr, scanBoardAddr, bBroadcast, totalPointInTable) {
    const $data = encodeUIntLE(totalPointInTable, AddressMapping.TotalPointInTableOccupancy);
    const req = new Request($data, bBroadcast, 'SetTotalPointInTable');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.TotalPointInTableAddr;
    return req;
}
Session.prototype.SetTotalPointInTable = async function SetTotalPointInTable(addr, portAddr, scanBoardAddr, bBroadcast, totalPointInTable) {
    const req = createSetTotalPointInTable(addr, portAddr, scanBoardAddr, bBroadcast, totalPointInTable);
    await this.connection.send(req);
};
Session.prototype.trySetTotalPointInTable = async function trySetTotalPointInTable(addr, portAddr, scanBoardAddr, totalPointInTable) {
    const req = createSetTotalPointInTable(addr, portAddr, scanBoardAddr, false, totalPointInTable);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetTotalPointInTable.js.map