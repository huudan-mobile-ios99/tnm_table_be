import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createFuncCard_SetPowerCtrlMode_1(addr, portAddr, funcCardAddr, bBroadcast, powerCtrlMode) {
    const $data = encodeUIntLE(powerCtrlMode, AddressMapping.FuncCard_PowerCtrlModeOccupancy);
    const req = new Request($data, bBroadcast, 'FuncCard_SetPowerCtrlMode_1');
    req.destination = addr;
    req.deviceType = 2;
    req.port = portAddr;
    req.rcvIndex = funcCardAddr;
    req.address = AddressMapping.FuncCard_PowerCtrlModeAddr;
    return req;
}
Session.prototype.FuncCard_SetPowerCtrlMode_1 = async function FuncCard_SetPowerCtrlMode_1(addr, portAddr, funcCardAddr, bBroadcast, powerCtrlMode) {
    const req = createFuncCard_SetPowerCtrlMode_1(addr, portAddr, funcCardAddr, bBroadcast, powerCtrlMode);
    await this.connection.send(req);
};
Session.prototype.tryFuncCard_SetPowerCtrlMode_1 = async function tryFuncCard_SetPowerCtrlMode_1(addr, portAddr, funcCardAddr, powerCtrlMode) {
    const req = createFuncCard_SetPowerCtrlMode_1(addr, portAddr, funcCardAddr, false, powerCtrlMode);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=FuncCard_SetPowerCtrlMode_1.js.map