import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetIoControl(addr, bBroadcast, ioControl) {
    const $data = encodeUIntLE(ioControl, AddressMapping.IoControlOccupancy);
    const req = new Request($data, bBroadcast, 'SetIoControl');
    req.destination = addr;
    req.address = AddressMapping.IoControlAddr;
    return req;
}
Session.prototype.SetIoControl = async function SetIoControl(addr, bBroadcast, ioControl) {
    const req = createSetIoControl(addr, bBroadcast, ioControl);
    await this.connection.send(req);
};
Session.prototype.trySetIoControl = async function trySetIoControl(addr, ioControl) {
    const req = createSetIoControl(addr, false, ioControl);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetIoControl.js.map