import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScannerResetCycleNum(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ResetCycleNumPointOccupancy, 'ReadScannerResetCycleNum');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ScannerResetCycleNumAddr;
    return req;
}
Session.prototype.ReadScannerResetCycleNum = async function ReadScannerResetCycleNum(addr, portAddr, scanBoardAddr) {
    const req = createReadScannerResetCycleNum(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadScannerResetCycleNum = async function tryReadScannerResetCycleNum(addr, portAddr, scanBoardAddr) {
    const req = createReadScannerResetCycleNum(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScannerResetCycleNum.js.map