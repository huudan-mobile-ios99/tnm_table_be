/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadStartPositionOfDataGroup(addr: number, portAddr: number, scanBoardAddr: number): Promise<Buffer>;
        tryReadStartPositionOfDataGroup(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadStartPositionOfDataGroup(addr: number, portAddr: number, scanBoardAddr: number): Request;
