import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSmokeWarningOfMonitorCard(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.Scanner_SmokeWarningOfMonitorCardOccupancy, 'ReadSmokeWarningOfMonitorCard');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Scanner_SmokeWarningOfMonitorCardAddr;
    return req;
}
Session.prototype.ReadSmokeWarningOfMonitorCard = async function ReadSmokeWarningOfMonitorCard(addr, portAddr, scanBoardAddr) {
    const req = createReadSmokeWarningOfMonitorCard(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadSmokeWarningOfMonitorCard =
    async function tryReadSmokeWarningOfMonitorCard(addr, portAddr, scanBoardAddr) {
        const req = createReadSmokeWarningOfMonitorCard(addr, portAddr, scanBoardAddr);
        return this.connection.trySend(req);
    };
//# sourceMappingURL=ReadSmokeWarningOfMonitorCard.js.map