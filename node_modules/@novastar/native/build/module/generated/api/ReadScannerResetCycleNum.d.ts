import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadScannerResetCycleNum(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadScannerResetCycleNum(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadScannerResetCycleNum(addr: number, portAddr: number, scanBoardAddr: number): Request;
