/// <reference types="node" />
import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetScannerSpiSpeedAdjust(Sender: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, speed: number[] | Buffer): Promise<void>;
        trySetScannerSpiSpeedAdjust(Sender: number, portAddr: number, scanBoardAddr: number, speed: number[] | Buffer): Promise<ErrorType | null>;
    }
}
export default function createSetScannerSpiSpeedAdjust<Broadcast extends boolean>(Sender: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, speed: number[] | Buffer): Request<Broadcast>;
