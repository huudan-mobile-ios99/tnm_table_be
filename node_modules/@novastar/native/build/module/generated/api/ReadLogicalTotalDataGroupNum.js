import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadLogicalTotalDataGroupNum(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.LogicalTotalDataGroupNumOccupancy, 'ReadLogicalTotalDataGroupNum');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.LogicalTotalDataGroupNumAddr;
    return req;
}
Session.prototype.ReadLogicalTotalDataGroupNum = async function ReadLogicalTotalDataGroupNum(addr, portAddr, scanBoardAddr) {
    const req = createReadLogicalTotalDataGroupNum(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadLogicalTotalDataGroupNum = async function tryReadLogicalTotalDataGroupNum(addr, portAddr, scanBoardAddr) {
    const req = createReadLogicalTotalDataGroupNum(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadLogicalTotalDataGroupNum.js.map