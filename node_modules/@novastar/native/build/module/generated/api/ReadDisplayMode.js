import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadDisplayMode(addr) {
    const req = new Request(AddressMapping.DisplayModeOccupancy, 'ReadDisplayMode');
    req.destination = addr;
    req.address = AddressMapping.DisplayModeAddr;
    return req;
}
Session.prototype.ReadDisplayMode = async function ReadDisplayMode(addr) {
    const req = createReadDisplayMode(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadDisplayMode = async function tryReadDisplayMode(addr) {
    const req = createReadDisplayMode(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadDisplayMode.js.map