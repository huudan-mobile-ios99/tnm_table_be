import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createGetScanerFunctionTable(Sender, portAddr, Scan) {
    const req = new Request(AddressMapping.ScannerFunctionTableOccupancy, 'GetScanerFunctionTable');
    req.destination = Sender;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = Scan;
    req.address = AddressMapping.ScannerFunctionTableAddr;
    return req;
}
Session.prototype.GetScanerFunctionTable = async function GetScanerFunctionTable(Sender, portAddr, Scan) {
    const req = createGetScanerFunctionTable(Sender, portAddr, Scan);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryGetScanerFunctionTable = async function tryGetScanerFunctionTable(Sender, portAddr, Scan) {
    const req = createGetScanerFunctionTable(Sender, portAddr, Scan);
    return this.connection.trySend(req);
};
//# sourceMappingURL=GetScanerFunctionTable.js.map