import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createGetGrayCoefficientEN(Sender, portAddr, Scan) {
    const req = new Request(AddressMapping.GrayCoefficientENAddrOccupancy, 'GetGrayCoefficientEN');
    req.destination = Sender;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = Scan;
    req.address = AddressMapping.GrayCoefficientENAddr;
    return req;
}
Session.prototype.GetGrayCoefficientEN = async function GetGrayCoefficientEN(Sender, portAddr, Scan) {
    const req = createGetGrayCoefficientEN(Sender, portAddr, Scan);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryGetGrayCoefficientEN = async function tryGetGrayCoefficientEN(Sender, portAddr, Scan) {
    const req = createGetGrayCoefficientEN(Sender, portAddr, Scan);
    return this.connection.trySend(req);
};
//# sourceMappingURL=GetGrayCoefficientEN.js.map