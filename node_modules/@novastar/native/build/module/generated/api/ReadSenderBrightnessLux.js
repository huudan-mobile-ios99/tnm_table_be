import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSenderBrightnessLux(addr) {
    const req = new Request(AddressMapping.BrightnessOccupancy, 'ReadSenderBrightnessLux');
    req.destination = addr;
    req.address = AddressMapping.BrightnessAddr;
    return req;
}
Session.prototype.ReadSenderBrightnessLux = async function ReadSenderBrightnessLux(addr) {
    const req = createReadSenderBrightnessLux(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadSenderBrightnessLux = async function tryReadSenderBrightnessLux(addr) {
    const req = createReadSenderBrightnessLux(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSenderBrightnessLux.js.map