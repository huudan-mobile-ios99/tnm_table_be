import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetTestPoint(addr, bBroadcast, testPoint) {
    const $data = encodeUIntLE(testPoint, AddressMapping.SortOrderOccupancy);
    const req = new Request($data, bBroadcast, 'SetTestPoint');
    req.destination = addr;
    req.address = AddressMapping.SortOrderAddr;
    return req;
}
Session.prototype.SetTestPoint = async function SetTestPoint(addr, bBroadcast, testPoint) {
    const req = createSetTestPoint(addr, bBroadcast, testPoint);
    await this.connection.send(req);
};
Session.prototype.trySetTestPoint = async function trySetTestPoint(addr, testPoint) {
    const req = createSetTestPoint(addr, false, testPoint);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetTestPoint.js.map