import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetScaleEn(addr, bBroadcast, isEnableScale) {
    const req = new Request(isEnableScale ? [88] : [255], bBroadcast, 'SetScaleEn');
    req.destination = addr;
    req.address = AddressMapping.DVIScaleEnAddr;
    return req;
}
Session.prototype.SetScaleEn = async function SetScaleEn(addr, bBroadcast, isEnableScale) {
    const req = createSetScaleEn(addr, bBroadcast, isEnableScale);
    await this.connection.send(req);
};
Session.prototype.trySetScaleEn = async function trySetScaleEn(addr, isEnableScale) {
    const req = createSetScaleEn(addr, false, isEnableScale);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetScaleEn.js.map