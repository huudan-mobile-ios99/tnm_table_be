import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSender_EncryptPassword(addr) {
    const req = new Request(AddressMapping.Sender_EncryptPasswordOccupancy, 'ReadSender_EncryptPassword');
    req.destination = addr;
    req.address = AddressMapping.Sender_EncryptPasswordAddr;
    return req;
}
Session.prototype.ReadSender_EncryptPassword = async function ReadSender_EncryptPassword(addr) {
    const req = createReadSender_EncryptPassword(addr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadSender_EncryptPassword = async function tryReadSender_EncryptPassword(addr) {
    const req = createReadSender_EncryptPassword(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSender_EncryptPassword.js.map