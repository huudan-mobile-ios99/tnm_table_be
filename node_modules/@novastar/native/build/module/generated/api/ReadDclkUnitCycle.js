import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadDclkUnitCycle(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.DclkUnitCycleOccupancy, 'ReadDclkUnitCycle');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.DclkUnitCycleAddr;
    return req;
}
Session.prototype.ReadDclkUnitCycle = async function ReadDclkUnitCycle(addr, portAddr, scanBoardAddr) {
    const req = createReadDclkUnitCycle(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadDclkUnitCycle = async function tryReadDclkUnitCycle(addr, portAddr, scanBoardAddr) {
    const req = createReadDclkUnitCycle(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadDclkUnitCycle.js.map