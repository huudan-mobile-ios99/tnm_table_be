import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadStartPositionOfDataGroup(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.StartPositionOfDataGroupOccupancy * AddressMapping.StartPositionOfDataGroupNum, 'ReadStartPositionOfDataGroup');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.StartPositionOfDataGroupAddr;
    return req;
}
Session.prototype.ReadStartPositionOfDataGroup = async function ReadStartPositionOfDataGroup(addr, portAddr, scanBoardAddr) {
    const req = createReadStartPositionOfDataGroup(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadStartPositionOfDataGroup = async function tryReadStartPositionOfDataGroup(addr, portAddr, scanBoardAddr) {
    const req = createReadStartPositionOfDataGroup(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadStartPositionOfDataGroup.js.map