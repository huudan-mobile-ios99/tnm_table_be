import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadScannerRecordTime(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadScannerRecordTime(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadScannerRecordTime(addr: number, portAddr: number, scanBoardAddr: number): Request;
