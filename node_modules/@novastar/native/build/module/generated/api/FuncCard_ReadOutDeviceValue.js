import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createFuncCard_ReadOutDeviceValue(addr) {
    const req = new Request(AddressMapping.FuncCard_ReadOutDeviceOccupancy, 'FuncCard_ReadOutDeviceValue');
    req.destination = addr;
    req.address = AddressMapping.FuncCard_ReadOutDeviceAddr;
    return req;
}
Session.prototype.FuncCard_ReadOutDeviceValue = async function FuncCard_ReadOutDeviceValue(addr) {
    const req = createFuncCard_ReadOutDeviceValue(addr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryFuncCard_ReadOutDeviceValue = async function tryFuncCard_ReadOutDeviceValue(addr) {
    const req = createFuncCard_ReadOutDeviceValue(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=FuncCard_ReadOutDeviceValue.js.map