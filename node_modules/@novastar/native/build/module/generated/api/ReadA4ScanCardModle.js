import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadA4ScanCardModle(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.A4ScanCardModleOccupancy, 'ReadA4ScanCardModle');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.A4ScanCardModleAddr;
    return req;
}
Session.prototype.ReadA4ScanCardModle = async function ReadA4ScanCardModle(addr, portAddr, scanBoardAddr) {
    const req = createReadA4ScanCardModle(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadA4ScanCardModle = async function tryReadA4ScanCardModle(addr, portAddr, scanBoardAddr) {
    const req = createReadA4ScanCardModle(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadA4ScanCardModle.js.map