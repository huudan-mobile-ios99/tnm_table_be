import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetHDEnable(addr, bBroadcast, hdEnable) {
    const $data = encodeUIntLE(hdEnable, AddressMapping.HDEnableOccupancy);
    const req = new Request($data, bBroadcast, 'SetHDEnable');
    req.destination = addr;
    req.address = AddressMapping.HDEnableAddr;
    return req;
}
Session.prototype.SetHDEnable = async function SetHDEnable(addr, bBroadcast, hdEnable) {
    const req = createSetHDEnable(addr, bBroadcast, hdEnable);
    await this.connection.send(req);
};
Session.prototype.trySetHDEnable = async function trySetHDEnable(addr, hdEnable) {
    const req = createSetHDEnable(addr, false, hdEnable);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetHDEnable.js.map