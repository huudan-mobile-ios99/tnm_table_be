import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadRotateAngle(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadRotateAngle(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadRotateAngle(addr: number, portAddr: number, scanBoardAddr: number): Request;
