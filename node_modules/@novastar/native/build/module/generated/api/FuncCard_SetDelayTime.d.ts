import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        FuncCard_SetDelayTime(addr: number, bBroadcast: boolean, delayTime: number): Promise<void>;
        tryFuncCard_SetDelayTime(addr: number, delayTime: number): Promise<ErrorType | null>;
    }
}
export default function createFuncCard_SetDelayTime<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, delayTime: number): Request<Broadcast>;
