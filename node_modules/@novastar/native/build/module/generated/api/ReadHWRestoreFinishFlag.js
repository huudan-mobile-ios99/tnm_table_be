import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadHWRestoreFinishFlag(addr, length) {
    const req = new Request(length, 'ReadHWRestoreFinishFlag');
    req.destination = addr;
    req.address = AddressMapping.HWRestoreFinishFlagAddr;
    return req;
}
Session.prototype.ReadHWRestoreFinishFlag = async function ReadHWRestoreFinishFlag(addr, length) {
    const req = createReadHWRestoreFinishFlag(addr, length);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadHWRestoreFinishFlag = async function tryReadHWRestoreFinishFlag(addr, length) {
    const req = createReadHWRestoreFinishFlag(addr, length);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadHWRestoreFinishFlag.js.map