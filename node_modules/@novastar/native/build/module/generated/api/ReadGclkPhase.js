import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadGclkPhase(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.GclkPhaseOccupancy, 'ReadGclkPhase');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.GclkPhaseAddr;
    return req;
}
Session.prototype.ReadGclkPhase = async function ReadGclkPhase(addr, portAddr, scanBoardAddr) {
    const req = createReadGclkPhase(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadGclkPhase = async function tryReadGclkPhase(addr, portAddr, scanBoardAddr) {
    const req = createReadGclkPhase(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadGclkPhase.js.map