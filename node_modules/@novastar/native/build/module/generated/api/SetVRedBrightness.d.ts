import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetVRedBrightness(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, vRedBrightness: number): Promise<void>;
        trySetVRedBrightness(addr: number, portAddr: number, scanBoardAddr: number, vRedBrightness: number): Promise<ErrorType | null>;
    }
}
export default function createSetVRedBrightness<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, vRedBrightness: number): Request<Broadcast>;
