import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetHDEnableEx(addr, bBroadcast, hdEnable) {
    const req = new Request([hdEnable], bBroadcast, 'SetHDEnableEx');
    req.destination = addr;
    req.address = AddressMapping.HDEnableAddr;
    return req;
}
Session.prototype.SetHDEnableEx = async function SetHDEnableEx(addr, bBroadcast, hdEnable) {
    const req = createSetHDEnableEx(addr, bBroadcast, hdEnable);
    await this.connection.send(req);
};
Session.prototype.trySetHDEnableEx = async function trySetHDEnableEx(addr, hdEnable) {
    const req = createSetHDEnableEx(addr, false, hdEnable);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetHDEnableEx.js.map