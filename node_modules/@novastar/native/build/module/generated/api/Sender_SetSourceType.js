import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSender_SetSourceType(addr, bBroadcast, sourceMode) {
    const $data = encodeUIntLE(sourceMode, AddressMapping.AudioControlOccupancy);
    const req = new Request($data, bBroadcast, 'Sender_SetSourceType');
    req.destination = addr;
    req.address = AddressMapping.SetSourceTypeAddr;
    return req;
}
Session.prototype.Sender_SetSourceType = async function Sender_SetSourceType(addr, bBroadcast, sourceMode) {
    const req = createSender_SetSourceType(addr, bBroadcast, sourceMode);
    await this.connection.send(req);
};
Session.prototype.trySender_SetSourceType = async function trySender_SetSourceType(addr, sourceMode) {
    const req = createSender_SetSourceType(addr, false, sourceMode);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=Sender_SetSourceType.js.map