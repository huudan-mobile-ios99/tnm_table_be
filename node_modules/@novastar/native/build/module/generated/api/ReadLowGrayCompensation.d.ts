import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadLowGrayCompensation(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadLowGrayCompensation(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadLowGrayCompensation(addr: number, portAddr: number, scanBoardAddr: number): Request;
