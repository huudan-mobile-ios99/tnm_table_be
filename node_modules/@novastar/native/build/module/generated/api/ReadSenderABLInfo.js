import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSenderABLInfo(addr) {
    const req = new Request(AddressMapping.SenderABLOccupancy, 'ReadSenderABLInfo');
    req.destination = addr;
    req.address = AddressMapping.SenderABLtionAddr;
    return req;
}
Session.prototype.ReadSenderABLInfo = async function ReadSenderABLInfo(addr) {
    const req = createReadSenderABLInfo(addr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadSenderABLInfo = async function tryReadSenderABLInfo(addr) {
    const req = createReadSenderABLInfo(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSenderABLInfo.js.map