import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetConfigRegisterWrite5(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    if (data.length !== AddressMapping.Config3RegisterOccupancy)
        throw new TypeError(`Invalid buffer size: ${data.length}`);
    const req = new Request(data, bBroadcast, 'SetConfigRegisterWrite5');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ConfigRegisterWriteAddr5;
    return req;
}
Session.prototype.SetConfigRegisterWrite5 = async function SetConfigRegisterWrite5(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createSetConfigRegisterWrite5(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.trySetConfigRegisterWrite5 = async function trySetConfigRegisterWrite5(addr, portAddr, scanBoardAddr, data) {
    const req = createSetConfigRegisterWrite5(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetConfigRegisterWrite5.js.map