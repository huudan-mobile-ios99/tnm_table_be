import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScanner_DMThird(addr, portAddr, scanBoardAddr, readLength) {
    const req = new Request(readLength, 'ReadScanner_DMThird');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Scanner_DmThirdAAddr;
    return req;
}
Session.prototype.ReadScanner_DMThird = async function ReadScanner_DMThird(addr, portAddr, scanBoardAddr, readLength) {
    const req = createReadScanner_DMThird(addr, portAddr, scanBoardAddr, readLength);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadScanner_DMThird = async function tryReadScanner_DMThird(addr, portAddr, scanBoardAddr, readLength) {
    const req = createReadScanner_DMThird(addr, portAddr, scanBoardAddr, readLength);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScanner_DMThird.js.map