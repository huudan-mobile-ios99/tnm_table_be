import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetSender_10bitGammaValue(addr, bBroadcast, gammaValue) {
    const req = new Request([gammaValue], bBroadcast, 'SetSender_10bitGammaValue');
    req.destination = addr;
    req.address = AddressMapping.Sender_10bitGammaValueAddr;
    return req;
}
Session.prototype.SetSender_10bitGammaValue = async function SetSender_10bitGammaValue(addr, bBroadcast, gammaValue) {
    const req = createSetSender_10bitGammaValue(addr, bBroadcast, gammaValue);
    await this.connection.send(req);
};
Session.prototype.trySetSender_10bitGammaValue = async function trySetSender_10bitGammaValue(addr, gammaValue) {
    const req = createSetSender_10bitGammaValue(addr, false, gammaValue);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetSender_10bitGammaValue.js.map