import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScanner_MutiChipRamA(addr, portAddr, scanBoardAddr, readLength) {
    const req = new Request(readLength, 'ReadScanner_MutiChipRamA');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Scanner_MutiChipRamAAddr;
    return req;
}
Session.prototype.ReadScanner_MutiChipRamA = async function ReadScanner_MutiChipRamA(addr, portAddr, scanBoardAddr, readLength) {
    const req = createReadScanner_MutiChipRamA(addr, portAddr, scanBoardAddr, readLength);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadScanner_MutiChipRamA = async function tryReadScanner_MutiChipRamA(addr, portAddr, scanBoardAddr, readLength) {
    const req = createReadScanner_MutiChipRamA(addr, portAddr, scanBoardAddr, readLength);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScanner_MutiChipRamA.js.map