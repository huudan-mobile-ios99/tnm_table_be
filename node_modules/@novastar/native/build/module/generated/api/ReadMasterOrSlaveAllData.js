import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadMasterOrSlaveAllData(addr) {
    const req = new Request(AddressMapping.Sender_AllDataSpaceOccupancy, 'ReadMasterOrSlaveAllData');
    req.destination = addr;
    req.address = AddressMapping.Sender_AllDataAddr;
    return req;
}
Session.prototype.ReadMasterOrSlaveAllData = async function ReadMasterOrSlaveAllData(addr) {
    const req = createReadMasterOrSlaveAllData(addr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadMasterOrSlaveAllData = async function tryReadMasterOrSlaveAllData(addr) {
    const req = createReadMasterOrSlaveAllData(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadMasterOrSlaveAllData.js.map