import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadAttachedMonitorCardModle(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.AttachedMonitorCardModleOccupancy, 'ReadAttachedMonitorCardModle');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.AttachedMonitorCardModleAddr;
    return req;
}
Session.prototype.ReadAttachedMonitorCardModle = async function ReadAttachedMonitorCardModle(addr, portAddr, scanBoardAddr) {
    const req = createReadAttachedMonitorCardModle(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadAttachedMonitorCardModle = async function tryReadAttachedMonitorCardModle(addr, portAddr, scanBoardAddr) {
    const req = createReadAttachedMonitorCardModle(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadAttachedMonitorCardModle.js.map