import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSender_VideoInputCut(addr) {
    const req = new Request(AddressMapping.Sender_VideoInputCutOccupancy, 'ReadSender_VideoInputCut');
    req.destination = addr;
    req.address = AddressMapping.Sender_VideoInputCutAddr;
    return req;
}
Session.prototype.ReadSender_VideoInputCut = async function ReadSender_VideoInputCut(addr) {
    const req = createReadSender_VideoInputCut(addr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadSender_VideoInputCut = async function tryReadSender_VideoInputCut(addr) {
    const req = createReadSender_VideoInputCut(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSender_VideoInputCut.js.map