import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createFuncCard_ReadTempInfoOfMonitorCard(addr) {
    const req = new Request(AddressMapping.FuncCard_TempInfoOfMonitorCardOccupancy, 'FuncCard_ReadTempInfoOfMonitorCard');
    req.destination = addr;
    req.address = AddressMapping.FuncCard_TempInfoOfMonitorCardAddr;
    return req;
}
Session.prototype.FuncCard_ReadTempInfoOfMonitorCard =
    async function FuncCard_ReadTempInfoOfMonitorCard(addr) {
        const req = createFuncCard_ReadTempInfoOfMonitorCard(addr);
        return decodeUIntLE(await this.connection.send(req));
    };
Session.prototype.tryFuncCard_ReadTempInfoOfMonitorCard =
    async function tryFuncCard_ReadTempInfoOfMonitorCard(addr) {
        const req = createFuncCard_ReadTempInfoOfMonitorCard(addr);
        return this.connection.trySend(req);
    };
//# sourceMappingURL=FuncCard_ReadTempInfoOfMonitorCard.js.map