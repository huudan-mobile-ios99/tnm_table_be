import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadPhysicalTotalDataGroupNum(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.PhysicalTotalDataGroupNumOccupancy, 'ReadPhysicalTotalDataGroupNum');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.PhysicalTotalDataGroupNumAddr;
    return req;
}
Session.prototype.ReadPhysicalTotalDataGroupNum = async function ReadPhysicalTotalDataGroupNum(addr, portAddr, scanBoardAddr) {
    const req = createReadPhysicalTotalDataGroupNum(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadPhysicalTotalDataGroupNum =
    async function tryReadPhysicalTotalDataGroupNum(addr, portAddr, scanBoardAddr) {
        const req = createReadPhysicalTotalDataGroupNum(addr, portAddr, scanBoardAddr);
        return this.connection.trySend(req);
    };
//# sourceMappingURL=ReadPhysicalTotalDataGroupNum.js.map