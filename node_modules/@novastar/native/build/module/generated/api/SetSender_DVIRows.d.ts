import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetSender_DVIRows(addr: number, bBroadcast: boolean, dviRows: number): Promise<void>;
        trySetSender_DVIRows(addr: number, dviRows: number): Promise<ErrorType | null>;
    }
}
export default function createSetSender_DVIRows<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, dviRows: number): Request<Broadcast>;
