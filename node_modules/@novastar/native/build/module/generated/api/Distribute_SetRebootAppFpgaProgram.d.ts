import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        Distribute_SetRebootAppFpgaProgram(addr: number, portAddr: number, distributeAddr: number, bBroadcast: boolean, distributeModle: number): Promise<void>;
        tryDistribute_SetRebootAppFpgaProgram(addr: number, portAddr: number, distributeAddr: number, distributeModle: number): Promise<ErrorType | null>;
    }
}
export default function createDistribute_SetRebootAppFpgaProgram<Broadcast extends boolean>(addr: number, portAddr: number, distributeAddr: number, bBroadcast: Broadcast, distributeModle: number): Request<Broadcast>;
