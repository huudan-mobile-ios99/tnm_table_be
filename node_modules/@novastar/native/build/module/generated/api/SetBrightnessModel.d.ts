import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetBrightnessModel(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, brightnessmodel: number): Promise<void>;
        trySetBrightnessModel(addr: number, portAddr: number, scanBoardAddr: number, brightnessmodel: number): Promise<ErrorType | null>;
    }
}
export default function createSetBrightnessModel<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, brightnessmodel: number): Request<Broadcast>;
