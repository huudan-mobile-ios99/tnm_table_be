import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetSender_HDR10MinLum(addr: number, bBroadcast: boolean, HDR10MinLum: number): Promise<void>;
        trySetSender_HDR10MinLum(addr: number, HDR10MinLum: number): Promise<ErrorType | null>;
    }
}
export default function createSetSender_HDR10MinLum<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, HDR10MinLum: number): Request<Broadcast>;
