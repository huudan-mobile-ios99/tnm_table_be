import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScanner_MutiChipRamB(addr, portAddr, scanBoardAddr, readLength) {
    const req = new Request(readLength, 'ReadScanner_MutiChipRamB');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Scanner_MutiChipRamBAddr;
    return req;
}
Session.prototype.ReadScanner_MutiChipRamB = async function ReadScanner_MutiChipRamB(addr, portAddr, scanBoardAddr, readLength) {
    const req = createReadScanner_MutiChipRamB(addr, portAddr, scanBoardAddr, readLength);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadScanner_MutiChipRamB = async function tryReadScanner_MutiChipRamB(addr, portAddr, scanBoardAddr, readLength) {
    const req = createReadScanner_MutiChipRamB(addr, portAddr, scanBoardAddr, readLength);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScanner_MutiChipRamB.js.map