import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetMBI5252ManualReset(addr, portAddr, scanBoardAddr, bBroadcast) {
    const req = new Request([1], bBroadcast, 'SetMBI5252ManualReset');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.MBI5252SetManualResetAddr;
    return req;
}
Session.prototype.SetMBI5252ManualReset = async function SetMBI5252ManualReset(addr, portAddr, scanBoardAddr, bBroadcast) {
    const req = createSetMBI5252ManualReset(addr, portAddr, scanBoardAddr, bBroadcast);
    await this.connection.send(req);
};
Session.prototype.trySetMBI5252ManualReset = async function trySetMBI5252ManualReset(addr, portAddr, scanBoardAddr) {
    const req = createSetMBI5252ManualReset(addr, portAddr, scanBoardAddr, false);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetMBI5252ManualReset.js.map