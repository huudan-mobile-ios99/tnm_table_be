import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadGclkHigh(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.GclkHighOccupancy, 'ReadGclkHigh');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.GclkHighAddr;
    return req;
}
Session.prototype.ReadGclkHigh = async function ReadGclkHigh(addr, portAddr, scanBoardAddr) {
    const req = createReadGclkHigh(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadGclkHigh = async function tryReadGclkHigh(addr, portAddr, scanBoardAddr) {
    const req = createReadGclkHigh(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadGclkHigh.js.map