import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadMGParmData(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.MGParmccupancy, 'ReadMGParmData');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.MGParmAddr;
    return req;
}
Session.prototype.ReadMGParmData = async function ReadMGParmData(addr, portAddr, scanBoardAddr) {
    const req = createReadMGParmData(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadMGParmData = async function tryReadMGParmData(addr, portAddr, scanBoardAddr) {
    const req = createReadMGParmData(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadMGParmData.js.map