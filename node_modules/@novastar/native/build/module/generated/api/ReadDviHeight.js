import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadDviHeight(addr, index) {
    const req = new Request(AddressMapping.DviHeightOccupancy, 'ReadDviHeight');
    req.destination = addr;
    req.address = AddressMapping.DviHeightAddr + AddressMapping.DviOccupancy * index;
    return req;
}
Session.prototype.ReadDviHeight = async function ReadDviHeight(addr, index) {
    const req = createReadDviHeight(addr, index);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadDviHeight = async function tryReadDviHeight(addr, index) {
    const req = createReadDviHeight(addr, index);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadDviHeight.js.map