import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSpecialFrameRate(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.SpecialFrameRateOccupancy, 'ReadSpecialFrameRate');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.SpecialFrameRateAddr;
    return req;
}
Session.prototype.ReadSpecialFrameRate = async function ReadSpecialFrameRate(addr, portAddr, scanBoardAddr) {
    const req = createReadSpecialFrameRate(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadSpecialFrameRate = async function tryReadSpecialFrameRate(addr, portAddr, scanBoardAddr) {
    const req = createReadSpecialFrameRate(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSpecialFrameRate.js.map