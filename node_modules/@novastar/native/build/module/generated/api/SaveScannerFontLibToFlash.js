import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSaveScannerFontLibToFlash(addr, portAddr, scanBoardAddr, bBroadcast) {
    const req = new Request([5], bBroadcast, 'SaveScannerFontLibToFlash');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.SaveScannerFontLibAddr;
    return req;
}
Session.prototype.SaveScannerFontLibToFlash = async function SaveScannerFontLibToFlash(addr, portAddr, scanBoardAddr, bBroadcast) {
    const req = createSaveScannerFontLibToFlash(addr, portAddr, scanBoardAddr, bBroadcast);
    await this.connection.send(req);
};
Session.prototype.trySaveScannerFontLibToFlash = async function trySaveScannerFontLibToFlash(addr, portAddr, scanBoardAddr) {
    const req = createSaveScannerFontLibToFlash(addr, portAddr, scanBoardAddr, false);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SaveScannerFontLibToFlash.js.map