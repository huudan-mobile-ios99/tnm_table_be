/// <reference types="node" />
import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetRtco(addr: number, bBroadcast: boolean, rtco: number[] | Buffer): Promise<void>;
        trySetRtco(addr: number, rtco: number[] | Buffer): Promise<ErrorType | null>;
    }
}
export default function createSetRtco<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, rtco: number[] | Buffer): Request<Broadcast>;
