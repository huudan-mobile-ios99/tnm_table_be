import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadDExtendMode(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.DExtendModeOccupancy, 'ReadDExtendMode');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.DExtendModeAddr;
    return req;
}
Session.prototype.ReadDExtendMode = async function ReadDExtendMode(addr, portAddr, scanBoardAddr) {
    const req = createReadDExtendMode(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadDExtendMode = async function tryReadDExtendMode(addr, portAddr, scanBoardAddr) {
    const req = createReadDExtendMode(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadDExtendMode.js.map