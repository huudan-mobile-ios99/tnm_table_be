import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createFuncCard_ReadDelayTime(addr) {
    const req = new Request(AddressMapping.FuncCard_DelayTimeOccupancy, 'FuncCard_ReadDelayTime');
    req.destination = addr;
    req.address = AddressMapping.FuncCard_DelayTimeAddr;
    return req;
}
Session.prototype.FuncCard_ReadDelayTime = async function FuncCard_ReadDelayTime(addr) {
    const req = createFuncCard_ReadDelayTime(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryFuncCard_ReadDelayTime = async function tryFuncCard_ReadDelayTime(addr) {
    const req = createFuncCard_ReadDelayTime(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=FuncCard_ReadDelayTime.js.map