import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetRedGammaTableData(addr, portAddr, scanBoardAddr, bBroadcast, data, is22Bit) {
    const req = new Request(data, bBroadcast, 'SetRedGammaTableData');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.RedGammaTableAddr;
    if (is22Bit) {
        req.address = AddressMapping.RedGammaTableAddr_22bit;
    }
    return req;
}
Session.prototype.SetRedGammaTableData = async function SetRedGammaTableData(addr, portAddr, scanBoardAddr, bBroadcast, data, is22Bit) {
    const req = createSetRedGammaTableData(addr, portAddr, scanBoardAddr, bBroadcast, data, is22Bit);
    await this.connection.send(req);
};
Session.prototype.trySetRedGammaTableData = async function trySetRedGammaTableData(addr, portAddr, scanBoardAddr, data, is22Bit) {
    const req = createSetRedGammaTableData(addr, portAddr, scanBoardAddr, false, data, is22Bit);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetRedGammaTableData.js.map