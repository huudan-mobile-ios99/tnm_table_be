import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetFlagEnableAccelerate(addr: number, bBroadcast: boolean, isOpen: boolean): Promise<void>;
        trySetFlagEnableAccelerate(addr: number, isOpen: boolean): Promise<ErrorType | null>;
    }
}
export default function createSetFlagEnableAccelerate<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, isOpen: boolean): Request<Broadcast>;
