import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadTemprature(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.TempratureOccupancy, 'ReadTemprature');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.TempratureAddr;
    return req;
}
Session.prototype.ReadTemprature = async function ReadTemprature(addr, portAddr, scanBoardAddr) {
    const req = createReadTemprature(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadTemprature = async function tryReadTemprature(addr, portAddr, scanBoardAddr) {
    const req = createReadTemprature(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadTemprature.js.map