import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        Distribute_SetDistributeMCUVersion(addr: number, portAddr: number, distributeAddr: number, bBroadcast: boolean, mcuVersion: number): Promise<void>;
        tryDistribute_SetDistributeMCUVersion(addr: number, portAddr: number, distributeAddr: number, mcuVersion: number): Promise<ErrorType | null>;
    }
}
export default function createDistribute_SetDistributeMCUVersion<Broadcast extends boolean>(addr: number, portAddr: number, distributeAddr: number, bBroadcast: Broadcast, mcuVersion: number): Request<Broadcast>;
