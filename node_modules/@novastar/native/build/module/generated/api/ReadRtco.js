import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadRtco(addr) {
    const req = new Request(AddressMapping.RtcoOccupancy, 'ReadRtco');
    req.destination = addr;
    req.address = AddressMapping.RtcoAddr;
    return req;
}
Session.prototype.ReadRtco = async function ReadRtco(addr) {
    const req = createReadRtco(addr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadRtco = async function tryReadRtco(addr) {
    const req = createReadRtco(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadRtco.js.map