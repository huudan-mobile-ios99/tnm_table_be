import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetCoefficienceFromDviCommon(addr, portAddr, scanBoardAddr, bBroadcast) {
    const req = new Request([1], bBroadcast, 'SetCoefficienceFromDviCommon');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.WriteCommonCoefficienceFromDviAddr;
    return req;
}
Session.prototype.SetCoefficienceFromDviCommon = async function SetCoefficienceFromDviCommon(addr, portAddr, scanBoardAddr, bBroadcast) {
    const req = createSetCoefficienceFromDviCommon(addr, portAddr, scanBoardAddr, bBroadcast);
    await this.connection.send(req);
};
Session.prototype.trySetCoefficienceFromDviCommon = async function trySetCoefficienceFromDviCommon(addr, portAddr, scanBoardAddr) {
    const req = createSetCoefficienceFromDviCommon(addr, portAddr, scanBoardAddr, false);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetCoefficienceFromDviCommon.js.map