import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetLowAshCompensation(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    if (data.length !== AddressMapping.LowAshCompensationOccupancy)
        throw new TypeError(`Invalid buffer size: ${data.length}`);
    const req = new Request(data, bBroadcast, 'SetLowAshCompensation');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.LowAshCompensationAddr;
    return req;
}
Session.prototype.SetLowAshCompensation = async function SetLowAshCompensation(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createSetLowAshCompensation(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.trySetLowAshCompensation = async function trySetLowAshCompensation(addr, portAddr, scanBoardAddr, data) {
    const req = createSetLowAshCompensation(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetLowAshCompensation.js.map