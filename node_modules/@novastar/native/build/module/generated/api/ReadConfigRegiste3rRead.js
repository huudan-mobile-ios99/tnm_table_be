import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadConfigRegiste3rRead(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.Config3RegisterOccupancy, 'ReadConfigRegiste3rRead');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Config3RegisterWriteAddr;
    return req;
}
Session.prototype.ReadConfigRegiste3rRead = async function ReadConfigRegiste3rRead(addr, portAddr, scanBoardAddr) {
    const req = createReadConfigRegiste3rRead(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadConfigRegiste3rRead = async function tryReadConfigRegiste3rRead(addr, portAddr, scanBoardAddr) {
    const req = createReadConfigRegiste3rRead(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadConfigRegiste3rRead.js.map