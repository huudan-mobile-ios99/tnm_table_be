import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSet660ConfigFileSendOver(addr, bBroadcast, data) {
    const $data = encodeUIntLE(data, AddressMapping.Sender660ConfigFileSendOverOccupancy);
    const req = new Request($data, bBroadcast, 'Set660ConfigFileSendOver');
    req.destination = addr;
    req.address = AddressMapping.Sender660ConfigFileSendOverAddr;
    return req;
}
Session.prototype.Set660ConfigFileSendOver = async function Set660ConfigFileSendOver(addr, bBroadcast, data) {
    const req = createSet660ConfigFileSendOver(addr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.trySet660ConfigFileSendOver = async function trySet660ConfigFileSendOver(addr, data) {
    const req = createSet660ConfigFileSendOver(addr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=Set660ConfigFileSendOver.js.map