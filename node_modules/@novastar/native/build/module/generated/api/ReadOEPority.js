import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadOEPority(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.OEPorityOccupancy, 'ReadOEPority');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.OEPorityAddr;
    return req;
}
Session.prototype.ReadOEPority = async function ReadOEPority(addr, portAddr, scanBoardAddr) {
    const req = createReadOEPority(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadOEPority = async function tryReadOEPority(addr, portAddr, scanBoardAddr) {
    const req = createReadOEPority(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadOEPority.js.map