import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadNoCorrectionThreshold(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.NoCorrectionThresholdOccupancy, 'ReadNoCorrectionThreshold');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.NoCorrectionThresholdAddr;
    return req;
}
Session.prototype.ReadNoCorrectionThreshold = async function ReadNoCorrectionThreshold(addr, portAddr, scanBoardAddr) {
    const req = createReadNoCorrectionThreshold(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadNoCorrectionThreshold = async function tryReadNoCorrectionThreshold(addr, portAddr, scanBoardAddr) {
    const req = createReadNoCorrectionThreshold(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadNoCorrectionThreshold.js.map