import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSourceBackupInfo(addr, readLength) {
    const req = new Request(readLength, 'ReadSourceBackupInfo');
    req.destination = addr;
    req.address = AddressMapping.SourceBackupAddr;
    return req;
}
Session.prototype.ReadSourceBackupInfo = async function ReadSourceBackupInfo(addr, readLength) {
    const req = createReadSourceBackupInfo(addr, readLength);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadSourceBackupInfo = async function tryReadSourceBackupInfo(addr, readLength) {
    const req = createReadSourceBackupInfo(addr, readLength);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSourceBackupInfo.js.map