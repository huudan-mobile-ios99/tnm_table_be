import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScanner_McuProgramRemarks(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.Scanner_McuProgramRemarksOccupancy, 'ReadScanner_McuProgramRemarks');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Scanner_McuProgramRemarksAddr;
    return req;
}
Session.prototype.ReadScanner_McuProgramRemarks = async function ReadScanner_McuProgramRemarks(addr, portAddr, scanBoardAddr) {
    const req = createReadScanner_McuProgramRemarks(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadScanner_McuProgramRemarks =
    async function tryReadScanner_McuProgramRemarks(addr, portAddr, scanBoardAddr) {
        const req = createReadScanner_McuProgramRemarks(addr, portAddr, scanBoardAddr);
        return this.connection.trySend(req);
    };
//# sourceMappingURL=ReadScanner_McuProgramRemarks.js.map