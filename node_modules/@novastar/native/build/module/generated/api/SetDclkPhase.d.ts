import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetDclkPhase(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, dclkPhase: number): Promise<void>;
        trySetDclkPhase(addr: number, portAddr: number, scanBoardAddr: number, dclkPhase: number): Promise<ErrorType | null>;
    }
}
export default function createSetDclkPhase<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, dclkPhase: number): Request<Broadcast>;
