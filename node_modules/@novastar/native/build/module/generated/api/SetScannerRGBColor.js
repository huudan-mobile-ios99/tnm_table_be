import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetScannerRGBColor(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = new Request(data, bBroadcast, 'SetScannerRGBColor');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ScannerRGBColorAddr;
    return req;
}
Session.prototype.SetScannerRGBColor = async function SetScannerRGBColor(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createSetScannerRGBColor(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.trySetScannerRGBColor = async function trySetScannerRGBColor(addr, portAddr, scanBoardAddr, data) {
    const req = createSetScannerRGBColor(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetScannerRGBColor.js.map