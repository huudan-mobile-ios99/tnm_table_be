import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSender_DVIResolutionHeight(addr) {
    const req = new Request(AddressMapping.Sender_DVIHeightOccupancy, 'ReadSender_DVIResolutionHeight');
    req.destination = addr;
    req.address = AddressMapping.Sender_DVIHeightAddr;
    return req;
}
Session.prototype.ReadSender_DVIResolutionHeight = async function ReadSender_DVIResolutionHeight(addr) {
    const req = createReadSender_DVIResolutionHeight(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadSender_DVIResolutionHeight =
    async function tryReadSender_DVIResolutionHeight(addr) {
        const req = createReadSender_DVIResolutionHeight(addr);
        return this.connection.trySend(req);
    };
//# sourceMappingURL=ReadSender_DVIResolutionHeight.js.map