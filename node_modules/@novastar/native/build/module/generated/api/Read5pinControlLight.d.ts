import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        Read5pinControlLight(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryRead5pinControlLight(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createRead5pinControlLight(addr: number, portAddr: number, scanBoardAddr: number): Request;
