import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
import { HWBrightAdjustTypeEnum } from '../HWBrightAdjustType';
export default function createSetSender_HWBrightAdjustType(addr, bBroadcast, enableAutoData) {
    const req = new Request(enableAutoData === HWBrightAdjustTypeEnum.HWAutoBright
        ? [125]
        : enableAutoData === HWBrightAdjustTypeEnum.HWProgram
            ? [91]
            : [255], bBroadcast, 'SetSender_HWBrightAdjustType');
    req.destination = addr;
    req.address = AddressMapping.Sender_HWBrightAdjustTypeAddr;
    return req;
}
Session.prototype.SetSender_HWBrightAdjustType = async function SetSender_HWBrightAdjustType(addr, bBroadcast, enableAutoData) {
    const req = createSetSender_HWBrightAdjustType(addr, bBroadcast, enableAutoData);
    await this.connection.send(req);
};
Session.prototype.trySetSender_HWBrightAdjustType = async function trySetSender_HWBrightAdjustType(addr, enableAutoData) {
    const req = createSetSender_HWBrightAdjustType(addr, false, enableAutoData);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetSender_HWBrightAdjustType.js.map