import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSender_RedundantStateNewSpace(addr) {
    const req = new Request(AddressMapping.Sender_RedundantStateSpaceOccupancy, 'ReadSender_RedundantStateNewSpace');
    req.destination = addr;
    req.address = AddressMapping.Sender_RedundantStateSpaceNewAddr;
    return req;
}
Session.prototype.ReadSender_RedundantStateNewSpace =
    async function ReadSender_RedundantStateNewSpace(addr) {
        const req = createReadSender_RedundantStateNewSpace(addr);
        return (await this.connection.send(req)).data;
    };
Session.prototype.tryReadSender_RedundantStateNewSpace =
    async function tryReadSender_RedundantStateNewSpace(addr) {
        const req = createReadSender_RedundantStateNewSpace(addr);
        return this.connection.trySend(req);
    };
//# sourceMappingURL=ReadSender_RedundantStateNewSpace.js.map