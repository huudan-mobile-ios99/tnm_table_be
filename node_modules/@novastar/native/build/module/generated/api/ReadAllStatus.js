import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadAllStatus(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.Scanner_AllMonitorDataOccupancy, 'ReadAllStatus');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Scanner_AllMonitorDataAddr;
    return req;
}
Session.prototype.ReadAllStatus = async function ReadAllStatus(addr, portAddr, scanBoardAddr) {
    const req = createReadAllStatus(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadAllStatus = async function tryReadAllStatus(addr, portAddr, scanBoardAddr) {
    const req = createReadAllStatus(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadAllStatus.js.map