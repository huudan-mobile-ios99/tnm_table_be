import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadVirtualCaptureEnable(addr) {
    const req = new Request(AddressMapping.VirtualCaptureEnableOccupancy, 'ReadVirtualCaptureEnable');
    req.destination = addr;
    req.address = AddressMapping.VirtualCaptureEnableAddr;
    return req;
}
Session.prototype.ReadVirtualCaptureEnable = async function ReadVirtualCaptureEnable(addr) {
    const req = createReadVirtualCaptureEnable(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadVirtualCaptureEnable = async function tryReadVirtualCaptureEnable(addr) {
    const req = createReadVirtualCaptureEnable(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadVirtualCaptureEnable.js.map