import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createGetDeviceLevel(addr) {
    const req = new Request(AddressMapping.SenderContrastResultOccupancy, 'GetDeviceLevel');
    req.destination = addr;
    req.address = AddressMapping.SenderLevelAddr;
    return req;
}
Session.prototype.GetDeviceLevel = async function GetDeviceLevel(addr) {
    const req = createGetDeviceLevel(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryGetDeviceLevel = async function tryGetDeviceLevel(addr) {
    const req = createGetDeviceLevel(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=GetDeviceLevel.js.map