import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetGenLinearScanBoardRunLineTable(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, genLinearTable: number): Promise<void>;
        trySetGenLinearScanBoardRunLineTable(addr: number, portAddr: number, scanBoardAddr: number, genLinearTable: number): Promise<ErrorType | null>;
    }
}
export default function createSetGenLinearScanBoardRunLineTable<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, genLinearTable: number): Request<Broadcast>;
