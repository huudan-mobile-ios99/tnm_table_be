/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadSender_SoftwareSpace(addr: number, dataLength: number, addrOffset: number): Promise<Buffer>;
        tryReadSender_SoftwareSpace(addr: number, dataLength: number, addrOffset: number): Promise<Packet | null>;
    }
}
export default function createReadSender_SoftwareSpace(addr: number, dataLength: number, addrOffset: number): Request;
