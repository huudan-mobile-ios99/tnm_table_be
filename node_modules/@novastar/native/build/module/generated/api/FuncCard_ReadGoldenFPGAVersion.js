import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createFuncCard_ReadGoldenFPGAVersion(addr) {
    const req = new Request(AddressMapping.FuncCard_GoldenFPGAVersionOccupancy, 'FuncCard_ReadGoldenFPGAVersion');
    req.destination = addr;
    req.address = AddressMapping.FuncCard_GoldenFPGAVersionAddr;
    return req;
}
Session.prototype.FuncCard_ReadGoldenFPGAVersion = async function FuncCard_ReadGoldenFPGAVersion(addr) {
    const req = createFuncCard_ReadGoldenFPGAVersion(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryFuncCard_ReadGoldenFPGAVersion =
    async function tryFuncCard_ReadGoldenFPGAVersion(addr) {
        const req = createFuncCard_ReadGoldenFPGAVersion(addr);
        return this.connection.trySend(req);
    };
//# sourceMappingURL=FuncCard_ReadGoldenFPGAVersion.js.map