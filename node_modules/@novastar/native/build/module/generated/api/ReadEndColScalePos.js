import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadEndColScalePos(addr) {
    const req = new Request(AddressMapping.EndColScalePosOccupancy, 'ReadEndColScalePos');
    req.destination = addr;
    req.address = AddressMapping.EndColScalePosAddr;
    return req;
}
Session.prototype.ReadEndColScalePos = async function ReadEndColScalePos(addr) {
    const req = createReadEndColScalePos(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadEndColScalePos = async function tryReadEndColScalePos(addr) {
    const req = createReadEndColScalePos(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadEndColScalePos.js.map