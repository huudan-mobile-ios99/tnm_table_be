import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetSerialColorNum(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, serialColorNum: number): Promise<void>;
        trySetSerialColorNum(addr: number, portAddr: number, scanBoardAddr: number, serialColorNum: number): Promise<ErrorType | null>;
    }
}
export default function createSetSerialColorNum<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, serialColorNum: number): Request<Broadcast>;
