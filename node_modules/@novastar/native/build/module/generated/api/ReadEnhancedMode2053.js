import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadEnhancedMode2053(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.EnhancedMode2053Occupancy, 'ReadEnhancedMode2053');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.EnhancedMode2053Addr;
    return req;
}
Session.prototype.ReadEnhancedMode2053 = async function ReadEnhancedMode2053(addr, portAddr, scanBoardAddr) {
    const req = createReadEnhancedMode2053(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadEnhancedMode2053 = async function tryReadEnhancedMode2053(addr, portAddr, scanBoardAddr) {
    const req = createReadEnhancedMode2053(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadEnhancedMode2053.js.map