import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetEndColScalePos(addr, bBroadcast, endColScalePos) {
    const $data = encodeUIntLE(endColScalePos, AddressMapping.ColScaleParAOccupancy);
    const req = new Request($data, bBroadcast, 'SetEndColScalePos');
    req.destination = addr;
    req.address = AddressMapping.EndColScalePosAddr;
    return req;
}
Session.prototype.SetEndColScalePos = async function SetEndColScalePos(addr, bBroadcast, endColScalePos) {
    const req = createSetEndColScalePos(addr, bBroadcast, endColScalePos);
    await this.connection.send(req);
};
Session.prototype.trySetEndColScalePos = async function trySetEndColScalePos(addr, endColScalePos) {
    const req = createSetEndColScalePos(addr, false, endColScalePos);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetEndColScalePos.js.map