import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        MoveCorrectionFromSdramToMasterFlash(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean): Promise<void>;
        tryMoveCorrectionFromSdramToMasterFlash(addr: number, portAddr: number, scanBoardAddr: number): Promise<ErrorType | null>;
    }
}
export default function createMoveCorrectionFromSdramToMasterFlash<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast): Request<Broadcast>;
