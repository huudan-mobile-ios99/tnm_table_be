import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSaveDistributeParameters(addr, portAddr, distributeAddr) {
    const req = new Request(AddressMapping.SaveDistributeParameterOccupancy, 'ReadSaveDistributeParameters');
    req.destination = addr;
    req.deviceType = 3;
    req.port = portAddr;
    req.rcvIndex = distributeAddr;
    req.address = AddressMapping.SaveDistributeParameterAddr;
    return req;
}
Session.prototype.ReadSaveDistributeParameters = async function ReadSaveDistributeParameters(addr, portAddr, distributeAddr) {
    const req = createReadSaveDistributeParameters(addr, portAddr, distributeAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadSaveDistributeParameters = async function tryReadSaveDistributeParameters(addr, portAddr, distributeAddr) {
    const req = createReadSaveDistributeParameters(addr, portAddr, distributeAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSaveDistributeParameters.js.map