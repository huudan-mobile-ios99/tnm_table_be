import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadHumiOfMonitorCard(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.Scanner_HumiOfMonitorCardOccupancy, 'ReadHumiOfMonitorCard');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Scanner_HumiOfMonitorCardAddr;
    return req;
}
Session.prototype.ReadHumiOfMonitorCard = async function ReadHumiOfMonitorCard(addr, portAddr, scanBoardAddr) {
    const req = createReadHumiOfMonitorCard(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadHumiOfMonitorCard = async function tryReadHumiOfMonitorCard(addr, portAddr, scanBoardAddr) {
    const req = createReadHumiOfMonitorCard(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadHumiOfMonitorCard.js.map