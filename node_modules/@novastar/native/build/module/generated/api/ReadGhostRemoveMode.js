import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadGhostRemoveMode(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.GhostRemoveModeOccupancy, 'ReadGhostRemoveMode');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.GhostRemoveModeAddr;
    return req;
}
Session.prototype.ReadGhostRemoveMode = async function ReadGhostRemoveMode(addr, portAddr, scanBoardAddr) {
    const req = createReadGhostRemoveMode(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadGhostRemoveMode = async function tryReadGhostRemoveMode(addr, portAddr, scanBoardAddr) {
    const req = createReadGhostRemoveMode(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadGhostRemoveMode.js.map