import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadCustomGammaConfigInfo(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.DelayTimeOccupancy, 'ReadCustomGammaConfigInfo');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.CustomGammaConfigInfoAddr;
    return req;
}
Session.prototype.ReadCustomGammaConfigInfo = async function ReadCustomGammaConfigInfo(addr, portAddr, scanBoardAddr) {
    const req = createReadCustomGammaConfigInfo(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadCustomGammaConfigInfo = async function tryReadCustomGammaConfigInfo(addr, portAddr, scanBoardAddr) {
    const req = createReadCustomGammaConfigInfo(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadCustomGammaConfigInfo.js.map