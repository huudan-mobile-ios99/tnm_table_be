import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadColorAdjustEnable(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ColorAdjustEnableOccupancy, 'ReadColorAdjustEnable');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ColorAdjustEnableAddr;
    return req;
}
Session.prototype.ReadColorAdjustEnable = async function ReadColorAdjustEnable(addr, portAddr, scanBoardAddr) {
    const req = createReadColorAdjustEnable(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadColorAdjustEnable = async function tryReadColorAdjustEnable(addr, portAddr, scanBoardAddr) {
    const req = createReadColorAdjustEnable(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadColorAdjustEnable.js.map