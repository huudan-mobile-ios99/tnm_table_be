import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSender_EdidInterlacedFlag(addr) {
    const req = new Request(AddressMapping.EdidInterlacedFlagOccupancy, 'ReadSender_EdidInterlacedFlag');
    req.destination = addr;
    req.address = AddressMapping.EdidInterlacedFlagAddr;
    return req;
}
Session.prototype.ReadSender_EdidInterlacedFlag = async function ReadSender_EdidInterlacedFlag(addr) {
    const req = createReadSender_EdidInterlacedFlag(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadSender_EdidInterlacedFlag =
    async function tryReadSender_EdidInterlacedFlag(addr) {
        const req = createReadSender_EdidInterlacedFlag(addr);
        return this.connection.trySend(req);
    };
//# sourceMappingURL=ReadSender_EdidInterlacedFlag.js.map