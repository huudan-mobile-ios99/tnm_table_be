import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadTVCardDVI0OffsetY(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.TVCardDVI0OffsetYOccupancy, 'ReadTVCardDVI0OffsetY');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.TVCardDVI0OffsetYAddr;
    return req;
}
Session.prototype.ReadTVCardDVI0OffsetY = async function ReadTVCardDVI0OffsetY(addr, portAddr, scanBoardAddr) {
    const req = createReadTVCardDVI0OffsetY(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadTVCardDVI0OffsetY = async function tryReadTVCardDVI0OffsetY(addr, portAddr, scanBoardAddr) {
    const req = createReadTVCardDVI0OffsetY(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadTVCardDVI0OffsetY.js.map