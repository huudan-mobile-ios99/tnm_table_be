import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadMoudleNumber(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ScannerMoudleNumberOccupancy, 'ReadMoudleNumber');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ScanneMoudleNumberAddr;
    return req;
}
Session.prototype.ReadMoudleNumber = async function ReadMoudleNumber(addr, portAddr, scanBoardAddr) {
    const req = createReadMoudleNumber(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadMoudleNumber = async function tryReadMoudleNumber(addr, portAddr, scanBoardAddr) {
    const req = createReadMoudleNumber(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadMoudleNumber.js.map