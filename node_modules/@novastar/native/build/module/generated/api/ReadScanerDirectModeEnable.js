import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScanerDirectModeEnable(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ScannerDirectModeEnableOccupancy, 'ReadScanerDirectModeEnable');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ScannderDirectModeEnableAddr;
    return req;
}
Session.prototype.ReadScanerDirectModeEnable = async function ReadScanerDirectModeEnable(addr, portAddr, scanBoardAddr) {
    const req = createReadScanerDirectModeEnable(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadScanerDirectModeEnable = async function tryReadScanerDirectModeEnable(addr, portAddr, scanBoardAddr) {
    const req = createReadScanerDirectModeEnable(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScanerDirectModeEnable.js.map