import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetScanner_McuProgramEdition(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, scanner_McuProgramEdition: number): Promise<void>;
        trySetScanner_McuProgramEdition(addr: number, portAddr: number, scanBoardAddr: number, scanner_McuProgramEdition: number): Promise<ErrorType | null>;
    }
}
export default function createSetScanner_McuProgramEdition<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, scanner_McuProgramEdition: number): Request<Broadcast>;
