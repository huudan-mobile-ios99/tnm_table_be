/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadScanBoardDataTo2500(addr: number, portAddr: number, scanBoardAddr: number, length: number): Promise<Buffer>;
        tryReadScanBoardDataTo2500(addr: number, portAddr: number, scanBoardAddr: number, length: number): Promise<Packet | null>;
    }
}
export default function createReadScanBoardDataTo2500(addr: number, portAddr: number, scanBoardAddr: number, length: number): Request;
