import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createFuncCard_ReadPowerCtrlMode(addr) {
    const req = new Request(AddressMapping.FuncCard_PowerCtrlModeOccupancy, 'FuncCard_ReadPowerCtrlMode');
    req.destination = addr;
    req.address = AddressMapping.FuncCard_PowerCtrlModeAddr;
    return req;
}
Session.prototype.FuncCard_ReadPowerCtrlMode = async function FuncCard_ReadPowerCtrlMode(addr) {
    const req = createFuncCard_ReadPowerCtrlMode(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryFuncCard_ReadPowerCtrlMode = async function tryFuncCard_ReadPowerCtrlMode(addr) {
    const req = createFuncCard_ReadPowerCtrlMode(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=FuncCard_ReadPowerCtrlMode.js.map