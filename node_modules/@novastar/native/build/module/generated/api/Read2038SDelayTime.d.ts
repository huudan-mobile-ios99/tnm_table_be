import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        Read2038SDelayTime(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryRead2038SDelayTime(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createRead2038SDelayTime(addr: number, portAddr: number, scanBoardAddr: number): Request;
