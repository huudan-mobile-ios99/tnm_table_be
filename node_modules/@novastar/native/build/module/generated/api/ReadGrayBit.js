import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadGrayBit(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.GrayBitOccupancy, 'ReadGrayBit');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.GrayBitAddr;
    return req;
}
Session.prototype.ReadGrayBit = async function ReadGrayBit(addr, portAddr, scanBoardAddr) {
    const req = createReadGrayBit(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadGrayBit = async function tryReadGrayBit(addr, portAddr, scanBoardAddr) {
    const req = createReadGrayBit(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadGrayBit.js.map