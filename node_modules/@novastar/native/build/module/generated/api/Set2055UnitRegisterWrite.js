import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSet2055UnitRegisterWrite(addr, portAddr, scanBoardAddr, bBroadcast, data, unit) {
    if (data.length !== AddressMapping.Config2055UnitRegisterOccupancy)
        throw new TypeError(`Invalid buffer size: ${data.length}`);
    const req = new Request(data, bBroadcast, 'Set2055UnitRegisterWrite');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Config2055RegisterAddr + unit * 8;
    return req;
}
Session.prototype.Set2055UnitRegisterWrite = async function Set2055UnitRegisterWrite(addr, portAddr, scanBoardAddr, bBroadcast, data, unit) {
    const req = createSet2055UnitRegisterWrite(addr, portAddr, scanBoardAddr, bBroadcast, data, unit);
    await this.connection.send(req);
};
Session.prototype.trySet2055UnitRegisterWrite = async function trySet2055UnitRegisterWrite(addr, portAddr, scanBoardAddr, data, unit) {
    const req = createSet2055UnitRegisterWrite(addr, portAddr, scanBoardAddr, false, data, unit);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=Set2055UnitRegisterWrite.js.map