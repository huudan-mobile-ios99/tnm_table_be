import { ErrorType, Request } from '@novastar/codec';
import { AudioFrequencyModeEnum } from '../AudioFrequencyMode';
declare module '@novastar/codec' {
    interface API {
        FuncCard_SetAudioFrequency_1(addr: number, portAddr: number, funcCardAddr: number, bBroadcast: boolean, freMode: AudioFrequencyModeEnum): Promise<void>;
        tryFuncCard_SetAudioFrequency_1(addr: number, portAddr: number, funcCardAddr: number, freMode: AudioFrequencyModeEnum): Promise<ErrorType | null>;
    }
}
export default function createFuncCard_SetAudioFrequency_1<Broadcast extends boolean>(addr: number, portAddr: number, funcCardAddr: number, bBroadcast: Broadcast, freMode: AudioFrequencyModeEnum): Request<Broadcast>;
