import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadControllerPhysical(addr) {
    const req = new Request(AddressMapping.ControllerPhysicalOccupancy, 'ReadControllerPhysical');
    req.destination = addr;
    req.address = AddressMapping.ControllerPhysicalAddr;
    return req;
}
Session.prototype.ReadControllerPhysical = async function ReadControllerPhysical(addr) {
    const req = createReadControllerPhysical(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadControllerPhysical = async function tryReadControllerPhysical(addr) {
    const req = createReadControllerPhysical(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadControllerPhysical.js.map