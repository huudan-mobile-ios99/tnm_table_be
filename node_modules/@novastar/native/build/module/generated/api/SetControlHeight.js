import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetControlHeight(addr, portAddr, scanBoardAddr, bBroadcast, controlHeight) {
    const $data = encodeUIntLE(controlHeight, AddressMapping.ControlHeightOccupancy);
    const req = new Request($data, bBroadcast, 'SetControlHeight');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ControlHeightAddr;
    return req;
}
Session.prototype.SetControlHeight = async function SetControlHeight(addr, portAddr, scanBoardAddr, bBroadcast, controlHeight) {
    const req = createSetControlHeight(addr, portAddr, scanBoardAddr, bBroadcast, controlHeight);
    await this.connection.send(req);
};
Session.prototype.trySetControlHeight = async function trySetControlHeight(addr, portAddr, scanBoardAddr, controlHeight) {
    const req = createSetControlHeight(addr, portAddr, scanBoardAddr, false, controlHeight);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetControlHeight.js.map