import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createDistribute_SetDistributeMCUVersion(addr, portAddr, distributeAddr, bBroadcast, mcuVersion) {
    const $data = encodeUIntLE(mcuVersion, AddressMapping.Distribute_MCUVersionOccupancy);
    const req = new Request($data, bBroadcast, 'Distribute_SetDistributeMCUVersion');
    req.destination = addr;
    req.deviceType = 3;
    req.port = portAddr;
    req.rcvIndex = distributeAddr;
    req.address = AddressMapping.Distribute_MCUVersionAddr;
    return req;
}
Session.prototype.Distribute_SetDistributeMCUVersion =
    async function Distribute_SetDistributeMCUVersion(addr, portAddr, distributeAddr, bBroadcast, mcuVersion) {
        const req = createDistribute_SetDistributeMCUVersion(addr, portAddr, distributeAddr, bBroadcast, mcuVersion);
        await this.connection.send(req);
    };
Session.prototype.tryDistribute_SetDistributeMCUVersion =
    async function tryDistribute_SetDistributeMCUVersion(addr, portAddr, distributeAddr, mcuVersion) {
        const req = createDistribute_SetDistributeMCUVersion(addr, portAddr, distributeAddr, false, mcuVersion);
        return (await this.connection.trySend(req))?.ack ?? null;
    };
//# sourceMappingURL=Distribute_SetDistributeMCUVersion.js.map