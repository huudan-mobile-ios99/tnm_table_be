import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadNoCorrectionAttenuation(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.NoCorrectionAttenuationOccupancy, 'ReadNoCorrectionAttenuation');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.NoCorrectionAttenuationAddr;
    return req;
}
Session.prototype.ReadNoCorrectionAttenuation = async function ReadNoCorrectionAttenuation(addr, portAddr, scanBoardAddr) {
    const req = createReadNoCorrectionAttenuation(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadNoCorrectionAttenuation = async function tryReadNoCorrectionAttenuation(addr, portAddr, scanBoardAddr) {
    const req = createReadNoCorrectionAttenuation(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadNoCorrectionAttenuation.js.map