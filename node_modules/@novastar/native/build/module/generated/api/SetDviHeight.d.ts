import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetDviHeight(addr: number, bBroadcast: boolean, dvi0Height: number, index: number): Promise<void>;
        trySetDviHeight(addr: number, dvi0Height: number, index: number): Promise<ErrorType | null>;
    }
}
export default function createSetDviHeight<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, dvi0Height: number, index: number): Request<Broadcast>;
