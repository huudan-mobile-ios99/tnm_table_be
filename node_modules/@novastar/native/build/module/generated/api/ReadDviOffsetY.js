import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadDviOffsetY(addr, index) {
    const req = new Request(AddressMapping.DviOffsetYOccupancy, 'ReadDviOffsetY');
    req.destination = addr;
    req.address = AddressMapping.DviOffsetYAddr + AddressMapping.DviOccupancy * index;
    return req;
}
Session.prototype.ReadDviOffsetY = async function ReadDviOffsetY(addr, index) {
    const req = createReadDviOffsetY(addr, index);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadDviOffsetY = async function tryReadDviOffsetY(addr, index) {
    const req = createReadDviOffsetY(addr, index);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadDviOffsetY.js.map