import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadGlobalBrightness(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.GlobalBrightnessOccupancy, 'ReadGlobalBrightness');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.GlobalBrightnessAddr;
    return req;
}
Session.prototype.ReadGlobalBrightness = async function ReadGlobalBrightness(addr, portAddr, scanBoardAddr) {
    const req = createReadGlobalBrightness(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadGlobalBrightness = async function tryReadGlobalBrightness(addr, portAddr, scanBoardAddr) {
    const req = createReadGlobalBrightness(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadGlobalBrightness.js.map