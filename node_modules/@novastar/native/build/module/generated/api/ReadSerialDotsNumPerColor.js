import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSerialDotsNumPerColor(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.SerialDotsNumPerColorOccupancy, 'ReadSerialDotsNumPerColor');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.SerialDotsNumPerColorAddr;
    return req;
}
Session.prototype.ReadSerialDotsNumPerColor = async function ReadSerialDotsNumPerColor(addr, portAddr, scanBoardAddr) {
    const req = createReadSerialDotsNumPerColor(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadSerialDotsNumPerColor = async function tryReadSerialDotsNumPerColor(addr, portAddr, scanBoardAddr) {
    const req = createReadSerialDotsNumPerColor(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSerialDotsNumPerColor.js.map