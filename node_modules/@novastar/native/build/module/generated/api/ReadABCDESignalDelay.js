import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadABCDESignalDelay(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ABCDESignalDelayOccupancy, 'ReadABCDESignalDelay');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ABCDESignalDelayAddr;
    return req;
}
Session.prototype.ReadABCDESignalDelay = async function ReadABCDESignalDelay(addr, portAddr, scanBoardAddr) {
    const req = createReadABCDESignalDelay(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadABCDESignalDelay = async function tryReadABCDESignalDelay(addr, portAddr, scanBoardAddr) {
    const req = createReadABCDESignalDelay(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadABCDESignalDelay.js.map