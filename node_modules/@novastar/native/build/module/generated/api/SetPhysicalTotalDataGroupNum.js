import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetPhysicalTotalDataGroupNum(addr, portAddr, scanBoardAddr, bBroadcast, totalDataGroupNum) {
    const $data = encodeUIntLE(totalDataGroupNum, AddressMapping.PhysicalTotalDataGroupNumOccupancy);
    const req = new Request($data, bBroadcast, 'SetPhysicalTotalDataGroupNum');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.PhysicalTotalDataGroupNumAddr;
    return req;
}
Session.prototype.SetPhysicalTotalDataGroupNum = async function SetPhysicalTotalDataGroupNum(addr, portAddr, scanBoardAddr, bBroadcast, totalDataGroupNum) {
    const req = createSetPhysicalTotalDataGroupNum(addr, portAddr, scanBoardAddr, bBroadcast, totalDataGroupNum);
    await this.connection.send(req);
};
Session.prototype.trySetPhysicalTotalDataGroupNum = async function trySetPhysicalTotalDataGroupNum(addr, portAddr, scanBoardAddr, totalDataGroupNum) {
    const req = createSetPhysicalTotalDataGroupNum(addr, portAddr, scanBoardAddr, false, totalDataGroupNum);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetPhysicalTotalDataGroupNum.js.map