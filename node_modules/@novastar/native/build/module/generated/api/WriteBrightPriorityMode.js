import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createWriteBrightPriorityMode(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    if (data.length !== AddressMapping.BrightPriorityModeOccupancy)
        throw new TypeError(`Invalid buffer size: ${data.length}`);
    const req = new Request(data, bBroadcast, 'WriteBrightPriorityMode');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.BrightPriorityModeAddr;
    return req;
}
Session.prototype.WriteBrightPriorityMode = async function WriteBrightPriorityMode(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createWriteBrightPriorityMode(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.tryWriteBrightPriorityMode = async function tryWriteBrightPriorityMode(addr, portAddr, scanBoardAddr, data) {
    const req = createWriteBrightPriorityMode(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=WriteBrightPriorityMode.js.map