import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetRealPhysicalGroup(addr, portAddr, scanBoardAddr, bBroadcast, realPhysical) {
    const req = new Request([realPhysical], bBroadcast, 'SetRealPhysicalGroup');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.RealPhysicalGroupNumAddr;
    return req;
}
Session.prototype.SetRealPhysicalGroup = async function SetRealPhysicalGroup(addr, portAddr, scanBoardAddr, bBroadcast, realPhysical) {
    const req = createSetRealPhysicalGroup(addr, portAddr, scanBoardAddr, bBroadcast, realPhysical);
    await this.connection.send(req);
};
Session.prototype.trySetRealPhysicalGroup = async function trySetRealPhysicalGroup(addr, portAddr, scanBoardAddr, realPhysical) {
    const req = createSetRealPhysicalGroup(addr, portAddr, scanBoardAddr, false, realPhysical);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetRealPhysicalGroup.js.map