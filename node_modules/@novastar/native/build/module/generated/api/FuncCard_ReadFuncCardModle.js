import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createFuncCard_ReadFuncCardModle(addr) {
    const req = new Request(AddressMapping.FuncCard_ModleOccupancy, 'FuncCard_ReadFuncCardModle');
    req.destination = addr;
    req.address = AddressMapping.FuncCard_ModleAddr;
    return req;
}
Session.prototype.FuncCard_ReadFuncCardModle = async function FuncCard_ReadFuncCardModle(addr) {
    const req = createFuncCard_ReadFuncCardModle(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryFuncCard_ReadFuncCardModle = async function tryFuncCard_ReadFuncCardModle(addr) {
    const req = createFuncCard_ReadFuncCardModle(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=FuncCard_ReadFuncCardModle.js.map