import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadTempInfoOfScanCard(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.Scanner_TempInfoOfScanCardOccupancy, 'ReadTempInfoOfScanCard');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Scanner_TempInfoOfScanCardhAddr;
    return req;
}
Session.prototype.ReadTempInfoOfScanCard = async function ReadTempInfoOfScanCard(addr, portAddr, scanBoardAddr) {
    const req = createReadTempInfoOfScanCard(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadTempInfoOfScanCard = async function tryReadTempInfoOfScanCard(addr, portAddr, scanBoardAddr) {
    const req = createReadTempInfoOfScanCard(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadTempInfoOfScanCard.js.map