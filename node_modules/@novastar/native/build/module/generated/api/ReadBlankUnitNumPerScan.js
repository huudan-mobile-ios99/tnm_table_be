import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadBlankUnitNumPerScan(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.BlankUnitNumPerScanOccupancy, 'ReadBlankUnitNumPerScan');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.BlankUnitNumPerScanAddr;
    return req;
}
Session.prototype.ReadBlankUnitNumPerScan = async function ReadBlankUnitNumPerScan(addr, portAddr, scanBoardAddr) {
    const req = createReadBlankUnitNumPerScan(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadBlankUnitNumPerScan = async function tryReadBlankUnitNumPerScan(addr, portAddr, scanBoardAddr) {
    const req = createReadBlankUnitNumPerScan(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadBlankUnitNumPerScan.js.map