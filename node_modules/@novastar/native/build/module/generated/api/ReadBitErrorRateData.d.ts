/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadBitErrorRateData(addr: number, portAddr: number, scanBoardAddr: number): Promise<Buffer>;
        tryReadBitErrorRateData(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadBitErrorRateData(addr: number, portAddr: number, scanBoardAddr: number): Request;
