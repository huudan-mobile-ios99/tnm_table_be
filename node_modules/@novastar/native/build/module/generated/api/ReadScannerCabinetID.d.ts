/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadScannerCabinetID(addr: number, portAddr: number, scanBoardAddr: number): Promise<Buffer>;
        tryReadScannerCabinetID(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadScannerCabinetID(addr: number, portAddr: number, scanBoardAddr: number): Request;
