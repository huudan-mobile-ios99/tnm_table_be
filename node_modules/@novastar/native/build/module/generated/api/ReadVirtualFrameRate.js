import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadVirtualFrameRate(addr) {
    const req = new Request(AddressMapping.VirtualFrameRateOccupancy, 'ReadVirtualFrameRate');
    req.destination = addr;
    req.address = AddressMapping.VirtualFrameRateAddr;
    return req;
}
Session.prototype.ReadVirtualFrameRate = async function ReadVirtualFrameRate(addr) {
    const req = createReadVirtualFrameRate(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadVirtualFrameRate = async function tryReadVirtualFrameRate(addr) {
    const req = createReadVirtualFrameRate(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadVirtualFrameRate.js.map