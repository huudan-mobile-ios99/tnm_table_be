import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createFuncCard_ReadFuncCardMCULength(addr) {
    const req = new Request(AddressMapping.FuncCard_MCUProgramLengthOccupancy, 'FuncCard_ReadFuncCardMCULength');
    req.destination = addr;
    req.address = AddressMapping.FuncCard_MCUProgramLengthAddr;
    return req;
}
Session.prototype.FuncCard_ReadFuncCardMCULength = async function FuncCard_ReadFuncCardMCULength(addr) {
    const req = createFuncCard_ReadFuncCardMCULength(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryFuncCard_ReadFuncCardMCULength =
    async function tryFuncCard_ReadFuncCardMCULength(addr) {
        const req = createFuncCard_ReadFuncCardMCULength(addr);
        return this.connection.trySend(req);
    };
//# sourceMappingURL=FuncCard_ReadFuncCardMCULength.js.map