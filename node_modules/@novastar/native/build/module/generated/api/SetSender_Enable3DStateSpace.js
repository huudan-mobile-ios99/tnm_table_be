import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetSender_Enable3DStateSpace(addr, bBroadcast, enable3DState) {
    const req = new Request([enable3DState], bBroadcast, 'SetSender_Enable3DStateSpace');
    req.destination = addr;
    req.address = AddressMapping.Enable3DAddr;
    return req;
}
Session.prototype.SetSender_Enable3DStateSpace = async function SetSender_Enable3DStateSpace(addr, bBroadcast, enable3DState) {
    const req = createSetSender_Enable3DStateSpace(addr, bBroadcast, enable3DState);
    await this.connection.send(req);
};
Session.prototype.trySetSender_Enable3DStateSpace = async function trySetSender_Enable3DStateSpace(addr, enable3DState) {
    const req = createSetSender_Enable3DStateSpace(addr, false, enable3DState);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetSender_Enable3DStateSpace.js.map