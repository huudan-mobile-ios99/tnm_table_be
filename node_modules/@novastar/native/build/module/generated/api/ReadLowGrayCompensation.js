import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadLowGrayCompensation(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.LowGrayCompensationOccupancy, 'ReadLowGrayCompensation');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.LowGrayCompensationAddr;
    return req;
}
Session.prototype.ReadLowGrayCompensation = async function ReadLowGrayCompensation(addr, portAddr, scanBoardAddr) {
    const req = createReadLowGrayCompensation(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadLowGrayCompensation = async function tryReadLowGrayCompensation(addr, portAddr, scanBoardAddr) {
    const req = createReadLowGrayCompensation(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadLowGrayCompensation.js.map