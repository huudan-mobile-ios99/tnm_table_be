import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetRestoreFinishFlag(addr, bBroadcast, writeData) {
    const req = new Request([writeData], bBroadcast, 'SetRestoreFinishFlag');
    req.destination = addr;
    req.address = AddressMapping.RestoreFinishFlagAddr;
    return req;
}
Session.prototype.SetRestoreFinishFlag = async function SetRestoreFinishFlag(addr, bBroadcast, writeData) {
    const req = createSetRestoreFinishFlag(addr, bBroadcast, writeData);
    await this.connection.send(req);
};
Session.prototype.trySetRestoreFinishFlag = async function trySetRestoreFinishFlag(addr, writeData) {
    const req = createSetRestoreFinishFlag(addr, false, writeData);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetRestoreFinishFlag.js.map