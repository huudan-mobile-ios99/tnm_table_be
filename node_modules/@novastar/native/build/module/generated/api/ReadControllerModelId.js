import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadControllerModelId(addr) {
    const req = new Request(AddressMapping.ControllerModelIdOccupancy, 'ReadControllerModelId');
    req.destination = addr;
    req.address = AddressMapping.ControllerModelIdAddr;
    return req;
}
Session.prototype.ReadControllerModelId = async function ReadControllerModelId(addr) {
    const req = createReadControllerModelId(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadControllerModelId = async function tryReadControllerModelId(addr) {
    const req = createReadControllerModelId(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadControllerModelId.js.map