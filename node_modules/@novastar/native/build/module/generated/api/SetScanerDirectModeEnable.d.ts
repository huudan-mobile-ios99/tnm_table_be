import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetScanerDirectModeEnable(addr: number, portAddr: number, scanBoardAddr: number, broadcast: boolean, enable: boolean): Promise<void>;
        trySetScanerDirectModeEnable(addr: number, portAddr: number, scanBoardAddr: number, enable: boolean): Promise<ErrorType | null>;
    }
}
export default function createSetScanerDirectModeEnable<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, broadcast: Broadcast, enable: boolean): Request<Broadcast>;
