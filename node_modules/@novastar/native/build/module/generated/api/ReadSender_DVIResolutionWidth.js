import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSender_DVIResolutionWidth(addr) {
    const req = new Request(AddressMapping.Sender_DVIWidthOccupancy, 'ReadSender_DVIResolutionWidth');
    req.destination = addr;
    req.address = AddressMapping.Sender_DVIWidthAddr;
    return req;
}
Session.prototype.ReadSender_DVIResolutionWidth = async function ReadSender_DVIResolutionWidth(addr) {
    const req = createReadSender_DVIResolutionWidth(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadSender_DVIResolutionWidth =
    async function tryReadSender_DVIResolutionWidth(addr) {
        const req = createReadSender_DVIResolutionWidth(addr);
        return this.connection.trySend(req);
    };
//# sourceMappingURL=ReadSender_DVIResolutionWidth.js.map