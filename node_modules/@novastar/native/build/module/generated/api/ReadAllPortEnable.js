import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadAllPortEnable(addr) {
    const req = new Request(AddressMapping.Sender_AllDataSpaceOccupancy, 'ReadAllPortEnable');
    req.destination = addr;
    req.address = AddressMapping.Sender_AllDataAddr;
    return req;
}
Session.prototype.ReadAllPortEnable = async function ReadAllPortEnable(addr) {
    const req = createReadAllPortEnable(addr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadAllPortEnable = async function tryReadAllPortEnable(addr) {
    const req = createReadAllPortEnable(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadAllPortEnable.js.map