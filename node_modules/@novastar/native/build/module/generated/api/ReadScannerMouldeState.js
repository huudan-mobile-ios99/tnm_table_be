import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScannerMouldeState(addr, portAddr, scanBoardAddr, readLength) {
    const req = new Request(readLength, 'ReadScannerMouldeState');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.SUM6060ReadMouldeStateAddr;
    return req;
}
Session.prototype.ReadScannerMouldeState = async function ReadScannerMouldeState(addr, portAddr, scanBoardAddr, readLength) {
    const req = createReadScannerMouldeState(addr, portAddr, scanBoardAddr, readLength);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadScannerMouldeState = async function tryReadScannerMouldeState(addr, portAddr, scanBoardAddr, readLength) {
    const req = createReadScannerMouldeState(addr, portAddr, scanBoardAddr, readLength);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScannerMouldeState.js.map