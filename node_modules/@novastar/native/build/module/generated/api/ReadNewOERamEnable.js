import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadNewOERamEnable(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.NewOERamEnableOccupancy, 'ReadNewOERamEnable');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.NewOERamEnableAddr;
    return req;
}
Session.prototype.ReadNewOERamEnable = async function ReadNewOERamEnable(addr, portAddr, scanBoardAddr) {
    const req = createReadNewOERamEnable(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadNewOERamEnable = async function tryReadNewOERamEnable(addr, portAddr, scanBoardAddr) {
    const req = createReadNewOERamEnable(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadNewOERamEnable.js.map