import { ErrorType, Request } from '@novastar/codec';
import { GrayModeTypeEnum } from '../GrayModeType';
import { GrayRealizeTypeEnum } from '../GrayRealizeType';
declare module '@novastar/codec' {
    interface API {
        SetGrayMode(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, grayRealize: GrayRealizeTypeEnum, grayMode: GrayModeTypeEnum): Promise<void>;
        trySetGrayMode(addr: number, portAddr: number, scanBoardAddr: number, grayRealize: GrayRealizeTypeEnum, grayMode: GrayModeTypeEnum): Promise<ErrorType | null>;
    }
}
export default function createSetGrayMode<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, grayRealize: GrayRealizeTypeEnum, grayMode: GrayModeTypeEnum): Request<Broadcast>;
