import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadPointNumPerDrive(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.PointNumPerDriveOccupancy, 'ReadPointNumPerDrive');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.PointNumPerDriveAddr;
    return req;
}
Session.prototype.ReadPointNumPerDrive = async function ReadPointNumPerDrive(addr, portAddr, scanBoardAddr) {
    const req = createReadPointNumPerDrive(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadPointNumPerDrive = async function tryReadPointNumPerDrive(addr, portAddr, scanBoardAddr) {
    const req = createReadPointNumPerDrive(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadPointNumPerDrive.js.map