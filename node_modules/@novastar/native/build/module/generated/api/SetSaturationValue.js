import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetSaturationValue(addr, bBroadcast, dht) {
    const $data = encodeUIntLE(dht, AddressMapping.SenderSaturationResultOccupancy);
    const req = new Request($data, bBroadcast, 'SetSaturationValue');
    req.destination = addr;
    req.address = AddressMapping.SenderSaturationResultAddr;
    return req;
}
Session.prototype.SetSaturationValue = async function SetSaturationValue(addr, bBroadcast, dht) {
    const req = createSetSaturationValue(addr, bBroadcast, dht);
    await this.connection.send(req);
};
Session.prototype.trySetSaturationValue = async function trySetSaturationValue(addr, dht) {
    const req = createSetSaturationValue(addr, false, dht);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetSaturationValue.js.map