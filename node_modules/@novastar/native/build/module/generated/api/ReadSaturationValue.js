import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSaturationValue(addr) {
    const req = new Request(AddressMapping.SenderSaturationResultOccupancy, 'ReadSaturationValue');
    req.destination = addr;
    req.address = AddressMapping.SenderSaturationResultAddr;
    return req;
}
Session.prototype.ReadSaturationValue = async function ReadSaturationValue(addr) {
    const req = createReadSaturationValue(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadSaturationValue = async function tryReadSaturationValue(addr) {
    const req = createReadSaturationValue(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSaturationValue.js.map