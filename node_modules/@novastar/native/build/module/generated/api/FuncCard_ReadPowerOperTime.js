import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createFuncCard_ReadPowerOperTime(addr) {
    const req = new Request(AddressMapping.FuncCard_OperPowerOccupancy, 'FuncCard_ReadPowerOperTime');
    req.destination = addr;
    req.address = AddressMapping.FuncCard_PowerOperTimeAddr;
    return req;
}
Session.prototype.FuncCard_ReadPowerOperTime = async function FuncCard_ReadPowerOperTime(addr) {
    const req = createFuncCard_ReadPowerOperTime(addr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryFuncCard_ReadPowerOperTime = async function tryFuncCard_ReadPowerOperTime(addr) {
    const req = createFuncCard_ReadPowerOperTime(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=FuncCard_ReadPowerOperTime.js.map