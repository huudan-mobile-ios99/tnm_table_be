import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadMagnitudes(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.MagnitudesAddrOccupancy, 'ReadMagnitudes');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.MagnitudesAddr;
    return req;
}
Session.prototype.ReadMagnitudes = async function ReadMagnitudes(addr, portAddr, scanBoardAddr) {
    const req = createReadMagnitudes(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadMagnitudes = async function tryReadMagnitudes(addr, portAddr, scanBoardAddr) {
    const req = createReadMagnitudes(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadMagnitudes.js.map