import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetDclkHigh(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, dclkHigh: number): Promise<void>;
        trySetDclkHigh(addr: number, portAddr: number, scanBoardAddr: number, dclkHigh: number): Promise<ErrorType | null>;
    }
}
export default function createSetDclkHigh<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, dclkHigh: number): Request<Broadcast>;
