import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetDehumidAdjustTime(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    if (data.length !== AddressMapping.DehumidAdjustTimeOccupancy)
        throw new TypeError(`Invalid buffer size: ${data.length}`);
    const req = new Request(data, bBroadcast, 'SetDehumidAdjustTime');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.DehumidAdjustTimeAddr;
    return req;
}
Session.prototype.SetDehumidAdjustTime = async function SetDehumidAdjustTime(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createSetDehumidAdjustTime(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.trySetDehumidAdjustTime = async function trySetDehumidAdjustTime(addr, portAddr, scanBoardAddr, data) {
    const req = createSetDehumidAdjustTime(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetDehumidAdjustTime.js.map