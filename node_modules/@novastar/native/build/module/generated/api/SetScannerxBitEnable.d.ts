import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetScannerxBitEnable(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, da: number): Promise<void>;
        trySetScannerxBitEnable(addr: number, portAddr: number, scanBoardAddr: number, da: number): Promise<ErrorType | null>;
    }
}
export default function createSetScannerxBitEnable<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, da: number): Request<Broadcast>;
