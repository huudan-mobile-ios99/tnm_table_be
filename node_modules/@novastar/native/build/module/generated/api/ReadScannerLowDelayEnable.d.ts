import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadScannerLowDelayEnable(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadScannerLowDelayEnable(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadScannerLowDelayEnable(addr: number, portAddr: number, scanBoardAddr: number): Request;
