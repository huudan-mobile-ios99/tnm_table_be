import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadModuelCols(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ModuleColsOccupancy, 'ReadModuelCols');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ModuleColsAddr;
    return req;
}
Session.prototype.ReadModuelCols = async function ReadModuelCols(addr, portAddr, scanBoardAddr) {
    const req = createReadModuelCols(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadModuelCols = async function tryReadModuelCols(addr, portAddr, scanBoardAddr) {
    const req = createReadModuelCols(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadModuelCols.js.map