import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadTemperature(addr) {
    const req = new Request(AddressMapping.TemperatureOccupancy, 'ReadTemperature');
    req.destination = addr;
    req.address = AddressMapping.TemperatureAddr;
    return req;
}
Session.prototype.ReadTemperature = async function ReadTemperature(addr) {
    const req = createReadTemperature(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadTemperature = async function tryReadTemperature(addr) {
    const req = createReadTemperature(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadTemperature.js.map