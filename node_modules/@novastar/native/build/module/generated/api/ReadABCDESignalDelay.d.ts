import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadABCDESignalDelay(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadABCDESignalDelay(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadABCDESignalDelay(addr: number, portAddr: number, scanBoardAddr: number): Request;
