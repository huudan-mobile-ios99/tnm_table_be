import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadRefNumPerVs(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.RefNumPerVsOccupancy, 'ReadRefNumPerVs');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.RefNumPerVsAddr;
    return req;
}
Session.prototype.ReadRefNumPerVs = async function ReadRefNumPerVs(addr, portAddr, scanBoardAddr) {
    const req = createReadRefNumPerVs(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadRefNumPerVs = async function tryReadRefNumPerVs(addr, portAddr, scanBoardAddr) {
    const req = createReadRefNumPerVs(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadRefNumPerVs.js.map