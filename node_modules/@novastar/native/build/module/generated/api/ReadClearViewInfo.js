import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadClearViewInfo(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ScannerClearViewOccupancy, 'ReadClearViewInfo');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ScannerClearViewAddr;
    return req;
}
Session.prototype.ReadClearViewInfo = async function ReadClearViewInfo(addr, portAddr, scanBoardAddr) {
    const req = createReadClearViewInfo(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadClearViewInfo = async function tryReadClearViewInfo(addr, portAddr, scanBoardAddr) {
    const req = createReadClearViewInfo(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadClearViewInfo.js.map