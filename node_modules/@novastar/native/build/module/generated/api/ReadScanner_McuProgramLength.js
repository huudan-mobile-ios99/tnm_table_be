import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScanner_McuProgramLength(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.Scanner_McuProgramLengthOccupancy, 'ReadScanner_McuProgramLength');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Scanner_McuProgramLengthAddr;
    return req;
}
Session.prototype.ReadScanner_McuProgramLength = async function ReadScanner_McuProgramLength(addr, portAddr, scanBoardAddr) {
    const req = createReadScanner_McuProgramLength(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadScanner_McuProgramLength = async function tryReadScanner_McuProgramLength(addr, portAddr, scanBoardAddr) {
    const req = createReadScanner_McuProgramLength(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScanner_McuProgramLength.js.map