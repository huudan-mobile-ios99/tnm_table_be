import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScanBoardDataTo2500(addr, portAddr, scanBoardAddr, length) {
    const req = new Request(length, 'ReadScanBoardDataTo2500');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.SDKNewReadOrWriteAddr;
    return req;
}
Session.prototype.ReadScanBoardDataTo2500 = async function ReadScanBoardDataTo2500(addr, portAddr, scanBoardAddr, length) {
    const req = createReadScanBoardDataTo2500(addr, portAddr, scanBoardAddr, length);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadScanBoardDataTo2500 = async function tryReadScanBoardDataTo2500(addr, portAddr, scanBoardAddr, length) {
    const req = createReadScanBoardDataTo2500(addr, portAddr, scanBoardAddr, length);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScanBoardDataTo2500.js.map