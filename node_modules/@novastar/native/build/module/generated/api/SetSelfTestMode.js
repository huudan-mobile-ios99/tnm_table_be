import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetSelfTestMode(addr, portAddr, scanBoardAddr, bBroadcast, selfTestMode) {
    const $data = encodeUIntLE(selfTestMode, AddressMapping.SelfTestModeOccupancy);
    const req = new Request($data, bBroadcast, 'SetSelfTestMode');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.SelfTestModeAddr;
    return req;
}
Session.prototype.SetSelfTestMode = async function SetSelfTestMode(addr, portAddr, scanBoardAddr, bBroadcast, selfTestMode) {
    const req = createSetSelfTestMode(addr, portAddr, scanBoardAddr, bBroadcast, selfTestMode);
    await this.connection.send(req);
};
Session.prototype.trySetSelfTestMode = async function trySetSelfTestMode(addr, portAddr, scanBoardAddr, selfTestMode) {
    const req = createSetSelfTestMode(addr, portAddr, scanBoardAddr, false, selfTestMode);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetSelfTestMode.js.map