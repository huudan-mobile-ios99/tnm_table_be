import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetClearViewEnableInfo(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, enable: boolean): Promise<void>;
        trySetClearViewEnableInfo(addr: number, portAddr: number, scanBoardAddr: number, enable: boolean): Promise<ErrorType | null>;
    }
}
export default function createSetClearViewEnableInfo<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, enable: boolean): Request<Broadcast>;
