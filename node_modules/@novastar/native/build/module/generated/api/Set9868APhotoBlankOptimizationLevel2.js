import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSet9868APhotoBlankOptimizationLevel2(addr, portAddr, scanBoardAddr, bBroadcast, blankLevelValue) {
    const req = new Request([blankLevelValue], bBroadcast, 'Set9868APhotoBlankOptimizationLevel2');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Scanner_9868APhotoBlankOptimizationLevelAddr2;
    return req;
}
Session.prototype.Set9868APhotoBlankOptimizationLevel2 =
    async function Set9868APhotoBlankOptimizationLevel2(addr, portAddr, scanBoardAddr, bBroadcast, blankLevelValue) {
        const req = createSet9868APhotoBlankOptimizationLevel2(addr, portAddr, scanBoardAddr, bBroadcast, blankLevelValue);
        await this.connection.send(req);
    };
Session.prototype.trySet9868APhotoBlankOptimizationLevel2 =
    async function trySet9868APhotoBlankOptimizationLevel2(addr, portAddr, scanBoardAddr, blankLevelValue) {
        const req = createSet9868APhotoBlankOptimizationLevel2(addr, portAddr, scanBoardAddr, false, blankLevelValue);
        return (await this.connection.trySend(req))?.ack ?? null;
    };
//# sourceMappingURL=Set9868APhotoBlankOptimizationLevel2.js.map