import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadCabinetSerialNum(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.CabinetSerialNumOccupancy, 'ReadCabinetSerialNum');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.CabinetSerialNumAddr;
    return req;
}
Session.prototype.ReadCabinetSerialNum = async function ReadCabinetSerialNum(addr, portAddr, scanBoardAddr) {
    const req = createReadCabinetSerialNum(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadCabinetSerialNum = async function tryReadCabinetSerialNum(addr, portAddr, scanBoardAddr) {
    const req = createReadCabinetSerialNum(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadCabinetSerialNum.js.map