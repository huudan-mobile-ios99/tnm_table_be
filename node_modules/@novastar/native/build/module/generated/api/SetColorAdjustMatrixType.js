import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetColorAdjustMatrixType(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = new Request(data, bBroadcast, 'SetColorAdjustMatrixType');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ColorAdjustMatrixTypeAddr;
    return req;
}
Session.prototype.SetColorAdjustMatrixType = async function SetColorAdjustMatrixType(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createSetColorAdjustMatrixType(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.trySetColorAdjustMatrixType = async function trySetColorAdjustMatrixType(addr, portAddr, scanBoardAddr, data) {
    const req = createSetColorAdjustMatrixType(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetColorAdjustMatrixType.js.map