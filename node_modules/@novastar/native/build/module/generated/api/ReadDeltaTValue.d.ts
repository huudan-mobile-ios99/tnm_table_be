import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadDeltaTValue(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadDeltaTValue(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadDeltaTValue(addr: number, portAddr: number, scanBoardAddr: number): Request;
