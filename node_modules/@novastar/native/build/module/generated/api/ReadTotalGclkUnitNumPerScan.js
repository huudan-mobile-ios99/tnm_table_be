import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadTotalGclkUnitNumPerScan(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.TotalGclkUnitNumPerScanOccupancy, 'ReadTotalGclkUnitNumPerScan');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.TotalGclkUnitNumPerScanAddr;
    return req;
}
Session.prototype.ReadTotalGclkUnitNumPerScan = async function ReadTotalGclkUnitNumPerScan(addr, portAddr, scanBoardAddr) {
    const req = createReadTotalGclkUnitNumPerScan(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadTotalGclkUnitNumPerScan = async function tryReadTotalGclkUnitNumPerScan(addr, portAddr, scanBoardAddr) {
    const req = createReadTotalGclkUnitNumPerScan(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadTotalGclkUnitNumPerScan.js.map