import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadTVCardDVI0Height(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.TVCardDVI0HeightOccupancy, 'ReadTVCardDVI0Height');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.TVCardDVI0HeightAddr;
    return req;
}
Session.prototype.ReadTVCardDVI0Height = async function ReadTVCardDVI0Height(addr, portAddr, scanBoardAddr) {
    const req = createReadTVCardDVI0Height(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadTVCardDVI0Height = async function tryReadTVCardDVI0Height(addr, portAddr, scanBoardAddr) {
    const req = createReadTVCardDVI0Height(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadTVCardDVI0Height.js.map