import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadAllModuelStatus(addr, portAddr, scanBoardAddr, readLength) {
    const req = new Request(readLength, 'ReadAllModuelStatus');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Scanner_AllMonitorModuelDataAddr;
    return req;
}
Session.prototype.ReadAllModuelStatus = async function ReadAllModuelStatus(addr, portAddr, scanBoardAddr, readLength) {
    const req = createReadAllModuelStatus(addr, portAddr, scanBoardAddr, readLength);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadAllModuelStatus = async function tryReadAllModuelStatus(addr, portAddr, scanBoardAddr, readLength) {
    const req = createReadAllModuelStatus(addr, portAddr, scanBoardAddr, readLength);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadAllModuelStatus.js.map