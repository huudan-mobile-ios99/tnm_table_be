import { ErrorType, Request } from '@novastar/codec';
import { HLGModelEnum } from '../HLGModel';
declare module '@novastar/codec' {
    interface API {
        SetSender_HLGModel(addr: number, bBroadcast: boolean, hLGModel: HLGModelEnum): Promise<void>;
        trySetSender_HLGModel(addr: number, hLGModel: HLGModelEnum): Promise<ErrorType | null>;
    }
}
export default function createSetSender_HLGModel<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, hLGModel: HLGModelEnum): Request<Broadcast>;
