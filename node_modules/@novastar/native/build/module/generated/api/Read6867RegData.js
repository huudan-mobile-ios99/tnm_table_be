import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createRead6867RegData(addr, portAddr, scanBoardAddr, dataLength) {
    const req = new Request(dataLength, 'Read6867RegData');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Register6867RegAddress;
    return req;
}
Session.prototype.Read6867RegData = async function Read6867RegData(addr, portAddr, scanBoardAddr, dataLength) {
    const req = createRead6867RegData(addr, portAddr, scanBoardAddr, dataLength);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryRead6867RegData = async function tryRead6867RegData(addr, portAddr, scanBoardAddr, dataLength) {
    const req = createRead6867RegData(addr, portAddr, scanBoardAddr, dataLength);
    return this.connection.trySend(req);
};
//# sourceMappingURL=Read6867RegData.js.map