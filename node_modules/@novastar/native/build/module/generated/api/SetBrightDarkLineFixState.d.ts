import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetBrightDarkLineFixState(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, stateByte: number): Promise<void>;
        trySetBrightDarkLineFixState(addr: number, portAddr: number, scanBoardAddr: number, stateByte: number): Promise<ErrorType | null>;
    }
}
export default function createSetBrightDarkLineFixState<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, stateByte: number): Request<Broadcast>;
