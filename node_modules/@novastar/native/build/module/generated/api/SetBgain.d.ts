import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetBgain(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, bgain: number): Promise<void>;
        trySetBgain(addr: number, portAddr: number, scanBoardAddr: number, bgain: number): Promise<ErrorType | null>;
    }
}
export default function createSetBgain<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, bgain: number): Request<Broadcast>;
