import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetIsSerdes(addr, bBroadcast, IsSerdes) {
    const req = new Request(IsSerdes ? [1] : [0], bBroadcast, 'SetIsSerdes');
    req.destination = addr;
    req.address = AddressMapping.SerdesEnableAddr;
    return req;
}
Session.prototype.SetIsSerdes = async function SetIsSerdes(addr, bBroadcast, IsSerdes) {
    const req = createSetIsSerdes(addr, bBroadcast, IsSerdes);
    await this.connection.send(req);
};
Session.prototype.trySetIsSerdes = async function trySetIsSerdes(addr, IsSerdes) {
    const req = createSetIsSerdes(addr, false, IsSerdes);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetIsSerdes.js.map