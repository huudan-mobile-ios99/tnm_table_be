import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetColorAdjustEnable(addr, portAddr, scanBoardAddr, bBroadcast, isEnable) {
    const req = new Request(isEnable ? [2] : [255], bBroadcast, 'SetColorAdjustEnable');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ColorAdjustEnableAddr;
    return req;
}
Session.prototype.SetColorAdjustEnable = async function SetColorAdjustEnable(addr, portAddr, scanBoardAddr, bBroadcast, isEnable) {
    const req = createSetColorAdjustEnable(addr, portAddr, scanBoardAddr, bBroadcast, isEnable);
    await this.connection.send(req);
};
Session.prototype.trySetColorAdjustEnable = async function trySetColorAdjustEnable(addr, portAddr, scanBoardAddr, isEnable) {
    const req = createSetColorAdjustEnable(addr, portAddr, scanBoardAddr, false, isEnable);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetColorAdjustEnable.js.map