import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetSender_SetHDRType(addr, bBroadcast, hDRType) {
    const $data = encodeUIntLE(hDRType, AddressMapping.HDREnableInfoOccupancy);
    const req = new Request($data, bBroadcast, 'SetSender_SetHDRType');
    req.destination = addr;
    req.address = AddressMapping.HDRInfoAddr;
    return req;
}
Session.prototype.SetSender_SetHDRType = async function SetSender_SetHDRType(addr, bBroadcast, hDRType) {
    const req = createSetSender_SetHDRType(addr, bBroadcast, hDRType);
    await this.connection.send(req);
};
Session.prototype.trySetSender_SetHDRType = async function trySetSender_SetHDRType(addr, hDRType) {
    const req = createSetSender_SetHDRType(addr, false, hDRType);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetSender_SetHDRType.js.map