import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSender_DVICols(addr) {
    const req = new Request(AddressMapping.Sender_DVIColsOccupancy, 'ReadSender_DVICols');
    req.destination = addr;
    req.address = AddressMapping.Sender_DVIColsAddr;
    return req;
}
Session.prototype.ReadSender_DVICols = async function ReadSender_DVICols(addr) {
    const req = createReadSender_DVICols(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadSender_DVICols = async function tryReadSender_DVICols(addr) {
    const req = createReadSender_DVICols(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSender_DVICols.js.map