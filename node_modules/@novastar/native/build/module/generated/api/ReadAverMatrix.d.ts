/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadAverMatrix(addr: number, portAddr: number, scanBoardAddr: number): Promise<Buffer>;
        tryReadAverMatrix(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadAverMatrix(addr: number, portAddr: number, scanBoardAddr: number): Request;
