import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadDecodeType(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.DecodeTypeOccupancy, 'ReadDecodeType');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.DecodeTypeAddr;
    return req;
}
Session.prototype.ReadDecodeType = async function ReadDecodeType(addr, portAddr, scanBoardAddr) {
    const req = createReadDecodeType(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadDecodeType = async function tryReadDecodeType(addr, portAddr, scanBoardAddr) {
    const req = createReadDecodeType(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadDecodeType.js.map