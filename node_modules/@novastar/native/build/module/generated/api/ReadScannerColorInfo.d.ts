import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadScannerColorInfo(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadScannerColorInfo(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadScannerColorInfo(addr: number, portAddr: number, scanBoardAddr: number): Request;
