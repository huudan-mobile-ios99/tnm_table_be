import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetAudioControl(addr, bBroadcast, audioControl) {
    const $data = encodeUIntLE(audioControl, AddressMapping.AudioControlOccupancy);
    const req = new Request($data, bBroadcast, 'SetAudioControl');
    req.destination = addr;
    req.address = AddressMapping.AudioControlAddr;
    return req;
}
Session.prototype.SetAudioControl = async function SetAudioControl(addr, bBroadcast, audioControl) {
    const req = createSetAudioControl(addr, bBroadcast, audioControl);
    await this.connection.send(req);
};
Session.prototype.trySetAudioControl = async function trySetAudioControl(addr, audioControl) {
    const req = createSetAudioControl(addr, false, audioControl);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetAudioControl.js.map