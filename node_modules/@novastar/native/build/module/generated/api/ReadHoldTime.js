import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadHoldTime(addr) {
    const req = new Request(AddressMapping.HoldTimeOccupancy, 'ReadHoldTime');
    req.destination = addr;
    req.address = AddressMapping.HoldTimeAddr;
    return req;
}
Session.prototype.ReadHoldTime = async function ReadHoldTime(addr) {
    const req = createReadHoldTime(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadHoldTime = async function tryReadHoldTime(addr) {
    const req = createReadHoldTime(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadHoldTime.js.map