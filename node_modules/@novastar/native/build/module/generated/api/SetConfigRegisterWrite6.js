import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetConfigRegisterWrite6(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    if (data.length !== AddressMapping.Config3RegisterOccupancy)
        throw new TypeError(`Invalid buffer size: ${data.length}`);
    const req = new Request(data, bBroadcast, 'SetConfigRegisterWrite6');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ConfigRegisterWriteAddr6;
    return req;
}
Session.prototype.SetConfigRegisterWrite6 = async function SetConfigRegisterWrite6(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createSetConfigRegisterWrite6(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.trySetConfigRegisterWrite6 = async function trySetConfigRegisterWrite6(addr, portAddr, scanBoardAddr, data) {
    const req = createSetConfigRegisterWrite6(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetConfigRegisterWrite6.js.map