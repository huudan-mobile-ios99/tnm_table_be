import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadSymmetricalOutputMode(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadSymmetricalOutputMode(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadSymmetricalOutputMode(addr: number, portAddr: number, scanBoardAddr: number): Request;
