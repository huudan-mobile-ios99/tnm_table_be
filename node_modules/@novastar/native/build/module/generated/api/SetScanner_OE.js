import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetScanner_OE(addr, portAddr, scanBoardAddr, bBroadcast, DMThird) {
    if (DMThird.length !== 0)
        throw new TypeError(`Invalid buffer size: ${DMThird.length}`);
    const req = new Request(DMThird, bBroadcast, 'SetScanner_OE');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Scanner_OEAAddr;
    return req;
}
Session.prototype.SetScanner_OE = async function SetScanner_OE(addr, portAddr, scanBoardAddr, bBroadcast, DMThird) {
    const req = createSetScanner_OE(addr, portAddr, scanBoardAddr, bBroadcast, DMThird);
    await this.connection.send(req);
};
Session.prototype.trySetScanner_OE = async function trySetScanner_OE(addr, portAddr, scanBoardAddr, DMThird) {
    const req = createSetScanner_OE(addr, portAddr, scanBoardAddr, false, DMThird);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetScanner_OE.js.map