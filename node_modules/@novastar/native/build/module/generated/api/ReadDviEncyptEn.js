import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadDviEncyptEn(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.DviEncyptEnOccupancy, 'ReadDviEncyptEn');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.DviEncyptEnAddr;
    return req;
}
Session.prototype.ReadDviEncyptEn = async function ReadDviEncyptEn(addr, portAddr, scanBoardAddr) {
    const req = createReadDviEncyptEn(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadDviEncyptEn = async function tryReadDviEncyptEn(addr, portAddr, scanBoardAddr) {
    const req = createReadDviEncyptEn(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadDviEncyptEn.js.map