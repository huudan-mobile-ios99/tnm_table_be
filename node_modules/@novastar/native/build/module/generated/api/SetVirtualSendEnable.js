import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetVirtualSendEnable(addr, bBroadcast, virtualSendEnable) {
    const $data = encodeUIntLE(virtualSendEnable ? 5 : 0, AddressMapping.VirtualSendEnableOccupancy);
    const req = new Request($data, bBroadcast, 'SetVirtualSendEnable');
    req.destination = addr;
    req.address = AddressMapping.VirtualSendEnableAddr;
    return req;
}
Session.prototype.SetVirtualSendEnable = async function SetVirtualSendEnable(addr, bBroadcast, virtualSendEnable) {
    const req = createSetVirtualSendEnable(addr, bBroadcast, virtualSendEnable);
    await this.connection.send(req);
};
Session.prototype.trySetVirtualSendEnable = async function trySetVirtualSendEnable(addr, virtualSendEnable) {
    const req = createSetVirtualSendEnable(addr, false, virtualSendEnable);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetVirtualSendEnable.js.map