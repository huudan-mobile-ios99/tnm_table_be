import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetMoudleNumber(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, MoudleNumber: number): Promise<void>;
        trySetMoudleNumber(addr: number, portAddr: number, scanBoardAddr: number, MoudleNumber: number): Promise<ErrorType | null>;
    }
}
export default function createSetMoudleNumber<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, MoudleNumber: number): Request<Broadcast>;
