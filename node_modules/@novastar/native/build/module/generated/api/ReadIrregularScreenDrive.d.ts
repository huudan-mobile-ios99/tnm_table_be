import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadIrregularScreenDrive(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadIrregularScreenDrive(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadIrregularScreenDrive(addr: number, portAddr: number, scanBoardAddr: number): Request;
