import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadDoubleCardSpace(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.DoubleModelCardSpaceCardSpaceOccupancy, 'ReadDoubleCardSpace');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.DoubleModelCardSpaceCardSpace;
    return req;
}
Session.prototype.ReadDoubleCardSpace = async function ReadDoubleCardSpace(addr, portAddr, scanBoardAddr) {
    const req = createReadDoubleCardSpace(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadDoubleCardSpace = async function tryReadDoubleCardSpace(addr, portAddr, scanBoardAddr) {
    const req = createReadDoubleCardSpace(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadDoubleCardSpace.js.map