import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadABCDRollOver(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadABCDRollOver(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadABCDRollOver(addr: number, portAddr: number, scanBoardAddr: number): Request;
