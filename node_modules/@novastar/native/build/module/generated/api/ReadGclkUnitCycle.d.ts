import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadGclkUnitCycle(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadGclkUnitCycle(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadGclkUnitCycle(addr: number, portAddr: number, scanBoardAddr: number): Request;
