import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetHDEnableEx(addr: number, bBroadcast: boolean, hdEnable: number): Promise<void>;
        trySetHDEnableEx(addr: number, hdEnable: number): Promise<ErrorType | null>;
    }
}
export default function createSetHDEnableEx<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, hdEnable: number): Request<Broadcast>;
