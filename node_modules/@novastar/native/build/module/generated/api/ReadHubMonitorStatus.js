import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadHubMonitorStatus(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.Scanner_HubMonitorDataOccupancy, 'ReadHubMonitorStatus');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Scanner_HubMonitorAllDataAddr;
    return req;
}
Session.prototype.ReadHubMonitorStatus = async function ReadHubMonitorStatus(addr, portAddr, scanBoardAddr) {
    const req = createReadHubMonitorStatus(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadHubMonitorStatus = async function tryReadHubMonitorStatus(addr, portAddr, scanBoardAddr) {
    const req = createReadHubMonitorStatus(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadHubMonitorStatus.js.map