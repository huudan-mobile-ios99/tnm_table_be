import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetDviOffsetY(addr: number, bBroadcast: boolean, dviOffsetY: number, index: number): Promise<void>;
        trySetDviOffsetY(addr: number, dviOffsetY: number, index: number): Promise<ErrorType | null>;
    }
}
export default function createSetDviOffsetY<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, dviOffsetY: number, index: number): Request<Broadcast>;
