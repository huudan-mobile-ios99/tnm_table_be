import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSymmetricalOutputMode(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.SymmetricalOutputModeOccupancy, 'ReadSymmetricalOutputMode');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.SymmetricalOutputModeAddr;
    return req;
}
Session.prototype.ReadSymmetricalOutputMode = async function ReadSymmetricalOutputMode(addr, portAddr, scanBoardAddr) {
    const req = createReadSymmetricalOutputMode(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadSymmetricalOutputMode = async function tryReadSymmetricalOutputMode(addr, portAddr, scanBoardAddr) {
    const req = createReadSymmetricalOutputMode(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSymmetricalOutputMode.js.map