import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadAddrExtend(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.AddrExtendOccupancy, 'ReadAddrExtend');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.AddrExtendAddr;
    return req;
}
Session.prototype.ReadAddrExtend = async function ReadAddrExtend(addr, portAddr, scanBoardAddr) {
    const req = createReadAddrExtend(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadAddrExtend = async function tryReadAddrExtend(addr, portAddr, scanBoardAddr) {
    const req = createReadAddrExtend(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadAddrExtend.js.map