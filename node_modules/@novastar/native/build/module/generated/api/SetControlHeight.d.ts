import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetControlHeight(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, controlHeight: number): Promise<void>;
        trySetControlHeight(addr: number, portAddr: number, scanBoardAddr: number, controlHeight: number): Promise<ErrorType | null>;
    }
}
export default function createSetControlHeight<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, controlHeight: number): Request<Broadcast>;
