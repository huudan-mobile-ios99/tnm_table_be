import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetRebootWorkMCUProgram(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, rebootWorkMCUProgram: number): Promise<void>;
        trySetRebootWorkMCUProgram(addr: number, portAddr: number, scanBoardAddr: number, rebootWorkMCUProgram: number): Promise<ErrorType | null>;
    }
}
export default function createSetRebootWorkMCUProgram<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, rebootWorkMCUProgram: number): Request<Broadcast>;
