import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadIsHasDVI(addr) {
    const req = new Request(AddressMapping.IsHasDVISignalOccupancy, 'ReadIsHasDVI');
    req.destination = addr;
    req.address = AddressMapping.IsHasDVISignalAddr;
    return req;
}
Session.prototype.ReadIsHasDVI = async function ReadIsHasDVI(addr) {
    const req = createReadIsHasDVI(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadIsHasDVI = async function tryReadIsHasDVI(addr) {
    const req = createReadIsHasDVI(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadIsHasDVI.js.map