import { Request, Session } from '@novastar/codec';
import makeOutDeviceBytes from '../../lib/common/makeOutDeviceBytes';
import AddressMapping from '../AddressMapping';
export default function createFuncCard_SetOutDevice_3(addr, portAddr, funcCardAddr, bBroadcast, outDeviceAddr, baudType, otherDeviceProtocol) {
    const $data = makeOutDeviceBytes(outDeviceAddr, baudType, otherDeviceProtocol);
    const req = new Request($data, bBroadcast, 'FuncCard_SetOutDevice_3');
    req.destination = addr;
    req.deviceType = 2;
    req.port = portAddr;
    req.rcvIndex = funcCardAddr;
    req.address = AddressMapping.FuncCard_WriteOutDeviceAddr;
    return req;
}
Session.prototype.FuncCard_SetOutDevice_3 = async function FuncCard_SetOutDevice_3(addr, portAddr, funcCardAddr, bBroadcast, outDeviceAddr, baudType, otherDeviceProtocol) {
    const req = createFuncCard_SetOutDevice_3(addr, portAddr, funcCardAddr, bBroadcast, outDeviceAddr, baudType, otherDeviceProtocol);
    await this.connection.send(req);
};
Session.prototype.tryFuncCard_SetOutDevice_3 = async function tryFuncCard_SetOutDevice_3(addr, portAddr, funcCardAddr, outDeviceAddr, baudType, otherDeviceProtocol) {
    const req = createFuncCard_SetOutDevice_3(addr, portAddr, funcCardAddr, false, outDeviceAddr, baudType, otherDeviceProtocol);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=FuncCard_SetOutDevice_3.js.map