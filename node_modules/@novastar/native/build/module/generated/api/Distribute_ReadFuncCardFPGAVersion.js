import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createDistribute_ReadFuncCardFPGAVersion(addr, portAddr, distributeAddr) {
    const req = new Request(AddressMapping.Distribute_FPGAVersionOccupancy, 'Distribute_ReadFuncCardFPGAVersion');
    req.destination = addr;
    req.deviceType = 3;
    req.port = portAddr;
    req.rcvIndex = distributeAddr;
    req.address = AddressMapping.Distribute_FPGAVersionAddr;
    return req;
}
Session.prototype.Distribute_ReadFuncCardFPGAVersion =
    async function Distribute_ReadFuncCardFPGAVersion(addr, portAddr, distributeAddr) {
        const req = createDistribute_ReadFuncCardFPGAVersion(addr, portAddr, distributeAddr);
        return decodeUIntLE(await this.connection.send(req));
    };
Session.prototype.tryDistribute_ReadFuncCardFPGAVersion =
    async function tryDistribute_ReadFuncCardFPGAVersion(addr, portAddr, distributeAddr) {
        const req = createDistribute_ReadFuncCardFPGAVersion(addr, portAddr, distributeAddr);
        return this.connection.trySend(req);
    };
//# sourceMappingURL=Distribute_ReadFuncCardFPGAVersion.js.map