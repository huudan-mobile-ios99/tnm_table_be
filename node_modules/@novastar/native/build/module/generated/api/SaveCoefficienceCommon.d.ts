import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SaveCoefficienceCommon(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, val: number): Promise<void>;
        trySaveCoefficienceCommon(addr: number, portAddr: number, scanBoardAddr: number, val: number): Promise<ErrorType | null>;
    }
}
export default function createSaveCoefficienceCommon<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, val: number): Request<Broadcast>;
