import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadDeltaTValue(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.DeltaTOccupancy, 'ReadDeltaTValue');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.DeltaTAddr;
    return req;
}
Session.prototype.ReadDeltaTValue = async function ReadDeltaTValue(addr, portAddr, scanBoardAddr) {
    const req = createReadDeltaTValue(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadDeltaTValue = async function tryReadDeltaTValue(addr, portAddr, scanBoardAddr) {
    const req = createReadDeltaTValue(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadDeltaTValue.js.map