import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetSpecialParam(addr, portAddr, scanBoardAddr, bBroadcast, rowChangePoint) {
    const $data = encodeUIntLE(rowChangePoint, AddressMapping.RowChangePointOccupancy);
    const req = new Request($data, bBroadcast, 'SetSpecialParam');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.StartAutoLinearTableAddr;
    return req;
}
Session.prototype.SetSpecialParam = async function SetSpecialParam(addr, portAddr, scanBoardAddr, bBroadcast, rowChangePoint) {
    const req = createSetSpecialParam(addr, portAddr, scanBoardAddr, bBroadcast, rowChangePoint);
    await this.connection.send(req);
};
Session.prototype.trySetSpecialParam = async function trySetSpecialParam(addr, portAddr, scanBoardAddr, rowChangePoint) {
    const req = createSetSpecialParam(addr, portAddr, scanBoardAddr, false, rowChangePoint);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetSpecialParam.js.map