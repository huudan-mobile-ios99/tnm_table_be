import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadDisplayModeWhenNoSignal(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadDisplayModeWhenNoSignal(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadDisplayModeWhenNoSignal(addr: number, portAddr: number, scanBoardAddr: number): Request;
