import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadIoControl(addr) {
    const req = new Request(AddressMapping.IoControlOccupancy, 'ReadIoControl');
    req.destination = addr;
    req.address = AddressMapping.IoControlAddr;
    return req;
}
Session.prototype.ReadIoControl = async function ReadIoControl(addr) {
    const req = createReadIoControl(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadIoControl = async function tryReadIoControl(addr) {
    const req = createReadIoControl(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadIoControl.js.map