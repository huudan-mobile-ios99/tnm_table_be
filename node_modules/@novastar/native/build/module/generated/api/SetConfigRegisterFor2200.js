import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetConfigRegisterFor2200(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = new Request(data, bBroadcast, 'SetConfigRegisterFor2200');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.WriteConfigRegisterAddrFor2200;
    return req;
}
Session.prototype.SetConfigRegisterFor2200 = async function SetConfigRegisterFor2200(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createSetConfigRegisterFor2200(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.trySetConfigRegisterFor2200 = async function trySetConfigRegisterFor2200(addr, portAddr, scanBoardAddr, data) {
    const req = createSetConfigRegisterFor2200(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetConfigRegisterFor2200.js.map