import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadGrayDepth(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.GrayDepthOccupancy, 'ReadGrayDepth');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.GrayDepthAddr;
    return req;
}
Session.prototype.ReadGrayDepth = async function ReadGrayDepth(addr, portAddr, scanBoardAddr) {
    const req = createReadGrayDepth(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadGrayDepth = async function tryReadGrayDepth(addr, portAddr, scanBoardAddr) {
    const req = createReadGrayDepth(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadGrayDepth.js.map