import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadRGBBrightness(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.RGBBrightnessOccupancy, 'ReadRGBBrightness');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.RGBBrightnessAddr;
    return req;
}
Session.prototype.ReadRGBBrightness = async function ReadRGBBrightness(addr, portAddr, scanBoardAddr) {
    const req = createReadRGBBrightness(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadRGBBrightness = async function tryReadRGBBrightness(addr, portAddr, scanBoardAddr) {
    const req = createReadRGBBrightness(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadRGBBrightness.js.map