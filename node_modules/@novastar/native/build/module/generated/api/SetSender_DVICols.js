import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetSender_DVICols(addr, bBroadcast, dviCols) {
    const $data = encodeUIntLE(dviCols, AddressMapping.Sender_DVIColsOccupancy);
    const req = new Request($data, bBroadcast, 'SetSender_DVICols');
    req.destination = addr;
    req.address = AddressMapping.Sender_DVIColsAddr;
    return req;
}
Session.prototype.SetSender_DVICols = async function SetSender_DVICols(addr, bBroadcast, dviCols) {
    const req = createSetSender_DVICols(addr, bBroadcast, dviCols);
    await this.connection.send(req);
};
Session.prototype.trySetSender_DVICols = async function trySetSender_DVICols(addr, dviCols) {
    const req = createSetSender_DVICols(addr, false, dviCols);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetSender_DVICols.js.map