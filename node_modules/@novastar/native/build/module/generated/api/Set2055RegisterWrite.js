import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSet2055RegisterWrite(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    if (data.length !== AddressMapping.Config2055RegisterOccupancy)
        throw new TypeError(`Invalid buffer size: ${data.length}`);
    const req = new Request(data, bBroadcast, 'Set2055RegisterWrite');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Config2055RegisterAddr;
    return req;
}
Session.prototype.Set2055RegisterWrite = async function Set2055RegisterWrite(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createSet2055RegisterWrite(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.trySet2055RegisterWrite = async function trySet2055RegisterWrite(addr, portAddr, scanBoardAddr, data) {
    const req = createSet2055RegisterWrite(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=Set2055RegisterWrite.js.map