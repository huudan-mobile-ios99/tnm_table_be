import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadABCDCode(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ABCDCodeOccupancy, 'ReadABCDCode');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ABCDCodeAddr;
    return req;
}
Session.prototype.ReadABCDCode = async function ReadABCDCode(addr, portAddr, scanBoardAddr) {
    const req = createReadABCDCode(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadABCDCode = async function tryReadABCDCode(addr, portAddr, scanBoardAddr) {
    const req = createReadABCDCode(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadABCDCode.js.map