import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetPortHeight(addr: number, bBroadcast: boolean, portHeight: number, index: number): Promise<void>;
        trySetPortHeight(addr: number, portHeight: number, index: number): Promise<ErrorType | null>;
    }
}
export default function createSetPortHeight<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, portHeight: number, index: number): Request<Broadcast>;
