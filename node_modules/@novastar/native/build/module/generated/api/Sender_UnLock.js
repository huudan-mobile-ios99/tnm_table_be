import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSender_UnLock(addr, bBroadcast, passWord) {
    const $data = encodeUIntLE(passWord, AddressMapping.Sender_UnLockOccupancy);
    const req = new Request($data, bBroadcast, 'Sender_UnLock');
    req.destination = addr;
    req.address = AddressMapping.Sender_UnLockAddr;
    return req;
}
Session.prototype.Sender_UnLock = async function Sender_UnLock(addr, bBroadcast, passWord) {
    const req = createSender_UnLock(addr, bBroadcast, passWord);
    await this.connection.send(req);
};
Session.prototype.trySender_UnLock = async function trySender_UnLock(addr, passWord) {
    const req = createSender_UnLock(addr, false, passWord);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=Sender_UnLock.js.map