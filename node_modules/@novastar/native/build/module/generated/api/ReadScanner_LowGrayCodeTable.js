import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScanner_LowGrayCodeTable(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.LowGrayCodeTableOccupancy, 'ReadScanner_LowGrayCodeTable');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.LowGrayCodeTableAddr;
    return req;
}
Session.prototype.ReadScanner_LowGrayCodeTable = async function ReadScanner_LowGrayCodeTable(addr, portAddr, scanBoardAddr) {
    const req = createReadScanner_LowGrayCodeTable(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadScanner_LowGrayCodeTable = async function tryReadScanner_LowGrayCodeTable(addr, portAddr, scanBoardAddr) {
    const req = createReadScanner_LowGrayCodeTable(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScanner_LowGrayCodeTable.js.map