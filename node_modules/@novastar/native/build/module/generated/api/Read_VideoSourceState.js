import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createRead_VideoSourceState(addr) {
    const req = new Request(AddressMapping.Sender_VideoSourceStateOccupancy, 'Read_VideoSourceState');
    req.destination = addr;
    req.address = AddressMapping.Sender_VideoSourceStateAddr;
    return req;
}
Session.prototype.Read_VideoSourceState = async function Read_VideoSourceState(addr) {
    const req = createRead_VideoSourceState(addr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryRead_VideoSourceState = async function tryRead_VideoSourceState(addr) {
    const req = createRead_VideoSourceState(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=Read_VideoSourceState.js.map