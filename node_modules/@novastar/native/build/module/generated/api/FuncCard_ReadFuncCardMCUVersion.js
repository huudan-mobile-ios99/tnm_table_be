import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createFuncCard_ReadFuncCardMCUVersion(addr) {
    const req = new Request(AddressMapping.FuncCard_MCUVersionOccupancy, 'FuncCard_ReadFuncCardMCUVersion');
    req.destination = addr;
    req.address = AddressMapping.FuncCard_MCUVersionAddr;
    return req;
}
Session.prototype.FuncCard_ReadFuncCardMCUVersion = async function FuncCard_ReadFuncCardMCUVersion(addr) {
    const req = createFuncCard_ReadFuncCardMCUVersion(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryFuncCard_ReadFuncCardMCUVersion =
    async function tryFuncCard_ReadFuncCardMCUVersion(addr) {
        const req = createFuncCard_ReadFuncCardMCUVersion(addr);
        return this.connection.trySend(req);
    };
//# sourceMappingURL=FuncCard_ReadFuncCardMCUVersion.js.map