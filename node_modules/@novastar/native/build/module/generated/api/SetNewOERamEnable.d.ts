import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetNewOERamEnable(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, newOERamEnable: number): Promise<void>;
        trySetNewOERamEnable(addr: number, portAddr: number, scanBoardAddr: number, newOERamEnable: number): Promise<ErrorType | null>;
    }
}
export default function createSetNewOERamEnable<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, newOERamEnable: number): Request<Broadcast>;
