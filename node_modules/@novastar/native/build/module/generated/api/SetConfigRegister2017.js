import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetConfigRegister2017(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    if (data.length !== AddressMapping.WriteConfigRegisterOccupancy2017TD)
        throw new TypeError(`Invalid buffer size: ${data.length}`);
    const req = new Request(data, bBroadcast, 'SetConfigRegister2017');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.WriteConfigRegisterAddr2017TD;
    return req;
}
Session.prototype.SetConfigRegister2017 = async function SetConfigRegister2017(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createSetConfigRegister2017(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.trySetConfigRegister2017 = async function trySetConfigRegister2017(addr, portAddr, scanBoardAddr, data) {
    const req = createSetConfigRegister2017(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetConfigRegister2017.js.map