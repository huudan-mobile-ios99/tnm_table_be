import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadVRedBrightness(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.VRedBrightnessOccupancy, 'ReadVRedBrightness');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.VRedBrightnessAddr;
    return req;
}
Session.prototype.ReadVRedBrightness = async function ReadVRedBrightness(addr, portAddr, scanBoardAddr) {
    const req = createReadVRedBrightness(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadVRedBrightness = async function tryReadVRedBrightness(addr, portAddr, scanBoardAddr) {
    const req = createReadVRedBrightness(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadVRedBrightness.js.map