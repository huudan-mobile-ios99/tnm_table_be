import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetRowChangePoint(addr, portAddr, scanBoardAddr, bBroadcast, rowChangePoint) {
    const $data = encodeUIntLE(rowChangePoint, AddressMapping.RowChangePointOccupancy);
    const req = new Request($data, bBroadcast, 'SetRowChangePoint');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.RowChangePointAddr;
    return req;
}
Session.prototype.SetRowChangePoint = async function SetRowChangePoint(addr, portAddr, scanBoardAddr, bBroadcast, rowChangePoint) {
    const req = createSetRowChangePoint(addr, portAddr, scanBoardAddr, bBroadcast, rowChangePoint);
    await this.connection.send(req);
};
Session.prototype.trySetRowChangePoint = async function trySetRowChangePoint(addr, portAddr, scanBoardAddr, rowChangePoint) {
    const req = createSetRowChangePoint(addr, portAddr, scanBoardAddr, false, rowChangePoint);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetRowChangePoint.js.map