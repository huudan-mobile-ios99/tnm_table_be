import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSet16259EnSwipErrorPoint(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    if (data.length !== AddressMapping.ErrorPoint16259Occupancy)
        throw new TypeError(`Invalid buffer size: ${data.length}`);
    const req = new Request(data, bBroadcast, 'Set16259EnSwipErrorPoint');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ErrorPoint16259Addr;
    return req;
}
Session.prototype.Set16259EnSwipErrorPoint = async function Set16259EnSwipErrorPoint(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createSet16259EnSwipErrorPoint(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.trySet16259EnSwipErrorPoint = async function trySet16259EnSwipErrorPoint(addr, portAddr, scanBoardAddr, data) {
    const req = createSet16259EnSwipErrorPoint(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=Set16259EnSwipErrorPoint.js.map