import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetFPGAGoldenProgramStore(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, FPGAGoldenProgramStore: number): Promise<void>;
        trySetFPGAGoldenProgramStore(addr: number, portAddr: number, scanBoardAddr: number, FPGAGoldenProgramStore: number): Promise<ErrorType | null>;
    }
}
export default function createSetFPGAGoldenProgramStore<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, FPGAGoldenProgramStore: number): Request<Broadcast>;
