import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createFuncCard_SetPowerPortCtrl(addr, bBroadcast, powerCtrlMode, powerIndex) {
    const $data = encodeUIntLE(powerCtrlMode, AddressMapping.FuncCard_PowerPortCtrlOccupancy);
    const req = new Request($data, bBroadcast, 'FuncCard_SetPowerPortCtrl');
    req.destination = addr;
    req.address =
        AddressMapping.FuncCard_PowerPortCtrlAddr +
            powerIndex * AddressMapping.FuncCard_PowerPortCtrlOccupancy;
    return req;
}
Session.prototype.FuncCard_SetPowerPortCtrl = async function FuncCard_SetPowerPortCtrl(addr, bBroadcast, powerCtrlMode, powerIndex) {
    const req = createFuncCard_SetPowerPortCtrl(addr, bBroadcast, powerCtrlMode, powerIndex);
    await this.connection.send(req);
};
Session.prototype.tryFuncCard_SetPowerPortCtrl = async function tryFuncCard_SetPowerPortCtrl(addr, powerCtrlMode, powerIndex) {
    const req = createFuncCard_SetPowerPortCtrl(addr, false, powerCtrlMode, powerIndex);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=FuncCard_SetPowerPortCtrl.js.map