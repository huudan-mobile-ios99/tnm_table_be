import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetSenderABLParmsState(addr, bBroadcast, info) {
    const req = new Request(info, bBroadcast, 'SetSenderABLParmsState');
    req.destination = addr;
    req.address = AddressMapping.SenderABLtionAddr;
    return req;
}
Session.prototype.SetSenderABLParmsState = async function SetSenderABLParmsState(addr, bBroadcast, info) {
    const req = createSetSenderABLParmsState(addr, bBroadcast, info);
    await this.connection.send(req);
};
Session.prototype.trySetSenderABLParmsState = async function trySetSenderABLParmsState(addr, info) {
    const req = createSetSenderABLParmsState(addr, false, info);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetSenderABLParmsState.js.map