/// <reference types="node" />
import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetScanner_NewOEHighTable(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, newOETable: number[] | Buffer): Promise<void>;
        trySetScanner_NewOEHighTable(addr: number, portAddr: number, scanBoardAddr: number, newOETable: number[] | Buffer): Promise<ErrorType | null>;
    }
}
export default function createSetScanner_NewOEHighTable<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, newOETable: number[] | Buffer): Request<Broadcast>;
