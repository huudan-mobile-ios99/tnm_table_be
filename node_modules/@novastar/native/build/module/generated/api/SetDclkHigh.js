import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetDclkHigh(addr, portAddr, scanBoardAddr, bBroadcast, dclkHigh) {
    const $data = encodeUIntLE(dclkHigh, AddressMapping.DclkHighOccupancy);
    const req = new Request($data, bBroadcast, 'SetDclkHigh');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.DclkHighAddr;
    return req;
}
Session.prototype.SetDclkHigh = async function SetDclkHigh(addr, portAddr, scanBoardAddr, bBroadcast, dclkHigh) {
    const req = createSetDclkHigh(addr, portAddr, scanBoardAddr, bBroadcast, dclkHigh);
    await this.connection.send(req);
};
Session.prototype.trySetDclkHigh = async function trySetDclkHigh(addr, portAddr, scanBoardAddr, dclkHigh) {
    const req = createSetDclkHigh(addr, portAddr, scanBoardAddr, false, dclkHigh);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetDclkHigh.js.map