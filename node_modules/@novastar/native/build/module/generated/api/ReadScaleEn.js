import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScaleEn(addr) {
    const req = new Request(AddressMapping.DVIScaleEnOccupancy, 'ReadScaleEn');
    req.destination = addr;
    req.address = AddressMapping.DVIScaleEnAddr;
    return req;
}
Session.prototype.ReadScaleEn = async function ReadScaleEn(addr) {
    const req = createReadScaleEn(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadScaleEn = async function tryReadScaleEn(addr) {
    const req = createReadScaleEn(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScaleEn.js.map