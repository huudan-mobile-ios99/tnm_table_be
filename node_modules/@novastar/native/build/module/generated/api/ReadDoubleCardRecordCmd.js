import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadDoubleCardRecordCmd(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.DoubleModelCardSpaceRecordCmdOccupancy, 'ReadDoubleCardRecordCmd');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.DoubleModelCardSpaceRecordCmd;
    return req;
}
Session.prototype.ReadDoubleCardRecordCmd = async function ReadDoubleCardRecordCmd(addr, portAddr, scanBoardAddr) {
    const req = createReadDoubleCardRecordCmd(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadDoubleCardRecordCmd = async function tryReadDoubleCardRecordCmd(addr, portAddr, scanBoardAddr) {
    const req = createReadDoubleCardRecordCmd(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadDoubleCardRecordCmd.js.map