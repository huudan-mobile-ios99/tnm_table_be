import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadSelfTestMode(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadSelfTestMode(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadSelfTestMode(addr: number, portAddr: number, scanBoardAddr: number): Request;
