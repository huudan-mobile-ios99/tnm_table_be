import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetTVCardDVI0Height(addr, portAddr, scanBoardAddr, bBroadcast, dviHeight) {
    const $data = encodeUIntLE(dviHeight, AddressMapping.TVCardDVI0HeightOccupancy);
    const req = new Request($data, bBroadcast, 'SetTVCardDVI0Height');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.TVCardDVI0HeightAddr;
    return req;
}
Session.prototype.SetTVCardDVI0Height = async function SetTVCardDVI0Height(addr, portAddr, scanBoardAddr, bBroadcast, dviHeight) {
    const req = createSetTVCardDVI0Height(addr, portAddr, scanBoardAddr, bBroadcast, dviHeight);
    await this.connection.send(req);
};
Session.prototype.trySetTVCardDVI0Height = async function trySetTVCardDVI0Height(addr, portAddr, scanBoardAddr, dviHeight) {
    const req = createSetTVCardDVI0Height(addr, portAddr, scanBoardAddr, false, dviHeight);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetTVCardDVI0Height.js.map