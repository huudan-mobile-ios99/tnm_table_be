import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createExtendModuleOperCmd(addr, portAddr, scanBoardAddr, bBroadcast, data, length) {
    if (data.length !== length)
        throw new TypeError(`Invalid buffer size: ${data.length}`);
    const req = new Request(data, bBroadcast, 'ExtendModuleOperCmd');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ModuleOperCmdAddr;
    return req;
}
Session.prototype.ExtendModuleOperCmd = async function ExtendModuleOperCmd(addr, portAddr, scanBoardAddr, bBroadcast, data, length) {
    const req = createExtendModuleOperCmd(addr, portAddr, scanBoardAddr, bBroadcast, data, length);
    await this.connection.send(req);
};
Session.prototype.tryExtendModuleOperCmd = async function tryExtendModuleOperCmd(addr, portAddr, scanBoardAddr, data, length) {
    const req = createExtendModuleOperCmd(addr, portAddr, scanBoardAddr, false, data, length);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=ExtendModuleOperCmd.js.map