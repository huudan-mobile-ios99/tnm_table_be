import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadAutoCorrectUpload(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.AutoCorrectUploadOccupancy, 'ReadAutoCorrectUpload');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.AutoCorrectUploadAddr;
    return req;
}
Session.prototype.ReadAutoCorrectUpload = async function ReadAutoCorrectUpload(addr, portAddr, scanBoardAddr) {
    const req = createReadAutoCorrectUpload(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadAutoCorrectUpload = async function tryReadAutoCorrectUpload(addr, portAddr, scanBoardAddr) {
    const req = createReadAutoCorrectUpload(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadAutoCorrectUpload.js.map