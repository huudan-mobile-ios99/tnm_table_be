import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadMGEnParmData(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadMGEnParmData(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadMGEnParmData(addr: number, portAddr: number, scanBoardAddr: number): Request;
