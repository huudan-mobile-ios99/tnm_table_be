import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        MoveCorrectionFromSdramToSlaveFlash(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean): Promise<void>;
        tryMoveCorrectionFromSdramToSlaveFlash(addr: number, portAddr: number, scanBoardAddr: number): Promise<ErrorType | null>;
    }
}
export default function createMoveCorrectionFromSdramToSlaveFlash<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast): Request<Broadcast>;
