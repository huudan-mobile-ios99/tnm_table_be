import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadBgain(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.BgainOccupancy, 'ReadBgain');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.BgainAddr;
    return req;
}
Session.prototype.ReadBgain = async function ReadBgain(addr, portAddr, scanBoardAddr) {
    const req = createReadBgain(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadBgain = async function tryReadBgain(addr, portAddr, scanBoardAddr) {
    const req = createReadBgain(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadBgain.js.map