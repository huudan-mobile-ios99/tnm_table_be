import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadMasterOrSlave(addr) {
    const req = new Request(AddressMapping.MasterOrSlaveOccupancy, 'ReadMasterOrSlave');
    req.destination = addr;
    req.address = AddressMapping.MasterOrSlaveAddr;
    return req;
}
Session.prototype.ReadMasterOrSlave = async function ReadMasterOrSlave(addr) {
    const req = createReadMasterOrSlave(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadMasterOrSlave = async function tryReadMasterOrSlave(addr) {
    const req = createReadMasterOrSlave(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadMasterOrSlave.js.map