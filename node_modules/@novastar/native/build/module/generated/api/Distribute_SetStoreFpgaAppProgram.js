import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createDistribute_SetStoreFpgaAppProgram(addr, portAddr, distributeAddr, bBroadcast, distributeModle) {
    const $data = encodeUIntLE(distributeModle, AddressMapping.Distribute_StoreFpgaAppProgramOccupancy);
    const req = new Request($data, bBroadcast, 'Distribute_SetStoreFpgaAppProgram');
    req.destination = addr;
    req.deviceType = 3;
    req.port = portAddr;
    req.rcvIndex = distributeAddr;
    req.address = AddressMapping.Distribute_StoreFpgaAppProgramAddr;
    return req;
}
Session.prototype.Distribute_SetStoreFpgaAppProgram =
    async function Distribute_SetStoreFpgaAppProgram(addr, portAddr, distributeAddr, bBroadcast, distributeModle) {
        const req = createDistribute_SetStoreFpgaAppProgram(addr, portAddr, distributeAddr, bBroadcast, distributeModle);
        await this.connection.send(req);
    };
Session.prototype.tryDistribute_SetStoreFpgaAppProgram =
    async function tryDistribute_SetStoreFpgaAppProgram(addr, portAddr, distributeAddr, distributeModle) {
        const req = createDistribute_SetStoreFpgaAppProgram(addr, portAddr, distributeAddr, false, distributeModle);
        return (await this.connection.trySend(req))?.ack ?? null;
    };
//# sourceMappingURL=Distribute_SetStoreFpgaAppProgram.js.map