import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetColScalePara(addr, bBroadcast, colScalePar) {
    const $data = encodeUIntLE(colScalePar, AddressMapping.ColScaleParAOccupancy);
    const req = new Request($data, bBroadcast, 'SetColScalePara');
    req.destination = addr;
    req.address = AddressMapping.ColScaleParaAddr;
    return req;
}
Session.prototype.SetColScalePara = async function SetColScalePara(addr, bBroadcast, colScalePar) {
    const req = createSetColScalePara(addr, bBroadcast, colScalePar);
    await this.connection.send(req);
};
Session.prototype.trySetColScalePara = async function trySetColScalePara(addr, colScalePar) {
    const req = createSetColScalePara(addr, false, colScalePar);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetColScalePara.js.map