import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetConfigRegisterWrite7(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    if (data.length !== AddressMapping.WriteConfigRegisterOccupancy7)
        throw new TypeError(`Invalid buffer size: ${data.length}`);
    const req = new Request(data, bBroadcast, 'SetConfigRegisterWrite7');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ConfigRegisterWriteAddr7;
    return req;
}
Session.prototype.SetConfigRegisterWrite7 = async function SetConfigRegisterWrite7(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createSetConfigRegisterWrite7(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.trySetConfigRegisterWrite7 = async function trySetConfigRegisterWrite7(addr, portAddr, scanBoardAddr, data) {
    const req = createSetConfigRegisterWrite7(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetConfigRegisterWrite7.js.map