import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadRotateAngle(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.RotateAngleOccupancy, 'ReadRotateAngle');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.RotateAngleAddr;
    return req;
}
Session.prototype.ReadRotateAngle = async function ReadRotateAngle(addr, portAddr, scanBoardAddr) {
    const req = createReadRotateAngle(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadRotateAngle = async function tryReadRotateAngle(addr, portAddr, scanBoardAddr) {
    const req = createReadRotateAngle(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadRotateAngle.js.map