import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSortOrder(addr) {
    const req = new Request(AddressMapping.SortOrderOccupancy, 'ReadSortOrder');
    req.destination = addr;
    req.address = AddressMapping.SortOrderAddr;
    return req;
}
Session.prototype.ReadSortOrder = async function ReadSortOrder(addr) {
    const req = createReadSortOrder(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadSortOrder = async function tryReadSortOrder(addr) {
    const req = createReadSortOrder(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSortOrder.js.map