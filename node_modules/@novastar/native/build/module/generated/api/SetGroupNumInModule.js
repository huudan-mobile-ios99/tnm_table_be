import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetGroupNumInModule(addr, portAddr, scanBoardAddr, bBroadcast, groupNumInModule) {
    const $data = encodeUIntLE(groupNumInModule, AddressMapping.GroupNumInModuleOccupancy);
    const req = new Request($data, bBroadcast, 'SetGroupNumInModule');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.GroupNumInModuleAddr;
    return req;
}
Session.prototype.SetGroupNumInModule = async function SetGroupNumInModule(addr, portAddr, scanBoardAddr, bBroadcast, groupNumInModule) {
    const req = createSetGroupNumInModule(addr, portAddr, scanBoardAddr, bBroadcast, groupNumInModule);
    await this.connection.send(req);
};
Session.prototype.trySetGroupNumInModule = async function trySetGroupNumInModule(addr, portAddr, scanBoardAddr, groupNumInModule) {
    const req = createSetGroupNumInModule(addr, portAddr, scanBoardAddr, false, groupNumInModule);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetGroupNumInModule.js.map