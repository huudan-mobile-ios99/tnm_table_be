import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetVRgain(addr, portAddr, scanBoardAddr, bBroadcast, vRgain) {
    const $data = encodeUIntLE(vRgain, AddressMapping.VRgainOccupancy);
    const req = new Request($data, bBroadcast, 'SetVRgain');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.VRgainAddr;
    return req;
}
Session.prototype.SetVRgain = async function SetVRgain(addr, portAddr, scanBoardAddr, bBroadcast, vRgain) {
    const req = createSetVRgain(addr, portAddr, scanBoardAddr, bBroadcast, vRgain);
    await this.connection.send(req);
};
Session.prototype.trySetVRgain = async function trySetVRgain(addr, portAddr, scanBoardAddr, vRgain) {
    const req = createSetVRgain(addr, portAddr, scanBoardAddr, false, vRgain);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetVRgain.js.map