import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadGreenBrightness(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.GreenBrightnessOccupancy, 'ReadGreenBrightness');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.GreenBrightnessAddr;
    return req;
}
Session.prototype.ReadGreenBrightness = async function ReadGreenBrightness(addr, portAddr, scanBoardAddr) {
    const req = createReadGreenBrightness(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadGreenBrightness = async function tryReadGreenBrightness(addr, portAddr, scanBoardAddr) {
    const req = createReadGreenBrightness(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadGreenBrightness.js.map