/// <reference types="node" />
import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadScannerCabinetCorrectLocationSize(addr: number, portAddr: number, scanBoardAddr: number): Promise<Buffer>;
        tryReadScannerCabinetCorrectLocationSize(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadScannerCabinetCorrectLocationSize(addr: number, portAddr: number, scanBoardAddr: number): Request;
