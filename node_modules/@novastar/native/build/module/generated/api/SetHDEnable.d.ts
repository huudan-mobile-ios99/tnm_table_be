import { ErrorType, Request } from '@novastar/codec';
import { HDEnableModeEnum } from '../HDEnableMode';
declare module '@novastar/codec' {
    interface API {
        SetHDEnable(addr: number, bBroadcast: boolean, hdEnable: HDEnableModeEnum): Promise<void>;
        trySetHDEnable(addr: number, hdEnable: HDEnableModeEnum): Promise<ErrorType | null>;
    }
}
export default function createSetHDEnable<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, hdEnable: HDEnableModeEnum): Request<Broadcast>;
