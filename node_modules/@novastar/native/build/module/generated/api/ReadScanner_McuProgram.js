import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScanner_McuProgram(addr, portAddr, scanBoardAddr, readLength) {
    const req = new Request(readLength, 'ReadScanner_McuProgram');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Scanner_McuProgramLengthAddr;
    return req;
}
Session.prototype.ReadScanner_McuProgram = async function ReadScanner_McuProgram(addr, portAddr, scanBoardAddr, readLength) {
    const req = createReadScanner_McuProgram(addr, portAddr, scanBoardAddr, readLength);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadScanner_McuProgram = async function tryReadScanner_McuProgram(addr, portAddr, scanBoardAddr, readLength) {
    const req = createReadScanner_McuProgram(addr, portAddr, scanBoardAddr, readLength);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScanner_McuProgram.js.map