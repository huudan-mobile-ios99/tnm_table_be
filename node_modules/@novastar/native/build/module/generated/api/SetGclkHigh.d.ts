import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetGclkHigh(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, gclkHigh: number): Promise<void>;
        trySetGclkHigh(addr: number, portAddr: number, scanBoardAddr: number, gclkHigh: number): Promise<ErrorType | null>;
    }
}
export default function createSetGclkHigh<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, gclkHigh: number): Request<Broadcast>;
