import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSender_EDIDSpace(addr) {
    const req = new Request(AddressMapping.Sender_EDIDSpaceOccupancy, 'ReadSender_EDIDSpace');
    req.destination = addr;
    req.address = AddressMapping.Sender_EDIDSpaceAddr;
    return req;
}
Session.prototype.ReadSender_EDIDSpace = async function ReadSender_EDIDSpace(addr) {
    const req = createReadSender_EDIDSpace(addr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadSender_EDIDSpace = async function tryReadSender_EDIDSpace(addr) {
    const req = createReadSender_EDIDSpace(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSender_EDIDSpace.js.map