import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetDviMode(addr, bBroadcast, dviMode) {
    const $data = encodeUIntLE(dviMode, AddressMapping.DviModeOccupancy);
    const req = new Request($data, bBroadcast, 'SetDviMode');
    req.destination = addr;
    req.address = AddressMapping.DviModeAddr;
    return req;
}
Session.prototype.SetDviMode = async function SetDviMode(addr, bBroadcast, dviMode) {
    const req = createSetDviMode(addr, bBroadcast, dviMode);
    await this.connection.send(req);
};
Session.prototype.trySetDviMode = async function trySetDviMode(addr, dviMode) {
    const req = createSetDviMode(addr, false, dviMode);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetDviMode.js.map