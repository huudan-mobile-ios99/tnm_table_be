import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetScannerDehumidPara(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    if (data.length !== AddressMapping.DehumidParaAddrOccupancy)
        throw new TypeError(`Invalid buffer size: ${data.length}`);
    const req = new Request(data, bBroadcast, 'SetScannerDehumidPara');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.DehumidParaAddr;
    return req;
}
Session.prototype.SetScannerDehumidPara = async function SetScannerDehumidPara(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createSetScannerDehumidPara(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.trySetScannerDehumidPara = async function trySetScannerDehumidPara(addr, portAddr, scanBoardAddr, data) {
    const req = createSetScannerDehumidPara(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetScannerDehumidPara.js.map