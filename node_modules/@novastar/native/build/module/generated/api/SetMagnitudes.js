import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetMagnitudes(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = new Request(data, bBroadcast, 'SetMagnitudes');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.MagnitudesAddr;
    return req;
}
Session.prototype.SetMagnitudes = async function SetMagnitudes(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createSetMagnitudes(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.trySetMagnitudes = async function trySetMagnitudes(addr, portAddr, scanBoardAddr, data) {
    const req = createSetMagnitudes(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetMagnitudes.js.map