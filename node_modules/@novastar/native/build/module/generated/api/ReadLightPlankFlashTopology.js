import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadLightPlankFlashTopology(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.LightPlankFlashTopologyOccupancy, 'ReadLightPlankFlashTopology');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.LightPlankFlashTopologyAddr;
    return req;
}
Session.prototype.ReadLightPlankFlashTopology = async function ReadLightPlankFlashTopology(addr, portAddr, scanBoardAddr) {
    const req = createReadLightPlankFlashTopology(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadLightPlankFlashTopology = async function tryReadLightPlankFlashTopology(addr, portAddr, scanBoardAddr) {
    const req = createReadLightPlankFlashTopology(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadLightPlankFlashTopology.js.map