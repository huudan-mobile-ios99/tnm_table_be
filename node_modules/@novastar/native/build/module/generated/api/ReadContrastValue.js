import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadContrastValue(addr) {
    const req = new Request(AddressMapping.SenderContrastResultOccupancy, 'ReadContrastValue');
    req.destination = addr;
    req.address = AddressMapping.SenderContrastResultAddr;
    return req;
}
Session.prototype.ReadContrastValue = async function ReadContrastValue(addr) {
    const req = createReadContrastValue(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadContrastValue = async function tryReadContrastValue(addr) {
    const req = createReadContrastValue(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadContrastValue.js.map