import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadTwentyDataGroup(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.TwentyDataGroupOccupancy, 'ReadTwentyDataGroup');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.TwentyDataGroupAddr;
    return req;
}
Session.prototype.ReadTwentyDataGroup = async function ReadTwentyDataGroup(addr, portAddr, scanBoardAddr) {
    const req = createReadTwentyDataGroup(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadTwentyDataGroup = async function tryReadTwentyDataGroup(addr, portAddr, scanBoardAddr) {
    const req = createReadTwentyDataGroup(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadTwentyDataGroup.js.map