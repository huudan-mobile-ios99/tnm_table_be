import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadDclkHigh(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.DclkHighOccupancy, 'ReadDclkHigh');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.DclkHighAddr;
    return req;
}
Session.prototype.ReadDclkHigh = async function ReadDclkHigh(addr, portAddr, scanBoardAddr) {
    const req = createReadDclkHigh(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadDclkHigh = async function tryReadDclkHigh(addr, portAddr, scanBoardAddr) {
    const req = createReadDclkHigh(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadDclkHigh.js.map