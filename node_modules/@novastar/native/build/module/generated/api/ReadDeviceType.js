import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadDeviceType(addr) {
    const req = new Request(AddressMapping.CompanyIdOccupancy, 'ReadDeviceType');
    req.destination = addr;
    req.address = AddressMapping.DeviceTypeAddr;
    return req;
}
Session.prototype.ReadDeviceType = async function ReadDeviceType(addr) {
    const req = createReadDeviceType(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadDeviceType = async function tryReadDeviceType(addr) {
    const req = createReadDeviceType(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadDeviceType.js.map