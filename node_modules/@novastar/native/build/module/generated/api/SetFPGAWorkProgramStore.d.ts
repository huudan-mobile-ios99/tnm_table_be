import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetFPGAWorkProgramStore(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, FPGAWorkProgramStore: number): Promise<void>;
        trySetFPGAWorkProgramStore(addr: number, portAddr: number, scanBoardAddr: number, FPGAWorkProgramStore: number): Promise<ErrorType | null>;
    }
}
export default function createSetFPGAWorkProgramStore<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, FPGAWorkProgramStore: number): Request<Broadcast>;
