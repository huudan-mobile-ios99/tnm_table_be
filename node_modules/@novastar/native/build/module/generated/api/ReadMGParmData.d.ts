import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadMGParmData(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadMGParmData(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadMGParmData(addr: number, portAddr: number, scanBoardAddr: number): Request;
