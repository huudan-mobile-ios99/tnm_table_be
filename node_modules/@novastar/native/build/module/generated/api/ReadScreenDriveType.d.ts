import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadScreenDriveType(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadScreenDriveType(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadScreenDriveType(addr: number, portAddr: number, scanBoardAddr: number): Request;
