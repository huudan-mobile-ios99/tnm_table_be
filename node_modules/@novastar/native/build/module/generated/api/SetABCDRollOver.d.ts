import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetABCDRollOver(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, isRollOver: boolean): Promise<void>;
        trySetABCDRollOver(addr: number, portAddr: number, scanBoardAddr: number, isRollOver: boolean): Promise<ErrorType | null>;
    }
}
export default function createSetABCDRollOver<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, isRollOver: boolean): Request<Broadcast>;
