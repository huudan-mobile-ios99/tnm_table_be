import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createFuncCard_ReadPowerPortCtrlTotal(addr) {
    const req = new Request(AddressMapping.FuncCard_PowerPortCtrlTotalOccupancy, 'FuncCard_ReadPowerPortCtrlTotal');
    req.destination = addr;
    req.address = AddressMapping.FuncCard_PowerPortCtrlTotalAddr;
    return req;
}
Session.prototype.FuncCard_ReadPowerPortCtrlTotal = async function FuncCard_ReadPowerPortCtrlTotal(addr) {
    const req = createFuncCard_ReadPowerPortCtrlTotal(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryFuncCard_ReadPowerPortCtrlTotal =
    async function tryFuncCard_ReadPowerPortCtrlTotal(addr) {
        const req = createFuncCard_ReadPowerPortCtrlTotal(addr);
        return this.connection.trySend(req);
    };
//# sourceMappingURL=FuncCard_ReadPowerPortCtrlTotal.js.map