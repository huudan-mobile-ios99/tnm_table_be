import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadDehumidAdjustTime(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.DehumidAdjustTimeOccupancy, 'ReadDehumidAdjustTime');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.DehumidAdjustTimeAddr;
    return req;
}
Session.prototype.ReadDehumidAdjustTime = async function ReadDehumidAdjustTime(addr, portAddr, scanBoardAddr) {
    const req = createReadDehumidAdjustTime(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadDehumidAdjustTime = async function tryReadDehumidAdjustTime(addr, portAddr, scanBoardAddr) {
    const req = createReadDehumidAdjustTime(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadDehumidAdjustTime.js.map