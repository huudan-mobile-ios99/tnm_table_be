import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetGrayBit(addr, portAddr, scanBoardAddr, bBroadcast, grayBit) {
    const $data = encodeUIntLE(grayBit, AddressMapping.GrayBitOccupancy);
    const req = new Request($data, bBroadcast, 'SetGrayBit');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.GrayBitAddr;
    return req;
}
Session.prototype.SetGrayBit = async function SetGrayBit(addr, portAddr, scanBoardAddr, bBroadcast, grayBit) {
    const req = createSetGrayBit(addr, portAddr, scanBoardAddr, bBroadcast, grayBit);
    await this.connection.send(req);
};
Session.prototype.trySetGrayBit = async function trySetGrayBit(addr, portAddr, scanBoardAddr, grayBit) {
    const req = createSetGrayBit(addr, portAddr, scanBoardAddr, false, grayBit);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetGrayBit.js.map