import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadCompanyID(addr) {
    const req = new Request(AddressMapping.CompanyIdOccupancy, 'ReadCompanyID');
    req.destination = addr;
    req.address = AddressMapping.CompanyIdAddr;
    return req;
}
Session.prototype.ReadCompanyID = async function ReadCompanyID(addr) {
    const req = createReadCompanyID(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadCompanyID = async function tryReadCompanyID(addr) {
    const req = createReadCompanyID(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadCompanyID.js.map