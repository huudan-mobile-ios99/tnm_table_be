import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadCorrectionState(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.BrightDarkLineFixStateOccupancy, 'ReadCorrectionState');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.CorrectionOnAddr;
    return req;
}
Session.prototype.ReadCorrectionState = async function ReadCorrectionState(addr, portAddr, scanBoardAddr) {
    const req = createReadCorrectionState(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadCorrectionState = async function tryReadCorrectionState(addr, portAddr, scanBoardAddr) {
    const req = createReadCorrectionState(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadCorrectionState.js.map