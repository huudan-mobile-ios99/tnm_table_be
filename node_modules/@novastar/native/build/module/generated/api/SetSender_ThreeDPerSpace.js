import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetSender_ThreeDPerSpace(addr, bBroadcast, enable3DState) {
    const req = new Request(enable3DState, bBroadcast, 'SetSender_ThreeDPerSpace');
    req.destination = addr;
    req.address = AddressMapping.ThreeDPerAddr;
    return req;
}
Session.prototype.SetSender_ThreeDPerSpace = async function SetSender_ThreeDPerSpace(addr, bBroadcast, enable3DState) {
    const req = createSetSender_ThreeDPerSpace(addr, bBroadcast, enable3DState);
    await this.connection.send(req);
};
Session.prototype.trySetSender_ThreeDPerSpace = async function trySetSender_ThreeDPerSpace(addr, enable3DState) {
    const req = createSetSender_ThreeDPerSpace(addr, false, enable3DState);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetSender_ThreeDPerSpace.js.map