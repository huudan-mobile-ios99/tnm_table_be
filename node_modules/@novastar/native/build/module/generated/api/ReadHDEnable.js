import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadHDEnable(addr) {
    const req = new Request(AddressMapping.HDEnableOccupancy, 'ReadHDEnable');
    req.destination = addr;
    req.address = AddressMapping.HDEnableAddr;
    return req;
}
Session.prototype.ReadHDEnable = async function ReadHDEnable(addr) {
    const req = createReadHDEnable(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadHDEnable = async function tryReadHDEnable(addr) {
    const req = createReadHDEnable(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadHDEnable.js.map