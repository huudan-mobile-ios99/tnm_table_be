import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSenderFunctionInfo(addr) {
    const req = new Request(AddressMapping.SenderFunctionOccupancy, 'ReadSenderFunctionInfo');
    req.destination = addr;
    req.address = AddressMapping.SenderFunctionAddr;
    return req;
}
Session.prototype.ReadSenderFunctionInfo = async function ReadSenderFunctionInfo(addr) {
    const req = createReadSenderFunctionInfo(addr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadSenderFunctionInfo = async function tryReadSenderFunctionInfo(addr) {
    const req = createReadSenderFunctionInfo(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSenderFunctionInfo.js.map