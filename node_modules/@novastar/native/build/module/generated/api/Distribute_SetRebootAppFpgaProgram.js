import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createDistribute_SetRebootAppFpgaProgram(addr, portAddr, distributeAddr, bBroadcast, distributeModle) {
    const $data = encodeUIntLE(distributeModle, AddressMapping.Distribute_RebootAppFpgaProgramOccupancy);
    const req = new Request($data, bBroadcast, 'Distribute_SetRebootAppFpgaProgram');
    req.destination = addr;
    req.deviceType = 3;
    req.port = portAddr;
    req.rcvIndex = distributeAddr;
    req.address = AddressMapping.Distribute_RebootAppFpgaProgramAddr;
    return req;
}
Session.prototype.Distribute_SetRebootAppFpgaProgram =
    async function Distribute_SetRebootAppFpgaProgram(addr, portAddr, distributeAddr, bBroadcast, distributeModle) {
        const req = createDistribute_SetRebootAppFpgaProgram(addr, portAddr, distributeAddr, bBroadcast, distributeModle);
        await this.connection.send(req);
    };
Session.prototype.tryDistribute_SetRebootAppFpgaProgram =
    async function tryDistribute_SetRebootAppFpgaProgram(addr, portAddr, distributeAddr, distributeModle) {
        const req = createDistribute_SetRebootAppFpgaProgram(addr, portAddr, distributeAddr, false, distributeModle);
        return (await this.connection.trySend(req))?.ack ?? null;
    };
//# sourceMappingURL=Distribute_SetRebootAppFpgaProgram.js.map