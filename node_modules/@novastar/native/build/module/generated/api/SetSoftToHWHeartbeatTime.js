import { Request, Session } from '@novastar/codec';
export default function createSetSoftToHWHeartbeatTime(addr, bBroadcast, data) {
    const req = new Request(data, bBroadcast, 'SetSoftToHWHeartbeatTime');
    req.destination = addr;
    req.address = 63;
    return req;
}
Session.prototype.SetSoftToHWHeartbeatTime = async function SetSoftToHWHeartbeatTime(addr, bBroadcast, data) {
    const req = createSetSoftToHWHeartbeatTime(addr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.trySetSoftToHWHeartbeatTime = async function trySetSoftToHWHeartbeatTime(addr, data) {
    const req = createSetSoftToHWHeartbeatTime(addr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetSoftToHWHeartbeatTime.js.map