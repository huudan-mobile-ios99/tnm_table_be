import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadHumiOfScanCard(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.Scanner_HumiOfScanCardOccupancy, 'ReadHumiOfScanCard');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Scanner_HumiOfScanCardAddr;
    return req;
}
Session.prototype.ReadHumiOfScanCard = async function ReadHumiOfScanCard(addr, portAddr, scanBoardAddr) {
    const req = createReadHumiOfScanCard(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadHumiOfScanCard = async function tryReadHumiOfScanCard(addr, portAddr, scanBoardAddr) {
    const req = createReadHumiOfScanCard(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadHumiOfScanCard.js.map