import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetGlobalBrightnessOf2053(addr, portAddr, scanBoardAddr, bBroadcast, globalBrightness) {
    const $data = encodeUIntLE(globalBrightness, AddressMapping.GlobalBrightnessOf2053Occupancy);
    const req = new Request($data, bBroadcast, 'SetGlobalBrightnessOf2053');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.GlobalBrightnessOf2053Addr;
    return req;
}
Session.prototype.SetGlobalBrightnessOf2053 = async function SetGlobalBrightnessOf2053(addr, portAddr, scanBoardAddr, bBroadcast, globalBrightness) {
    const req = createSetGlobalBrightnessOf2053(addr, portAddr, scanBoardAddr, bBroadcast, globalBrightness);
    await this.connection.send(req);
};
Session.prototype.trySetGlobalBrightnessOf2053 = async function trySetGlobalBrightnessOf2053(addr, portAddr, scanBoardAddr, globalBrightness) {
    const req = createSetGlobalBrightnessOf2053(addr, portAddr, scanBoardAddr, false, globalBrightness);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetGlobalBrightnessOf2053.js.map