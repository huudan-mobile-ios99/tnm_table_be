import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadDclkHighRatio(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.DclkHighRatioOccupancy, 'ReadDclkHighRatio');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.DclkHighRatioAddr;
    return req;
}
Session.prototype.ReadDclkHighRatio = async function ReadDclkHighRatio(addr, portAddr, scanBoardAddr) {
    const req = createReadDclkHighRatio(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadDclkHighRatio = async function tryReadDclkHighRatio(addr, portAddr, scanBoardAddr) {
    const req = createReadDclkHighRatio(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadDclkHighRatio.js.map