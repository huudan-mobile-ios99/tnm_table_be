import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadDviOf1600OffestInfo(addr) {
    const req = new Request(16, 'ReadDviOf1600OffestInfo');
    req.destination = addr;
    req.address = AddressMapping.DVIOfOffsetInfoAddr;
    return req;
}
Session.prototype.ReadDviOf1600OffestInfo = async function ReadDviOf1600OffestInfo(addr) {
    const req = createReadDviOf1600OffestInfo(addr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadDviOf1600OffestInfo = async function tryReadDviOf1600OffestInfo(addr) {
    const req = createReadDviOf1600OffestInfo(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadDviOf1600OffestInfo.js.map