import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadDoubleEaraseStateCmd(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadDoubleEaraseStateCmd(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadDoubleEaraseStateCmd(addr: number, portAddr: number, scanBoardAddr: number): Request;
