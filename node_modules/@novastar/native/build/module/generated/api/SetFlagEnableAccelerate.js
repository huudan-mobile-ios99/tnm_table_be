import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetFlagEnableAccelerate(addr, bBroadcast, isOpen) {
    const req = new Request(isOpen ? [1] : [0], bBroadcast, 'SetFlagEnableAccelerate');
    req.destination = addr;
    req.address = AddressMapping.Sender_SetFlagForRemove3SecondAddr;
    return req;
}
Session.prototype.SetFlagEnableAccelerate = async function SetFlagEnableAccelerate(addr, bBroadcast, isOpen) {
    const req = createSetFlagEnableAccelerate(addr, bBroadcast, isOpen);
    await this.connection.send(req);
};
Session.prototype.trySetFlagEnableAccelerate = async function trySetFlagEnableAccelerate(addr, isOpen) {
    const req = createSetFlagEnableAccelerate(addr, false, isOpen);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetFlagEnableAccelerate.js.map