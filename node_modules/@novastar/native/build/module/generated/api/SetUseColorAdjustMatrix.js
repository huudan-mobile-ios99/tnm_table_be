import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetUseColorAdjustMatrix(addr, portAddr, scanBoardAddr, bBroadcast, isEnableClrMatrix) {
    const req = new Request(isEnableClrMatrix ? [5] : [255], bBroadcast, 'SetUseColorAdjustMatrix');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.UseColorAdjustMatrixAddr;
    return req;
}
Session.prototype.SetUseColorAdjustMatrix = async function SetUseColorAdjustMatrix(addr, portAddr, scanBoardAddr, bBroadcast, isEnableClrMatrix) {
    const req = createSetUseColorAdjustMatrix(addr, portAddr, scanBoardAddr, bBroadcast, isEnableClrMatrix);
    await this.connection.send(req);
};
Session.prototype.trySetUseColorAdjustMatrix = async function trySetUseColorAdjustMatrix(addr, portAddr, scanBoardAddr, isEnableClrMatrix) {
    const req = createSetUseColorAdjustMatrix(addr, portAddr, scanBoardAddr, false, isEnableClrMatrix);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetUseColorAdjustMatrix.js.map