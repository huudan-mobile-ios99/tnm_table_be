import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetSortOrder(addr, bBroadcast, sortOrder) {
    const $data = encodeUIntLE(sortOrder, AddressMapping.SortOrderOccupancy);
    const req = new Request($data, bBroadcast, 'SetSortOrder');
    req.destination = addr;
    req.address = AddressMapping.SortOrderAddr;
    return req;
}
Session.prototype.SetSortOrder = async function SetSortOrder(addr, bBroadcast, sortOrder) {
    const req = createSetSortOrder(addr, bBroadcast, sortOrder);
    await this.connection.send(req);
};
Session.prototype.trySetSortOrder = async function trySetSortOrder(addr, sortOrder) {
    const req = createSetSortOrder(addr, false, sortOrder);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetSortOrder.js.map