import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadIsHasGenLock(addr) {
    const req = new Request(AddressMapping.IsHasGenLockOccupancy, 'ReadIsHasGenLock');
    req.destination = addr;
    req.address = AddressMapping.IsHasGenLockAddr;
    return req;
}
Session.prototype.ReadIsHasGenLock = async function ReadIsHasGenLock(addr) {
    const req = createReadIsHasGenLock(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadIsHasGenLock = async function tryReadIsHasGenLock(addr) {
    const req = createReadIsHasGenLock(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadIsHasGenLock.js.map