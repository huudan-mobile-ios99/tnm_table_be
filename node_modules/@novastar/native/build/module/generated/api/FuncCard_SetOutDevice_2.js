import { Request, Session } from '@novastar/codec';
import makeOutDeviceBytes from '../../lib/common/makeOutDeviceBytes';
import AddressMapping from '../AddressMapping';
export default function createFuncCard_SetOutDevice_2(addr, bBroadcast, outDeviceAddr, baudType, otherDeviceProtocol) {
    const $data = makeOutDeviceBytes(outDeviceAddr, baudType, otherDeviceProtocol);
    const req = new Request($data, bBroadcast, 'FuncCard_SetOutDevice_2');
    req.destination = addr;
    req.address = AddressMapping.FuncCard_WriteOutDeviceAddr;
    return req;
}
Session.prototype.FuncCard_SetOutDevice_2 = async function FuncCard_SetOutDevice_2(addr, bBroadcast, outDeviceAddr, baudType, otherDeviceProtocol) {
    const req = createFuncCard_SetOutDevice_2(addr, bBroadcast, outDeviceAddr, baudType, otherDeviceProtocol);
    await this.connection.send(req);
};
Session.prototype.tryFuncCard_SetOutDevice_2 = async function tryFuncCard_SetOutDevice_2(addr, outDeviceAddr, baudType, otherDeviceProtocol) {
    const req = createFuncCard_SetOutDevice_2(addr, false, outDeviceAddr, baudType, otherDeviceProtocol);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=FuncCard_SetOutDevice_2.js.map