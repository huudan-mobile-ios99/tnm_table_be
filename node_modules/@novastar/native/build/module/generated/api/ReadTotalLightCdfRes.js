import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadTotalLightCdfRes(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.TotalLightCdfResOccupancy, 'ReadTotalLightCdfRes');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.TotalLightCdfResAddr;
    return req;
}
Session.prototype.ReadTotalLightCdfRes = async function ReadTotalLightCdfRes(addr, portAddr, scanBoardAddr) {
    const req = createReadTotalLightCdfRes(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadTotalLightCdfRes = async function tryReadTotalLightCdfRes(addr, portAddr, scanBoardAddr) {
    const req = createReadTotalLightCdfRes(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadTotalLightCdfRes.js.map