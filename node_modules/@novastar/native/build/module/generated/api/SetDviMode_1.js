import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
import { VedioSelectModeEnum } from '../VedioSelectMode';
export default function createSetDviMode_1(addr, bBroadcast, dviMode) {
    const $data = encodeUIntLE(dviMode !== VedioSelectModeEnum.Manual ? 255 : 90, AddressMapping.DviModeOccupancy);
    const req = new Request($data, bBroadcast, 'SetDviMode_1');
    req.destination = addr;
    req.address = AddressMapping.DviModeAddr;
    return req;
}
Session.prototype.SetDviMode_1 = async function SetDviMode_1(addr, bBroadcast, dviMode) {
    const req = createSetDviMode_1(addr, bBroadcast, dviMode);
    await this.connection.send(req);
};
Session.prototype.trySetDviMode_1 = async function trySetDviMode_1(addr, dviMode) {
    const req = createSetDviMode_1(addr, false, dviMode);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetDviMode_1.js.map