import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScannerColorInfo(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ScannerColorInfoOccupancy, 'ReadScannerColorInfo');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ScannerColorInfoAddr;
    return req;
}
Session.prototype.ReadScannerColorInfo = async function ReadScannerColorInfo(addr, portAddr, scanBoardAddr) {
    const req = createReadScannerColorInfo(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadScannerColorInfo = async function tryReadScannerColorInfo(addr, portAddr, scanBoardAddr) {
    const req = createReadScannerColorInfo(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScannerColorInfo.js.map