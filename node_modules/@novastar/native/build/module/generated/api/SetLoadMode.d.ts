import { ErrorType, Request } from '@novastar/codec';
import { SourceSelectLoadModeEnum } from '../SourceSelectLoadMode';
declare module '@novastar/codec' {
    interface API {
        SetLoadMode(addr: number, bBroadcast: boolean, loadMode: SourceSelectLoadModeEnum): Promise<void>;
        trySetLoadMode(addr: number, loadMode: SourceSelectLoadModeEnum): Promise<ErrorType | null>;
    }
}
export default function createSetLoadMode<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, loadMode: SourceSelectLoadModeEnum): Request<Broadcast>;
