import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetDHTValue(addr, portAddr, scanBoardAddr, bBroadcast, dht) {
    const req = new Request([dht], bBroadcast, 'SetDHTValue');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.DHTAddr;
    return req;
}
Session.prototype.SetDHTValue = async function SetDHTValue(addr, portAddr, scanBoardAddr, bBroadcast, dht) {
    const req = createSetDHTValue(addr, portAddr, scanBoardAddr, bBroadcast, dht);
    await this.connection.send(req);
};
Session.prototype.trySetDHTValue = async function trySetDHTValue(addr, portAddr, scanBoardAddr, dht) {
    const req = createSetDHTValue(addr, portAddr, scanBoardAddr, false, dht);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetDHTValue.js.map