import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetBackUpOrRestoreCmd(addr, bBroadcast, writeData) {
    const req = new Request([writeData], bBroadcast, 'SetBackUpOrRestoreCmd');
    req.destination = addr;
    req.address = AddressMapping.SetBackUpOrRestoreAddr;
    return req;
}
Session.prototype.SetBackUpOrRestoreCmd = async function SetBackUpOrRestoreCmd(addr, bBroadcast, writeData) {
    const req = createSetBackUpOrRestoreCmd(addr, bBroadcast, writeData);
    await this.connection.send(req);
};
Session.prototype.trySetBackUpOrRestoreCmd = async function trySetBackUpOrRestoreCmd(addr, writeData) {
    const req = createSetBackUpOrRestoreCmd(addr, false, writeData);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetBackUpOrRestoreCmd.js.map