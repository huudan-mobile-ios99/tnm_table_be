import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadBrightnessModel(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.BrightnessModelOccupancy, 'ReadBrightnessModel');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.BrightnessModelAddr;
    return req;
}
Session.prototype.ReadBrightnessModel = async function ReadBrightnessModel(addr, portAddr, scanBoardAddr) {
    const req = createReadBrightnessModel(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadBrightnessModel = async function tryReadBrightnessModel(addr, portAddr, scanBoardAddr) {
    const req = createReadBrightnessModel(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadBrightnessModel.js.map