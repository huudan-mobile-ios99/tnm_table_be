/// <reference types="node" />
import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetScanner_DMPM(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, DMPM: number[] | Buffer): Promise<void>;
        trySetScanner_DMPM(addr: number, portAddr: number, scanBoardAddr: number, DMPM: number[] | Buffer): Promise<ErrorType | null>;
    }
}
export default function createSetScanner_DMPM<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, DMPM: number[] | Buffer): Request<Broadcast>;
