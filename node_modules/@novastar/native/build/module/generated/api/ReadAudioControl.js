import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadAudioControl(addr) {
    const req = new Request(AddressMapping.AudioControlOccupancy, 'ReadAudioControl');
    req.destination = addr;
    req.address = AddressMapping.AudioControlAddr;
    return req;
}
Session.prototype.ReadAudioControl = async function ReadAudioControl(addr) {
    const req = createReadAudioControl(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadAudioControl = async function tryReadAudioControl(addr) {
    const req = createReadAudioControl(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadAudioControl.js.map