import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createDistribute_ReadDistributeMCUVersion(addr, portAddr, distributeAddr) {
    const req = new Request(AddressMapping.Distribute_MCUVersionOccupancy, 'Distribute_ReadDistributeMCUVersion');
    req.destination = addr;
    req.deviceType = 3;
    req.port = portAddr;
    req.rcvIndex = distributeAddr;
    req.address = AddressMapping.Distribute_MCUVersionAddr;
    return req;
}
Session.prototype.Distribute_ReadDistributeMCUVersion =
    async function Distribute_ReadDistributeMCUVersion(addr, portAddr, distributeAddr) {
        const req = createDistribute_ReadDistributeMCUVersion(addr, portAddr, distributeAddr);
        return decodeUIntLE(await this.connection.send(req));
    };
Session.prototype.tryDistribute_ReadDistributeMCUVersion =
    async function tryDistribute_ReadDistributeMCUVersion(addr, portAddr, distributeAddr) {
        const req = createDistribute_ReadDistributeMCUVersion(addr, portAddr, distributeAddr);
        return this.connection.trySend(req);
    };
//# sourceMappingURL=Distribute_ReadDistributeMCUVersion.js.map