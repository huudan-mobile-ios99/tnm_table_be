import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetScanner_MGEnParmData(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = new Request([data], bBroadcast, 'SetScanner_MGEnParmData');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.MGEnParmAddr;
    return req;
}
Session.prototype.SetScanner_MGEnParmData = async function SetScanner_MGEnParmData(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createSetScanner_MGEnParmData(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.trySetScanner_MGEnParmData = async function trySetScanner_MGEnParmData(addr, portAddr, scanBoardAddr, data) {
    const req = createSetScanner_MGEnParmData(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetScanner_MGEnParmData.js.map