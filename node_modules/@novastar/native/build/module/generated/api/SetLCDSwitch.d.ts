import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetLCDSwitch(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, tem: number): Promise<void>;
        trySetLCDSwitch(addr: number, portAddr: number, scanBoardAddr: number, tem: number): Promise<ErrorType | null>;
    }
}
export default function createSetLCDSwitch<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, tem: number): Request<Broadcast>;
