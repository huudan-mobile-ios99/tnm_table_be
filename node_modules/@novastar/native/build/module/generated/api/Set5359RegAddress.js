import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSet5359RegAddress(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = new Request(data, bBroadcast, 'Set5359RegAddress');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Register5359Addres;
    return req;
}
Session.prototype.Set5359RegAddress = async function Set5359RegAddress(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createSet5359RegAddress(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.trySet5359RegAddress = async function trySet5359RegAddress(addr, portAddr, scanBoardAddr, data) {
    const req = createSet5359RegAddress(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=Set5359RegAddress.js.map