import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetMirrorMode(addr: number, bBroadcast: boolean, mirrorModeData: number, isPreposition: boolean): Promise<void>;
        trySetMirrorMode(addr: number, mirrorModeData: number, isPreposition: boolean): Promise<ErrorType | null>;
    }
}
export default function createSetMirrorMode<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, mirrorModeData: number, isPreposition: boolean): Request<Broadcast>;
