import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetSender_BackUpTestMode(addr, bBroadcast, backUpTestMode) {
    const req = new Request(backUpTestMode ? [1] : [0], bBroadcast, 'SetSender_BackUpTestMode');
    req.destination = addr;
    req.address = AddressMapping.BackUpTestModeAddr;
    return req;
}
Session.prototype.SetSender_BackUpTestMode = async function SetSender_BackUpTestMode(addr, bBroadcast, backUpTestMode) {
    const req = createSetSender_BackUpTestMode(addr, bBroadcast, backUpTestMode);
    await this.connection.send(req);
};
Session.prototype.trySetSender_BackUpTestMode = async function trySetSender_BackUpTestMode(addr, backUpTestMode) {
    const req = createSetSender_BackUpTestMode(addr, false, backUpTestMode);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetSender_BackUpTestMode.js.map