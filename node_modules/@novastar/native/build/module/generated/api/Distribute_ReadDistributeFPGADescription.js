import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createDistribute_ReadDistributeFPGADescription(addr, portAddr, distributeAddr) {
    const req = new Request(AddressMapping.Distribute_FPGADescriptionnOccupancy, 'Distribute_ReadDistributeFPGADescription');
    req.destination = addr;
    req.deviceType = 3;
    req.port = portAddr;
    req.rcvIndex = distributeAddr;
    req.address = AddressMapping.Distribute_FPGADescriptionAddr;
    return req;
}
Session.prototype.Distribute_ReadDistributeFPGADescription =
    async function Distribute_ReadDistributeFPGADescription(addr, portAddr, distributeAddr) {
        const req = createDistribute_ReadDistributeFPGADescription(addr, portAddr, distributeAddr);
        return (await this.connection.send(req)).data;
    };
Session.prototype.tryDistribute_ReadDistributeFPGADescription =
    async function tryDistribute_ReadDistributeFPGADescription(addr, portAddr, distributeAddr) {
        const req = createDistribute_ReadDistributeFPGADescription(addr, portAddr, distributeAddr);
        return this.connection.trySend(req);
    };
//# sourceMappingURL=Distribute_ReadDistributeFPGADescription.js.map