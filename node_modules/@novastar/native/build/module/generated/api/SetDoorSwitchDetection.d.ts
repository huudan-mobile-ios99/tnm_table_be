import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetDoorSwitchDetection(addr: number, bBroadcast: boolean, data: number): Promise<void>;
        trySetDoorSwitchDetection(addr: number, data: number): Promise<ErrorType | null>;
    }
}
export default function createSetDoorSwitchDetection<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, data: number): Request<Broadcast>;
