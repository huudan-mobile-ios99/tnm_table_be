import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScannerCabinetID(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ScannerCabinetIDOccupancy, 'ReadScannerCabinetID');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ScannerCabinetIDAddr;
    return req;
}
Session.prototype.ReadScannerCabinetID = async function ReadScannerCabinetID(addr, portAddr, scanBoardAddr) {
    const req = createReadScannerCabinetID(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadScannerCabinetID = async function tryReadScannerCabinetID(addr, portAddr, scanBoardAddr) {
    const req = createReadScannerCabinetID(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScannerCabinetID.js.map