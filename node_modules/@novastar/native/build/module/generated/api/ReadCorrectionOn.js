import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadCorrectionOn(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.CorrectionOnOccupancy, 'ReadCorrectionOn');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.CorrectionOnAddr;
    return req;
}
Session.prototype.ReadCorrectionOn = async function ReadCorrectionOn(addr, portAddr, scanBoardAddr) {
    const req = createReadCorrectionOn(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadCorrectionOn = async function tryReadCorrectionOn(addr, portAddr, scanBoardAddr) {
    const req = createReadCorrectionOn(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadCorrectionOn.js.map