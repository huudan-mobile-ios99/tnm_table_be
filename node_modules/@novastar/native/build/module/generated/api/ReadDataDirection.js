import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadDataDirection(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.DataDirectionOccupancy, 'ReadDataDirection');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.DataDirectionAddr;
    return req;
}
Session.prototype.ReadDataDirection = async function ReadDataDirection(addr, portAddr, scanBoardAddr) {
    const req = createReadDataDirection(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadDataDirection = async function tryReadDataDirection(addr, portAddr, scanBoardAddr) {
    const req = createReadDataDirection(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadDataDirection.js.map