import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetRGBBrightness(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, redBrightness: number, greenBrightness: number, blueBrightness: number, vRedBrightness: number): Promise<void>;
        trySetRGBBrightness(addr: number, portAddr: number, scanBoardAddr: number, redBrightness: number, greenBrightness: number, blueBrightness: number, vRedBrightness: number): Promise<ErrorType | null>;
    }
}
export default function createSetRGBBrightness<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, redBrightness: number, greenBrightness: number, blueBrightness: number, vRedBrightness: number): Request<Broadcast>;
