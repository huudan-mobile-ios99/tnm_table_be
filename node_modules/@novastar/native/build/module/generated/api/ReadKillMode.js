import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadKillMode(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.KillModeOccupancy, 'ReadKillMode');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.KillModeAddr;
    return req;
}
Session.prototype.ReadKillMode = async function ReadKillMode(addr, portAddr, scanBoardAddr) {
    const req = createReadKillMode(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadKillMode = async function tryReadKillMode(addr, portAddr, scanBoardAddr) {
    const req = createReadKillMode(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadKillMode.js.map