import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScanner_DMSecond(addr, portAddr, scanBoardAddr, readLength) {
    const req = new Request(readLength, 'ReadScanner_DMSecond');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Scanner_DmSecondAddr;
    return req;
}
Session.prototype.ReadScanner_DMSecond = async function ReadScanner_DMSecond(addr, portAddr, scanBoardAddr, readLength) {
    const req = createReadScanner_DMSecond(addr, portAddr, scanBoardAddr, readLength);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadScanner_DMSecond = async function tryReadScanner_DMSecond(addr, portAddr, scanBoardAddr, readLength) {
    const req = createReadScanner_DMSecond(addr, portAddr, scanBoardAddr, readLength);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScanner_DMSecond.js.map