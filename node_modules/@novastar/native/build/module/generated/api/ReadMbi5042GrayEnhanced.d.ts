import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadMbi5042GrayEnhanced(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadMbi5042GrayEnhanced(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadMbi5042GrayEnhanced(addr: number, portAddr: number, scanBoardAddr: number): Request;
