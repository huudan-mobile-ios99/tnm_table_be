import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadModule_McuProgramLength(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.Scanner_McuProgramLengthOccupancy, 'ReadModule_McuProgramLength');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Module_McuProgramLengthAddr;
    return req;
}
Session.prototype.ReadModule_McuProgramLength = async function ReadModule_McuProgramLength(addr, portAddr, scanBoardAddr) {
    const req = createReadModule_McuProgramLength(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadModule_McuProgramLength = async function tryReadModule_McuProgramLength(addr, portAddr, scanBoardAddr) {
    const req = createReadModule_McuProgramLength(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadModule_McuProgramLength.js.map