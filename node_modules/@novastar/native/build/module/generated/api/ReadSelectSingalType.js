import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadSelectSingalType(addr) {
    const req = new Request(AddressMapping.SelectSingalTypeOccupancy, 'ReadSelectSingalType');
    req.destination = addr;
    req.address = AddressMapping.SourceSelectSingalBitAddr;
    return req;
}
Session.prototype.ReadSelectSingalType = async function ReadSelectSingalType(addr) {
    const req = createReadSelectSingalType(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadSelectSingalType = async function tryReadSelectSingalType(addr) {
    const req = createReadSelectSingalType(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadSelectSingalType.js.map