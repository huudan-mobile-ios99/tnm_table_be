import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadCheckModuleFlashResult(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.CheckModuleFlashResultOccupancy, 'ReadCheckModuleFlashResult');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.CheckModuleFlashResultAddr;
    return req;
}
Session.prototype.ReadCheckModuleFlashResult = async function ReadCheckModuleFlashResult(addr, portAddr, scanBoardAddr) {
    const req = createReadCheckModuleFlashResult(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadCheckModuleFlashResult = async function tryReadCheckModuleFlashResult(addr, portAddr, scanBoardAddr) {
    const req = createReadCheckModuleFlashResult(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadCheckModuleFlashResult.js.map