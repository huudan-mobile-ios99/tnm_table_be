import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScannerDehumidPara(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.DehumidParaAddrOccupancy, 'ReadScannerDehumidPara');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.DehumidParaAddr;
    return req;
}
Session.prototype.ReadScannerDehumidPara = async function ReadScannerDehumidPara(addr, portAddr, scanBoardAddr) {
    const req = createReadScannerDehumidPara(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadScannerDehumidPara = async function tryReadScannerDehumidPara(addr, portAddr, scanBoardAddr) {
    const req = createReadScannerDehumidPara(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScannerDehumidPara.js.map