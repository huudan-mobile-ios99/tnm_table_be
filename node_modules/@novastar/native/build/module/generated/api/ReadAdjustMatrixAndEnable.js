import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadAdjustMatrixAndEnable(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ColorAdjustEnableOccupancy, 'ReadAdjustMatrixAndEnable');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ColorAdjustMatrixAndEnableAddr;
    return req;
}
Session.prototype.ReadAdjustMatrixAndEnable = async function ReadAdjustMatrixAndEnable(addr, portAddr, scanBoardAddr) {
    const req = createReadAdjustMatrixAndEnable(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadAdjustMatrixAndEnable = async function tryReadAdjustMatrixAndEnable(addr, portAddr, scanBoardAddr) {
    const req = createReadAdjustMatrixAndEnable(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadAdjustMatrixAndEnable.js.map