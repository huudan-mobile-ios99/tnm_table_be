import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadBackUpParamFromSender(addr, length) {
    const req = new Request(length, 'ReadBackUpParamFromSender');
    req.destination = addr;
    req.address = AddressMapping.BackUpFileAddr;
    return req;
}
Session.prototype.ReadBackUpParamFromSender = async function ReadBackUpParamFromSender(addr, length) {
    const req = createReadBackUpParamFromSender(addr, length);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadBackUpParamFromSender = async function tryReadBackUpParamFromSender(addr, length) {
    const req = createReadBackUpParamFromSender(addr, length);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadBackUpParamFromSender.js.map