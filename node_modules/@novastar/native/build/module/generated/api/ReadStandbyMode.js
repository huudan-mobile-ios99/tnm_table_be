import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadStandbyMode(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.StandbyModeOccupancy, 'ReadStandbyMode');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.StandbyModeAddr;
    return req;
}
Session.prototype.ReadStandbyMode = async function ReadStandbyMode(addr, portAddr, scanBoardAddr) {
    const req = createReadStandbyMode(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadStandbyMode = async function tryReadStandbyMode(addr, portAddr, scanBoardAddr) {
    const req = createReadStandbyMode(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadStandbyMode.js.map