import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetScanner_FPGAProgramRemarks(addr, portAddr, scanBoardAddr, bBroadcast, scanner_FPGAProgramRemarks) {
    if (scanner_FPGAProgramRemarks.length !== AddressMapping.Scanner_FPGAProgramRemarksOccupancy)
        throw new TypeError(`Invalid buffer size: ${scanner_FPGAProgramRemarks.length}`);
    const req = new Request(scanner_FPGAProgramRemarks, bBroadcast, 'SetScanner_FPGAProgramRemarks');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Scanner_FPGAProgramRemarksAddr;
    return req;
}
Session.prototype.SetScanner_FPGAProgramRemarks = async function SetScanner_FPGAProgramRemarks(addr, portAddr, scanBoardAddr, bBroadcast, scanner_FPGAProgramRemarks) {
    const req = createSetScanner_FPGAProgramRemarks(addr, portAddr, scanBoardAddr, bBroadcast, scanner_FPGAProgramRemarks);
    await this.connection.send(req);
};
Session.prototype.trySetScanner_FPGAProgramRemarks =
    async function trySetScanner_FPGAProgramRemarks(addr, portAddr, scanBoardAddr, scanner_FPGAProgramRemarks) {
        const req = createSetScanner_FPGAProgramRemarks(addr, portAddr, scanBoardAddr, false, scanner_FPGAProgramRemarks);
        return (await this.connection.trySend(req))?.ack ?? null;
    };
//# sourceMappingURL=SetScanner_FPGAProgramRemarks.js.map