import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadPowerWorkState(addr, portAddr, scanBoardAddr, powerBackNumber) {
    const req = new Request(powerBackNumber, 'ReadPowerWorkState');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.PowerWorkStateAddr;
    return req;
}
Session.prototype.ReadPowerWorkState = async function ReadPowerWorkState(addr, portAddr, scanBoardAddr, powerBackNumber) {
    const req = createReadPowerWorkState(addr, portAddr, scanBoardAddr, powerBackNumber);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadPowerWorkState = async function tryReadPowerWorkState(addr, portAddr, scanBoardAddr, powerBackNumber) {
    const req = createReadPowerWorkState(addr, portAddr, scanBoardAddr, powerBackNumber);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadPowerWorkState.js.map