import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadIrregularScreenDrive(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.IrregularScreenDriveOccupancy, 'ReadIrregularScreenDrive');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.IrregularScreenDriveAddr;
    return req;
}
Session.prototype.ReadIrregularScreenDrive = async function ReadIrregularScreenDrive(addr, portAddr, scanBoardAddr) {
    const req = createReadIrregularScreenDrive(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadIrregularScreenDrive = async function tryReadIrregularScreenDrive(addr, portAddr, scanBoardAddr) {
    const req = createReadIrregularScreenDrive(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadIrregularScreenDrive.js.map