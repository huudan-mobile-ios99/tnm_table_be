import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScanner_FPGAProgramLength(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.Scanner_FPGAProgramLengthOccupancy, 'ReadScanner_FPGAProgramLength');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.Scanner_FPGAProgramLengthAddr;
    return req;
}
Session.prototype.ReadScanner_FPGAProgramLength = async function ReadScanner_FPGAProgramLength(addr, portAddr, scanBoardAddr) {
    const req = createReadScanner_FPGAProgramLength(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadScanner_FPGAProgramLength =
    async function tryReadScanner_FPGAProgramLength(addr, portAddr, scanBoardAddr) {
        const req = createReadScanner_FPGAProgramLength(addr, portAddr, scanBoardAddr);
        return this.connection.trySend(req);
    };
//# sourceMappingURL=ReadScanner_FPGAProgramLength.js.map