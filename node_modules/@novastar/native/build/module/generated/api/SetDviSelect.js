import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetDviSelect(addr, bBroadcast, dviSelect) {
    const $data = encodeUIntLE(dviSelect, AddressMapping.DviSelectOccupancy);
    const req = new Request($data, bBroadcast, 'SetDviSelect');
    req.destination = addr;
    req.address = AddressMapping.DviSelectAddr;
    return req;
}
Session.prototype.SetDviSelect = async function SetDviSelect(addr, bBroadcast, dviSelect) {
    const req = createSetDviSelect(addr, bBroadcast, dviSelect);
    await this.connection.send(req);
};
Session.prototype.trySetDviSelect = async function trySetDviSelect(addr, dviSelect) {
    const req = createSetDviSelect(addr, false, dviSelect);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetDviSelect.js.map