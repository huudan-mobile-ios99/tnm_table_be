import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetScanner_MGParmData(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = new Request(data, bBroadcast, 'SetScanner_MGParmData');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.MGParmAddr;
    return req;
}
Session.prototype.SetScanner_MGParmData = async function SetScanner_MGParmData(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createSetScanner_MGParmData(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.trySetScanner_MGParmData = async function trySetScanner_MGParmData(addr, portAddr, scanBoardAddr, data) {
    const req = createSetScanner_MGParmData(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetScanner_MGParmData.js.map