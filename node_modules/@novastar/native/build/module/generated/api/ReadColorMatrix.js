import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadColorMatrix(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ColorMatrixOccupancy * AddressMapping.ColorMatrixNum, 'ReadColorMatrix');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ColorMatrixAddr;
    return req;
}
Session.prototype.ReadColorMatrix = async function ReadColorMatrix(addr, portAddr, scanBoardAddr) {
    const req = createReadColorMatrix(addr, portAddr, scanBoardAddr);
    return (await this.connection.send(req)).data;
};
Session.prototype.tryReadColorMatrix = async function tryReadColorMatrix(addr, portAddr, scanBoardAddr) {
    const req = createReadColorMatrix(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadColorMatrix.js.map