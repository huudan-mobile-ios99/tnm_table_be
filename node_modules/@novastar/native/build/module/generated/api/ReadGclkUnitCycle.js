import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadGclkUnitCycle(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.GclkUnitCycleOccupancy, 'ReadGclkUnitCycle');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.GclkUnitCycleAddr;
    return req;
}
Session.prototype.ReadGclkUnitCycle = async function ReadGclkUnitCycle(addr, portAddr, scanBoardAddr) {
    const req = createReadGclkUnitCycle(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadGclkUnitCycle = async function tryReadGclkUnitCycle(addr, portAddr, scanBoardAddr) {
    const req = createReadGclkUnitCycle(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadGclkUnitCycle.js.map