import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetDistributeEthernetPortScannerXY(addr, portAddr, distributeAddr, bBroadcast, ethernetPortScannerX, ethernetPortScannerY, portIndex, scannerIndex) {
    const $data = encodeUIntLE((ethernetPortScannerX << 16) + ethernetPortScannerY, AddressMapping.DistributeEthernetPortScannerXOccupancy +
        AddressMapping.DistributeEthernetPortScannerYOccupancy);
    const req = new Request($data, bBroadcast, 'SetDistributeEthernetPortScannerXY');
    req.destination = addr;
    req.deviceType = 3;
    req.port = portAddr;
    req.rcvIndex = distributeAddr;
    req.address =
        AddressMapping.DistributeEthernetPortOccupancy * portIndex +
            AddressMapping.DistributeEthernetPortScannerXAddr +
            (AddressMapping.DistributeEthernetPortScannerXOccupancy +
                AddressMapping.DistributeEthernetPortScannerYOccupancy) *
                scannerIndex;
    return req;
}
Session.prototype.SetDistributeEthernetPortScannerXY =
    async function SetDistributeEthernetPortScannerXY(addr, portAddr, distributeAddr, bBroadcast, ethernetPortScannerX, ethernetPortScannerY, portIndex, scannerIndex) {
        const req = createSetDistributeEthernetPortScannerXY(addr, portAddr, distributeAddr, bBroadcast, ethernetPortScannerX, ethernetPortScannerY, portIndex, scannerIndex);
        await this.connection.send(req);
    };
Session.prototype.trySetDistributeEthernetPortScannerXY =
    async function trySetDistributeEthernetPortScannerXY(addr, portAddr, distributeAddr, ethernetPortScannerX, ethernetPortScannerY, portIndex, scannerIndex) {
        const req = createSetDistributeEthernetPortScannerXY(addr, portAddr, distributeAddr, false, ethernetPortScannerX, ethernetPortScannerY, portIndex, scannerIndex);
        return (await this.connection.trySend(req))?.ack ?? null;
    };
//# sourceMappingURL=SetDistributeEthernetPortScannerXY.js.map