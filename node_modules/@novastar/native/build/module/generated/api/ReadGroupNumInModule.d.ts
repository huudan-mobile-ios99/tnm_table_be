import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadGroupNumInModule(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadGroupNumInModule(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadGroupNumInModule(addr: number, portAddr: number, scanBoardAddr: number): Request;
