import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetCorrectionOn_2(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = new Request([data], bBroadcast, 'SetCorrectionOn_2');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.CorrectionOnAddr;
    return req;
}
Session.prototype.SetCorrectionOn_2 = async function SetCorrectionOn_2(addr, portAddr, scanBoardAddr, bBroadcast, data) {
    const req = createSetCorrectionOn_2(addr, portAddr, scanBoardAddr, bBroadcast, data);
    await this.connection.send(req);
};
Session.prototype.trySetCorrectionOn_2 = async function trySetCorrectionOn_2(addr, portAddr, scanBoardAddr, data) {
    const req = createSetCorrectionOn_2(addr, portAddr, scanBoardAddr, false, data);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetCorrectionOn_2.js.map