import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScannerBacklightSwitch(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ScannerBacklightSwitchOccupancy, 'ReadScannerBacklightSwitch');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ScannerBacklightSwitchAddr;
    return req;
}
Session.prototype.ReadScannerBacklightSwitch = async function ReadScannerBacklightSwitch(addr, portAddr, scanBoardAddr) {
    const req = createReadScannerBacklightSwitch(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadScannerBacklightSwitch = async function tryReadScannerBacklightSwitch(addr, portAddr, scanBoardAddr) {
    const req = createReadScannerBacklightSwitch(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScannerBacklightSwitch.js.map