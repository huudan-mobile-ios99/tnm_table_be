import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetLockMode(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, lockMode: number): Promise<void>;
        trySetLockMode(addr: number, portAddr: number, scanBoardAddr: number, lockMode: number): Promise<ErrorType | null>;
    }
}
export default function createSetLockMode<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, lockMode: number): Request<Broadcast>;
