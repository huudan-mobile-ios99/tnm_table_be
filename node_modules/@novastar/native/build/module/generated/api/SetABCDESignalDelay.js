import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetABCDESignalDelay(addr, portAddr, scanBoardAddr, bBroadcast, ABCDESignalDelay) {
    const req = new Request([ABCDESignalDelay], bBroadcast, 'SetABCDESignalDelay');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ABCDESignalDelayAddr;
    return req;
}
Session.prototype.SetABCDESignalDelay = async function SetABCDESignalDelay(addr, portAddr, scanBoardAddr, bBroadcast, ABCDESignalDelay) {
    const req = createSetABCDESignalDelay(addr, portAddr, scanBoardAddr, bBroadcast, ABCDESignalDelay);
    await this.connection.send(req);
};
Session.prototype.trySetABCDESignalDelay = async function trySetABCDESignalDelay(addr, portAddr, scanBoardAddr, ABCDESignalDelay) {
    const req = createSetABCDESignalDelay(addr, portAddr, scanBoardAddr, false, ABCDESignalDelay);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetABCDESignalDelay.js.map