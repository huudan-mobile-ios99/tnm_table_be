import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadModule_McuProgramEdition(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadModule_McuProgramEdition(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadModule_McuProgramEdition(addr: number, portAddr: number, scanBoardAddr: number): Request;
