import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadPhysicalTotalDataGroupNum(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadPhysicalTotalDataGroupNum(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadPhysicalTotalDataGroupNum(addr: number, portAddr: number, scanBoardAddr: number): Request;
