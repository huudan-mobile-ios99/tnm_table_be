import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createFuncCard_SetAudioFrequency(addr, bBroadcast, freMode) {
    const req = new Request([freMode], bBroadcast, 'FuncCard_SetAudioFrequency');
    req.destination = addr;
    req.address = AddressMapping.FuncCard_AudioFrequencyAddr;
    return req;
}
Session.prototype.FuncCard_SetAudioFrequency = async function FuncCard_SetAudioFrequency(addr, bBroadcast, freMode) {
    const req = createFuncCard_SetAudioFrequency(addr, bBroadcast, freMode);
    await this.connection.send(req);
};
Session.prototype.tryFuncCard_SetAudioFrequency = async function tryFuncCard_SetAudioFrequency(addr, freMode) {
    const req = createFuncCard_SetAudioFrequency(addr, false, freMode);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=FuncCard_SetAudioFrequency.js.map