import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetSender_VideoScalingMode(addr, bBoradcast, scalingMode) {
    const req = new Request([scalingMode], bBoradcast, 'SetSender_VideoScalingMode');
    req.destination = addr;
    req.address = AddressMapping.Sender_VideoScalingModeAddr;
    return req;
}
Session.prototype.SetSender_VideoScalingMode = async function SetSender_VideoScalingMode(addr, bBoradcast, scalingMode) {
    const req = createSetSender_VideoScalingMode(addr, bBoradcast, scalingMode);
    await this.connection.send(req);
};
Session.prototype.trySetSender_VideoScalingMode = async function trySetSender_VideoScalingMode(addr, scalingMode) {
    const req = createSetSender_VideoScalingMode(addr, false, scalingMode);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetSender_VideoScalingMode.js.map