import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadScreenEncrypt(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.ScreenEncryptedOccupancy, 'ReadScreenEncrypt');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ScreenEncryptedAddr;
    return req;
}
Session.prototype.ReadScreenEncrypt = async function ReadScreenEncrypt(addr, portAddr, scanBoardAddr) {
    const req = createReadScreenEncrypt(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadScreenEncrypt = async function tryReadScreenEncrypt(addr, portAddr, scanBoardAddr) {
    const req = createReadScreenEncrypt(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadScreenEncrypt.js.map