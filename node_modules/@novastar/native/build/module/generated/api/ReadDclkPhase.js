import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadDclkPhase(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.DclkPhaseOccupancy, 'ReadDclkPhase');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.DclkPhaseAddr;
    return req;
}
Session.prototype.ReadDclkPhase = async function ReadDclkPhase(addr, portAddr, scanBoardAddr) {
    const req = createReadDclkPhase(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadDclkPhase = async function tryReadDclkPhase(addr, portAddr, scanBoardAddr) {
    const req = createReadDclkPhase(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadDclkPhase.js.map