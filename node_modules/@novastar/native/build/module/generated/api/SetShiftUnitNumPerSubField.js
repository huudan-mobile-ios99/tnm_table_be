import { encodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetShiftUnitNumPerSubField(addr, portAddr, scanBoardAddr, bBroadcast, shiftUnitNumPerSubField) {
    const $data = encodeUIntLE(shiftUnitNumPerSubField, AddressMapping.ShiftUnitNumPerSubFieldOccupancy);
    const req = new Request($data, bBroadcast, 'SetShiftUnitNumPerSubField');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.ShiftUnitNumPerSubFieldAddr;
    return req;
}
Session.prototype.SetShiftUnitNumPerSubField = async function SetShiftUnitNumPerSubField(addr, portAddr, scanBoardAddr, bBroadcast, shiftUnitNumPerSubField) {
    const req = createSetShiftUnitNumPerSubField(addr, portAddr, scanBoardAddr, bBroadcast, shiftUnitNumPerSubField);
    await this.connection.send(req);
};
Session.prototype.trySetShiftUnitNumPerSubField = async function trySetShiftUnitNumPerSubField(addr, portAddr, scanBoardAddr, shiftUnitNumPerSubField) {
    const req = createSetShiftUnitNumPerSubField(addr, portAddr, scanBoardAddr, false, shiftUnitNumPerSubField);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetShiftUnitNumPerSubField.js.map