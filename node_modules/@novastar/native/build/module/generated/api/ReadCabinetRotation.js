import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadCabinetRotation(addr) {
    const req = new Request(AddressMapping.CabinetRotationOccupancy, 'ReadCabinetRotation');
    req.destination = addr;
    req.address = AddressMapping.CabinetRotationAddr;
    return req;
}
Session.prototype.ReadCabinetRotation = async function ReadCabinetRotation(addr) {
    const req = createReadCabinetRotation(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadCabinetRotation = async function tryReadCabinetRotation(addr) {
    const req = createReadCabinetRotation(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadCabinetRotation.js.map