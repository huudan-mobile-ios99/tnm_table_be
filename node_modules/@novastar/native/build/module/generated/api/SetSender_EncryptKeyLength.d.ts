import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetSender_EncryptKeyLength(addr: number, bBroadcast: boolean, keyLength: number): Promise<void>;
        trySetSender_EncryptKeyLength(addr: number, keyLength: number): Promise<ErrorType | null>;
    }
}
export default function createSetSender_EncryptKeyLength<Broadcast extends boolean>(addr: number, bBroadcast: Broadcast, keyLength: number): Request<Broadcast>;
