import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadLowDelayMode(addr) {
    const req = new Request(AddressMapping.LowDelayOccupancy, 'ReadLowDelayMode');
    req.destination = addr;
    req.address = AddressMapping.LowDelayAddr;
    return req;
}
Session.prototype.ReadLowDelayMode = async function ReadLowDelayMode(addr) {
    const req = createReadLowDelayMode(addr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadLowDelayMode = async function tryReadLowDelayMode(addr) {
    const req = createReadLowDelayMode(addr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadLowDelayMode.js.map