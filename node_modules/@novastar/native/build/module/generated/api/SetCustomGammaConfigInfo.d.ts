import { ErrorType, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        SetCustomGammaConfigInfo(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: boolean, customGammaConfigInfo: number): Promise<void>;
        trySetCustomGammaConfigInfo(addr: number, portAddr: number, scanBoardAddr: number, customGammaConfigInfo: number): Promise<ErrorType | null>;
    }
}
export default function createSetCustomGammaConfigInfo<Broadcast extends boolean>(addr: number, portAddr: number, scanBoardAddr: number, bBroadcast: Broadcast, customGammaConfigInfo: number): Request<Broadcast>;
