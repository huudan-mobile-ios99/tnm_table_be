import { Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createSetScanner_AutoRefreshRate(addr, portAddr, scanBoardAddr, bBroadcast, autoRefreshRateData) {
    const req = new Request(autoRefreshRateData, bBroadcast, 'SetScanner_AutoRefreshRate');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.AutoRefreshRateAddr;
    return req;
}
Session.prototype.SetScanner_AutoRefreshRate = async function SetScanner_AutoRefreshRate(addr, portAddr, scanBoardAddr, bBroadcast, autoRefreshRateData) {
    const req = createSetScanner_AutoRefreshRate(addr, portAddr, scanBoardAddr, bBroadcast, autoRefreshRateData);
    await this.connection.send(req);
};
Session.prototype.trySetScanner_AutoRefreshRate = async function trySetScanner_AutoRefreshRate(addr, portAddr, scanBoardAddr, autoRefreshRateData) {
    const req = createSetScanner_AutoRefreshRate(addr, portAddr, scanBoardAddr, false, autoRefreshRateData);
    return (await this.connection.trySend(req))?.ack ?? null;
};
//# sourceMappingURL=SetScanner_AutoRefreshRate.js.map