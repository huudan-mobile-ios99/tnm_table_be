import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadNewOERamEnable(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadNewOERamEnable(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadNewOERamEnable(addr: number, portAddr: number, scanBoardAddr: number): Request;
