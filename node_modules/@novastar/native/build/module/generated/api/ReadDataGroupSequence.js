import { decodeUIntLE, Request, Session } from '@novastar/codec';
import AddressMapping from '../AddressMapping';
export default function createReadDataGroupSequence(addr, portAddr, scanBoardAddr) {
    const req = new Request(AddressMapping.DataGroupSequenceOccupancy, 'ReadDataGroupSequence');
    req.destination = addr;
    req.deviceType = 1;
    req.port = portAddr;
    req.rcvIndex = scanBoardAddr;
    req.address = AddressMapping.DataGroupSequenceAddr;
    return req;
}
Session.prototype.ReadDataGroupSequence = async function ReadDataGroupSequence(addr, portAddr, scanBoardAddr) {
    const req = createReadDataGroupSequence(addr, portAddr, scanBoardAddr);
    return decodeUIntLE(await this.connection.send(req));
};
Session.prototype.tryReadDataGroupSequence = async function tryReadDataGroupSequence(addr, portAddr, scanBoardAddr) {
    const req = createReadDataGroupSequence(addr, portAddr, scanBoardAddr);
    return this.connection.trySend(req);
};
//# sourceMappingURL=ReadDataGroupSequence.js.map