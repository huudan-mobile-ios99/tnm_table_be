import { Packet, Request } from '@novastar/codec';
declare module '@novastar/codec' {
    interface API {
        ReadCtrlEndPoint(addr: number, portAddr: number, scanBoardAddr: number): Promise<number>;
        tryReadCtrlEndPoint(addr: number, portAddr: number, scanBoardAddr: number): Promise<Packet | null>;
    }
}
export default function createReadCtrlEndPoint(addr: number, portAddr: number, scanBoardAddr: number): Request;
